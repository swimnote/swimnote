build:
  name: iOS Production Build
  steps:
    - eas/checkout
    - eas/install_node_modules
    - eas/prebuild:
        inputs:
          clean: true
          platform: ios
    - eas/install_pods
    - eas/build:
        inputs:
          platform: ios
    - eas/upload_build_artifacts

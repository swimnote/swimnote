/**
 * misconception/replitdb-repository.ts — stub
 *
 * Misconception Hunter 모듈은 제거되었습니다 (c3b07a58).
 * 이 파일은 app.ts의 import를 유지하면서 startup 오류를 방지하기 위한 no-op stub입니다.
 */

export async function seedIfEmpty(): Promise<void> {
  // no-op: Misconception Hunter module removed
}

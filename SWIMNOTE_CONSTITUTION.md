# SwimNote 앱 운영 헌법

> 이 문서는 SwimNote 앱·웹 운영의 기본 원칙입니다. 변경 작업 전 반드시 참고하세요.

---

## 1. 기능 우선순위 원칙

**앱이 기준이다. 웹은 앱을 따른다.**

- 새로운 기능은 앱에서 먼저 구현
- 웹은 앱 기능을 기반으로 반영
- 웹 기준으로 기능을 설계하지 않는다

---

## 2. 업데이트 범위 원칙

| 상황 | 수정 범위 |
|------|----------|
| 앱 기능 추가/변경 | `lib/app-content/src/index.ts` + 앱 + 웹 동시 반영 |
| 웹 디자인/레이아웃 변경 | 웹 파일만 수정 |
| 대시보드 템플릿 변경 | 웹 파일만 수정 → 전체 수영장 자동 반영 |

---

## 3. 공유 콘텐츠 관리 원칙

앱·웹이 함께 사용하는 콘텐츠는 **단일 소스**에서 관리한다.

**위치:** `lib/app-content/src/index.ts`

**공유 항목:**
- `keyFeatures` — 핵심 기능 4가지
- `adminFeatures` — 관리자 기능 목록
- `teacherFeatures` — 선생님 기능 목록
- `parentFeatures` — 학부모 기능 목록
- `upcomingFeatures` — 예정 기능
- `flowSteps` — 운영 흐름 단계
- `APP_VERSION` — 현재 앱 버전
- `APP_STORE_URL` / `PLAY_STORE_URL` — 스토어 링크

앱 업데이트 시 위 항목 해당 여부 확인 후 함께 수정한다.

---

## 4. 수영장 홈페이지 원칙

| 영역 | 업데이트 방식 |
|------|------------|
| 수영장별 소개 섹션 | 수동 — 각 수영장마다 개별 제작 |
| 소개 이후 탭 (교육시스템·앱·고객지원) | 자동 — 코드 push 시 전체 수영장 동시 반영 |
| 대시보드 템플릿 | 자동 — 코드 push 시 전체 수영장 동시 반영 |

**건드리지 않는 것 (수영장별 DB 데이터):**
- 홈페이지 URL (슬러그)
- 수영장 이름
- 관리자 로그인 계정 정보
- 수영장별 소개 콘텐츠 (주소, 전화번호, 소개글)

---

## 5. 개발 테스트 원칙

- **샘플 수영장** (`sample@swimnote.kr` / `swim1234` / PIN `1111`)을 기본 테스트 환경으로 사용
- 샘플 수영장에서 확인 후 → GitHub push → Render 배포 → 전체 수영장 자동 반영
- 심사(앱스토어 리뷰) 계정: `sample@swimnote.kr` / `swim1234`

---

## 6. 멀티테넌트 아키텍처 원칙

- 모든 수영장은 **동일한 코드(웹·앱)** 를 공유
- 수영장별 차이는 **DB 데이터**로만 구분 (이름, 주소, 테마색, 로고 등)
- 코드 업데이트 = 전체 수영장 동시 반영

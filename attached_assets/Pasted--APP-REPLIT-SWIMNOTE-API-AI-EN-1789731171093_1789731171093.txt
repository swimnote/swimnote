[보낼 곳: APP REPLIT]

[SWIMNOTE — 공지 음성인식 + 수영 훈련용어 검색 동시 오류 공통원인 조사]

아직 코드 수정하지 마십시오.
API/AI ENGINE 배포 금지.
APP OTA 금지.
환경변수 변경 금지.

현재 실제 사용자 환경에서:

1.
관리자 공지사항
→ 음성으로 작성
→ “음성인식에 실패했습니다”

2.
수영 훈련용어 검색
→ 오류 발생

두 AI 관련 기능에서 비슷한 시점에 문제가 발생했습니다.

이번 조사 목표는:

“두 오류가 각각 독립적인 APP 버그인지,
아니면 공통 AI/API 계층 문제인지”

를 정확히 구분하는 것입니다.

━━━━━━━━━━━━━━━━━━━━━━
1. 훈련용어 검색 실제 SOURCE 확인
━━━━━━━━━━━━━━━━━━━━━━

“수영 훈련용어 검색” 기능을 실제 렌더링하는
정확한 APP 파일을 찾으십시오.

검색 문자열/화면명으로 실제 source를 특정.

보고:

- screen/component
- 검색 handler
- 호출 API endpoint
- request method
- auth 방식

파일명을 추측하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 훈련용어 검색 호출 FLOW 추적
━━━━━━━━━━━━━━━━━━━━━━

실제 흐름을 끝까지 확인:

APP
→ SWIMNOTE API 여부
→ AI ENGINE 여부
→ 외부 AI/OpenAI 여부
→ response
→ APP 표시

특히 최종적으로 어느 서비스가
수영 전문 검색/추론을 수행하는지 확인하십시오.

AI ENGINE:
https://swimnote.ai.kr

를 사용하는지 여부를
실제 코드로 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 공지 음성인식 FLOW와 나란히 비교
━━━━━━━━━━━━━━━━━━━━━━

공지 STT:

APP
→ endpoint
→ service
→ 외부 provider

훈련용어 검색:

APP
→ endpoint
→ service
→ AI ENGINE/provider

를 나란히 정리하십시오.

다음 공통 요소가 있는지 확인:

- 동일 API server
- 동일 OpenAI API key
- 동일 AI ENGINE
- 동일 auth middleware
- 동일 fetch/API client
- 동일 environment variable
- 동일 proxy
- 동일 rate-limit/budget guard

━━━━━━━━━━━━━━━━━━━━━━
4. 실제 오류를 각각 1건씩 확인
━━━━━━━━━━━━━━━━━━━━━━

Production 최근 실패 요청에서
각각 가장 최근 관련 요청 1건만 확인하십시오.

[A] 공지 음성인식

- endpoint
- HTTP status
- 최초 실제 error
- 실패 단계

[B] 훈련용어 검색

- endpoint
- HTTP status
- 최초 실제 error
- 실패 단계

전체 로그 audit 금지.

관련 요청 한 건씩만 확인.

민감한:

API key
JWT
사용자 음성내용
검색 개인정보

보고 금지.

━━━━━━━━━━━━━━━━━━━━━━
5. AI PROVIDER / ENGINE 상태 확인
━━━━━━━━━━━━━━━━━━━━━━

두 기능 중 실제로 사용하는 것만 확인하십시오.

확인 대상 예:

- OpenAI 호출 성공/실패
- API key 존재 여부
- key invalid / expired
- quota / billing / rate limit
- model identifier 문제
- timeout
- AI ENGINE health
- API → AI ENGINE network error

사용하지 않는 서비스를
괜히 조사하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 환경변수 최근 불일치 여부
━━━━━━━━━━━━━━━━━━━━━━

APP/API/AI ENGINE에서
해당 기능들이 사용하는 env key 이름을 확인.

최근 Production에서:

- env 누락
- key 이름 변경
- Render/Replit env 차이
- 잘못된 base URL
- staging URL
- old model name

등이 있는지
실제 실패 기능 관련 값만 확인.

secret 원문 보고 금지.

존재 여부/소스만 보고.

━━━━━━━━━━━━━━━━━━━━━━
7. AI ENGINE 자체 장애 여부
━━━━━━━━━━━━━━━━━━━━━━

훈련용어 검색이 AI ENGINE을 사용한다면

AI ENGINE 자체의
관련 health/read endpoint 또는
해당 검색 endpoint가 정상인지
비파괴 방식으로 확인하십시오.

다른 엔진 기능 전체 테스트 금지.

훈련용어 검색에 필요한 경로만 확인.

공지 STT가 AI ENGINE을 사용하지 않는다면
두 오류를 억지로 같은 엔진 문제로 묶지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. 공통원인 분류
━━━━━━━━━━━━━━━━━━━━━━

최종적으로 다음 중 하나로 분류하십시오.

A.
두 기능 모두 같은 AI/provider 문제

B.
두 기능 모두 같은 API auth/env 문제

C.
공지 STT만 별도 문제

D.
훈련용어 검색만 AI ENGINE 문제

E.
완전히 서로 다른 두 버그

F.
기타

추측 금지.

실제 endpoint / HTTP status / server error
근거로 분류하십시오.

━━━━━━━━━━━━━━━━━━━━━━
9. 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

이번 단계에서:

- OpenAI key 변경
- model 변경
- AI ENGINE 코드 수정
- APP 코드 수정
- endpoint 변경
- auth 완화
- API 배포
- AI ENGINE 배포
- OTA

전부 금지.

원인 조사만.

━━━━━━━━━━━━━━━━━━━━━━
10. 최종 보고
━━━━━━━━━━━━━━━━━━━━━━

[A. NOTICE STT]

- APP source
- endpoint
- 최종 provider
- HTTP status
- 실제 실패지점

[B. TRAINING TERM SEARCH]

- APP source
- endpoint
- SWIMNOTE API / AI ENGINE 흐름
- HTTP status
- 실제 실패지점

[C. SHARED DEPENDENCY]

두 기능이 공유하는:

- server
- auth
- env
- OpenAI
- AI ENGINE

존재 여부

[D. ROOT CAUSE]

A~F 중 분류하고
근거 한 문장.

[E. FIX PLAN]

아직 수정하지 말고:

- 수정 대상
- APP/API/AI ENGINE 중 어디인지
- 배포 대상
- 최소 수정안

만 제안.

확인하지 못한 것은
‘확인하지 못함’이라고 명시하십시오.
[보낼 곳: APP REPLIT]

SWIMNOTE AI 성장리포트
발급정책 3/2 후보 DRY-RUN ONLY

이번 작업에서는 정책을 실제 코드에 반영하지 마십시오.

확인하려는 후보 정책:

- report_month 기준 재원
- attendance_count >= 3
- valid source_event_count >= 2

입니다.

현재 Production 정책 후보인:

attendance >= 3
diary >= 3

과 비교해서
발급 가능 인원이 얼마나 달라지는지만 확인합니다.

Production deploy / migration / status 변경 / correction 실행 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 168명 전체 재계산
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

주 pool의 현재 9월 리포트 후보 168명을 대상으로
read-only dry-run 하십시오.

기준:

A.
report_month 기준 재원 상태 충족

B.
attendance_count >= 3

C.
source_event_count >= 2

모두 만족하면:

ELIGIBLE_3_2

로 분류하십시오.

현재 attendance_count 계산 방식과
현재 valid source_event_count predicate는
이번 dry-run에서 변경하지 마십시오.

이번 작업 목적은
오직 "3/3 → 3/2" threshold 변화 효과 측정입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. 3/3과 3/2 비교
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 기준:

attendance >= 3
AND
diary >= 3

의 ELIGIBLE 수와

새 후보:

attendance >= 3
AND
diary >= 2

의 ELIGIBLE 수를 나란히 제출하십시오.

필수:

기존 3/3 ELIGIBLE
신규 3/2 ELIGIBLE
증가 인원
증가율

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. Diary 정확히 2건인 학생 집중 분석
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 source_event_count = 2인 학생만 별도로 추출하십시오.

이 그룹에서:

attendance 0
attendance 1
attendance 2
attendance 3
attendance 4
attendance 5+

분포를 제출하십시오.

특히:

source_event_count = 2
AND
attendance_count >= 3

학생 수를 정확히 제출하십시오.

이 숫자가
3/2 정책으로 새로 발급되는 핵심 인원입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 최종 Matrix
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

168명을 아래 matrix로 제출하십시오.

재원O / 출석>=3 / 일지>=3
재원O / 출석>=3 / 일지=2
재원O / 출석>=3 / 일지=1
재원O / 출석>=3 / 일지=0

재원O / 출석<3 / 일지>=3
재원O / 출석<3 / 일지=2
재원O / 출석<3 / 일지=1
재원O / 출석<3 / 일지=0

재원X 별도

각 그룹 인원수의 합이
정확히 168이어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 새 정책 예상 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정책을:

attendance >= 3
AND
source_event_count >= 2

로 바꾼다고 가정했을 때:

- ELIGIBLE 수
- EXCLUDED 수
- 발급률
- 기존 3/3 대비 추가 발급 수

를 제출하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 새로 ELIGIBLE 되는 학생 품질 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3/3에서는 제외됐지만
3/2에서는 새로 ELIGIBLE 되는 학생들을
마스킹하여 샘플 10명까지 확인하십시오.

각 학생:

- attendance_count
- source_event_count
- diary lesson dates
- diary 내용이 실제 유효한지
- 빈 note 여부
- 동일 diary 중복 여부
- 중간입회 여부

를 확인하십시오.

목적:

"일지 2건이면 실제 성장리포트를 만들기에
최소한의 근거가 있는가"

를 데이터로 확인하는 것입니다.

AI 호출은 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 일지 2건의 시간 간격 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

source_event_count = 2인 학생 중
attendance >=3인 학생에 대해

두 일지 날짜 간 간격을 계산하십시오.

예:

08-03 / 08-24
→ 21일

08-20 / 08-27
→ 7일

분포:

같은 주 또는 0~3일
4~7일
8~14일
15일 이상

정도로 제출하십시오.

두 번의 관찰이 너무 붙어 있는 경우와
월초/월말처럼 성장 비교에 유리한 경우를
구분하기 위한 조사입니다.

이번에는 기준으로 사용하지 않습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 기존 데이터 변경 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

절대 하지 말 것:

- GROWTH_REPORT_MIN_SOURCE_RECORDS 3→2 실제 변경
- eligibility_version 변경
- EXCLUDED 실제 처리
- REVIEW_REQUIRED 변경
- migration
- deploy
- AI ENGINE 호출
- correction script APPLY

read-only dry-run만 수행하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 완료보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

아래만 보고하십시오.

1. 기존 3/3 ELIGIBLE 수
2. 신규 3/2 ELIGIBLE 수
3. 증가 인원
4. 신규 3/2 EXCLUDED 수
5. 신규 발급률
6. source_event_count=2 총 학생 수
7. 그중 attendance>=3 학생 수
8. 168명 전체 matrix
9. 새로 ELIGIBLE 되는 학생 수
10. 샘플 학생 검증 결과
11. 일지 2건 날짜 간격 분포
12. 데이터 품질 이상 여부
13. Production 변경 여부

보고 후 멈추십시오.
설계안 B를 승인합니다.

기존 student_class_history를 날짜 기준 학생-반 소속의 권한 소스로 확장하십시오.

단, 아래 조건을 반드시 반영하십시오.

1. effective_date 필수

반이동, 미배정, 신규 배정 API에
effective_date: YYYY-MM-DD를 필수로 추가하십시오.

기본 의미:

- 이전 반 left_at = effective_date
- 새 반 enrolled_at = effective_date

left_at은 제외가 시작되는 날짜입니다.

조회 조건:

enrolled_at <= target_date
AND (left_at IS NULL OR left_at > target_date)

2. Transaction 필수

반이동 시 아래 작업을 단일 DB transaction으로 묶으십시오.

- 기존 student_class_history row의 left_at 갱신
- 새 반 student_class_history row INSERT
- students.assigned_class_ids 갱신
- students.class_group_id 갱신
- schedule_labels 갱신

하나라도 실패하면 전체 rollback하십시오.

3. 반이동 API 검증 기준 통일

현재 화면은

assigned_class_ids.includes(group.id)
OR class_group_id === group.id

로 학생을 표시하지만 서버는 assigned_class_ids만 확인합니다.

서버 검증을 현재 상태와 history 기준으로 통일하십시오.

이동 대상 학생이 effective_date 기준 from_class에 소속돼 있는지
student_class_history로 검증하십시오.

단순 assigned_class_ids 누락만으로 400 처리하지 마십시오.

4. 학생목록 날짜 API

날짜 화면의 학생목록은 앱에서 현재 students 배열을 필터링하지 마십시오.

다음 API를 사용하도록 변경하십시오.

GET /class-groups/:id/students?date=YYYY-MM-DD

서버는 student_class_history 기준으로 조회합니다.

SELECT s.*
FROM students s
JOIN student_class_history h
  ON h.student_id = s.id
WHERE h.class_group_id = :classGroupId
  AND h.enrolled_at <= :targetDate
  AND (h.left_at IS NULL OR h.left_at > :targetDate)
  AND s.deleted_at IS NULL;

pool/tenant 격리도 반드시 포함하십시오.

5. 현재 상태 캐시 유지

students.assigned_class_ids와 class_group_id는 삭제하지 마십시오.

현재 배정 캐시로 유지하되,
과거 날짜 조회의 권한 소스로 사용하지 마십시오.

6. UI 오류 피드백

반이동 실패 시 응답을 묵살하지 마십시오.

- HTTP status
- server error code
- 사용자용 오류 메시지

를 표시하십시오.

성공 시 반드시 학생목록을 다시 조회하십시오.

7. 반이동 버튼 경로

ClassDetailSheet의 반이동 성공 후:

- 모달 닫기
- 선택값 초기화
- 해당 날짜 학생목록 재조회

실패 시 모달을 유지하고 오류 메시지를 표시하십시오.

8. 기존 데이터 Backfill

계정 생성일을 무조건 enrolled_at으로 사용하지 마십시오.

우선순위:

1) 기존 student_class_history
2) 해당 반의 가장 이른 attendance.date
3) 해당 반의 가장 이른 일지/수업 기록
4) 마지막 수단으로 student created_at

추정 데이터와 확정 데이터의 처리 방식을 보고하십시오.

9. 영향 보호

다음 기존 데이터는 변경하거나 삭제하지 마십시오.

- attendance
- class_diaries
- student_notes
- 사진
- 영상
- push 기록
- 통계 원본

10. 필수 테스트

A. 8월 1일 토10 → 화4 이동
- 7월 31일 토10 목록에 학생 존재
- 8월 1일 화4 목록에 학생 존재
- 8월 1일 토10 목록에는 없음

B. 8월 1일 미배정
- 7월 과거 목록 유지
- 8월 1일부터 현재 반에서 제외

C. 반이동 성공
- 목록 즉시 갱신

D. 반이동 실패
- 오류 메시지 표시
- 무반응 금지

E. 출결 3명
- 과거 화면에서 3명 이름 유지

F. 같은 학생이 여러 반에 배정된 경우
- 다른 반 history는 훼손하지 않음

G. tenant 격리
- 타 센터 학생 조회·이동 차단

11. 완료 보고

- 변경 파일
- migration 여부
- transaction 코드
- effective_date 규칙
- history SQL
- 날짜별 학생목록 API
- backfill 결과
- 테스트 결과
- Git commit
- Render Deploy ID
- OTA 필요 여부
- 롤백 방법

먼저 구현하되 Production 배포와 OTA는 테스트 결과 보고 후 별도 승인받으십시오.
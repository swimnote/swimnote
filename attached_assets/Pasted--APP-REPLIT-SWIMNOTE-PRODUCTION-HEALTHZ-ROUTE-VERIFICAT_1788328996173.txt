[보낼 곳: APP REPLIT]

# SWIMNOTE PRODUCTION HEALTHZ ROUTE VERIFICATION + MINIMAL FIX

현재 Render Production:

branch:
deploy-photo-clone

deployed commit:
58319c0f

Render status:
Live / Deploy succeeded

하지만 실제 브라우저 확인:

GET https://swimnote-api.onrender.com/api/healthz

response:
{"status":"ok"}

이번 진단 배포에서 기대했던:

boot_id
uptime_seconds
commit
service_version

필드가 전혀 나오지 않음.

목표:
왜 Production의 /api/healthz가 새 진단 healthz 응답을 사용하지 않는지
실제 route registration chain을 추적하고,
필요하면 최소 수정 후 테스트/커밋/Production branch push까지 수행.

============================================================
1. 먼저 실제 route chain 확인
============================================================

deploy-photo-clone 현재 HEAD 기준으로 확인:

- app.ts에서 healthz가 어디에 정의되어 있는지
- routes/index.ts 또는 별도 health route 존재 여부
- /api prefix mount 구조
- GET /healthz
- GET /api/healthz

각 URL이 실제로 어떤 handler를 타는지 추적

특히 repository 전체에서 검색:

healthz
status: "ok"
status:'ok'
{ status: "ok" }
{status:"ok"}

현재 Production에서 보이는:

{"status":"ok"}

를 반환하는 정확한 source handler를 찾아라.

============================================================
2. 원인 분류
============================================================

다음 중 무엇인지 명확히 보고:

A. /api/healthz가 다른 route에 의해 먼저 처리됨
B. app.ts healthz는 /healthz이고 /api/healthz는 별도 route
C. middleware/router mount order 문제
D. deploy-photo-clone에 별도 legacy health handler 존재
E. 다른 원인

추측 금지.
실제 코드 경로로 확정.

============================================================
3. 수정 원칙
============================================================

필요한 경우에만 최소 수정.

Production에서 기존 /api/healthz contract를 깨지 말 것.

현재:
{"status":"ok"}

를 사용 중인 consumer가 있을 가능성 있으므로
기존 status:"ok"는 유지 가능하면 유지하고
진단 필드를 additive로 추가:

status: "ok"
boot_id
uptime_seconds
commit
service_version

기존 health endpoint가 다른 consumer에 사용된다면
삭제/rename 금지.

새 endpoint를 추가하는 것보다
현재 실제 Production /api/healthz에 additive fields를 넣는 것을 우선.

============================================================
4. 절대 금지
============================================================

client 변경 NO
OTA NO
DB migration NO
RevenueCat NO
Stage8 외부설정 NO
auth behavior 변경 NO
main branch 변경 NO
release/v2.0.0 merge NO

============================================================
5. 테스트
============================================================

반드시 확인:

GET /api/healthz
→ HTTP 200
→ status:"ok" 유지
→ boot_id non-empty
→ uptime_seconds number
→ commit field 존재
→ service_version field 존재

기존 health route가 별도로 있다면
그 route도 기존 contract 유지.

login diagnostics tests:
PASS

safe invalid unified-login:
401 유지

PII logging:
0

============================================================
6. 커밋/푸시
============================================================

수정이 필요했다면:

branch:
deploy-photo-clone

commit 후:
origin/deploy-photo-clone push

Production branch 이외 push 금지.

수정이 필요 없고 단순 URL 오해라면
코드 변경/커밋 금지하고
정확한 health diagnostics URL만 보고.

============================================================
7. 완료보고 형식
============================================================

# SWIMNOTE HEALTHZ ROUTE VERIFICATION REPORT

Production branch:
HEAD Before:
HEAD After:

## ROOT CAUSE

Actual /api/healthz handler:
파일:
라인/route chain:

Why response was {"status":"ok"}:
...

Classification:
A/B/C/D/E

## FIX

Code change required:
YES / NO

Changed files:
...

## CONTRACT

GET /api/healthz response:
...

status:"ok":
YES / NO

boot_id:
...

uptime_seconds:
...

commit:
...

service_version:
...

## LOGIN REGRESSION

diagnostic tests:
...

safe invalid login:
...

PII:
...

## PUSH

deploy-photo-clone:
PUSHED / NOT NEEDED

commit:
...

## FINAL

HEALTH DIAGNOSTICS VERIFIED — READY

or

FIX PUSHED — RENDER DEPLOY REQUIRED

or

BLOCKED
```text
[보낼 곳: APP REPLIT]

# WP3 — REVENUECAT CORE SAFETY + NEW PRODUCTS
# FINAL MASTER WP V6.1
# PLAN FREEZE — 이번 WP 범위 밖 계획 변경 금지

목적:

RevenueCat billing layer를
중복 webhook에 안전하게 만들고,

공식 신규 6개 상품을 정확히 mapping하며,

Paid / Manual / Management Override의 의미를
완전히 분리된 상태로 검증합니다.

이번 작업은 WP3만 수행합니다.

WP5 Push,
WP4 Notice,
WP6 이후 작업은 절대 선행하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
0. 최상위 규칙
━━━━━━━━━━━━━━━━━━━━━━

FINAL MASTER WP는 Freeze 상태입니다.

금지:

- WP 순서 변경
- roadmap 재설계
- 관련 없는 billing 리팩터링
- Subscription architecture 전체 재작성
- Manual Grant UI 구현
- Toykids management override cleanup
- WP5 Push 작업
- WP4 Notice 작업

새로운 실제 P0 blocker를 발견하면
임의로 계획을 바꾸지 말고
evidence만 보고하고 STOP하십시오.

작업 방식:

최소 조사
→ 실제 RevenueCat 현재 구조 확인
→ 필요한 gap만 수정
→ 관련 테스트
→ 완료보고
→ STOP

━━━━━━━━━━━━━━━━━━━━━━
1. Branch 기준
━━━━━━━━━━━━━━━━━━━━━━

작업 branch:

release/v2.0.0

현재 required WP2 commit:

00ab5863

시작 전 최소 확인:

- local release HEAD
- origin/release HEAD
- working tree clean
- 00ab5863 ancestry 포함

이전 WP를 재검수하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 절대 의미 분리
━━━━━━━━━━━━━━━━━━━━━━

다음을 절대 혼동하지 않습니다.

기능 접근:
canonical mode resolver

실제 유료 결제:
RevenueCat / x_paid_entitlement

Super Admin 무상 부여:
x_manual_entitlement

본사/비상 override:
x_management_override

강제 비활성:
x_force_disabled

청구 상태:
RevenueCat subscription state

절대 금지:

mode === 'x'
→ paid subscriber

라는 판단.

Toykids는 현재:

mode=x
paid=false

인 정상 사례입니다.

RevenueCat webhook은 원칙적으로:

x_paid_entitlement

만 관리해야 합니다.

RevenueCat event가:

- x_manual_entitlement
- x_management_override

를 임의 변경하면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
3. 공식 신규 상품
━━━━━━━━━━━━━━━━━━━━━━

공식 상품:

BASE:
SWIMNOTE

X:
X300
X500
X1000

DATA:
DATA100
DATA300

현재 repository의:

- RevenueCat product identifiers
- entitlement mapping
- internal plan key
- subscription plan catalog

을 최소 범위로 확인하고
현재 실제 구조와 맞춰 연결하십시오.

Legacy product/history를 삭제하지 마십시오.

Legacy billing history 보존.

신규 운영 mapping만 공식 6개 기준으로 완결하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. Webhook Idempotency
━━━━━━━━━━━━━━━━━━━━━━

가장 중요한 P0.

현재 RevenueCat webhook 처리 흐름을 확인한 뒤
event_id 기반 중복 방지를 canonical하게 구현하십시오.

목표 구조:

receive
→ event_id dedupe/persist
→ process
→ result 기록

권장 table 개념:

rc_webhook_events

단 repository에 이미 동일 목적 canonical table/event log가 존재하면
중복 테이블 만들지 말고 재사용하십시오.

최소 정보:

- event_id
- event type
- pool/user/reference
- received_at
- processed_at
- processing result
- payload reference 또는 안전한 raw event data

핵심:

event_id UNIQUE

동일 RevenueCat event가
2번 / 3번 / 재전송되어도

실제 billing/X state 변경은 한 번만 일어나야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
5. Migration 규칙
━━━━━━━━━━━━━━━━━━━━━━

Webhook event log를 위해
새 schema가 실제 필요하다면:

신규 runtime DDL 추가 금지.

반드시 canonical migration file로 처리하십시오.

중요:

A. Git에 migration file 존재

B. Staging DB에 적용

C. Production DB에 적용

은 서로 별개입니다.

이번 작업에서 Production migration 자동 실행 금지.

Migration이 필요하면:

1. migration 작성
2. Staging 적용
3. schema verify
4. idempotency/second-run 검증
5. data preservation 확인
6. Production required 여부 보고
7. STOP

Production DB 적용은 사용자 승인 후 별도.

기존 legacy runtime DDL 전체 리팩터링 금지.

━━━━━━━━━━━━━━━━━━━━━━
6. RevenueCat Event 검증
━━━━━━━━━━━━━━━━━━━━━━

최소 실제 처리 확인 대상:

- INITIAL_PURCHASE / purchase
- renewal/resubscribe
- cancellation
- expiration
- restore
- billing issue

현재 repository에서 실제 사용 중인 RevenueCat event type 명칭을 따르십시오.

억지로 새로운 event vocabulary를 만들지 마십시오.

각 event에서 확인:

- paid entitlement 최종값
- plan mapping
- subscription/billing state
- duplicate event safety
- manual unchanged
- management override unchanged

━━━━━━━━━━━━━━━━━━━━━━
7. X 상품 처리
━━━━━━━━━━━━━━━━━━━━━━

X300 / X500 / X1000 구매 시:

공식 internal plan key가 정확히 설정되는지 확인.

Paid purchase는:

x_paid_entitlement=true

를 의미할 수 있으나,

Manual/Management 값을 덮어쓰지 않습니다.

Cancel / Expiry 시:

x_paid_entitlement=false

하지만:

manual=true

이면 X access 유지 가능.

management_override=true

이면 X access 유지 가능.

즉:

RevenueCat event
≠ canonical mode 직접 강제

최종 기능 접근은 기존 canonical resolver가 판단합니다.

━━━━━━━━━━━━━━━━━━━━━━
8. Manual / Override Regression
━━━━━━━━━━━━━━━━━━━━━━

각 주요 RC event 처리 전후:

x_manual_entitlement:
unchanged

x_management_override:
unchanged

검증 필수.

금지:

RC expiry
→ manual=false

RC cancel
→ management_override=false

RC purchase
→ manual=true

RC restore
→ management override 변경

이런 동작은 모두 오류입니다.

━━━━━━━━━━━━━━━━━━━━━━
9. Official Manual X Grant와의 관계
━━━━━━━━━━━━━━━━━━━━━━

이번 WP에서는:

Manual Grant UI를 완성하지 않습니다.

Toykids를 management override에서
manual로 전환하지 않습니다.

그 작업은 WP7.

이번 WP에서 해야 하는 것은:

"Manual X Grant가 RevenueCat billing과 독립적이다"

라는 semantics를 backend/billing layer에서 보장하는 것뿐입니다.

현재 Super Admin manual route에서
config/readiness 관련 기존 문제가 보여도
WP7 범위까지 확장해서 수정하지 마십시오.

단 billing layer가 manual을 변경하는 문제가 있으면
이번 WP에서 수정.

━━━━━━━━━━━━━━━━━━━━━━
10. DATA100 / DATA300
━━━━━━━━━━━━━━━━━━━━━━

공식:

DATA100:
+100GB

DATA300:
+300GB

기본/X quota에 additive.

회원 limit와 무관.

구매 처리 시
현재 repository의 canonical extra storage 구조를 사용하십시오.

현재 direct billing 구조가:

extra_storage_gb += N

형태라면 실제 중복/restore semantics를 반드시 검토하십시오.

중요:

RevenueCat duplicate webhook 때문에
DATA100이 두 번 +100GB 되어서는 안 됩니다.

DATA300도 동일.

━━━━━━━━━━━━━━━━━━━━━━
11. DATA Cancellation / Expiration — USER DECISION GATE
━━━━━━━━━━━━━━━━━━━━━━

이 부분은 아직 사용자 정책 미확정입니다.

절대 임의 구현 금지.

확정된 것:

DATA addon 취소/만료 시
이미 저장된 데이터를 자동 삭제하지 않는다.

미확정:

사용량 > 남은 quota일 때

- 신규 업로드 차단 여부
- grace period
- 기존 파일 열람 허용
- 기존 파일 다운로드 허용
- 재구독 유도 정책
- quota recovery UX

따라서:

DATA cancellation/expiration을 구현해야 하는 지점에 도달하면

현재 코드가 어떤 선택을 요구하는지
정확한 options와 영향만 보고하십시오.

사용자 결정을 받기 전
임의 정책을 넣지 마십시오.

단:

Webhook idempotency,
product mapping,
purchase recognition

등 정책 결정과 무관한 부분은 완료할 수 있습니다.

━━━━━━━━━━━━━━━━━━━━━━
12. Billing Reconciliation 범위
━━━━━━━━━━━━━━━━━━━━━━

RevenueCat ↔ 내부 DB 전체 reconciliation 시스템은
이번 WP에서 새로 만들지 않습니다.

그건 WP20 출시 후 P1.

이번 WP:

- webhook safety
- product mapping
- event correctness
- entitlement separation

까지만.

━━━━━━━━━━━━━━━━━━━━━━
13. Security
━━━━━━━━━━━━━━━━━━━━━━

Webhook endpoint는 현재 RevenueCat verification/auth 구조를 재사용/검증하십시오.

Secret 원문 로그 금지.

RevenueCat secret
Authorization header
service key
raw sensitive identifiers

완료보고에 출력 금지.

raw payload 저장이 필요하면
민감정보 최소화/기존 convention 준수.

━━━━━━━━━━━━━━━━━━━━━━
14. 필수 테스트
━━━━━━━━━━━━━━━━━━━━━━

전체 test suite 실행 금지.

WP3 관련 테스트만.

최소:

A. 신규 SWIMNOTE product mapping

B. X300 purchase
→ paid=true
→ plan=x300

C. X500 purchase
→ paid=true
→ plan=x500

D. X1000 purchase
→ paid=true
→ plan=x1000

E. 동일 X purchase event_id 두 번
→ state mutation 1회

F. cancel
→ paid false 또는 현재 공식 cancel semantics

G. expiry
→ paid=false

H. restore/resubscribe
→ paid 정상 복구

I. billing issue 처리

J. manual=true 상태에서 RC expiry
→ manual remains true

K. management_override=true 상태에서 RC expiry
→ management remains true

L. RC purchase
→ manual unchanged

M. RC event
→ management override unchanged

N. DATA100 purchase
→ +100GB 정확히 1회

O. DATA100 동일 event 중복
→ +100GB 추가 중복 0

P. DATA300 purchase
→ +300GB 정확히 1회

Q. DATA300 duplicate
→ 추가 중복 0

R. unknown/unsupported product
→ 안전하게 처리

S. duplicate webhook 재전송
→ idempotent PASS

Production RevenueCat 실제 결제 생성 금지.

Fake Production purchase 금지.

Test/Staging/mocked event로 검증.

━━━━━━━━━━━━━━━━━━━━━━
15. 기존 데이터 보존
━━━━━━━━━━━━━━━━━━━━━━

절대 삭제 금지:

- historical billing
- legacy subscriptions
- RevenueCat history
- existing plan rows
- past entitlement evidence

신규 구조 때문에 cleanup하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
16. 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

- WP5 Push
- WP4 Notice
- WP6 Cross-pool full audit
- WP7 Manual Grant UI
- Toykids management override OFF
- Growth Report
- Admin Notes
- Pagination
- Version/runtime
- Legacy cleanup
- RevenueCat reconciliation platform
- 신규 대형 billing architecture
- unrelated refactor

━━━━━━━━━━━━━━━━━━━━━━
17. Render / OTA
━━━━━━━━━━━━━━━━━━━━━━

서버 코드 변경 예상.

Render required:
YES/NO 보고.

Replit이 Production Render deploy 하지 마십시오.

DB migration 필요 시:

Production migration:
NOT AUTOMATIC

OTA:
NO

Native:
NO

━━━━━━━━━━━━━━━━━━━━━━
18. 완료조건
━━━━━━━━━━━━━━━━━━━━━━

WP3 PASS 조건:

1. webhook event_id idempotency
2. 공식 6개 상품 mapping
3. X300/X500/X1000 paid 처리 정상
4. cancel/expiry/restore/resubscribe 기본 billing flow 정상
5. manual entitlement unchanged
6. management override unchanged
7. mode를 paid 여부로 사용하지 않음
8. DATA100/300 purchase duplicate-safe
9. historical billing 보존
10. 신규 runtime DDL 없음
11. migration 필요 시 Staging 검증
12. 관련 테스트 PASS
13. Commit SHA 존재

단:

DATA cancellation/expiration quota 정책은
사용자 결정 전 완료조건에서 제외합니다.

정책 결정 필요 상태가 발생하면
그 항목만 HOLD decision으로 보고하고
임의 구현하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
19. 완료보고 형식
━━━━━━━━━━━━━━━━━━━━━━

# WP3 REVENUECAT CORE SAFETY COMPLETION REPORT

Target:
APP

Branch:
...

HEAD before:
...

HEAD after:
...

origin/release HEAD:
...

Commit SHA:
...

Commit message:
...

Files changed:
...

Current RC webhook entry:
...

Webhook authentication/verification:
...

Event log implementation:
...

Event ID unique:
YES / NO

Migration required:
YES / NO

Migration file:
...

Staging migration:
PASS / FAIL / NOT REQUIRED

Production migration:
NOT RUN

Official product mappings:

SWIMNOTE:
...

X300:
...

X500:
...

X1000:
...

DATA100:
...

DATA300:
...

Purchase:
PASS / FAIL

Duplicate event:
PASS / FAIL

Cancel:
PASS / FAIL

Expiry:
PASS / FAIL

Restore:
PASS / FAIL

Resubscribe:
PASS / FAIL

Billing issue:
PASS / FAIL

x_paid_entitlement behavior:
...

x_manual_entitlement unchanged:
PASS / FAIL

x_management_override unchanged:
PASS / FAIL

mode used as billing source:
YES / NO

DATA100 purchase:
PASS / FAIL

DATA100 duplicate-safe:
PASS / FAIL

DATA300 purchase:
PASS / FAIL

DATA300 duplicate-safe:
PASS / FAIL

DATA cancellation/expiration policy:

IMPLEMENTED / USER DECISION REQUIRED / NOT REACHED

If USER DECISION REQUIRED:
- exact decision point
- option A
- option B
- option C
- current data behavior
- implementation impact

Historical billing deleted:
0

Production data changed:
NO

Tests:
...

Test result:
PASS / FAIL

DB changes:
YES / NO

Schema changes:
YES / NO

Render required:
YES / NO

OTA required:
NO

Native binary required:
NO

Remaining blocker:
NONE / USER DECISION REQUIRED / ...

Final:
PASS / HOLD / FAIL

중요:

FINAL MASTER WP는 Freeze 상태입니다.

WP3 범위 밖의 기능을 구현하지 마십시오.

특히:

WP5 Push,
WP4 Notice,
WP7 Manual Grant UI,
Toykids management override cleanup

선행 금지.

새로운 실제 P0 blocker가 발견되면
roadmap 변경 없이 evidence만 보고하십시오.

완료조건 충족 후 즉시 STOP하십시오.
```

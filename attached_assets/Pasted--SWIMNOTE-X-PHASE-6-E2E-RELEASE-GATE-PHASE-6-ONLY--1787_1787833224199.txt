[SWIMNOTE X 관리자 운영허브 — PHASE 6 E2E RELEASE GATE]

승인된 전체 계획의 마지막 단계.

이번 작업은 PHASE 6 ONLY.

목적:
PHASE 2~5에서 구현한 관리자 허브의 수치/상태가
실제 Teacher/Parent/X 기능과 동일한 source of truth를 사용하고 있는지
최종 교차검증.

중요:
새 기능 구현 금지.
새 화면 추가 금지.
새 KPI 추가 금지.
DB/API 설계 변경 금지.
상품화 기능 추가 금지.

이번 PHASE는 검증만 수행.

━━━━━━━━━━━━━━━━━━━━━━
0. RELEASE GATE 원칙
━━━━━━━━━━━━━━━━━━━━━━

PHASE 6는 선택사항이 아님.

아래 E2E equality 중 하나라도 실제로 불일치하면
X 운영허브 전체 완료 판정 금지.

완료 조건:

Admin 수치
=
실제 Teacher/Parent 사용자 기능 상태
=
동일 DB/API source

정적 코드만 비슷하다고 PASS 금지.

가능한 항목은 실제 production 데이터로 비교.

실기기 토큰/테스트 데이터가 없어 실제 비교 불가능한 항목은
"STATIC ONLY"로 명확히 구분.

STATIC ONLY를
E2E PASS로 부르지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
1. 검증 대상 화면
━━━━━━━━━━━━━━━━━━━━━━

Admin X:

1. /(admin)/report-hub
2. /(admin)/diary-hub
3. /(admin)/curriculum-hub
4. /(admin)/x-hub

기존 연결 화면:

5. /(admin)/x-growth

Teacher:

6. 실제 Teacher diary 작성/조회
7. 실제 Teacher Growth Report review flow

Parent:

8. 실제 Parent Growth Report
9. 실제 Parent diary feed
10. 실제 Parent curriculum search

기존 X 설정:

11. x-setup
12. x-subscription

━━━━━━━━━━━━━━━━━━━━━━
2. REPORT EQUALITY — 최우선
━━━━━━━━━━━━━━━━━━━━━━

검증 A:

x-hub의
"이번 달 발행 리포트 N"

=

report-hub의
해당 월 PUBLISHED N

=

Parent가 실제 조회 가능한
published report N

모두 동일해야 함.

공통 기준:

- same swimming_pool_id
- same month
- product_status='PUBLISHED'
- Parent 실제 조회 조건 포함

report-hub가 period_start 기준이고
Parent가 published_at 기준 등
서로 다른 월 기준을 쓰고 있다면
즉시 불일치로 보고.

이번 PHASE에서 임의 수정하지 말고
원인만 보고.

━━━━━━━━━━━━━━━━━━━━━━
3. REPORT DETAIL E2E
━━━━━━━━━━━━━━━━━━━━━━

실제 flow:

Admin report-hub
→ 학생 row
→ x-growth
→ preselect_student_id
→ 해당 학생 자동선택
→ 실제 해당 학생 report 확인

검증:

- report-hub의 student_id
- x-growth selectedStu.id
- 실제 growth report student_id

동일해야 함.

다른 학생이 선택되거나
수동으로 다시 찾아야 하면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
4. TEACHER REVIEW ↔ ADMIN ↔ PARENT
━━━━━━━━━━━━━━━━━━━━━━

가능한 실제 report 1건으로 검증:

Teacher Review:
REVIEW_REQUIRED
→ review/approve
→ APPROVED/PUBLISHED

Admin report-hub:
상태 즉시 동일하게 반영

Parent:
PUBLISHED가 된 동일 report만 조회 가능

검증:

Teacher의 상태
=
growth_reports 실제 상태
=
Admin 상태 chip
=
Parent 노출 여부

━━━━━━━━━━━━━━━━━━━━━━
5. DIARY EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

검증 B:

x-hub의
"오늘 일지 N"

=

diary-hub의
오늘 일지 N

=

Parent feed에서
동일 날짜/동일 pool 기준으로 조회 가능한 diary N

현재 확정 조건:

class_diaries
WHERE lesson_date=today
AND is_deleted=false

Parent 노출:

content/common_content 존재 조건 포함.

중요:
diary-hub는 class_diary_student_notes JOIN 때문에
한 diary에 여러 note가 있을 때 row/count가 중복되지 않는지 실제 확인.

total_diaries는 반드시
DISTINCT class_diaries.id.

━━━━━━━━━━━━━━━━━━━━━━
6. DIARY DETAIL E2E
━━━━━━━━━━━━━━━━━━━━━━

실제 flow:

Admin diary-hub
→ row tap
→ /(teacher)/diary
→ editDiaryId
→ viewOnly=true
→ 실제 동일 diary 표시

검증:

- diary_id 동일
- student note 동일
- common_content 동일
- 사진 동일
- 좋아요/감사 동일
- 댓글 동일

Admin viewOnly에서는:

- 저장 없음
- 수정 없음
- 삭제 없음
- AI 생성/재생성 없음

Teacher 직접 진입에서는
기존 write 기능 그대로 유지.

━━━━━━━━━━━━━━━━━━━━━━
7. DIARY REACTION EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

실제 diary 1건 기준:

Parent가 남긴:

- like
- thanks
- diary_comment

수를 확인.

Admin diary-hub:

reaction_count
=
like + thanks 실제 수

comment_count
=
삭제되지 않은 top-level diary_comment 실제 수

Teacher 알림/반응 화면과
같은 source인지 확인.

━━━━━━━━━━━━━━━━━━━━━━
8. PHOTO EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

실제 diary 사진이 있는 건으로:

Parent/Teacher에서 표시되는 attached photo 수

=

Admin diary-hub photo_count

기준:

photo_assets_meta
journal_id=diary_id
media_status='attached'

동일해야 함.

━━━━━━━━━━━━━━━━━━━━━━
9. CURRICULUM ASSIGNMENT EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

검증 C:

x-hub의
"배정 학생 N"

=

curriculum-hub의
"배정 학생 N"

동일 기준:

student_curriculum_assignments
WHERE is_active=true
DISTINCT student_id
AND current pool

정확히 동일해야 함.

━━━━━━━━━━━━━━━━━━━━━━
10. UNASSIGNED EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub 오늘 확인:

"미배정 학생 N"

=

curriculum-hub
"미배정 학생 N"

동일 기준:

현재 재원:
student_class_history.left_at IS NULL

AND

active assignment NOT EXISTS

동일해야 함.

중복 active class history가 있는 학생은
DISTINCT student 기준인지 확인.

한 학생이 두 반에 있다고
2명으로 세면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
11. CURRICULUM STUDENT DETAIL
━━━━━━━━━━━━━━━━━━━━━━

실제 학생 1명 선택:

curriculum-hub의:

- student_id
- 현재 반
- 담당 선생님
- assignment
- growth_event count
- latest growth_event

를 실제 DB source와 비교.

특히 growth_event:

- is_invalidated=false
- curriculum_item_id 연결

조건 확인.

진행률 %는 없어야 함.

━━━━━━━━━━━━━━━━━━━━━━
12. PARENT AI EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

검증 D:

x-hub
Parent AI 이번 달 검색 N

=

curriculum-hub
Parent AI 이번 달 검색 N

=

실제 event_logs:

category='AI'
metadata.feature='parent_curriculum_search'
current pool
current month

COUNT

동일해야 함.

가능하면 실제 Parent curriculum search를 1회 수행 후
count가 +1 되는지 확인.

가능하지 않으면 STATIC ONLY로 표시.

━━━━━━━━━━━━━━━━━━━━━━
13. PARENT AI USER FLOW
━━━━━━━━━━━━━━━━━━━━━━

실제 flow 확인:

Parent
→ curriculum-chat / curriculum search
→ 실제 search API
→ saveAiTrace
→ event_logs insert
→ Admin curriculum-hub usage
→ x-hub Parent AI count

전체 연결이 실제로 이어지는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
14. X GLOBAL 분리 검증
━━━━━━━━━━━━━━━━━━━━━━

curriculum-hub에서:

교육 커리큘럼:

curriculum_versions
curriculum_items
student_curriculum_assignments
growth_events

와

X Global AI 일지 템플릿:

global_template_sets
diary_templates(scope='x_global')

이 UI/데이터에서 완전히 분리돼 있는지 확인.

검증:

- item_count에 x_global template 섞이지 않음
- assigned student에 global template 영향 없음
- growth_event에 template count 섞이지 않음
- Parent AI count와 global template 섞이지 않음

하나라도 합산되면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
15. X STATUS EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub 상단:

- enabled
- subscription_status
- tier
- started_at
- expires_at

이 기존:

x-subscription

과 동일 source인지 비교.

x-hub에서 별도 판정 로직 때문에
x-subscription과 상태가 다르면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
16. X SETUP EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub 설정 완성도

=

x-setup/status 실제 상태

확인:

- setup_status
- curriculum_status
- website_status
- logo_status
- photos_status

x-hub 표시가
x-setup 실제 데이터와 동일해야 함.

━━━━━━━━━━━━━━━━━━━━━━
17. AI USAGE EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub:

이번 달 AI 호출 N
이번 달 예상 비용 $N

실제 event_logs와 비교.

기준:

pool_id=current pool
category='AI'
created_at >= current month start

calls:
COUNT(*)

cost:
SUM(metadata.estimated_cost_usd)

확인:

- NULL cost 처리
- string→numeric cast
- invalid metadata가 전체 SUM을 깨지 않는지
- 다른 pool 포함 없음

━━━━━━━━━━━━━━━━━━━━━━
18. STORAGE EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub 저장공간

vs

기존 admin storage/settings 화면

동일해야 함.

검증:

- total_bytes
- quota_bytes
- used_pct

같은 source/기준.

photo/video만 합산하고
기존 admin/storage가 messenger/diary/notice/system도 포함하는데
x-hub가 일부만 쓴다면
불일치로 보고.

반드시 실제 기존 storage 계산과 비교.

━━━━━━━━━━━━━━━━━━━━━━
19. OPERATIONS EQUALITY
━━━━━━━━━━━━━━━━━━━━━━

x-hub:

재원 학생 N
선생님 N
연결 학부모 N

실제 기존 Admin 관리 화면과 비교.

학생:
현재 재원 학생 기준

선생님:
현재 pool teacher 기준

연결 학부모:
parent_students.status='approved'
DISTINCT parent_id
current pool 학생 연결 기준

cross-pool parent 포함 금지.

━━━━━━━━━━━━━━━━━━━━━━
20. ROUTE E2E
━━━━━━━━━━━━━━━━━━━━━━

실기기 또는 route-level 확인:

x-hub:

AI 일지
→ diary-hub

성장추적
→ x-growth

AI 리포트
→ report-hub

커리큘럼
→ curriculum-hub

X 세팅
→ x-setup

구독관리
→ x-subscription

오늘확인:
검토대기
→ report-hub

오늘확인:
미배정
→ curriculum-hub

운영:
학생
→ people

선생님
→ people-teachers

학부모
→ parents-list

모든 route 실제 screen 존재 확인.

broken/as any placeholder route 없어야 함.

━━━━━━━━━━━━━━━━━━━━━━
21. SECURITY RELEASE GATE
━━━━━━━━━━━━━━━━━━━━━━

각 신규 API:

/api/admin/reports/summary
/api/admin/diaries/summary
/api/admin/curriculum/summary
/api/admin/curriculum/students
/api/admin/x-hub/summary

모두 검증:

A.
unauthenticated
→ 401/403 JSON

B.
Normal/non-X
→ requireXMode 차단

C.
X pool_admin
→ 허용

D.
client 임의 pool_id
→ 무시

E.
cross-pool 학생/report/diary/curriculum
→ 조회 불가

가능하면 코드/production 둘 다 확인.

━━━━━━━━━━━━━━━━━━━━━━
22. NORMAL MODE REGRESSION GATE
━━━━━━━━━━━━━━━━━━━━━━

반드시 확인:

Normal Admin
Normal Teacher
Normal Parent

기존 기능 영향 없음.

특히:

- Teacher diary write
- Parent diary feed
- Parent reaction/comment
- Parent curriculum search
- 기존 admin people
- 기존 settings
- X setup
- X subscription

PHASE 2~5 때문에
Normal mode 동작이 바뀌면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
23. DATA INTEGRITY CHECK
━━━━━━━━━━━━━━━━━━━━━━

신규 migration은 없었으므로
schema 변경 없음 확인.

확인:

- write flow 변경 없음
- 기존 table UPDATE/INSERT 로직 변경 없음
- 신규 Admin API는 조회 중심
- viewOnly 처리 외 Teacher diary write logic 보존

관리자 조회 때문에
실제 데이터를 수정하는 side effect가 있으면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
24. PERFORMANCE BASIC CHECK
━━━━━━━━━━━━━━━━━━━━━━

상품 출시 전 최소 성능 확인.

report-hub:
- pagination
- 가나다/filter

diary-hub:
- pagination
- date/filter

curriculum-hub:
- FlatList virtualization
- pagination
- 가나다/filter

x-hub:
- 14개 병렬 query

확인:

- 무한 rerender 없음
- 동일 API 반복 폭주 없음
- filter 변경 시 stale append 없음
- pagination duplicate 없음
- x-hub Promise.all 중 1 query error로 전체가 과도하게 자주 죽지 않는지

대규모 최적화 금지.
명백한 release blocker만 보고.

━━━━━━━━━━━━━━━━━━━━━━
25. UI BASIC RELEASE CHECK
━━━━━━━━━━━━━━━━━━━━━━

실기기 기준 확인:

4개 hub 모두:

- header
- back
- loading
- empty
- error
- scroll
- keyboard/search
- filter modal
- long Korean names
- 숫자 0건
- 큰 숫자

레이아웃 깨짐 없는지.

이번 PHASE에서 디자인 리뉴얼 금지.

release blocker만 수정 후보로 보고.

━━━━━━━━━━━━━━━━━━━━━━
26. PHASE 6 결과 등급
━━━━━━━━━━━━━━━━━━━━━━

각 항목을:

PASS
FAIL
STATIC ONLY
NOT APPLICABLE

중 하나로만 판정.

모호한 "대체로 PASS" 금지.

━━━━━━━━━━━━━━━━━━━━━━
27. FAIL 처리 규칙
━━━━━━━━━━━━━━━━━━━━━━

FAIL 발견 시:

임의 수정 금지.

반드시:

[FAIL ID]
[영향]
[원인]
[최소 수정안]
[수정 파일]
[Server/OTA 필요 여부]

만 보고.

사용자 승인 전 수정 금지.

PHASE 추가 금지.

수정이 필요해도
PHASE 6 안에서 release blocker fix로 처리.

━━━━━━━━━━━━━━━━━━━━━━
28. 최종 RELEASE GATE
━━━━━━━━━━━━━━━━━━━━━━

필수 equality:

R1.
x-hub report
=
report-hub
=
Parent report

R2.
x-hub diary
=
diary-hub
=
Parent diary feed

R3.
x-hub curriculum assigned
=
curriculum-hub assigned

R4.
x-hub unassigned
=
curriculum-hub unassigned

R5.
x-hub Parent AI
=
curriculum-hub Parent AI
=
event_logs

R6.
x-hub X status
=
x-subscription

R7.
x-hub setup
=
x-setup

R8.
x-hub storage
=
기존 admin storage

R9.
Admin diary detail
=
실제 diary data
+
viewOnly

R10.
Admin report detail
=
실제 선택 학생 report

하나라도 FAIL이면:

PHASE 6 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
29. 최종 완료보고 형식
━━━━━━━━━━━━━━━━━━━━━━

[PHASE 6 RELEASE GATE]

[ENVIRONMENT]
- production API
- OTA
- tested SHA
- runtimeVersion
- test pool
- 실제 인증 여부

[REPORT E2E]
R1
R10
Teacher review
Parent view

[DIARY E2E]
R2
R9
reaction/comment/photo

[CURRICULUM E2E]
R3
R4
student assignment
growth events

[PARENT AI E2E]
R5

[X STATUS E2E]
R6
R7

[STORAGE]
R8

[OPERATIONS]
students
teachers
parents

[SECURITY]
5개 신규 API

[NORMAL REGRESSION]
Admin
Teacher
Parent

[PERFORMANCE]
report
diary
curriculum
x-hub

[UI DEVICE]
4개 hub

[FAILURES]
없으면 NONE.
있으면 FAIL ID별 작성.

[RELEASE GATE]
R1~R10 각각:
PASS / FAIL / STATIC ONLY

[PHASE 6 FINAL]
PASS / FAIL

중요:
STATIC ONLY가 남아 있으면
그 항목이 실제 출시 차단 검증인지 판단해서
FINAL PASS로 뭉개지 말 것.

코드 수정 없이 검증 먼저.

완료 후 STOP.
상품화 감사 단계는 PHASE 6 완료 후 별도로 진행.
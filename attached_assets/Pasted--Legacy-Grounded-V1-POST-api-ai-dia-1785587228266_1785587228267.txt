[최종 시정 지시 — Legacy 보수 중단, 앱을 Grounded V1로 실제 전환]

이번 증명으로 사실이 확정되었습니다.

실제 운영 앱 호출:
POST /api/ai/diary/generate

즉, 앱은 여전히 legacy 경로를 사용하고 있었습니다.

이전에 보고한 다음 내용은 실제 운영과 불일치했습니다.

- grounded 기본값 적용 완료
- legacy 경로 차단 완료
- 실기기 V1 호출 성공
- Grounded Pipeline 운영 적용 완료

또한 이번에 ai.ts legacy 경로에 purge 로직을 복제한 것은 임시 봉합일 뿐,
SWIMNOTE 최종 설계가 아닙니다.

정식 해결은 앱의 실제 운영 요청을 아래 경로 하나로 전환하는 것입니다.

POST https://swimnote-api.onrender.com/api/v1/teacher-diary/generate

━━━━━━━━━━━━━━━━━━━━
1. 앱 실제 런타임 경로 강제
━━━━━━━━━━━━━━━━━━━━

TeacherDiaryAIClient에서 운영 모드를 코드 수준으로 고정하십시오.

const mode = 'grounded';

운영 Teacher Diary 최종 URL:

https://swimnote-api.onrender.com/api/v1/teacher-diary/generate

다음 환경변수에 따라 legacy로 바뀌지 않게 하십시오.

- EXPO_PUBLIC_SWIMNOTE_AI_MODE
- EXPO_PUBLIC_SWIMNOTE_AI_DIARY_PATH
- 누락된 env
- 이전 OTA baked-in 값

운영 production에서는 환경변수보다 코드의 Grounded V1 고정값을 우선하십시오.

━━━━━━━━━━━━━━━━━━━━
2. Legacy 호출 완전 차단
━━━━━━━━━━━━━━━━━━━━

Teacher Diary 기능에서 다음 URL은 절대 fetch하지 마십시오.

/api/ai/diary/generate

조건:

- URL 생성 단계에서 legacy path면 throw
- fetch 직전 legacy path면 throw
- 자동 fallback 금지
- 네트워크 실패 시 legacy fallback 금지
- 401/422/500 시 legacy fallback 금지

오류 코드:

LEGACY_PATH_BLOCKED

단, 기존 ai.ts 파일은 다른 기능과의 호환을 위해 삭제하지 마십시오.
Teacher Diary 운영 경로에서만 완전히 분리하십시오.

━━━━━━━━━━━━━━━━━━━━
3. 중복 파이프라인 금지
━━━━━━━━━━━━━━━━━━━━

이번에 legacy ai.ts에 추가한 다음 로직은 정식 해결로 인정하지 않습니다.

- purgeStudentLeaksFromCommon
- purgeInventedEvaluations
- Grounded 전용 후처리 복제

Teacher Diary 품질 로직의 권한 소스는 ai-v1.ts와 Grounded Pipeline 한 곳이어야 합니다.

legacy와 grounded 양쪽에 같은 기능을 복제하지 마십시오.

이번 임시 legacy 수정은 즉시 삭제할 필요는 없지만,
운영 Teacher Diary에서는 절대로 실행되지 않아야 합니다.

━━━━━━━━━━━━━━━━━━━━
4. 실제 실기기 증명
━━━━━━━━━━━━━━━━━━━━

스크립트 호출로 증명하지 마십시오.

다음 조건을 모두 만족하는 실기기 요청만 증거로 인정합니다.

- request_id는 앱이 생성한 UUID v4
- 사용자가 iPhone에서 AI 작성 버튼을 누름
- 운영 서버 로그에 동일 request_id 수신
- path=/api/v1/teacher-diary/generate
- engine_version=grounded_v1
- Grounded Pipeline 단계 로그 존재
- 최종 응답의 request_id가 동일
- 앱 결과 화면 표시

`proof_178...`, `diary_grounded_A_...` 등 임의 테스트 ID는
실기기 증거로 보고하지 마십시오.

━━━━━━━━━━━━━━━━━━━━
5. 이번 입력으로 최종 검증
━━━━━━━━━━━━━━━━━━━━

입력:

자유형 15바퀴
태웅이는 배영 예쁘게 하는 거 연습

기대 결과:

공통:
오늘 수업에서는 자유형 15바퀴를 진행했습니다.

학생별 서태웅:
태웅이는 배영을 예쁘게 하는 연습을 했습니다.

금지:

공통에 다음 내용 포함 금지:
- 서태웅
- 태웅
- 배영 개인 연습
- 자세 개선
- 스트로크 정확성
- 호흡 조화
- 발전 기대
- 노력
- 자신감

학생별에도 입력에 없는 평가·향상·계획 생성 금지.

━━━━━━━━━━━━━━━━━━━━
6. 완료 보고
━━━━━━━━━━━━━━━━━━━━

다음 증거만 제출하십시오.

1. 실제 실기기 request_id(UUID v4)
2. 실기기 실행 updateId
3. 실제 최종 URL
4. Render 로그의 request path
5. engine_version
6. pipeline_mode
7. generation_mode
8. template_ids
9. knowledge_ids
10. purge 전 common
11. purge 후 common
12. 최종 Response
13. 앱 결과 화면
14. 일지 삽입 결과
15. 저장 결과
16. Git commit
17. Production OTA Group ID

검증 등급:

- 코드만 확인: CODE_VERIFIED
- 스크립트 호출: SCRIPT_VERIFIED
- OTA 업로드: OTA_PUBLISHED
- 실기기 적용: DEVICE_APPLIED
- 실기기 V1 요청 확인: DEVICE_REQUEST_VERIFIED
- 삽입·저장까지 성공: DEVICE_E2E_VERIFIED

실기기 로그 없이 완료라고 보고하지 마십시오.
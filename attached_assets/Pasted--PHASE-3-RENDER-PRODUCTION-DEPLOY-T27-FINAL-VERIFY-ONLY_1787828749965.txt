[PHASE 3 — RENDER PRODUCTION DEPLOY + T27 FINAL VERIFY ONLY]

현재 PHASE 3 앱/코드/UI/OTA는 PASS.

남은 것은:
1. Render production에 34fe7228 배포
2. T27 production API JSON 확인

뿐입니다.

새 기능 구현 금지.
코드 수정 금지.
DB migration 금지.
OTA 추가 배포 금지.
PHASE 4 시작 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 배포 대상
━━━━━━━━━━━━━━━━━━━━━━

branch:
deploy-photo-clone

SHA:
34fe7228

34fe7228이
cdf85657 서버 구현과
이전 PHASE 2 변경을 모두 ancestor로 포함하는지
마지막으로 확인.

포함되면
Render production 수동 배포.

━━━━━━━━━━━━━━━━━━━━━━
2. Render 수동 배포
━━━━━━━━━━━━━━━━━━━━━━

Render dashboard에서:

34fe7228 기준 manual deploy 실행.

반드시:

BUILD
→ DEPLOY
→ LIVE

완료 후 검증.

중간 상태에서 PASS 보고 금지.

확인:

- build PASS
- startup PASS
- crash/restart 없음
- health PASS

━━━━━━━━━━━━━━━━━━━━━━
3. T27 API ROUTE VERIFY
━━━━━━━━━━━━━━━━━━━━━━

production:

GET /api/admin/diaries/summary

인증 없이 호출.

정상 조건:

HTTP 401 또는 403
content-type application/json
HTML 없음

예상 body:

{
  "success": false,
  "message": "인증이 필요합니다."
}

SPA HTML 반환 시 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
4. AUTHENTICATED 확인
━━━━━━━━━━━━━━━━━━━━━━

가능하면 실제 X 관리자 token으로 호출.

확인:

HTTP 200

response keys:

- summary
- diaries
- class_groups
- pagination

summary 안에 실제:

- total_diaries
- total_notes

존재 확인.

0건이어도 contract 정상이라면 PASS.

━━━━━━━━━━━━━━━━━━━━━━
5. DATA SOURCE 확인
━━━━━━━━━━━━━━━━━━━━━━

production에서 실제 코드 기준:

total_diaries
= DISTINCT class_diaries.id

total_notes
= class_diary_student_notes count

reaction_count
= diary_reactions like + thanks

comment_count
= diary_messages
  diary_comment
  is_deleted=false
  parent_comment_id IS NULL

photo_count
= photo_assets_meta
  journal_id = diary_id
  media_status='attached'

기준 유지 확인.

━━━━━━━━━━━━━━━━━━━━━━
6. SECURITY 확인
━━━━━━━━━━━━━━━━━━━━━━

- unauthenticated → 401/403 JSON
- non-X → requireXMode 차단
- X pool_admin → 200
- client 임의 pool_id 무시
- getAdminPoolId(req) 기반 pool isolation

━━━━━━━━━━━━━━━━━━━━━━
7. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[RENDER FINAL]

- deploy SHA:
- build:
- live:
- health:
- crash/restart:

[T27]

- status:
- content-type:
- HTML 여부:
- route registered:

[AUTHENTICATED]

- status:
- response keys:
- total_diaries:
- total_notes:
- pool isolation:

[PHASE 3 FINAL]

PASS / FAIL

PASS일 경우에도
PHASE 4 자동 시작 금지.

STOP.
SWIMNOTE X — WP6 구현 전면 감사 및 실제 API 통합검증 지시

현재 제출된 WP6 구현 결과를 아직 최종 승인하지 않는다.

모듈 단위 테스트 74개 통과는 확인했으나, 실제 Repository diff·기존 AI 일지 회귀·운영형 API Contract 통합검증 증거가 부족하다.

이번 단계에서는 임의 기능 추가 없이 현재 구현만 전면 감사하고 테스트한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 절대 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 설계 변경 금지
- 신규 기능 추가 금지
- WP7 구현 금지
- growth_events 저장 금지
- 운영 DB 데이터 생성 금지
- 운영 학생·일지 데이터 사용 금지
- Commit 금지
- Push 금지
- OTA 금지
- Render 배포 금지
- POOL_B 상태 변경 금지
- 기존 일반 AI 일지 코드 임의 수정 금지

감사 중 결함을 발견하면 임의 수정하지 말고 먼저 증거를 제출한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. Git 실제 상태
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 원문을 제출한다.

git branch --show-current
git rev-parse HEAD
git status --short
git diff --stat
git diff --name-status
git diff -- artifacts/api-server/src/routes/ai-v1.ts
git diff -- artifacts/api-server/src/config/growth-confidence-config.ts
git diff -- artifacts/api-server/src/config/ai-pricing.ts
git diff -- artifacts/api-server/src/lib/curriculum-confidence.ts
git diff -- artifacts/api-server/src/lib/curriculum-candidate-search.ts
git diff -- artifacts/api-server/src/lib/match-token.ts
git diff -- artifacts/api-server/src/lib/__tests__/wp6.test.ts

신규 파일은 git diff --no-index 또는 전체 원문으로 증명한다.

확인:

- 실제 변경 파일이 보고서와 일치하는가
- 허용 목록 외 코드 파일이 수정됐는가
- attached_assets가 코드 변경에 섞였는가
- package.json 또는 lockfile이 바뀌었는가
- 자동 Commit이 생성됐는가

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 신규 파일 전체 코드 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 파일은 생략·요약 없이 전체 코드를 제출한다.

- growth-confidence-config.ts
- ai-pricing.ts
- curriculum-confidence.ts
- curriculum-candidate-search.ts
- match-token.ts
- wp6.test.ts

단, secret 원문·토큰 원문·개인정보는 출력 금지.

ai-v1.ts는 전체 파일이 너무 길면 다음 WP6 변경 구간을 앞뒤 40줄과 함께 제출한다.

- import 추가부
- contract version validation
- pool mode resolution
- Phase 0
- candidate search
- confidence 계산
- match_token 생성
- Phase 8 response assembly
- 오류 처리
- 503 X_MODE_TOKEN_NOT_CONFIGURED
- 최종 res.json 구간

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. ai-v1.ts 기존 Contract 회귀검수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

변경 전과 변경 후를 비교하여 다음을 증명한다.

- 기존 request body 필드 삭제 없음
- 기존 contract 1.0 요청 정상
- 기존 result.common 동일
- 기존 result.students 동일
- 기존 meta 필드 동일
- request_id 동일
- 기존 오류 응답 변경 없음
- 기존 GPT 호출 횟수 변화 없음
- 기존 template search 변화 없음
- 기존 knowledge search 변화 없음
- 기존 grounding validation 변화 없음
- Diary DB 저장 추가 없음

비교표를 제출한다.

항목 | 변경 전 | 변경 후 | 판정

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 실제 Mode 분기 검수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ai-v1.ts에서 mode를 어떻게 판정하는지 실제 코드를 제출한다.

확인:

- JWT 또는 요청 body의 poolId를 신뢰하지 않는가
- 서버 DB 기준 pool을 결정하는가
- resolvePoolMode 사용 여부
- normal이면 candidate search 미실행
- x_pending이면 candidate search 미실행
- x이면 candidate search 실행
- contract 1.0이면 mode=x여도 WP6 로직 미실행
- contract 1.3 + normal → curriculum_matches:null
- contract 1.3 + x_pending → curriculum_matches:null
- contract 1.3 + x → [] 또는 match 배열

잘못된 pool의 curriculum이 검색될 가능성이 없는지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. student_ref 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 코드를 기준으로 확인한다.

- student_ref가 students.id와 동일한가
- 요청 student_ref를 DB에서 다시 검증하는가
- students.swimming_pool_id가 현재 pool과 일치해야 하는가
- deleted_at IS NULL 조건이 있는가
- 다른 pool 학생을 넣으면 match가 제외되는가
- 미등록 student_ref가 기존 일지 생성 전체를 실패시키지 않는가
- 학생 이름 기반 매핑이 없는가

해당 SQL 또는 Drizzle 조건 원문을 제출한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. Curriculum DB 스키마 정합성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

운영 DB에는 SELECT만 실행한다.

curriculum_versions
curriculum_items
student_curriculum_assignments

각 테이블의 실제 컬럼을 information_schema에서 조회하고, candidate search 코드가 참조하는 모든 컬럼과 비교한다.

제출:

코드 참조 컬럼 | 운영 DB 존재 | 타입 | nullable | 판정

특히 확인:

- swimming_pool_id
- curriculum_version_id
- curriculum_item_id
- student_id
- is_active
- deactivated_at
- archived_at
- title 또는 display_label 근거 컬럼
- description 근거 컬럼
- keywords 또는 confidence 계산 근거 컬럼
- sort_order

코드가 존재하지 않는 컬럼을 참조하면 즉시 중단한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. candidate_id 보안 검수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

확인:

- candidate_id가 randomBytes(16) 기반인가
- candidate_index가 완전히 제거됐는가
- curriculum_item_id가 응답 JSON에 직접 노출되지 않는가
- curriculum_item_id가 token payload 안에는 존재하는가
- candidate_id가 token payload와 일치하는가
- 요청 내 후보마다 고유한가
- 인메모리 candidateMap이 없는가
- 서버 재시작과 무관한가

grep 결과 제출:

grep -RIn "candidate_index" artifacts/api-server/src
grep -RIn "candidateMap" artifacts/api-server/src
grep -RIn "curriculum_item_id" artifacts/api-server/src/routes/ai-v1.ts

응답 노출과 내부 사용을 구분한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. match_token 보안 감사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 전체 코드를 기준으로 확인한다.

- HMAC-SHA256
- MATCH_TOKEN_SECRET 전용
- JWT_SECRET 참조 없음
- 24시간 만료
- token_version
- key_id
- token_id
- issued_at
- expires_at
- pool_id
- student_id
- curriculum_version_id
- curriculum_item_id
- candidate_id
- confidence
- request_id
- matching_algorithm_version
- confidence_config_version

검증 함수에서 확인:

- 토큰 3부분 구조
- Base64URL 파싱
- malformed 처리
- 최대 2048자
- timingSafeEqual
- signature 길이 확인
- 만료 확인
- key_id 확인
- token_version 확인
- expected pool/student/candidate 검증
- secret 미설정 시 lazy fail
- 토큰 또는 payload 로그 없음

`createMatchToken()`과 `verifyMatchToken()`의 payload 타입이 동일한지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. Confidence 감사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

확인:

- reviewThreshold=0.50
- EXPERIMENTAL_V1 주석
- threshold 미달 후보 제외
- threshold 이상은 PENDING_REVIEW
- AUTO_ACCEPTED 할당 없음
- confidence 값 0~1 제한
- NaN 처리
- 빈 keywords 처리
- 문자열 정규화 방식
- token_overlap_v1 계산식
- 동일 단어 중복 처리
- 한국어 입력 처리 가능 여부
- description/title 중 어느 필드를 비교하는지

중요:

confidence 계산에 사용되는 교사 입력 텍스트가 무엇인지 명시한다.

- 전체 원문
- COMMON 분리 텍스트
- 학생별 관찰 텍스트
- GPT 생성 결과
- 기타

학생별 후보에는 해당 학생의 관찰 텍스트만 사용해야 한다.
전체 공통 텍스트만 사용해 모든 학생에게 같은 후보가 붙는 구조인지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 실제 API 통합 테스트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

운영 데이터 변경 없이 수행한다.

가능하면 API 모듈을 테스트 앱으로 mount하고 DB는 mock 또는 transaction rollback을 사용한다.

반드시 실제 Route handler를 통과한다.

테스트:

TC-01 contract 1.0 + normal
기대:
- HTTP 200
- 기존 common/students 구조
- curriculum_matches 필드 없음
- pipeline_version 없음

TC-02 contract 1.0 + x
기대:
- 기존 응답 그대로
- token 생성 없음

TC-03 contract 1.3 + normal
기대:
- HTTP 200
- pipeline_version=v2.0
- curriculum_matches=null

TC-04 contract 1.3 + x_pending
기대:
- HTTP 200
- 기존 일지 생성 유지
- curriculum_matches=null

TC-05 contract 1.3 + x + candidate 없음
기대:
- HTTP 200
- curriculum_matches=[]

TC-06 contract 1.3 + x + candidate 1개
기대:
- curriculum_matches 1개 이상
- candidate_id 존재
- match_token 존재
- curriculum_item_id 응답 미노출
- match_status=PENDING_REVIEW

TC-07 여러 학생 중 일부만 candidate 있음
기대:
- 해당 학생만 match
- 다른 학생 기존 일지 결과 유지

TC-08 다른 pool student_ref
기대:
- 해당 student match 제외
- 전체 일지 생성 실패 없음

TC-09 MATCH_TOKEN_SECRET 미설정 + normal
기대:
- 정상 200

TC-10 MATCH_TOKEN_SECRET 미설정 + x_pending
기대:
- 정상 200

TC-11 MATCH_TOKEN_SECRET 미설정 + x + 실제 candidate 있음
기대:
- HTTP 503
- X_MODE_TOKEN_NOT_CONFIGURED

TC-12 미지원 contract_version
기대:
- 명시된 4xx 오류
- 기존 서버 크래시 없음

각 테스트에 HTTP status와 응답 shape를 제출한다.

LLM 실제 유료 호출은 금지한다.
LLM·template·knowledge 결과는 mock한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 환경변수 실제 적용 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

값은 출력하지 않는다.

Replit:

- MATCH_TOKEN_SECRET: SET
- MATCH_TOKEN_KEY_ID: SET
- 현재 실행 프로세스에서 접근 가능 여부

Render:

- 환경변수 목록에 존재 여부
- 마지막 환경변수 변경으로 자동 배포 또는 재시작이 발생했는지
- 발생했다면 Deploy ID와 상태
- WP6 코드는 아직 Push되지 않았으므로 운영 서버에는 WP6 코드가 없음을 명시

환경변수 설정과 코드 배포를 혼동하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. DB 무변경 증거
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

운영 DB에서 테스트 전후 SELECT:

- growth_events count
- audit_logs count
- curriculum_versions count
- curriculum_items count
- student_curriculum_assignments count
- diary_templates count
- global_template_sets count

전후 차이 0이어야 한다.

기존 POOL_B X Audit 1건은 유지돼야 한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. WP7 위험사항 고정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 내용을 WP7 차단조건으로 기록한다.

growth_events.growth_match_status 기본값은 AUTO_ACCEPTED다.

따라서 WP7 INSERT에서는 반드시 다음 값을 명시한다.

growth_match_status = 'PENDING_REVIEW'

DB 기본값에 의존 금지.

또한:

- match_token 전체 저장 금지
- 검증된 token_id만 match_token_id에 저장
- 같은 token_id 재사용 시 UNIQUE 위반 전에 명시적 409 처리
- match_token 검증 실패 시 INSERT 금지
- 만료 token INSERT 금지
- pool/student/curriculum 불일치 시 INSERT 금지

이번 WP6에서는 구현하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. 최종 제출 형식
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Git 실제 상태
2. 실제 신규·수정 파일
3. 허용 범위 밖 변경
4. 신규 파일 전체 코드
5. ai-v1 변경 구간
6. Contract 전후 비교
7. Mode 분기
8. student_ref 검증
9. DB 컬럼 정합성
10. candidate_id 검수
11. match_token 보안 감사
12. confidence 계산 감사
13. TypeScript 결과
14. 기존 74개 테스트 재실행
15. API 통합 테스트 TC-01~12
16. DB 전후 건수
17. 환경변수 적용 상태
18. Render 자동 재배포 여부
19. 일반모드 회귀 결과
20. 발견된 결함
21. 코드 추가 수정 여부
22. Commit 여부
23. Push 여부
24. OTA 여부
25. Render 배포 여부
26. WP6 최종 승인 가능 여부 자체 판단

결함 발견 시 임의 수정하지 말고 증거만 제출하고 중단한다.
[SWIMNOTE 안전 재구성 버전 배포 승인]

STEP 8 정적 검수 결과를 승인한다.

기준선:
- 651b1a95

작업 브랜치:
- fix/safe-recompose

허용 변경 파일은 정확히 아래 4개뿐이다.

1. artifacts/swim-app/context/auth/SessionContext.tsx
2. artifacts/swim-app/app/(teacher)/makeups.tsx
3. artifacts/swim-app/components/teacher/my-schedule/ClassDetailSheet.tsx
4. artifacts/api-server/src/routes/teachers.ts

목표:

1. error_code 없는 일반 HTTP 403을 pool_deactivated로 오판하지 않음
2. 서버가 명시적으로 pool_deactivated를 반환할 때만 구독 중단 화면 표시
3. 보강 회차 조회 실패 시 실제 HTTP status와 error code 표시
4. 같은 보강·같은 반 중복 요청 차단
5. 빠른 반 전환 시 이전 응답 무시

━━━━━━━━━━━━━━━━━━━━━━
STEP 1. 커밋 전 최종 확인
━━━━━━━━━━━━━━━━━━━━━━

git status --short를 확인한다.

커밋에 포함할 파일은 위 4개만 허용한다.

다음은 절대 포함하지 않는다.

- attached_assets
- 진단 txt/zip/patch
- debug-occ-log endpoint
- debug-occ-errors endpoint
- 공개 비밀번호 query
- 진단 스크립트
- package.json
- pnpm-lock.yaml
- admin.ts
- 구독 상태 하드코딩
- API_BASE 변경

4개 파일 외 변경이 있으면 커밋하지 말고 보고한다.

━━━━━━━━━━━━━━━━━━━━━━
STEP 2. 커밋 및 Push
━━━━━━━━━━━━━━━━━━━━━━

정상 확인 후 커밋한다.

권장 메시지:

fix: prevent 403 subscription misclassification and expose makeup errors

force push 금지.
rebase 금지.
일반 push만 수행한다.

제출:

- commit SHA
- parent SHA
- origin SHA
- 변경 파일 4개 확인

━━━━━━━━━━━━━━━━━━━━━━
STEP 3. Render 서버 배포
━━━━━━━━━━━━━━━━━━━━━━

teachers.ts 변경이 있으므로 서버를 먼저 배포한다.

확인:

- 배포 commit이 신규 commit과 일치
- build 성공
- live 상태
- health check 정상
- migration 없음
- DB schema 변경 없음
- debug 공개 endpoint 없음

제출:

- deployment ID
- 배포 SHA
- 완료 시각
- health 결과

서버가 live 되기 전 OTA 금지.

━━━━━━━━━━━━━━━━━━━━━━
STEP 4. iOS OTA 배포
━━━━━━━━━━━━━━━━━━━━━━

Render 정상 확인 후 production과 preview에 순차 배포한다.

메시지:

hotfix: fix subscription 403 handling and makeup occurrence diagnostics

조건:

- runtimeVersion 1.6.1 유지
- 새 native build 금지
- 현재 안전 재구성 commit 기준으로 번들 생성
- production 먼저 배포
- production 성공 후 preview 배포

제출:

- production Update Group ID
- preview Update Group ID
- 포함 commit SHA
- runtimeVersion
- 배포 시각

━━━━━━━━━━━━━━━━━━━━━━
STEP 5. 구독 실기기 검증
━━━━━━━━━━━━━━━━━━━━━━

앱 완전 종료 후 재실행한다.

먼저 다음을 확인한다.

1. 정상 구독 계정 홈 진입
2. 구독취소 화면 미노출
3. 데이터 삭제 경고 미노출
4. 회원관리 진입
5. 수업관리 진입
6. 스케줄러 진입

여기서 구독취소 화면이 다시 나오면 보강 테스트를 진행하지 않는다.

즉시 제출:

- 실제 적용 Update ID
- production 최신 Update Group ID
- 화면 발생 시점의 API
- HTTP status
- error_code
- Render live SHA

추가 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
STEP 6. 보강 실기기 검증
━━━━━━━━━━━━━━━━━━━━━━

구독 정상 확인 후 아래 경로를 실행한다.

보강 대기
→ 학생 선택
→ 보강반 선택
→ 반 터치

결과 A — 회차 목록이 표시됨

다음을 확인한다.

- 지난 수업
- 오늘
- 예정 수업
- 날짜 정상 표시
- 반 빠른 전환 정상
- 동일 반 연속 탭 중복 요청 없음

이 경우 추가 수정 없이 성공 보고한다.

결과 B — 회차 조회 실패

화면에 표시된 값을 정확히 제출한다.

예:

- 403 / POOL_NOT_FOUND
- 403 / MAKEUP_POOL_MISMATCH
- 404 / CLASS_NOT_FOUND
- 500 / ELIGIBLE_OCCURRENCES_FAILED
- 503 / DB_CONNECTION_LIMIT
- 기타 status/code

다음도 제출한다.

- 사용한 화면: makeups.tsx 또는 ClassDetailSheet
- 선택한 반
- 발생 시각 KST
- Render 로그의 같은 시각 오류

오류 코드 확보 후 추가 수정 없이 멈춘다.

━━━━━━━━━━━━━━━━━━━━━━
STEP 7. 롤백 기준
━━━━━━━━━━━━━━━━━━━━━━

기준:

- 651b1a95
- 정상 롤백 OTA 2c52db66

문제 발생 시 현재 브랜치를 임의 수정하지 않는다.
사용자 승인 후 651b1a95 전체 번들을 다시 발행한다.

━━━━━━━━━━━━━━━━━━━━━━
금지
━━━━━━━━━━━━━━━━━━━━━━

- 구독 활성 상태 하드코딩
- 구독 화면 강제 숨김
- DB 구독값 수정
- API 오류를 빈 배열로 숨김
- 공개 디버그 API 재추가
- 추가 진단 OTA
- 보강 외 기능 변경
- X모드 재개

배포와 실기기 검증 결과를 보고한 뒤 멈춘다.
The user wants me to be careful and systematic: delete old files before creating new ones, avoid conflicts, and make sure everything works without UX issues.

Let me approach this more carefully. I'll:

Read each file fully before modifying
Delete and recreate files rather than editing (per earlier instruction)
Work through each task systematically
Let me start fresh with T001 (already done partially), then move to T002-T010.

The key principle the user wants: no conflicts, no broken UX. So I need to:

Understand the full current state before changing
Delete old files and recreate (not overwrite)
Make sure all references are updated
I've already updated constants/auth.ts to rename pool_admin→대표 and sub_admin→관리자, so now I need to handle org-role-select.tsx which has hardcoded labels that need updating, then continue systematically through the remaining tasks.
The user emphasized deleting and recreating files with significant changes rather than editing them to avoid Metro bundler cache issues, so I'll use bash rm followed by write for major modifications and reserve edit for minor tweaks.

I need to read through the remaining files I haven't examined yet—like parent-signup.tsx, the admin settings screen where I need to hide 운영설정, the parent home screen, and some teacher-related files—to map out all the changes before executing them.

Now I'm mapping out the four main tasks: renaming roles (mostly complete except for the hardcoded label in org-role-select.tsx), restructuring the signup flow to show only 선생님 and 학부모 with the latter redirecting to an invite-only screen, adding a solo versus affiliated branch for teacher signup with a new API endpoint, and converting the parent signup to an invite-only screen.

Beyond that, I need to tackle multi-pool support with a new pool selection screen and login updates, restrict sub-admin access to 운영설정, implement parent invitations through SMS deep links, add parent absence and withdrawal request flows, handle student data deletion on withdrawal, and integrate RevenueCat. Before diving in, I should review the key files to understand the current structure and dependencies.
알겠습니다. 파일을 먼저 충분히 파악한 후에 삭제→새로 생성하는 방식으로 작업하겠습니다. 관련 파일들 먼저 전부 읽겠습니다.
좋습니다. 현재 구조 파악 완료됐습니다. 이제 체계적으로 파일 삭제 후 새로 만들겠습니다.

가장 먼저 signup-role.tsx부터 — 학부모 직접 가입 제거, 2개 옵션으로 변경합니다.
이제 학부모 초대 안내 화면을 만들겠습니다. 동시에 teacher-signup도 새로 작성합니다.

학부모 가입 화면 확인 — 라우터 구조 먼저 파악:
구조 파악 완료. 이제 여러 파일을 동시에 읽겠습니다.
이제 본격 구현을 시작합니다. 파일들을 읽으면서 충돌 없이 진행합니다.

먼저 API backend에서 선생님(solo)→대표 가입 엔드포인트 확인:
API 구조 파악 완료. 이제 순서대로 구현합니다. 동시에 여러 파일 작업합니다.

1. parent-invite-info.tsx 신규 생성 + 2. teacher-signup.tsx 삭제 후 새로 작성:
이제 teacher-signup.tsx를 새로 작성합니다 (소속/무소속 분기 추가):
API 등록 엔드포인트 확인:
완벽하게 파악했습니다. 이제 API 엔드포인트 + 프론트엔드를 동시에 작성합니다.

API: solo-teacher-signup 엔드포인트 추가:
이제 solo-teacher-signup API 엔드포인트를 추가합니다:
이제 teacher-signup.tsx 신규 작성합니다:
계속해서 org-role-select.tsx와 settings.tsx 업데이트합니다:
이제 settings.tsx에서 sub_admin 권한 제한 추가합니다:
계속해서 남은 기능들을 확인하겠습니다:
컴포넌트 위치 확인하고 동시에 나머지 기능 구현합니다:
Expo 포트 문제. 먼저 나머지 구현 마치고 재시작합니다.
기존 구조 파악 완료. 이제 남은 기능들을 구현합니다.

SMS 초대 기능 개선 (MemberParentTab):
phoneUtils.ts 업데이트 (SMS body 지원) + MemberParentTab.tsx SMS 초대 버튼 강화:
MemberParentTab에 SMS 초대 버튼 추가:
이제 T008(학부모 결석/퇴원 신청)을 확인합니다:
T008 구현 - 학부모 결석/연기/퇴원 신청. 우선 API 추가:
이제 API 엔드포인트를 추가합니다. 먼저 admin route에 /parent-requests?student_id= 추가:
API route 버그 수정 (db.execute 반환값):
이제 학부모 요청 화면 생성:
AuthContext 확인:
requests.tsx에서 token 수정:
이제 more.tsx에 수업 요청 메뉴 추가:
T009(퇴원자 소각)와 T010(RevenueCat) 상태 확인:
먼저 T009(퇴원자 소각 API)를 추가하고, RevenueCat 통합 설정합니다. 병렬로 실행:
T009 퇴원자 소각 API 추가 + RevenueCat 초기 설정 병렬 처리:
T009 퇴원자 소각 API 추가:
퇴원자 소각 API + RevenueCat 상태를 병렬 확인:
RevenueCat 연동 상태 확인:
RevenueCat 연동 신청합니다:
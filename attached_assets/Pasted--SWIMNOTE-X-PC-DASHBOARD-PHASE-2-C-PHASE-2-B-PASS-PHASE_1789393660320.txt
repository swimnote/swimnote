[SWIMNOTE X PC DASHBOARD — PHASE 2-C
반 · 전체 스케줄 · 보강 실제 구현]

PHASE 2-B 회원관리 PASS.

지금부터 PHASE 2-C를 구현합니다.

이번 WP 한 번에:

1. 전체 스케줄
2. 반 관리
3. 반 생성/수정
4. 담당 선생님/요일/시간/정원 관리
5. 반 회원 확인
6. 보강 현황
7. 보강 배정
8. 보강 취소/상태처리
9. 필요한 React Query 동기화
10. 필수 QA

까지 완료하십시오.

별도 설계보고 없이
실제 Backend contract 확인 → 구현 → QA → 짧은 완료보고
순서로 바로 진행합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0. 안전규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Repository:
https://github.com/swimnote/swimnote

현재 작업중인 Dashboard 변경사항을 보존하십시오.

현재 branch를 임의 변경하지 마십시오.
작업 시작 시 실제 branch만 확인.

금지:

git push
Render deploy
Render 설정 변경
Static Site branch 변경
Legacy /admin 수정
Marketing website 수정
AI ENGINE 수정
DB migration
임의 신규 schema 생성

이번 WP는 local implementation + QA까지.

기존 APP/API Production 동작을
깨지 않도록 최소 변경 원칙.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 절대 원칙 — API 추측 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 2-A에서 endpoint 추측으로 버그가 발생했으므로
이번에는 반드시:

Backend route
→ request schema
→ response schema
→ 기존 APP/Legacy 사용법
→ Dashboard 연결

순서로 확인하십시오.

아래에 적힌 endpoint 이름도
실제 코드와 다르면 실제 contract를 우선합니다.

존재하지 않는 endpoint를 가정해서
frontend를 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. 전체 스케줄
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Route:

/admin/schedule

Placeholder를 실제 화면으로 교체.

목표:

수영장 관리자가
전체 반의 주간 수업 스케줄을
한 화면에서 빠르게 확인할 수 있어야 합니다.

기본은 PC용 WEEK VIEW.

월~일 또는
실제 운영 요일 기준 열.

시간대 기준으로
수업 반을 읽기 쉽게 배치.

모바일 앱 캘린더를 확대해놓은 UI 금지.

PC 관리프로그램처럼
정보밀도가 적절한 주간 스케줄로 구현.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 스케줄에 표시할 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

각 수업 블록에는
실제 API가 제공하는 범위에서:

반 이름
수업 시간
담당 선생님
현재 인원 / 정원

표시.

예:

초급 A
15:00 - 15:50
김선생
4 / 5

실제 데이터가 없는 값은
가짜로 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 스케줄 필터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최소:

선생님
반

필터.

필요하면 요일 필터 가능.

필터 선택 후 즉시 반영.

"전체" 복귀 가능.

Dropdown은:

선택
→ 즉시 닫힘

Outside click
→ 닫힘

ESC
→ 닫힘

Route 이동
→ DOM 제거

ghost overlay 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 스케줄에서 반 상세 진입
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

수업 블록 클릭:

해당 반 상세 또는
반 편집 Drawer/Dialog로 연결.

클릭 안 되는 장식 블록 금지.

최소:

반 이름
담당 선생님
요일
시간
정원
현재 회원
상태

확인 가능.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 반 관리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Route:

/admin/schedule/classes

실제 반 목록 DataTable 구현.

표시 후보:

반 이름
요일
시간
담당 선생님
현재 인원
정원
상태
관리

실제 contract에 있는 field만 사용.

검색/필터:

반 이름
담당 선생님
요일

가능한 범위에서 제공.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 반 생성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 Backend의
반 생성 API가 실제 존재하면 연결.

신규 반 등록 UI 제공.

입력값은
실제 Backend 필수값 기준.

예상 후보:

반 이름
요일
시작 시간
종료 시간
담당 선생님
정원

하지만 실제 schema 확인 후 확정.

임의 필수값 생성 금지.

저장:

validation
→ loading
→ duplicate submit 차단
→ API
→ canonical result
→ classes/schedule query invalidate

새로고침 없이 즉시 반영.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 반 수정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 API를 이용해:

반 이름
요일
시간
담당 선생님
정원
기타 실제 허용 필드

수정.

수정 성공 후:

반 목록
전체 스케줄
관련 회원 상세

필요 query invalidate.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 담당 선생님
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

담당 선생님 선택은
기존 teacher API에서
현재 pool 선생님만 가져옵니다.

다른 pool teacher 노출 금지.

main teacher / co-teacher 구조가 실제 존재한다면
현재 API가 지원하는 범위까지만 구현.

새로운 역할 구조 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 반 정원
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정원 표시:

현재 인원 / 최대 정원

예:
4 / 5

정원 초과 상태가 실제 데이터에서 발생하면
시각적으로 확인 가능하게 하되
과도한 경고색 사용 금지.

정원 수정은
기존 API contract가 허용하는 경우에만.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 반 회원 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

반 상세에서
현재 배정된 회원 목록 표시.

회원 이름 클릭 시
가능하면 기존 회원 Detail Drawer 또는
회원 route로 연결.

반 상세 안에서
별도 회원 DB/복제 데이터 만들지 마십시오.

PHASE 2-B의 동일 student source 사용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 반 회원 배정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 2-B에서 이미 구현한
학생 반 배정 contract가 있으므로
중복 API/로직 만들지 마십시오.

반 화면에서도 배정 기능이 필요하면
동일 mutation helper 또는 동일 endpoint 재사용.

반 기준으로 학생 추가/제거 API가
기존에 안정적으로 있으면 사용 가능.

없으면 이번 WP에서
불필요하게 신규 endpoint를 만들지 말고
기존 회원 상세의 반 배정 기능을 사용하도록 연결.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. 보강 현황
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Route:

/admin/makeups

Placeholder를 실제 업무 화면으로 교체.

기존 보강 API를 먼저 조사.

PHASE 0에서 확인된 기능:

- pending
- eligible classes
- assign
- cancel
- expire

실제 현재 route/schema를 확인해서 사용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. 보강 목록 UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PC용 DataTable 중심.

실제 API에 존재하는 범위에서:

회원
원래 반
결석일
보강 상태
보강 가능 기한
배정된 보강반
보강일
관리

표시.

가짜 field 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. 보강 상태
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 서버 status 값을
그대로 source of truth로 사용.

UI에서 임의 status 체계 만들지 마십시오.

예:

미배정
배정
완료
취소
만료

등 실제 API 상태를
사용자에게 읽기 쉬운 한국어로 표시.

서버 상태와 UI 상태가
서로 다르면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. 보강 필터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최소:

상태
회원 검색

가능하면:

원래 반
보강 예정일

도 실제 API 지원 범위에서 제공.

기본 화면은
관리자가 처리해야 할 보강이
먼저 보이도록 구성.

단 정렬 기준도
현재 API/데이터 구조를 확인해서 구현.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
17. 보강 배정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Route:

/admin/makeups/assign

또는 보강 목록에서
[배정] 클릭 후 Drawer/Dialog.

기존 eligible classes API를 이용.

선택한 보강 대상에 대해
실제로 가능한 반만 표시.

반 이름만 보여주지 말고
가능하면:

반 이름
날짜
시간
현재 인원 / 정원

표시.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
18. 보강 배정 안전성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

보강 배정 시:

회원
배정 대상 반
날짜/시간

을 사용자가 최종적으로 확인할 수 있어야 합니다.

저장 버튼:

loading
중복 클릭 방지.

성공 후:

보강 목록
Home pending makeup stats
관련 class/member query

invalidate.

새로고침 없이 반영.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
19. 보강 취소
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 cancel endpoint가 있으면 구현.

Confirm 필수.

예:

"김OO 회원의 9월 18일 보강 배정을 취소합니다."

[취소] [보강 취소]

실제 어떤 기록이 변경되는지
명확한 문구 사용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
20. 만료 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 서버에
expire 처리 contract가 있고
관리자가 직접 처리하도록 되어 있다면 연결.

서버에서 자동 만료되는 구조라면
PC에서 별도 수동 버튼 만들지 마십시오.

실제 정책 우선.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
21. 보강 정원 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

eligible class / assignment API가
정원을 검증하는지 확인.

Frontend 숫자만 믿고
정원 초과 배정을 허용하면 안 됩니다.

Server validation이 authoritative.

API에서 정원 초과를 거절하면
사용자에게 정확한 오류 표시.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
22. 날짜/시간
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

날짜/시간 표시를 통일.

한국 사용자 기준으로 읽기 쉽게:

9월 18일 (금)
15:00

등.

DB/API의 timezone 값을
임의 UTC 변환해서 하루 밀리는 문제 금지.

기존 APP에서 사용하는
날짜 처리 방식을 참고.

Invalid Date가 UI에 노출되지 않게 방어.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
23. 상태별 Empty / Error
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

반 없음:
"등록된 반이 없습니다."

스케줄 없음:
"해당 조건의 수업이 없습니다."

보강 없음:
"처리할 보강이 없습니다."

API failure:
"불러오지 못했습니다."

Empty와 Error 반드시 구분.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
24. Loading
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

전체 페이지 blank spinner만 오래 보여주지 마십시오.

Table/schedule 영역 loading state.

Mutation button loading.

중복 클릭 차단.

데이터 로드 중 기존 데이터가 있으면
React Query 방식으로 자연스럽게 유지 가능.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
25. Data Sync
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 WP에서도 SSE는 아직 구현하지 않습니다.

PC에서 mutation 성공하면:

schedule
classes
students
makeups
dashboard-stats

중 실제 영향받는 query만 invalidate.

전역 invalidate 남발 금지.

APP → 열린 PC 자동 실시간 반영은
PHASE 2-G에서 구현.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
26. HOME 연계
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 2-B에서 Home에 연결한:

오늘 출석/수업 관련 summary
미배정 보강

등 실제 stats가
이번 mutation 후 낡아있지 않도록
관련 query invalidate.

Home endpoint contract 자체를
다시 뜯지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
27. 디자인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 2-A/2-B 디자인 유지.

White / Off-white / Gray 중심.

X Accent 절제.

스케줄 반마다
무지개 색상 지정 금지.

구분이 꼭 필요하면
neutral + subtle accent 사용.

반 관리/보강은
DataTable 중심.

과도한 카드 UI 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
28. 위험 Action
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

반 삭제/비활성
보강 취소
기타 되돌리기 어려운 동작

은 반드시 Confirm.

Confirm 문구에:

대상
행동

명시.

"정말 삭제하시겠습니까?"
같이 대상 없는 문구 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
29. 반 삭제
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 API에 실제 반 삭제/비활성 기능이 있다면
현재 APP 운영 정책을 확인.

회원이 배정된 반을
무조건 hard delete하는 UI를 만들지 마십시오.

현재 서버가 soft delete/status 방식을 쓰면
그대로 사용.

안전한 contract가 불명확하면
이번 WP에서 삭제 UI 생략.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
30. Backend 변경
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

우선 기존 API로 구현.

기존 Backend contract에
명백한 P0/P1 버그가 있어
정상 기능 구현이 불가능한 경우에만:

최소 API 수정 가능.

단:

DB schema 변경 금지
migration 금지
새로운 병렬 데이터 모델 금지

API를 수정했다면
완료보고에 반드시 명시.

단순히 frontend 구현이 편하다는 이유로
API 신규 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
31. 이번 WP에서 하지 않는 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

출결 전체 관리
성장리포트
커리큘럼
일지/피드
선생님 관리
매출/정산
Excel Import
Realtime SSE
Render 통합

이번에 넘어가지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
32. QA — 크레딧 절약형
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

전체 Dashboard 재검사하지 마십시오.

이번 변경만 확인.

필수:

A.
Dashboard production build PASS

B.
swimnote-web regression build PASS

C.
전체 스케줄 데이터 로딩

D.
반 목록 로딩

E.
반 생성 contract

F.
반 수정 contract

G.
담당 선생님 선택

H.
반 회원 표시

I.
보강 목록

J.
eligible class 조회

K.
보강 배정

L.
보강 취소

M.
mutation 후 query invalidate

N.
Empty / Error 구분

O.
Drawer/Dialog 닫은 후 overlay 잔존 없음

실계정이 없어 write test가 불가능하면
Production 가짜 데이터 생성 금지.

READ E2E:
PASS / NOT TESTED

WRITE E2E:
PASS / NOT TESTED

구분.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
33. 브라우저
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Browser 도구 사용 가능하면 실제:

스케줄
반 관리
보강

3개 화면만 확인.

모든 Dashboard 화면 재검증 금지.

Browser unavailable이면:

BROWSER E2E NOT AVAILABLE

한 줄이면 충분.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
34. Build
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

반드시 마지막에:

pnpm --filter @workspace/swimnote-dashboard run build

PASS.

그리고:

pnpm --filter @workspace/swimnote-web run build

regression PASS.

수정하지 않은 다른 전체 monorepo를
불필요하게 전부 빌드하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
35. 완료보고 — 짧게
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

아래 형식만 사용.

A. BUILD

Dashboard:
Web:


B. API CONTRACT

Schedule:
Classes:
Teachers:
Makeups:
Eligible:
Assign:
Cancel:


C. SCHEDULE

주간 화면:
필터:
반 클릭:
표시정보:


D. CLASSES

목록:
생성:
수정:
담당선생님:
정원:
회원목록:
삭제/비활성:


E. MAKEUPS

목록:
필터:
배정:
취소:
만료:
정원검증:


F. DATA SYNC

invalidate:


G. QA

Browser:
Read E2E:
Write E2E:


H. BUG

P0:
P1:
Blocker:


I. GIT

branch:
API 수정 여부:
push:
deploy:
범위 밖 변경:


J. 판정

PASS / HOLD / FAIL

이유 3줄 이내.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
36. PASS 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 전체 스케줄 실제 데이터 연결

[ ] 스케줄 필터

[ ] 반 클릭/상세

[ ] 반 목록

[ ] 반 생성

[ ] 반 수정

[ ] 담당 선생님 연결

[ ] 정원 표시/수정

[ ] 반 회원 확인

[ ] 보강 목록

[ ] 보강 검색/필터

[ ] eligible class 조회

[ ] 보강 배정

[ ] 보강 취소

[ ] 실제 서버 status 사용

[ ] mutation 즉시 UI 반영

[ ] Empty/Error 구분

[ ] ghost overlay 없음

[ ] 가짜 API 없음

[ ] 가짜 데이터 없음

[ ] Dashboard build PASS

[ ] Web regression PASS

[ ] push 없음

[ ] deploy 없음

완료 후 STOP.

다음은 PHASE 2-D
AI 성장리포트입니다.
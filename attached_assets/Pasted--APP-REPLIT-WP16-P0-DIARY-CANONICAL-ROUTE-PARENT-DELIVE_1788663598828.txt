[보낼 곳: APP REPLIT]

# WP16 P0 — DIARY CANONICAL ROUTE + PARENT DELIVERY FINAL REPAIR
# 현재 WP16 상태 = FAIL
# 코드상 PASS 보고 금지
# 동일 Diary edit 화면이 2개 존재하는 실제 Production 증상 확인
# Parent 전달도 실제 Production에서 FAIL
# Diary 관련 경로만 전수조사
# 관련 없는 repo audit 금지
# 수정 완료 시 iOS production-v2 OTA까지 자동 수행
# Render Production deploy는 절대 수행 금지

━━━━━━━━━━━━━━━━━━━━━━
0. USER REAL DEVICE FACTS
━━━━━━━━━━━━━━━━━━━━━━

현재 실제 iPhone 결과:

A.
이미 작성된 9/3 Diary를 눌렀을 때
서로 다른 "일지 수정" 화면 2개가 존재.

화면 1:
날짜 변경 가능

화면 2:
날짜 변경 불가

같은 기존 Diary edit 기능인데
진입경로에 따라 다른 UI/로직이 열림.

이 상태는 허용하지 않습니다.

B.
Teacher가 Diary를 새로 작성해도
Parent에게 전달되지 않음.

C.
Teacher가 기존 Diary를 수정/재작성해도
Parent에게 전달되지 않음.

즉 현재 핵심 문제:

Teacher Diary
→ Save/Update
→ Parent Delivery

라인이 실제 Production에서 끊겨 있음.

기존 보고의:
"코드상 PASS"
"API 경로 정상"
"Feed rendered YES"

는 사용자 실기기 결과와 충돌하므로
이번 작업의 truth로 사용 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. CREDIT CONTROL
━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 금지:

- APP 전체 repo audit
- AI Engine 재조사
- contract_version 재조사
- X architecture 재조사
- Growth Report 전체 조사
- RevenueCat 조사
- unrelated navigation 조사
- 동일 grep 반복
- 기존 PASS navigation 재설계
- 대규모 rewrite

Diary 관련 route/component/API만 조사.

조사는 최초 1회 dependency map 작성 후
그 map으로 끝까지 수정.

━━━━━━━━━━━━━━━━━━━━━━
2. DIARY EDIT ENTRY POINT 전수조사
━━━━━━━━━━━━━━━━━━━━━━

같은 기존 Diary를 수정하는
모든 실제 entry point를 전부 찾으십시오.

검색 범위:

Teacher Diary 관련:
- diary.tsx
- diary index/history
- diary detail
- editDiaryId
- subView='edit'
- router.push
- router.replace
- router.back
- openEditDiary
- handleEdit
- diary list item onPress
- schedule에서 기존 diary open
- past diary에서 open
- 작성 완료 후 재진입

각 entry를 표로 정리:

ENTRY
source screen
route
params
component
edit state source
date editable YES/NO
save handler
update endpoint
return destination

"수정 화면 2개"가
정확히 어떤 2개 경로인지 확정.

━━━━━━━━━━━━━━━━━━━━━━
3. CANONICAL EDIT SCREEN 하나로 통일
━━━━━━━━━━━━━━━━━━━━━━

최종적으로 기존 Diary 수정은
하나의 canonical edit path만 사용해야 합니다.

같은 diaryId를 수정하는데:

- 날짜 변경 가능 화면
- 날짜 변경 불가 화면

두 개를 유지하지 마십시오.

기준:

기존 수업 Diary의 lesson_date는
기존 수업과 연결된 기록이면
일반 edit에서 임의 변경하지 않는 방향을 우선 검토.

단 제품의 기존 확정 정책/기능에서
날짜 수정이 실제로 필요한 별도 기능이 있다면
그 기능과 일반 Diary edit를 명확히 분리.

중요:
같은 "일지 수정" action이
두 UI로 랜덤하게 갈라지면 FAIL.

목표:

Past Diary
→ Diary 선택
→ canonical Edit Screen

Schedule에서 existing Diary 선택
→ 동일 canonical Edit Screen

기타 existing Diary open
→ 동일 canonical Edit Screen

동일:
- media 처리
- text 처리
- save/update
- validation
- return path

━━━━━━━━━━━━━━━━━━━━━━
4. 중복 구현 제거 원칙
━━━━━━━━━━━━━━━━━━━━━━

두 edit screen 중
하나가 legacy/duplicate라면:

- 실제 entry에서 더 이상 사용하지 않게 제거/차단
- 공통 handler/component 재사용
- 불필요한 duplicate save/update path 제거

하지만:
안전한 기능까지 대규모 삭제 금지.

"두 개 다 유지하면서 분기 수정"보다
하나의 canonical path가 목표.

보고에 반드시:

LEGACY_EDIT_PATH_FOUND:
YES / NO

LEGACY_EDIT_PATH_DISABLED:
YES / NO

CANONICAL_EDIT_ROUTE:
...

CANONICAL_EDIT_COMPONENT:
...

━━━━━━━━━━━━━━━━━━━━━━
5. CREATE / EDIT 저장 API 실제 분리 확인
━━━━━━━━━━━━━━━━━━━━━━

Teacher CREATE와 EDIT가
실제로 어떤 endpoint를 쓰는지 확인.

CREATE:

screen state
→ save handler
→ request method
→ endpoint
→ body
→ response
→ diaryId

EDIT:

screen state
→ update handler
→ request method
→ endpoint
→ body
→ response
→ same diaryId

반드시 확인:

- create 시 diaryId 생성
- edit 시 새 diaryId를 잘못 생성하지 않는지
- edit가 DELETE+CREATE처럼 동작하지 않는지
- edit 시 student diary relation 유실 없는지
- edit 시 notification/parent visibility relation이 깨지지 않는지

━━━━━━━━━━━━━━━━━━━━━━
6. SAME DIARY ID TRACE — 필수
━━━━━━━━━━━━━━━━━━━━━━

실제 테스트 Diary 하나를 기준으로
동일 diaryId를 끝까지 추적.

Diary 전문/개인정보는 마스킹.

STEP 1
Teacher save/update response

HTTP:
...

diaryId:
...

STEP 2
DB/API에서 exact diaryId 존재:
YES / NO

STEP 3
class_group_id:
present / absent

STEP 4
lesson_date:
...

STEP 5
student relations:
count ...

STEP 6
Parent relation:
count ...

STEP 7
Parent Diary API exact diaryId:
YES / NO

STEP 8
Parent unified feed exact diaryId:
YES / NO

STEP 9
Parent rendered UI:
YES / NO

여기서 실제로 사라지는 지점을 확정.

━━━━━━━━━━━━━━━━━━━━━━
7. PARENT DELIVERY — 기존 fallback 다시 추측 금지
━━━━━━━━━━━━━━━━━━━━━━

이전 수정:

student_class_history
UNION
students.class_group_id

fallback이 있다고 보고됐음.

하지만 실제 Production에서는
여전히 Parent에게 Diary가 안 보임.

따라서 이번에는
"query가 맞다"로 끝내지 마십시오.

실제 exact diaryId 기준으로:

GET /parent/students/{studentId}/diary

를 확인.

HTTP:
...

exact diaryId:
YES / NO

CASE P1
API에 없음
→ server query/save relation 문제

CASE P2
API에 있음
→ client normalize/merge/render 문제

CASE P3
studentId 자체가 잘못됨

CASE P4
lesson_date/class relation 때문에 제외

CASE P5
parent/student relation mismatch

CASE P6
create/update 후 cache/invalidation 문제

CASE P7
기타 — exact evidence 필요

하나로 확정.

━━━━━━━━━━━━━━━━━━━━━━
8. CREATE와 EDIT 모두 Parent Delivery 검증
━━━━━━━━━━━━━━━━━━━━━━

두 경우를 따로 테스트.

TEST A — NEW CREATE

Teacher
→ 새 Diary 작성
→ save
→ diaryId A

Parent API:
A 있음 / 없음

Parent Feed:
A 보임 / 안보임

TEST B — EXISTING EDIT

Teacher
→ 기존 Diary 수정
→ update
→ same diaryId B

Parent API:
B 최신 내용 / 이전 내용 / 없음

Parent Feed:
B 최신 내용 / 이전 내용 / 없음

중요:

"새로 작성하면 전달되는데 수정하면 안 됨"
또는
"수정하면 보이는데 신규 작성은 안 됨"

둘 다 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
9. PARENT FEED MERGE 실제 단계 추적
━━━━━━━━━━━━━━━━━━━━━━

Parent API에 diaryId가 존재하는 경우:

API response
→ normalization
→ unified feed merge
→ dedupe
→ sort
→ React state
→ rendered item

각 단계 exact diaryId:

API:
YES

normalized:
YES / NO

merged:
YES / NO

dedupe 후:
YES / NO

state:
YES / NO

render:
YES / NO

Diary와 Growth Report의
key collision/dedupe collision 확인.

예:
둘 다 id만 key로 써서
다른 item type과 충돌하는지 확인.

필요하면 canonical key:
type + id

같은 기존 패턴 사용.

대규모 feed rewrite 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. PARENT FEED REFRESH
━━━━━━━━━━━━━━━━━━━━━━

새 Diary 생성 또는 수정 후

Parent 앱에서:

현재 feed 화면에 계속 머물러 있는 경우
→ 최소 기존 refresh policy에 맞게 최신 반영

다른 화면 갔다 돌아오는 경우
→ focus 시 refetch

앱 재시작 필요하면 FAIL.

확인:

useFocusEffect 실제 request fired:
YES / NO

stale closure:
YES / NO

studentId stale:
YES / NO

query params:
correct / incorrect

중복 fetch는 제거.

━━━━━━━━━━━━━━━━━━━━━━
11. UPDATE 이후 Parent cache 문제 확인
━━━━━━━━━━━━━━━━━━━━━━

특히 edit/update에서 확인:

서버 response는 최신인데
Parent에는 이전 내용이 남는 경우:

- client cache
- memo
- feed dedupe
- key reuse
- updated_at handling

확인.

필요하면 기존 query invalidation/focus reload 패턴만 사용.

새 caching library 금지.

━━━━━━━━━━━━━━━━━━━━━━
12. NOTIFICATION은 Parent Delivery와 동일 diaryId만
━━━━━━━━━━━━━━━━━━━━━━

Parent Feed 정상화 후
같은 diaryId 기준으로 확인.

Diary save/update
→ notification row
→ studentId
→ diaryId
→ deep_link

중요:

Parent API에 없는 Diary인데
Notification만 생성하면 FAIL.

Push/Notification recipient 조건과
Parent Diary visibility 조건을
동일 의미로 맞추십시오.

CREATE:
알림 정책에 맞게 발송.

EDIT:
기존 제품 정책상 수정 시 재알림 여부 확인.
정책이 없으면 임의로 새 정책 만들지 말고
현재 동작 유지.

━━━━━━━━━━━━━━━━━━━━━━
13. ADMIN도 같은 canonical diaryId 확인
━━━━━━━━━━━━━━━━━━━━━━

동일 diaryId가:

Teacher
Parent
Admin

세 곳에서 같은 기록이어야 함.

Admin summary/detail에서
same diaryId 확인.

새 Diary:
Admin YES

수정 Diary:
Admin 최신 내용 YES

새로운 Admin 기능 만들지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
14. MEDIA PREVIEW와 canonical edit 결합 검증
━━━━━━━━━━━━━━━━━━━━━━

두 edit screen이 하나로 통일되면
media behavior도 동일해야 함.

CREATE:
photo preview
video preview

EDIT:
existing photo/video
new photo/video
remove media

모두 canonical component/path에서 처리.

화면 A에서는 되고 화면 B에서는 안 되는 상태 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. AI Diary도 canonical write/edit path에만 연결
━━━━━━━━━━━━━━━━━━━━━━

AI ENGINE은 정상 확정.

APP canonical create/write에서:

AI result
→ common/student state
→ save

를 유지.

중복 edit/write screen 때문에
한쪽은 AI 결과를 사용하고
다른 쪽은 raw/manual path를 쓰는지 확인.

단 AI Engine 재조사 금지.

이번 작업의 핵심은
duplicate Diary UI/path가
state/save/delivery를 갈라놓는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
16. SERVER ROOT CAUSE 발견 시
━━━━━━━━━━━━━━━━━━━━━━

Parent API/save relation 문제면
server 최소 수정 허용.

server main에 commit/push.

Render Production deploy:
절대 수행 금지.

보고:

SERVER_CHANGED:
YES

SERVER_SHA:
...

RENDER_DEPLOY_REQUIRED:
YES

사용자가 Render 직접 배포.

━━━━━━━━━━━━━━━━━━━━━━
17. CLIENT 수정 후 OTA 자동 수행
━━━━━━━━━━━━━━━━━━━━━━

Client 수정 발생 시:

release/v2.0.0에 반영/push.

추가 사용자 승인 기다리지 말고
preflight PASS 시
iOS production-v2 OTA까지 완료.

영구 preflight:

branch =
release/v2.0.0

working tree =
clean

HEAD =
origin/release/v2.0.0

APP_CONTRACT_VERSION =
1.0

Teacher Diary Engine =
https://swimnote-professional-engine.onrender.com

App Version =
2.0.1

RuntimeVersion =
2.1.0

Native incompatible =
NO

Android =
NO

전부 PASS 필수.

main에서 production-v2 OTA 절대 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. 최종 E2E TEST
━━━━━━━━━━━━━━━━━━━━━━

T1
9/3 기존 Diary 선택
→ 항상 동일 canonical edit screen

T2
다른 entry에서 동일 Diary 선택
→ 동일 canonical edit screen

T3
Create save
→ correct history/list

T4
Edit save
→ correct history/list

T5
Create exact diaryId
→ Parent API

T6
Create exact diaryId
→ Parent Feed 실제 render

T7
Edit same diaryId
→ Parent API 최신 내용

T8
Edit same diaryId
→ Parent Feed 최신 내용

T9
Notification
→ correct diary/student

T10
Admin
→ same diaryId

T11
Photo/video preview
→ canonical create/edit 모두

T12
AI generated content
→ canonical save path

코드상 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
19. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

# WP16 DIARY CANONICAL ROUTE + PARENT DELIVERY REPORT

APP
release before:
release after:
client commit:

SERVER
main before:
main after:
server commit:
SERVER_CHANGED:
YES / NO

━━━━━━━━━━
EDIT ROUTES
━━━━━━━━━━

Existing edit entry count before:
...

Edit path #1:
...

Date editable:
YES / NO

Edit path #2:
...

Date editable:
YES / NO

Root cause of duplicate edit UI:
...

LEGACY_EDIT_PATH_FOUND:
YES / NO

LEGACY_EDIT_PATH_DISABLED:
YES / NO

CANONICAL_EDIT_ROUTE:
...

CANONICAL_EDIT_COMPONENT:
...

All existing Diary entry points use canonical edit:
YES / NO

━━━━━━━━━━
CREATE
━━━━━━━━━━

Create endpoint:
...

Create diaryId:
masked

DB exists:
YES / NO

Parent API exact diaryId:
YES / NO

Parent Feed rendered:
YES / NO

Admin same diaryId:
YES / NO

━━━━━━━━━━
EDIT
━━━━━━━━━━

Update endpoint:
...

Same diaryId preserved:
YES / NO

Parent API latest:
YES / NO

Parent Feed latest:
YES / NO

Admin latest:
YES / NO

━━━━━━━━━━
PARENT FAILURE
━━━━━━━━━━

Failure classification:
P1 / P2 / P3 / P4 / P5 / P6 / P7

Exact root cause:
...

API:
PASS / FAIL

Normalize:
PASS / FAIL

Merge:
PASS / FAIL

Dedupe:
PASS / FAIL

State:
PASS / FAIL

Render:
PASS / FAIL

Focus refetch:
PASS / FAIL

━━━━━━━━━━
MEDIA
━━━━━━━━━━

Create photo:
PASS / FAIL

Create video:
PASS / FAIL

Edit photo:
PASS / FAIL

Edit video:
PASS / FAIL

━━━━━━━━━━
AI
━━━━━━━━━━

Canonical write uses Engine result:
YES / NO

Generated state:
YES / NO

Save payload generated:
YES / NO

━━━━━━━━━━
NOTIFICATION
━━━━━━━━━━

Same diaryId:
YES / NO

Correct studentId:
YES / NO

Parent visibility consistent:
YES / NO

━━━━━━━━━━
ADMIN
━━━━━━━━━━

Same diaryId:
YES / NO

Latest content:
YES / NO

HTTP:
...

━━━━━━━━━━
SERVER
━━━━━━━━━━

SERVER_CHANGED:
YES / NO

SERVER_SHA:
...

RENDER_DEPLOY_REQUIRED:
YES / NO

Render deploy:
NO

━━━━━━━━━━
OTA
━━━━━━━━━━

CLIENT_CHANGED:
YES / NO

Source branch:
release/v2.0.0

Source SHA:
...

origin match:
YES / NO

Preflight:
PASS / FAIL

Published:
YES / NO

Channel:
production-v2

Platform:
iOS ONLY

Group ID:
...

Update ID:
...

Android OTA:
NO

main OTA:
NO

━━━━━━━━━━
FINAL
━━━━━━━━━━

Duplicate edit screens remaining:
0 / ...

Create → Parent:
PASS / FAIL

Edit → Parent:
PASS / FAIL

Teacher → Parent → Admin same diaryId:
PASS / FAIL

Remaining Diary defects:
NONE / ...

Final:
READY_FOR_REAL_DEVICE
/
HOLD_RENDER_DEPLOY
/
FAIL

STOP.
WP17 선행 금지.
[보낼 곳: APP REPLIT]

[SWIMNOTE — Banner V2 WEB SUPER ADMIN 최종 통합 + Production 배포]

현재 상태:

- APP Super Admin Banner V2 완료
- custom 배경색 / 글자색 편집 완료
- APP Parent 배너에서는 수정한 custom 색상이 정상 반영됨
- 공개 WEB 홈페이지에서는 같은 배너의 색상 변경이 반영되지 않음
- 홈페이지 BannerCarousel 자체는 이미 존재하므로 유지
- 이제 WEB Super Admin에서도 APP과 동일하게 배너를 관리할 수 있어야 함

이번 작업 목표:

1. WEB 공개 배너 custom 색상 동기화 문제 해결
2. WEB Super Admin에 APP과 동일한 Banner V2 관리 기능 구현
3. APP/WEB 관리자 모두 동일 platform_banners 수정
4. 수정 즉시 APP + 홈페이지 양쪽에 반영
5. 실제 Production 배포 및 LIVE 확인

━━━━━━━━━━━━━━━━━━━━━━
1. 먼저 WEB 색상 미반영 원인 확정
━━━━━━━━━━━━━━━━━━━━━━

“캐시/딜레이일 것”이라고 추측하지 마십시오.

실제 현재 등록된 custom color_theme 값을 하나 확인하고

APP ParentPromoStrip이 그 값을 어떻게 해석하는지

WEB BannerCarousel이 같은 값을 어떻게 해석하는지

비교하십시오.

특히 확인:

- DB platform_banners.color_theme 실제 값
- GET /platform/banners 응답값
- GET /public/banners 응답값
- APP theme parser/helper
- WEB BannerCarousel theme parsing

현재 APP에서 저장하는 custom 형식이 예를 들어:

custom:#163A5F:#FFFFFF

와 같은 구조라면

WEB도 정확히 같은 의미로 해석해야 합니다.

WEB에서:

teal
purple
orange

같은 preset만 처리하고
custom 문자열을 fallback 색상으로 처리하고 있다면
그것이 버그입니다.

━━━━━━━━━━━━━━━━━━━━━━
2. 색상 의미를 APP/WEB 동일하게
━━━━━━━━━━━━━━━━━━━━━━

최종 원칙:

같은 platform_banners.color_theme
→ APP과 WEB에서 같은 backgroundColor/textColor

이어야 합니다.

가능하면 WEB에도 동일 의미의 helper를 두십시오.

예:

parseBannerTheme(colorTheme)

결과:

{
  backgroundColor,
  textColor
}

기존 preset:

teal
purple
orange
...

정상 유지.

custom:

배경색
+
글자색

정상 해석.

WEB만 별도 색상표를 하드코딩해서
APP과 다른 색이 나오면 안 됩니다.

DB migration 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. CACHE / DELAY 여부도 최소 확인
━━━━━━━━━━━━━━━━━━━━━━

색상 parser 수정 전/후와 별개로

public banner 요청이
오래된 응답을 캐시하고 있는지도 확인하십시오.

확인:

GET /public/banners

를 직접 호출했을 때
Super Admin에서 방금 변경한 color_theme가
즉시 응답되는지.

A.

API 응답부터 옛 값
→ 서버/cache 문제.

B.

API 응답은 새 값인데 화면만 옛 색
→ WEB renderer 문제.

둘을 정확히 구분하십시오.

전체 웹 캐시 설정 변경 금지.

필요하면 banner fetch에만
최소한의 fresh/revalidation 정책 적용.

목표:

Super Admin 저장 후
페이지 새로고침 기준으로
최신 banner가 바로 보여야 함.

몇 분씩 기다려야 하는 구조 금지.

━━━━━━━━━━━━━━━━━━━━━━
4. WEB SUPER ADMIN 배너 관리 구현
━━━━━━━━━━━━━━━━━━━━━━

현재 swimnote-web의
Super Admin 영역에

“배너 관리”

메뉴/페이지를 추가 또는 기존 기능을 완성하십시오.

APP Super Admin의 Banner V2를
기능 기준으로 사용합니다.

WEB 전용 배너 시스템을 새로 만들지 마십시오.

반드시 동일:

platform_banners

및 기존 Super Admin Banner API 사용.

━━━━━━━━━━━━━━━━━━━━━━
5. WEB SUPER ADMIN 필수 기능
━━━━━━━━━━━━━━━━━━━━━━

WEB에서도 최소 다음 전부 지원:

[목록]

- 가로 프로모션 strip 목록
- 카드 배너 slider 목록이 현재 APP에 존재하면 동일 관리
- 상태
- 순서
- 노출기간

[등록 / 수정]

- 신규 등록
- 기존 수정
- 활성 / 비활성

[TEXT]

- 제목
- 설명
- 빠른 preset 색상
- 직접 배경색
- 직접 글자색
- HEX 입력
- 실시간 Preview

[IMAGE]

- 이미지 선택
- 기존 R2 banner-upload 사용
- 업로드 후 display_url Preview
- TEXT / IMAGE 타입 선택

[노출]

- display_start
- display_end
- display_seconds
- sort_order
- link 설정
- status

[strip]

- 최대 4개 정책
- 순서 ← →
- 실제 배너 Preview

APP에서 가능한 핵심 관리가
WEB에서 빠지면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
6. PC WEB UI
━━━━━━━━━━━━━━━━━━━━━━

APP UI를 그대로 확대하지 마십시오.

PC에 맞게 심플하게 구성.

권장:

좌측:
배너 목록

우측:
선택 배너 편집

상단:
실제 Preview

예:

┌─────────────────────────────────────────────┐
│ 배너 관리                                    │
├──────────────┬──────────────────────────────┤
│ [1] 환영     │          Preview             │
│ [2] 이미지   │                              │
│ [3] 이벤트   │ TEXT / IMAGE                 │
│ [+ 등록]     │ 제목                         │
│              │ 설명                         │
│              │ 빠른 색상 / 직접 색상        │
│              │ 이미지                       │
│              │ 링크                         │
│              │ 전환시간                     │
│              │ 노출기간                     │
│              │ 상태                         │
│              │        [저장]                │
└──────────────┴──────────────────────────────┘

목표:

처음 사용하는 운영자도
설명 없이 수정 가능.

━━━━━━━━━━━━━━━━━━━━━━
7. CUSTOM COLOR WEB EDITOR
━━━━━━━━━━━━━━━━━━━━━━

APP에서 완료한 custom color 기능을
WEB에도 동일하게 제공합니다.

빠른 색상:
기존 preset

직접 색상:

배경색:
- HTML color input 사용 가능
- HEX 직접입력

글자색:
- HTML color input
- HEX 직접입력

WEB에서는 브라우저 native:

<input type="color">

를 활용해도 됩니다.

무거운 color picker dependency 추가 금지.

HEX validation:

#RRGGBB

실시간 Preview.

━━━━━━━━━━━━━━━━━━━━━━
8. PREVIEW는 실제 노출과 동일한 색상 규칙
━━━━━━━━━━━━━━━━━━━━━━

WEB Super Admin Preview
WEB 공개 BannerCarousel
APP ParentPromoStrip

세 곳이 모두 같은 저장값을
같은 색상 의미로 표현해야 합니다.

관리 Preview에서는 파란색인데
실제 홈페이지에서는 보라색

같은 불일치 금지.

━━━━━━━━━━━━━━━━━━━━━━
9. IMAGE PIPELINE 재사용
━━━━━━━━━━━━━━━━━━━━━━

이미 Production에서 검증된:

banner-upload
→ Cloudflare R2
→ image_key
→ display_url
→ HTTPS image/*

그대로 사용.

WEB용 이미지 업로드 시스템 새로 만들지 마십시오.

WEB Super Admin도
동일 upload endpoint 사용.

이미지 업로드 후:

HTTP 2xx
→ display_url
→ Preview

까지 동작.

━━━━━━━━━━━━━━━━━━━━━━
10. 동일 API / 동일 DB
━━━━━━━━━━━━━━━━━━━━━━

APP Super Admin
WEB Super Admin

둘 다:

GET /super/banners
POST /super/banners
PUT /super/banners/:id
banner-upload

현재 실제 Super Admin Banner API를 재사용.

WEB 전용:

web_banners
website_banners

금지.

APP에서 수정하면 WEB 관리자에서도
다음 조회 시 같은 값.

WEB에서 수정하면 APP 관리자에서도
같은 값.

━━━━━━━━━━━━━━━━━━━━━━
11. 공개 홈페이지도 즉시 같은 값 사용
━━━━━━━━━━━━━━━━━━━━━━

공개 BannerCarousel은:

GET /public/banners

계속 사용.

Super Admin WEB 또는 APP에서 수정한:

- title
- description
- color_theme
- image
- sort_order
- display_seconds
- status
- 기간

이 동일 platform_banners row에 저장되고

다음 public 조회에서 그대로 반영되어야 합니다.

홈페이지용 복사/Publish 데이터 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━
12. PUBLIC WEB 색상 문제 최종 조건
━━━━━━━━━━━━━━━━━━━━━━

APP에서 배경색을 예:

#163A5F

글자색:

#FFFFFF

로 저장했다면

GET /public/banners
→ 같은 custom color_theme 반환

그리고 swimnote.kr BannerCarousel
→ 실제 #163A5F / #FFFFFF

로 렌더링되어야 합니다.

이것을 실제 브라우저 DOM/computed style 또는
실제 렌더 결과로 확인하십시오.

코드만 보고 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
13. SUPER ADMIN 권한
━━━━━━━━━━━━━━━━━━━━━━

WEB 배너 관리 메뉴는
super_admin만 접근.

pool_admin
teacher
parent

접근 금지.

기존 WEB Super Admin auth/role 체계 재사용.

새 권한체계 금지.

━━━━━━━━━━━━━━━━━━━━━━
14. 메뉴 위치
━━━━━━━━━━━━━━━━━━━━━━

WEB Super Admin 기존 navigation에:

배너 관리

메뉴 하나만 추가.

중복 메뉴 금지.

가로 프로모션 / 카드 배너가 둘 다 있다면

“배너 관리”

한 화면 안에서:

상단 프로모션
카드 배너

탭/섹션으로 관리.

기존 APP 정책과 동일.

━━━━━━━━━━━━━━━━━━━━━━
15. 현재 공개 홈페이지 BannerCarousel은 유지
━━━━━━━━━━━━━━━━━━━━━━

이미 구현된 공개 홈페이지 BannerCarousel은
삭제하지 마십시오.

이번 작업에서는:

- custom 색상 동기화
- 최신 데이터 반영
- 같은 platform_banners 사용

만 정확하게 맞춥니다.

현재 크기/위치 디자인을
이번 작업에서 다시 재설계하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
16. 대표 QA — ADMIN SYNC
━━━━━━━━━━━━━━━━━━━━━━

A.

APP Super Admin에서
TEXT 배너 색상 변경

→ DB 저장

→ WEB Super Admin 새로고침

→ 같은 HEX 표시

PASS

B.

WEB Super Admin에서
배경색/글자색 변경

→ 저장

→ APP Super Admin 재조회

→ 같은 custom 색상 확인

PASS

C.

WEB Super Admin에서 title 수정

→ 공개 홈페이지 새로고침

→ 수정 title 반영

PASS

D.

WEB Super Admin에서 IMAGE 업로드

→ R2 업로드 성공

→ Preview 성공

PASS

E.

WEB에서 sort_order 변경

→ APP + 공개 WEB 같은 순서

PASS

F.

WEB에서 display_seconds 변경

→ APP + 공개 WEB 동일 설정 사용

PASS

G.

WEB에서 inactive 전환

→ APP/공개 WEB 활성 목록에서 제외

PASS

━━━━━━━━━━━━━━━━━━━━━━
17. 대표 QA — COLOR SYNC
━━━━━━━━━━━━━━━━━━━━━━

H.

custom:

background #163A5F
text #FFFFFF

APP Parent:
정상

WEB 공개:
동일 색상

PASS

I.

custom:

background #F5F1E8
text #111827

APP Parent:
정상

WEB 공개:
동일 색상

PASS

J.

기존 teal preset

APP/WEB 기존과 동일

PASS

K.

GET /public/banners가
수정 직후 최신 color_theme 반환

PASS

L.

API 새 값
→ 웹 새로고침
→ 실제 새 색상 렌더

PASS

━━━━━━━━━━━━━━━━━━━━━━
18. DEPLOY 전에 실제 HOSTING 경로 확인
━━━━━━━━━━━━━━━━━━━━━━

이전 작업에서
swimnote-web 배포 경로가 혼동된 적이 있습니다.

따라서 이번에는 먼저 실제:

swimnote.kr

Production hosting/deployment 방식을
현재 설정 기준으로 확인하십시오.

Replit Publish인지
Render swimnote-web인지
현재 실제 Production 경로를 확인.

추측해서 엉뚱한 provider에 배포 금지.

현재 Production provider가 무엇이든
실제 swimnote.kr에 연결된 곳에 배포하십시오.

━━━━━━━━━━━━━━━━━━━━━━
19. 배포
━━━━━━━━━━━━━━━━━━━━━━

WEB 수정:

실제 swimnote.kr Production host에 배포.

API:

기존 API contract로 충분하면 재배포 금지.

WEB Super Admin 기능 때문에
정말 필요한 API 최소 수정이 있을 때만
swimnote-api Production 배포.

APP:

이번 작업에서는
이미 색상 기능 정상 완료됐으므로
APP OTA 하지 마십시오.

DB migration:

없어야 함.

━━━━━━━━━━━━━━━━━━━━━━
20. LIVE PRODUCTION 검증
━━━━━━━━━━━━━━━━━━━━━━

배포 후 반드시 실제 Production에서:

1.
WEB Super Admin 로그인

2.
“배너 관리” 메뉴 존재

3.
기존 banner 목록 조회

4.
기존 custom color 값 표시

5.
TEXT 편집 UI

6.
IMAGE 업로드 UI

7.
Preview

8.
저장 가능 구조

확인.

운영 데이터를 테스트 때문에
불필요하게 변경하지 마십시오.

안전하게 변경 가능한 테스트 banner가 있으면
한 번만 실제 수정 → 되돌리기 가능.

없다면 read-only 확인 + 기존 값 기준 검증.

━━━━━━━━━━━━━━━━━━━━━━
21. 공개 홈페이지 실제 색상도 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 APP에서 이미 custom 색상이 변경된
실제 활성 banner가 있다면

swimnote.kr에서 그 banner가:

APP과 동일한 배경색/글자색으로

실제로 보이는지 확인.

“API 값은 같음”만으로 PASS 금지.

실제 브라우저 표시까지 확인.

━━━━━━━━━━━━━━━━━━━━━━
22. 금지
━━━━━━━━━━━━━━━━━━━━━━

- 신규 banner DB
- DB migration
- slides JSONB
- APP 재설계
- Banner API 전면 재설계
- R2 구조 변경
- 공개 홈페이지 BannerCarousel 삭제
- 정산 관련 수정
- unrelated dashboard refactor
- 전체 웹사이트 redesign

━━━━━━━━━━━━━━━━━━━━━━
23. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[A. COLOR ROOT CAUSE]

WEB에 색상 변경이 안 보였던 실제 원인:

- parser 차이
- cache
- deploy 미반영
- 기타

중 정확한 원인.

[B. WEB SUPER ADMIN]

- 메뉴
- route
- component/file
- 목록
- 등록
- 수정
- TEXT
- IMAGE
- R2 upload
- preset
- custom background
- custom text
- HEX
- preview
- sort_order
- display_seconds
- status
- 기간
- link

각 구현 여부.

[C. SHARED DATA]

APP / WEB Admin / Public WEB가

동일:

platform_banners
color_theme
sort_order
display_seconds
display_url

사용 여부.

[D. QA]

A~L
PASS / FAIL.

[E. PRODUCTION]

- swimnote.kr 실제 hosting provider
- full WEB Production SHA 40자 또는 실제 제공되는 revision
- Deploy/Publish ID
- status/live

API 수정했다면:
- API full LIVE SHA
- Render Deploy ID
- status

APP:
- 기존 OTA 유지

Migration:
- 없음

[F. ACTUAL LIVE]

실제 Production에서:

1.
WEB Super Admin 배너관리 화면
2.
custom 색상 표시
3.
공개 홈페이지 동일 custom 색상 표시

각각 실제 확인 여부.

코드만 존재하고
Production에서 확인하지 못했다면
PASS라고 보고하지 마십시오.

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은
‘확인하지 못함’이라고 명시하십시오.

구조상 blocker가 없으면:

원인 확인
→ WEB Super Admin 구현
→ 공개 WEB 색상 동기화 수정
→ Production 배포
→ LIVE 확인

까지 한 번에 진행하십시오.
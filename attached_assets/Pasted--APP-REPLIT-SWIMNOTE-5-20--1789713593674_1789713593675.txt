[보낼 곳: APP REPLIT]

[SWIMNOTE — 반 개설 관리 기본 정원 5명 저장 후 반 관리에서 20명 표시되는 문제 원인 조사]

아직 코드 수정하지 마십시오.

사용자 실제 기기에서 다음 현상이 확인됐습니다.

1.
관리자:
반 개설 관리

기본 반 정원을:

20명
→ 5명

으로 변경 후 저장.

화면에는 정상적으로:

기본 정원 5명

으로 표시됨.

2.
그 후 관리자:

수업 관리

화면으로 이동하면

반 현황:

전체 반 0
1회성 반 0
전체 회원 0
평균 정원 20명

으로 계속 표시됨.

즉:

“기본 반 정원 5명”

을 저장했는데

수업 관리의:

“평균 정원”

은 계속 20명입니다.

이번 단계에서는
원인만 정확히 확인하십시오.

코드 수정 금지.
DB migration 금지.
배포 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 기본 정원 저장 Source 추적
━━━━━━━━━━━━━━━━━━━━━━

“반 개설 관리” 화면을 실제 렌더링하는
정확한 파일을 찾으십시오.

확인:

- 기본 정원 UI field
- 초기값 load API
- 저장 API
- 실제 request payload field명
- API route
- DB table/column

즉:

APP 화면
→ API
→ DB

까지 정확히 추적.

사용자가 5명으로 저장한 뒤
Production DB에도 실제 값이 5로 저장됐는지 확인하십시오.

보고 예:

pool_settings.default_class_capacity = 5

처럼 실제 table/column 기준으로 보고.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. “평균 정원 20명” Source 추적
━━━━━━━━━━━━━━━━━━━━━━

수업 관리 화면의:

“평균 정원 20명”

을 실제 렌더링하는
정확한 파일/컴포넌트를 찾으십시오.

그 숫자 20이 어디서 오는지 끝까지 추적.

가능성은 가정일 뿐이며
실제 코드로 확인해야 합니다.

확인 대상:

A.
frontend hardcoded 20

B.
API response에서 20

C.
DB의 다른 default capacity column

D.
class_groups.capacity 평균

E.
반이 0개일 때 fallback 20

F.
초기 state default 20

G.
stale cache/state

H.
기타

정확히 하나 또는 복수 원인을 코드 근거로 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 특히 “반 0개인데 평균 정원 20” 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 스크린샷에서는:

전체 반 = 0

인데:

평균 정원 = 20명

입니다.

따라서 실제 평균 계산이라면
이 값의 의미가 이상합니다.

확인:

반이 0개일 때 현재 코드가:

averageCapacity = 20

같은 fallback을 쓰는지.

또는:

defaultCapacity

를 표시하려다가
구버전 기본값 20을 쓰는지.

또는 실제 API가:

average_capacity: 20

을 반환하는지.

이 부분을 반드시 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. BUSINESS SEMANTICS도 확인
━━━━━━━━━━━━━━━━━━━━━━

반 개설 관리 화면의 설명은 현재:

“새 반을 개설할 때 기본으로 적용되는 정원입니다.
반별로 개설 시 개별 수정이 가능합니다.”

입니다.

따라서 현재 설계 의미상:

기본 반 정원
=
새 반 생성 시 사용하는 default

로 보입니다.

확인할 것:

A.
기본 정원 변경이
기존 class_group.capacity까지 일괄 변경하는 설계인지

또는

B.
기존 반은 그대로 두고
새 반 생성 기본값에만 적용하는 설계인지

현재 실제 코드 기준으로 보고하십시오.

임의로 정책을 바꾸지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 신규 반 생성 시 실제 5명 적용 여부
━━━━━━━━━━━━━━━━━━━━━━

코드상 신규 반 생성 flow를 확인하십시오.

기본 정원이 5로 저장된 이후
새 반 생성 화면을 열면:

capacity 기본값이 5로 들어오는지

확인.

추적:

default capacity DB
→ class creation API/UI
→ new class capacity initial value

실제 운영 데이터에
테스트 반을 생성하지 마십시오.

코드/API contract 기준으로 확인.

━━━━━━━━━━━━━━━━━━━━━━
6. 수업 관리 “평균 정원”의 정확한 정의 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 “평균 정원”이 무엇을 의미하도록 만들어졌는지 확인하십시오.

가능한 의미:

1.
등록된 모든 반 capacity의 평균

2.
센터 기본 정원

3.
현재 활성 반들의 평균 정원

4.
고정 기본값

실제 구현 기준으로 확정.

이 숫자가:

“기본 정원”

인지

“반별 실제 정원의 평균”

인지

사용자 UI label과 계산 의미가 일치하는지도 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. API / DB 값을 나란히 비교
━━━━━━━━━━━━━━━━━━━━━━

현재 사용자 pool 기준으로
다음 값을 가능한 범위에서 숫자로 보고하십시오.

A.
저장된 기본 반 정원

B.
현재 활성 class_group 수

C.
각 class_group에 capacity field가 있다면
실제 값 분포

D.
수업 관리 API가 반환하는 average_capacity 또는 유사값

E.
frontend 최종 표시값

예:

DB default = 5
active class count = 0
API averageCapacity = 20
frontend = 20

처럼
어느 단계에서 값이 20으로 바뀌는지
한눈에 알 수 있게 보고.

개인정보 불필요.

━━━━━━━━━━━━━━━━━━━━━━
8. CACHE 가능성 확인
━━━━━━━━━━━━━━━━━━━━━━

기본 정원 저장 후:

- API fresh fetch를 하는지
- 화면 재진입 시 refetch하는지
- React state에 초기 20이 남는지
- query cache를 쓰는지

이 부분만 최소 확인.

전체 캐시 시스템 audit 금지.

실제 문제 화면 관련 state/query만 확인.

━━━━━━━━━━━━━━━━━━━━━━
9. 이번 조사에서 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

아직 다음 작업 금지:

- 20 → 5 하드코딩 교체
- averageCapacity 계산 수정
- DB column 변경
- 기존 class capacity 일괄 업데이트
- 신규 migration
- API 변경
- APP OTA

원인을 확정하기 전에는
어떤 것도 수정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. 최종 보고 형식
━━━━━━━━━━━━━━━━━━━━━━

다음만 보고:

[A. DEFAULT CAPACITY SOURCE]

- 화면 파일
- API route
- payload field
- DB table/column
- 현재 실제 저장값

[B. CLASS MANAGEMENT SOURCE]

“평균 정원 20명”의:

- 화면 파일
- API route
- response field
- frontend 계산식/초기값

[C. ROOT CAUSE]

왜 기본 정원을 5로 저장했는데
수업 관리에서는 20이 나오는지
한 문장으로 정확히 설명.

[D. ZERO CLASS CASE]

전체 반 0개인데
20명이 표시되는 이유.

[E. NEW CLASS]

새 반 생성 시
기본 정원 5가 실제 initial capacity로 사용되는지
YES / NO

[F. EXISTING CLASS POLICY]

기본 정원 변경이:

- 신규 반에만 적용
또는
- 기존 반에도 적용

현재 코드 기준으로 보고.

[G. EXPECTED FIX]

코드 수정은 하지 말고
최소 수정안만 제안.

예:

- hardcoded fallback 제거
- 0개일 때 “-” 표시
- default capacity API 값 사용
- 잘못된 DB field 연결 수정

중 실제 원인에 맞는 것.

코드 수정 금지.
migration 금지.
배포 금지.

완료했다고만 보고하지 마십시오.
확인하지 못한 것은
‘확인하지 못함’이라고 명시하십시오.
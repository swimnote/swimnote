SWIMNOTE Teacher Diary AI
FINAL PRODUCTION INSTALL
운영 서버 배포 + 실제 앱 OTA 반영 + 실기기 최종 검증

최종 목적

Teacher Diary AI를 Replit Preview에서만 작동시키는 것이 아니다.

현재 구현된 Teacher Diary AI를 다음 두 곳에 모두 실제 반영한다.

1. SWIMNOTE 실제 Production API 서버
2. 사용자들이 설치하여 사용하는 실제 SWIMNOTE 모바일 앱

최종적으로 실제 설치 앱에서 다음 흐름이 성공해야 한다.

선생님 로그인
→ 일지 작성 화면
→ AI 작성
→ 텍스트 또는 음성 입력
→ AI 일지 생성
→ 학생별 결과 확인
→ 일지에 삽입
→ 최종 저장
→ 저장 결과 재조회

Preview 성공만으로 완료 처리하지 마라.

================================
절대 원칙
================================

- Preview 서버에서만 테스트하고 끝내지 마라.
- 다른 테스트 서버나 다른 API 도메인으로 우회하지 마라.
- 모바일 앱이 실제 사용하는 Production API URL만 사용한다.
- 실제 앱에 OTA를 올리지 않고 실기기 검증 완료라고 하지 마라.
- 서버 배포와 앱 OTA를 구분하여 각각 완료한다.
- 새 기능 추가 금지
- 대규모 리팩터링 금지
- AI Contract 변경 금지
- Save Payload 변경 금지
- Student Resolution 변경 금지
- 기존 일지 구조 변경 금지
- force push 금지
- 증거 없는 PASS 금지

실패하면 즉시 멈추고 원인을 최소 수정한 뒤
동일 단계부터 다시 검증한다.

================================
1. 실제 앱과 서버 배포 대상 고정
================================

이번 작업의 대상은 다음으로 고정한다.

Production API 서버:

- artifacts/api-server
- 실제 모바일 앱이 호출하는 API 도메인
- https://swimnote.kr/api

모바일 앱:

- artifacts/swim-app
- 실제 배포된 SWIMNOTE Expo 앱
- 현재 운영 빌드가 사용하는 EAS Update branch/channel/runtimeVersion

Replit Preview용 웹이나 별도 API URL은
이번 최종 검증의 성공 근거로 사용하지 않는다.

다음을 실제 코드에서 확인한다.

- Expo 앱의 EXPO_PUBLIC_API_URL
- 최종 Production API URL
- AI Generate endpoint
- Whisper endpoint
- EAS projectId
- updates URL
- runtimeVersion
- production channel 또는 branch
- Android/iOS 운영 빌드와 OTA 호환 여부

판정:

PASS 또는 FAIL

================================
2. 현재 Git 및 배포 커밋 확인
================================

실행:

git status
git branch -vv
git rev-parse HEAD
git rev-parse origin/deploy-photo-clone
git log --oneline --decorate -20

확인:

- branch = deploy-photo-clone
- local과 origin SHA 일치
- 현재 최신 커밋에 AI route 수정 포함
- 현재 최신 커밋에 openai build 수정 포함
- WP10~WP12 앱 코드 포함
- working tree clean
- 미커밋 코드 없음

현재 보고된 최신 커밋 9514e1e가 맞는지 확인하되,
실제 Git 결과를 기준으로 기록한다.

판정:

PASS 또는 FAIL

================================
3. Production API 서버 빌드 완성
================================

Render에 올리기 전에 로컬에서 실제 Production 서버 빌드를 수행한다.

대상:

artifacts/api-server

확인:

- openai dependency가 package.json dependencies에 존재
- pnpm lockfile에 존재
- build.ts가 openai를 처리하는 방식
- build exit code
- dist/index.mjs 생성
- dist 실행 시 openai 모듈 로드 가능
- 신규 TypeScript 오류 0
- 기존 API route가 빌드 결과에 포함

반드시 Render와 최대한 같은 명령으로 실행한다.

예:

pnpm install --frozen-lockfile
pnpm --filter @workspace/api-server run build

가능하면 빌드 산출물을 실제로 시작하여 확인한다.

빌드 또는 실행 중 다음 오류가 있으면 FAIL:

- Could not resolve "openai"
- Cannot find module "openai"
- ERR_MODULE_NOT_FOUND
- route registration 오류
- 환경변수 로드 전 서버 crash

로컬 빌드 성공 전 Render 배포 금지.

판정:

PASS 또는 FAIL

================================
4. Production 서버 환경변수 확인
================================

swimnote.kr에 연결된 실제 Render 서비스의 환경변수를 확인한다.

필수 항목은 코드에서 직접 추적한다.

최소 확인:

- DATABASE_URL
- JWT_SECRET
- OPENAI_API_KEY
- NODE_ENV
- 필요한 R2·업로드 관련 변수
- CORS 또는 앱 관련 설정

값 자체는 출력하지 않는다.

각 변수별로 다음만 기록한다.

- 변수명
- 필수 여부
- 존재 여부
- Production 적용 여부

OPENAI_API_KEY가 없으면 배포 진행 금지.

판정:

PASS 또는 FAIL

================================
5. 실제 Production 서버 배포
================================

swimnote.kr 커스텀 도메인이 연결된
실제 Render Production 서비스에 최신 커밋을 배포한다.

반드시 확인:

- 대상 Git branch
- 대상 commit SHA
- Build 성공
- Start 성공
- Health check 성공
- 재시작 반복 없음
- openai 런타임 오류 없음
- DB 연결 정상
- 기존 API 정상

다른 Render 서비스는 성공 근거로 사용하지 않는다.

배포 후 확인:

GET https://swimnote.kr/api/health

기록:

- HTTP status
- Content-Type
- uptime 초기화
- 서버 정상 기동 여부
- 배포 commit SHA

판정:

PASS 또는 FAIL

================================
6. Production AI API 선검증
================================

실제 앱 OTA 전에 Production API부터 검증한다.

무인증 요청:

POST https://swimnote.kr/api/ai/diary/generate

정상 조건:

- HTTP 401 또는 정의된 JSON 인증 오류
- Content-Type application/json
- HTML fallback 아님
- /api/ai/ai 경로 아님

Whisper endpoint도 동일하게 확인한다.

그다음 실제 테스트 선생님 계정으로:

- POST /api/auth/login
- GET /api/auth/me
- POST /api/ai/diary/generate

검증:

- teacher 권한
- 실제 앱과 동일한 request contract
- HTTP 200
- JSON response
- common content
- 학생별 결과
- studentId 매칭
- 서버 crash 없음
- 민감정보 로그 없음

AI Generate는 최소 2회 성공시킨다.

Production AI API가 성공하기 전에는
앱 OTA를 올리지 않는다.

판정:

PASS 또는 FAIL

================================
7. 실제 앱 OTA 배포 준비
================================

대상:

artifacts/swim-app

현재 설치된 운영 앱이 어떤 EAS Update 설정을 사용하는지 확인한다.

반드시 확인:

- app.json 또는 app.config
- eas.json
- projectId
- updates URL
- runtimeVersion
- Android 운영 빌드 runtimeVersion
- iOS 운영 빌드 runtimeVersion
- production channel
- branch-channel mapping
- 현재 운영 앱이 OTA를 받을 수 있는 상태인지

중요:

잘못된 branch/channel에 OTA를 올리지 마라.

운영 앱이 실제로 구독하는 channel을 확정하지 못하면
OTA 실행 금지 후 필요한 정보만 보고한다.

판정:

PASS 또는 FAIL

================================
8. 앱 Production API 고정 확인
================================

OTA에 포함될 모바일 앱이 실제로 다음 서버를 호출하는지 확인한다.

https://swimnote.kr/api

확인 대상:

- EXPO_PUBLIC_API_URL
- API client
- useDiaryAI
- Whisper 요청
- Generate 요청
- 일지 저장 요청

금지:

- localhost
- Replit preview URL
- swimnote-api.onrender.com
- 임시 테스트 URL
- 하드코딩된 개발 URL

Generate, Whisper, Save가 모두 동일한 실제 Production API를 사용해야 한다.

판정:

PASS 또는 FAIL

================================
9. 실제 운영 앱 OTA 배포
================================

§5 Production 서버 배포와
§6 Production AI API 검증이 PASS한 경우에만 진행한다.

기존 프로젝트의 EAS Update 방식을 사용한다.

예시일 뿐 실제 설정을 먼저 확인한다.

eas update \
  --channel <실제 운영 channel> \
  --message "Teacher Diary AI production release"

또는 현재 프로젝트가 branch 방식이면
운영 channel과 연결된 정확한 branch에 게시한다.

반드시 기록:

- EAS project
- channel
- branch
- runtimeVersion
- update group ID
- Android update ID
- iOS update ID
- message
- 게시 시각
- 포함 commit SHA

OTA 게시 후 EAS에서 update 상태를 확인한다.

잘못된 channel이나 runtimeVersion이면 즉시 FAIL.

판정:

PASS 또는 FAIL

================================
10. 실제 설치 앱 OTA 수신 확인
================================

Preview 앱이나 Expo Go가 아니라
실제 배포된 SWIMNOTE 앱에서 확인한다.

절차:

1. 앱 완전 종료
2. 앱 재실행
3. OTA 다운로드 대기
4. 필요하면 한 번 더 완전 종료 후 재실행
5. 새 Teacher Diary AI UI가 반영됐는지 확인
6. 운영 API URL을 사용 중인지 확인
7. 앱 버전과 runtimeVersion 확인
8. 업데이트 수신 증거 기록

가능하면 앱 내부 진단 정보 또는 EAS update ID로
실제 OTA 수신을 확인한다.

화면이 보인다는 이유만으로 OTA 수신을 추정하지 않는다.

판정:

PASS 또는 FAIL

================================
11. 실제 앱 Teacher Diary AI 최종 검증
================================

실제 설치 앱과 실제 Production 서버로 검증한다.

테스트 계정과 테스트 학생을 사용한다.

텍스트 경로:

1. 선생님 로그인
2. 일지 화면 진입
3. AI 작성 열기
4. 텍스트 입력
5. AI Generate
6. 공통 결과 확인
7. 학생별 결과 확인
8. studentId 매칭 확인
9. 일지에 삽입
10. 중복 Insert 차단
11. Save
12. 중복 Save 차단
13. 저장 결과 재조회

음성 경로:

1. 음성 입력
2. Whisper/STT
3. 변환 텍스트 확인
4. AI Generate
5. Insert
6. Save
7. 저장 결과 재조회

WP12 UX:

- 빈 입력 상태 닫기
- 입력 후 닫기 Alert
- 계속 작성 시 입력 유지
- Generate 중 Android Back 차단
- Generate 취소
- Abort 후 일반 오류 미표시
- 실패 후 입력 유지
- 재시도
- Timeout 안내
- 버튼 영구 disabled 없음
- Alert/Toast 중복 없음

DB/API 확인:

- 일지 레코드 중복 없음
- note_content 정상
- 학생별 저장 정상
- 기존 사진·영상 attachment 흐름 정상

실제로 확인하지 못한 항목은 UNVERIFIED로 기록한다.

핵심 Generate → Insert → Save → 재조회 중
하나라도 실패하면 운영 완료 판정 금지.

판정:

PASS 또는 FAIL

================================
12. 최종 완료 및 운영 판정
================================

반드시 다음 표를 작성한다.

- Production API 배포 SHA
- Render Build
- Render Start
- Production health
- Production 인증
- Production AI Generate 1차
- Production AI Generate 2차
- Production Whisper
- EAS channel
- EAS branch
- runtimeVersion
- OTA update group ID
- Android 실제 수신
- iOS 실제 수신
- 앱 Generate
- Student Resolution
- Insert
- Save
- 저장 재조회
- 중복 요청 차단
- Abort
- Retry
- Android Back
- 민감정보 로그
- 신규 TypeScript 오류

최종 판정은 다음 중 하나만 선택한다.

1. FINAL PRODUCTION INSTALL FAIL

서버, OTA 또는 핵심 앱 E2E 실패.
실제 운영 사용 금지.

2. PRODUCTION INSTALLED / PARTIALLY VERIFIED

Production 서버와 실제 앱 OTA 반영 완료.
Generate → Insert → Save 핵심 흐름 성공.
일부 보조 UX 또는 한 운영체제만 미검증.

3. TEACHER DIARY AI FULLY PRODUCTION READY

다음이 모두 실제 완료:

- Production 서버 배포
- Production AI API 성공
- 실제 운영 앱 OTA 게시
- 실제 설치 앱 OTA 수신
- 텍스트 Generate 성공
- 음성 STT 성공
- Student Resolution 성공
- Insert 성공
- Save 성공
- 저장 재조회 성공
- 중복 방지 확인
- Abort/Retry 확인
- 치명적 오류 없음

================================
최종 보고 형식
================================

1. Git 최종 상태

2. Production 서버 로컬 빌드 결과

3. Render 환경변수 결과

4. Render Production 배포 결과

5. Production AI API 결과

6. EAS 운영 설정

7. OTA 배포 결과

8. 실제 설치 앱 OTA 수신 결과

9. 텍스트 AI 일지 E2E

10. 음성 AI 일지 E2E

11. WP11·WP12 런타임 결과

12. 최종 판정

추가 기록:

- Production 서버 commit SHA
- OTA update group ID
- 남은 UNVERIFIED
- 기존 TypeScript 오류
- 수정 커밋
- rollback 필요 여부

중요:

Preview 결과를 최종 성공 증거로 쓰지 않는다.

실제 Production 서버와
실제 설치 SWIMNOTE 앱에서 성공해야만
Teacher Diary AI 완료로 판정한다.
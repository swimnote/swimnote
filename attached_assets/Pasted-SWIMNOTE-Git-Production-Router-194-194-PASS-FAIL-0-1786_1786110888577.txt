SWIMNOTE 보강 회귀 복구 — 최종 Git 정리 후 Production 배포

기능 검증 승인.

실제 Router 연결 테스트 포함:
194 / 194 PASS
FAIL 0

회귀 복구 내용도 승인:

A. waiting ownership filter 제거
B. eligible-occurrences is_one_time 제한 제거
C. 기간 지난 보강 separator 복원

기존 날짜 기능·정원초과 보강 기능 생존 확인 완료.

이제 배포 진행한다.

단, Push 전 Git 상태 모순을 먼저 정리한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1. 실제 Commit 상태 확정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 보고서에는:

13664bd0 Update teacher makeups and add supporting notes

라는 Commit이 존재하면서 동시에
"Commit 없음 / working tree dirty"라고 되어 있다.

다음 원문부터 제출:

git branch --show-current
git rev-parse HEAD
git status --short
git log --oneline --decorate -10
git diff --name-status
git diff --cached --name-status
git show --stat --oneline HEAD

그리고:

git diff --name-status origin/deploy-photo-clone..HEAD

확인한다.

목적:

- 13664bd0에 실제 복구 코드가 들어갔는지
- 복구 코드 중 아직 working tree에 남은 것이 무엇인지
- 테스트 파일 2개가 Commit됐는지
- attached_assets가 Commit됐는지
- Push 시 정확히 어떤 파일이 origin에 추가되는지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2. 최종 허용 코드 파일
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 복구 배포에 필요한 파일은 정확히 4개:

1.
artifacts/api-server/src/routes/teachers.ts

2.
artifacts/swim-app/app/(teacher)/makeups.tsx

3.
artifacts/api-server/src/routes/__tests__/makeup-capacity-gate.test.ts

4.
artifacts/api-server/src/routes/__tests__/makeup-regression-gate.test.ts

attached_assets는 배포용 코드 Commit에서 제외한다.

기타 운영 코드 파일 추가 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3. 필요한 경우 최종 Commit
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

위 4개 중 아직 미커밋 파일만 Commit한다.

이미 13664bd0에 포함된 파일은 다시 Commit하지 않는다.

기존 Commit amend/rebase/reset 금지.

Commit 후:

git status --short
git log --oneline -5
git diff --name-status origin/deploy-photo-clone..HEAD

제출.

attached_assets만 untracked로 남는 것은 허용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4. Push
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최종 코드 상태가 확정되면:

deploy-photo-clone
→ origin/deploy-photo-clone

Push.

Push 후 반드시:

LOCAL=$(git rev-parse HEAD)
REMOTE=$(git rev-parse origin/deploy-photo-clone)

두 SHA 동일 확인.

ahead/behind = 0 / 0 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 5. Render 배포
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Render가 Push된 최종 SHA를 배포하는지 확인.

Status = Live까지 기다린다.

제출:

- Deploy ID
- Commit SHA
- Started At
- Finished At
- Status

Render Commit SHA가 GitHub origin SHA와 다르면
OTA 금지하고 즉시 중단.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 6. Health Check
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GET /api/health
GET /api/healthz

둘 다 HTTP 200 확인.

실패 시 OTA 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 7. Production OTA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Render Live + Health 200 이후에만 OTA.

Production 채널 배포.

기존 운영 방식대로 Preview도 함께 배포한다면 같이 진행.

제출:

- Production Update Group ID
- Production iOS Update ID
- Preview Update Group ID
- Preview iOS Update ID
- runtimeVersion
- channel
- bundle SHA-256
- bundle size
- OTA 기준 Commit SHA

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 8. 최종 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 최종 Commit SHA
2. origin SHA
3. Push ahead/behind
4. 실제 Push된 파일
5. attached_assets 제외 여부
6. Render Deploy ID
7. Render Commit SHA
8. Render Live 여부
9. /api/health 결과
10. /api/healthz 결과
11. Production OTA Group ID
12. Production iOS Update ID
13. Preview Group ID
14. runtimeVersion
15. bundle SHA
16. git status
17. DB 변경 여부
18. 추가 코드 수정 여부

추가 코드 수정 금지.
DB 변경 금지.
Auth/JWT/Login/일지/X/WP6 변경 금지.

배포 완료 후 중단한다.
현재 AI 일지 깜빡임 문제에 대한 추가 부분 수정은 잠시 중단하십시오.

이번 작업의 목표는 기존 모달의 버그를 계속 추적하는 것이 아니라,
현재 수정 중인 SWIMNOTE AI 엔진의 실제 일지 생성 파이프라인에 맞춰
AI 일지 모달의 레이아웃과 연결 구조를 다시 설계하는 것입니다.

아직 코드 수정, 커밋, OTA 배포를 진행하지 마십시오.
먼저 전체 경로를 다시 추적한 뒤 설계도만 제출하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. 먼저 기존 코드 경로를 기능별로 완전히 분리해서 보고
━━━━━━━━━━━━━━━━━━━━━━

아래 파일과 호출 경로를 전부 다시 추적하십시오.

- components/ai/core/AIStateMachine.ts
- components/ai/core/AIContext.tsx
- components/ai/hooks/useAIStateMachine.ts
- components/ai/features/diary/useDiaryAI.ts
- components/ai/features/diary/DiaryAIContent.tsx
- components/ai/features/diary/DiaryAIActionBar.tsx
- components/ai/features/diary/DiaryAIButton.tsx
- components/ai/core/BaseAIModal.tsx
- components/ai/components/AIErrorView.tsx

각 코드를 다음 세 부류로 분류하십시오.

A. 반드시 유지해야 하는 실제 AI 일지 기능
B. 단순 모달 레이아웃 및 화면 표시 기능
C. 현재 깜빡임과 관련된 상태머신·렌더링·닫기 처리 기능

특히 아래 항목은 실제 AI 기능과 무관한 모달 중간 구조인지 명확히 구분하십시오.

- CLOSED
- OPENING
- BaseAIModal 2단계 열기·닫기
- nativeVisible
- rendered
- AIProvider 주입
- AIStateMachine의 화면 전환
- ActionBar default 처리

이 항목들이 AI 생성 API, DB 검색, GPT 생성, 결과 삽입 기능과 직접 연결된 것인지,
아니면 UI 표시만 담당하는 것인지 파일과 함수 단위로 작성하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 새 AI 일지 모달의 기준 파이프라인
━━━━━━━━━━━━━━━━━━━━━━

새 모달은 반드시 아래 실제 처리 구조를 기준으로 설계하십시오.

1. 교사가 텍스트 또는 음성으로 수업 내용을 입력
2. 음성 입력인 경우 Whisper STT로 텍스트 변환
3. 교사의 일지 작성 의도와 수업 내용을 정리
4. 해당 학생 및 반의 커리큘럼·진도·템플릿 확인
5. SWIMNOTE DB 서버에서 관련 지식 검색
6. 교사 입력에 부족한 수영교육 내용을 DB 검색 결과로 보충
7. 교사 입력과 검색된 DB 자료만 GPT에 전달
8. GPT가 자연스러운 일지 문장 생성
9. 전체 공통 일지 생성
10. 학생별 개별 피드백 생성
11. 결과를 전체 일지와 학생별 일지로 구분하여 표시
12. 교사가 결과를 확인·수정
13. 전체 일지와 개별 일지를 기존 일지 작성 화면의 각 입력란에 삽입
14. 기존 일지 저장 절차 사용

이 파이프라인의 각 단계가 현재 어느 파일과 함수에 연결되어 있는지 추적하십시오.

예:

교사 입력
→ 함수명 / 파일명

Whisper STT
→ 함수명 / API 경로 / 파일명

템플릿 확인
→ 함수명 / API 경로 / 파일명

DB 검색
→ 함수명 / API 경로 / 파일명

GPT 생성
→ 함수명 / API 경로 / 파일명

전체 일지 결과
→ 응답 JSON 필드 / 상태 저장 위치

학생별 일지 결과
→ studentId 기준 응답 구조 / 삽입 함수

기존 일지 입력란 삽입
→ 함수명 / 대상 필드 / 파일명

현재 구현되지 않았거나 엔진 수정 후 연결 예정인 단계는
추측하지 말고 ‘미구현’, ‘연결 예정’, ‘확인 필요’로 표시하십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 새 모달 설계 원칙
━━━━━━━━━━━━━━━━━━━━━━

새 모달은 기존 AI 기능을 전부 유지하되,
기존 BaseAIModal과 복잡한 상태 전환 구조를 복사하지 마십시오.

새 모달 가칭:

DiaryAIModalV2

새 모달은 UI 표시를 위한 최소 상태만 사용하십시오.

- INPUT
- RECORDING
- TRANSCRIBING
- SEARCHING
- GENERATING
- RESULT
- ERROR

CLOSED와 OPENING은 화면 상태로 사용하지 마십시오.

모달이 열리면 바로 INPUT 화면을 렌더링하고,
모달이 닫히면 내부 컴포넌트를 완전히 제거하거나
다음 실행 시 모든 상태가 초기화되도록 설계하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 기존 레이아웃 규칙 유지
━━━━━━━━━━━━━━━━━━━━━━

기존 AI 일지 모달의 디자인과 사용 흐름은 최대한 그대로 유지하십시오.

반드시 유지할 항목:

- 기존 상단 제목 영역
- 기존 닫기 버튼 위치
- 기존 여백과 패딩 규칙
- 기존 입력창 크기와 스타일
- 기존 음성 입력 버튼
- 녹음 중 표시
- STT 변환 중 표시
- AI 작성 버튼
- 취소 버튼
- 로딩 화면
- 오류 화면
- 결과 표시 영역
- 결과 수정 기능
- 다시 생성 기능
- 전체 일지 표시
- 학생별 개별 피드백 표시
- 일지에 삽입 버튼
- 키보드 대응
- 스크롤 동작
- Safe Area
- iOS와 Android 공통 레이아웃
- 기존 색상, 폰트, 버튼 높이, 모서리 규칙
- 기존 접근성 규칙
- 기존 모달 바깥 터치 처리 정책
- 기존 생성 중 버튼 비활성화 규칙

레이아웃을 새로 설계한다는 것은 디자인을 임의로 바꾸라는 의미가 아닙니다.

기존 레이아웃 규칙과 사용자 경험은 유지하고,
그 안의 상태관리와 기능 연결 구조만 더 단순하고 안정적으로 다시 구성하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 절대로 빠뜨리면 안 되는 기능
━━━━━━━━━━━━━━━━━━━━━━

다음 기능을 한 개도 누락하지 마십시오.

입력 기능

- 직접 텍스트 입력
- 음성 녹음 시작
- 음성 녹음 중지
- 마이크 권한 요청
- 권한 거부 안내
- Whisper STT 업로드
- STT 결과를 입력창에 삽입
- STT 결과 추가 수정

AI 처리 기능

- 반 정보 전달
- 수업 정보 전달
- 학생 정보 전달
- 학생별 커리큘럼 정보 전달
- 템플릿 확인
- DB 서버 검색
- 관련 지식 보충
- GPT 문장 생성
- 전체 공통 일지 생성
- 학생별 개별 피드백 생성
- 응답 검증
- 자동 재시도
- 오류 처리
- 중복 생성 요청 차단

결과 기능

- 전체 일지 결과 표시
- 학생별 결과 표시
- 결과 직접 수정
- 다시 생성
- 전체 일지 삽입
- studentId 기준 학생별 일지 삽입
- 기존 일지 작성 화면의 저장 흐름 유지
- 결과가 없는 학생 처리
- 매칭되지 않은 학생 처리
- 일부 학생 결과만 반환된 경우 처리

안정성 기능

- 모달 재오픈 시 이전 결과 초기화
- 이전 오류 상태 초기화
- 이전 로딩 상태 초기화
- 이전 requestId 응답 무시
- 모달 닫은 후 늦게 도착한 응답 무시
- 생성 중 중복 탭 차단
- 녹음 중 모달 종료 처리
- STT 중 모달 종료 처리
- AI 생성 중 모달 종료 처리
- 네트워크 오류 후 재시도
- 연속 열기·닫기 시 중복 마운트 방지

━━━━━━━━━━━━━━━━━━━━━━
6. 서버와 엔진 영역은 임의 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 다음 영역은 임의로 변경하지 마십시오.

- SWIMNOTE AI 엔진
- DB 구조
- 템플릿 DB
- 커리큘럼 DB
- GPT 프롬프트
- Whisper API
- 일지 생성 API
- 기존 일지 저장 API
- 학생 ID 구조
- 반 ID 구조
- 응답 JSON 구조

새 모달 연결에 필요한 변경이 있다면,
먼저 어떤 이유로 어떤 필드나 API 변경이 필요한지 설계서에 작성하십시오.

승인 전 서버와 DB를 변경하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 설계도에 반드시 포함할 내용
━━━━━━━━━━━━━━━━━━━━━━

코드를 작성하기 전에 아래 내용을 포함한 설계도를 제출하십시오.

1. 기존 구조와 새 구조 비교도

기존:

DiaryAIButton
→ BaseAIModal
→ AIProvider
→ AIStateMachine
→ DiaryAIContent
→ useDiaryAI
→ API

새 구조:

DiaryAIButton
→ DiaryAIModalV2
→ Diary AI 전용 Hook 또는 Service
→ STT / 템플릿 / DB 검색 / GPT 생성
→ 전체·개별 결과
→ 일지 삽입

2. 새 파일 목록

- 새로 만들 파일
- 기존 그대로 사용할 파일
- 일부 수정할 파일
- 사용 중단할 파일
- 삭제하지 않고 보존할 파일

3. 함수 연결표

기능
→ 기존 함수
→ 새 모달에서 호출할 함수
→ 수정 필요 여부

4. 상태 전이표

INPUT
→ RECORDING
→ TRANSCRIBING
→ INPUT
→ SEARCHING
→ GENERATING
→ RESULT

실패 시:

RECORDING / TRANSCRIBING / SEARCHING / GENERATING
→ ERROR
→ 재시도 대상 상태

5. API 호출표

- API 경로
- 요청 데이터
- 응답 데이터
- 호출 파일
- 오류 처리
- 재시도 여부

6. 전체·개별 일지 데이터 구조

예:

{
  commonDiary: "...",
  studentFeedback: [
    {
      studentId: "...",
      feedback: "..."
    }
  ]
}

실제 현재 응답 구조가 다르면 현재 코드의 실제 구조를 그대로 기재하십시오.

7. 기존 레이아웃 유지 항목 체크리스트

8. 누락 가능성이 있는 기존 기능 목록

9. 테스트 항목

10. 예상 수정 파일 수와 작업 범위

━━━━━━━━━━━━━━━━━━━━━━
8. 실제 기기 테스트 기준
━━━━━━━━━━━━━━━━━━━━━━

구현 후 아래 테스트를 전부 통과해야 합니다.

기본

- 텍스트 입력 후 AI 일지 생성
- 음성 입력 후 STT 완료
- STT 텍스트 수정 후 생성
- 전체 일지 생성
- 학생별 개별 일지 생성
- 결과 수정
- 일지에 삽입
- 기존 저장 기능 실행

반복 사용

- 모달 열기·닫기 10회
- 생성 후 닫고 다시 열기
- 오류 후 닫고 다시 열기
- 결과 화면에서 닫고 다시 열기
- 생성 중 닫고 다시 열기
- STT 중 닫고 다시 열기

오류 상황

- 마이크 권한 거부
- Whisper 실패
- DB 검색 실패
- AI 생성 실패
- 빈 응답
- 일부 학생 결과 누락
- 학생 ID 불일치
- 느린 네트워크
- 요청 타임아웃

플랫폼

- iOS 실제 기기
- Android 실제 기기
- 키보드 열림 상태
- 긴 일지 결과
- 학생 수가 많은 반
- 화면 회전 정책
- 앱 백그라운드 후 복귀

━━━━━━━━━━━━━━━━━━━━━━
9. 작업 방식
━━━━━━━━━━━━━━━━━━━━━━

이번 단계에서는 설계도만 제출하십시오.

아직 다음 작업은 하지 마십시오.

- 코드 수정
- 기존 파일 삭제
- 기존 모달 삭제
- 서버 수정
- DB 수정
- 커밋
- OTA 배포
- 운영 앱 반영

설계도 승인 후 DiaryAIModalV2를 별도로 구현하고,
기존 DiaryAIButton의 진입점만 V2로 교체하십시오.

기존 모달은 V2 검증 완료 전까지 삭제하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. 최종 목표
━━━━━━━━━━━━━━━━━━━━━━

이번 작업의 목표는 단순히 깜빡임을 숨기는 것이 아닙니다.

SWIMNOTE AI 일지의 실제 엔진 파이프라인인

교사 입력
→ 일지 작성 의도 분석
→ 템플릿 확인
→ DB 서버 검색
→ 내용 보충
→ GPT 문장 생성
→ 전체 공통 일지 생성
→ 학생별 개별 일지 생성
→ 기존 일지 입력란 삽입

구조에 맞는 안정적인 AI 일지 모달을 만드는 것입니다.

기존 기능은 한 개도 삭제하지 말고,
기존 레이아웃 규칙은 그대로 유지하며,
모달 상태관리와 렌더링 구조만 단순하고 안정적으로 다시 설계하십시오.
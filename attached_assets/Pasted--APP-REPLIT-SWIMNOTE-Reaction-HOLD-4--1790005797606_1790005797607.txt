[보낼 곳: APP REPLIT]

[SWIMNOTE — 템플릿/Reaction 영구방지 작업 최종 보완]

현재 완료보고는 HOLD입니다.

핵심 개선은 적용됐지만
다음 4개가 아직 완료 기준을 충족하지 못했습니다.

1.
Reaction student 검증이
“부모-자녀 approved + 같은 pool”
까지만 되어 있음.

2.
feedback-custom의 reachable legacy 장문 TextInput이
공통 TemplateInputModal 외부에 아직 독립 구현으로 남아 있음.

3.
reaction unique constraint:
(diary_id, parent_id, reaction_type)
가 multi-child context와 충돌할 가능성 미확인.

4.
보고된 full Git SHA가 실제 조회값이 아님.

이번 작업에서 이 네 가지를 정확히 마무리하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. REACTION — student가 diary를 실제 볼 수 있는지 검증
━━━━━━━━━━━━━━━━━━━━━━

현재 검증:

A.
parent_students approved 관계

B.
student.pool_id == diary.pool_id

만으로는 부족합니다.

같은 수영장 내 다른 반 학생 ID도
통과할 수 있기 때문입니다.

Parent가 전달한 student_id가
해당 diary를 실제로 조회 가능한 학생인지
현재 Parent diary/feed 접근 로직과 동일한 기준으로
서버에서 검증하십시오.

새 권한 정책을 만들지 마십시오.

현재 정상 Parent feed가
“이 student에게 이 diary를 보여줘도 된다”
고 판단하는 canonical 조건을 찾고
동일 의미를 재사용하십시오.

확인 대상 예:

- diary.class_group_id
- student 현재 class
- 학생 반 history
- diary lesson date
- 기존 parent diary visibility helper/query

실제 현재 시스템 기준을 따르십시오.

검증 실패 시 403.

단순 pool 일치만으로 허용 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. MULTI-CHILD + UNIQUE CONSTRAINT 의미 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 unique:

(diary_id, parent_id, reaction_type)

를 실제 reaction toggle/upsert 로직과 함께 확인하십시오.

다음 상황을 검토:

Parent P
Child A
Child B

동일 class diary D가
A와 B 둘 다에게 실제 표시될 수 있는 경우.

A context에서 like
→ row(student_id=A)

그 후 B context에서 같은 diary like

현재 unique constraint 때문에:

A.
기존 row가 toggle/delete되는지

B.
student_id가 B로 overwrite되는지

C.
두 번째 row 생성이 막히는지

정확히 확인하십시오.

중요:

제품 의미를 먼저 확정하십시오.

좋아요가:

“부모 계정당 diary 1개”

인지

또는

“부모-학생 context당 diary 1개”

인지

현재 APP UX 및 데이터 의미 기준으로 판단하십시오.

추측 금지.

만약 학생 context가 표시명/귀속에 중요하고
동일 부모의 두 자녀가 같은 diary를 볼 수 있다면

unique key도 student context를 포함해야 할 수 있습니다.

그 경우에만 최소 schema 변경안을 제시/적용하십시오.

불필요한 migration 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. student_id 없는 신규 reaction 처리
━━━━━━━━━━━━━━━━━━━━━━

현재 보고:

신규 OTA 앱은 student_id 전달
구버전 앱은 student_id 미전달 시 NULL 저장

으로 되어 있습니다.

이 정책을 명확히 하십시오.

구버전 호환을 위해 NULL 허용이 필요하다면:

- 잘못된 child 추론은 절대 하지 않음
- teacher 화면에는 parent_name만 표시

를 유지.

다만 완료보고에서는
“신규 row student_id NULL 방지”라고 쓰지 마십시오.

정확히:

“새 OTA 경로에서는 student_id 필수,
legacy client 요청은 NULL 허용”

으로 보고하십시오.

가능하면 API request에서
client/app version 구분 없이
현재 parent context를 서버에서 안전하게 결정할 방법이 있는지
딱 해당 route만 확인.

추론이 안전하지 않으면
legacy NULL 유지가 더 낫습니다.

━━━━━━━━━━━━━━━━━━━━━━
4. TEMPLATE — reachable duplicate 제거
━━━━━━━━━━━━━━━━━━━━━━

현재 보고에 따르면:

feedback-custom.tsx의 legacy 모드가 reachable이고
그 안에 독립 multiline TextInput이 남아 있습니다.

따라서:

“duplicate implementation 제거 완료”

라고 볼 수 없습니다.

실제 reachable legacy add/edit path를 확인하고
장문 입력 semantics가 공통 TemplateInputModal과 동일하다면
공통 component를 사용하도록 변경하십시오.

만약 legacy modal이 다른 필드/UX 때문에
공통 TemplateInputModal 자체를 사용할 수 없다면

최소한 장문 입력 부분을:

- 공통 shared component
또는
- 공통 canonical style/component

로 분리하여

maxHeight
scrollEnabled
keyboard dismiss

정책이 한 곳에서만 정의되도록 하십시오.

reachable 장문 입력 화면마다
개별 숫자 200을 복사해두는 방식은 재발 방지가 아닙니다.

최종적으로
“내 템플릿 추가/수정”의 multiline 입력 정책은
한 canonical source에서만 관리되어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
5. REGRESSION CHECK
━━━━━━━━━━━━━━━━━━━━━━

TEMPLATE:

A.
admin curriculum
B.
admin legacy
C.
teacher curriculum
D.
teacher legacy

reachable 4경로 모두:

- canonical multiline component/style 사용
- maxHeight 정책 동일
- scrollEnabled 동일
- keyboard dismiss 동일

REACTION:

A.
single-child parent → 정상

B.
multi-child parent / Child A → 정확히 A

C.
multi-child parent / Child B → 정확히 B

D.
같은 pool이지만 diary 접근권한 없는 child_id
→ 403

E.
parent 소유 아닌 child_id
→ 403

F.
legacy student_id=NULL reaction
→ parent_name만 표시

G.
동일 parent의 두 child가 같은 diary를 볼 수 있는 경우
→ unique/toggle semantics가 제품 의미와 일치

━━━━━━━━━━━━━━━━━━━━━━
6. SHA 증빙 정정
━━━━━━━━━━━━━━━━━━━━━━

현재 보고된:

ada49a4cd2b5e1f3a7c8d9e0f1b2c3d4e5f6a7b8

는 실제 full SHA로 확인된 값이 아닙니다.

추측하거나 short SHA를 임의 확장하지 마십시오.

다음 명령/저장소 기준 실제 값을 조회해서
정확한 40-character SHA만 보고하십시오.

- Work HEAD full SHA
- APP OTA 생성 시점 full SHA
- API Production LIVE full SHA

확인하지 못하면
“확인하지 못함”으로 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 변경 범위
━━━━━━━━━━━━━━━━━━━━━━

필요한 최소 범위만 수정.

APP:
- template canonicalization 보완
- reaction payload/표시가 필요한 경우만

API:
- diary/student 실제 접근 검증
- reaction unique/toggle semantics 필요 시 보완

DB:
- unique constraint 변경이 실제로 필요한 경우에만 migration

기존 reactions 임의 backfill 금지.
기존 diary 데이터 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
8. 배포
━━━━━━━━━━━━━━━━━━━━━━

코드 수정 발생 시:

APP:
production-v2
runtimeVersion 2.2.0
OTA

API:
swimnote-api Render

Native rebuild 금지.

━━━━━━━━━━━━━━━━━━━━━━
9. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[A. DIARY ACCESS VALIDATION]

- 기존 canonical parent visibility 조건
- reaction POST에서 재사용한 검증
- 같은 pool만으로 통과 불가 확인

[B. UNIQUE SEMANTICS]

- 현재 constraint
- 동일 parent + 두 child + 동일 diary 사례 동작
- 최종 제품 의미
- migration 필요 여부

[C. LEGACY REACTION]

- student_id 없는 요청 정책
- 잘못된 자녀 추론 없음

[D. TEMPLATE CANONICAL SOURCE]

- admin curriculum
- admin legacy
- teacher curriculum
- teacher legacy

각 경로가 사용하는 canonical component/style

[E. QA]

위 regression A~G 결과

[F. DEPLOY]

APP:
- 실제 full 40-character Work SHA
- 실제 OTA Git SHA
- EAS Update Group ID
- iOS Update ID
- branch/channel
- runtimeVersion

API:
- 실제 full 40-character Production LIVE SHA
- Render Deploy ID
- status

DB:
- migration 여부

[G. UNCHANGED]

- 기존 reaction backfill 없음
- 기존 diary 데이터 변경 없음
- Native build 없음

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은 ‘확인하지 못함’이라고 명시하고
추측해서 SHA나 정상 상태를 만들어내지 마십시오.
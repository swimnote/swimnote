[보낼 곳: APP REPLIT]

[SWIMNOTE — 반복 발생 UI/데이터 귀속 오류 구조적 원인 조사]

아직 코드 수정하지 마십시오.
APP OTA 금지.
API 배포 금지.
DB 수정 금지.
DB migration 금지.

실제 사용자 환경에서 두 가지 오류가 다시 확인되었습니다.

━━━━━━━━━━━━━━━━━━━━━━
[현상 1] 일지 템플릿 장문 입력
━━━━━━━━━━━━━━━━━━━━━━

경로:

일지 템플릿
→ 내 템플릿 추가
→ 장문 입력

실제 기기에서 장문을 입력하면:

- 입력창/화면 스크롤 문제가 발생
- 키보드가 열린 뒤 하단 접근이 어려움
- 키보드를 정상적으로 내리기 어렵거나
  편집 화면에서 빠져나오기 어려운 상태가 발생

이 문제는 최근:

TemplateInputModal에

maxHeight: 200
scrollEnabled={true}

를 추가하여 수정/OTA까지 했다고 보고된 문제와
매우 유사합니다.

그런데 실제 사용자에게 다시 재현되고 있습니다.

이번에는
“같은 화면이 실제로 다시 깨진 것인지”
또는
“유사한 다른 템플릿 입력 경로/컴포넌트가 따로 존재하는지”

부터 정확히 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
[현상 2] 학부모 좋아요 귀속 이름 오류
━━━━━━━━━━━━━━━━━━━━━━

실제 상황:

학부모 계정 한 개에
자녀가 2명 이상 등록되어 있습니다.

학부모가
‘이연우’ 학생의 일지에서 좋아요를 눌렀습니다.

그런데 선생님:

반응 & 댓글

화면에는

문지은(이연우)

형태로 표시됩니다.

즉:

좋아요가 발생한 일지의 실제 학생은 이연우인데
다른 이름 또는 학부모/다른 자녀와 연관된 이름이
앞쪽 표시명에 섞여 있습니다.

한 부모 계정에 복수 자녀가 연결될 때
reaction의 student 귀속을
parent user identity와 혼동하는 문제인지 조사하십시오.

━━━━━━━━━━━━━━━━━━━━━━
0. 이번 조사의 핵심 목표
━━━━━━━━━━━━━━━━━━━━━━

이번에는 개별 증상만 고치는 조사가 아닙니다.

다음 두 가지를 반드시 구분해서 확인하십시오.

A.
각 버그의 직접적인 root cause

B.
왜 SWIMNOTE에서
이미 수정한 것과 유사한 오류가
다른 화면/경로에서 반복되는지에 대한
구조적 root cause

예:

- 동일 기능의 duplicate component
- 역할별로 복제된 UI
- 동일 API 의미를 화면마다 다르게 해석
- 공통 helper 없이 각 화면에서 자체 구현
- parent/student/user identity 의미 혼재
- canonical endpoint 없이 여러 조회경로 사용
- 오래된 legacy 코드가 실제 route에 남아 있음
- 화면 이름은 같지만 실제 구현 파일이 여러 개

이 중 실제 존재하는 것만 증거로 보고하십시오.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 템플릿 입력 — 실제 렌더 SOURCE 재확인
━━━━━━━━━━━━━━━━━━━━━━

스크린샷의 실제 화면을
문자열/route/navigation 흐름으로 다시 특정하십시오.

검색:

- 내 템플릿 추가
- 템플릿 추가
- 템플릿 선택
- TemplateInputModal
- multiline TextInput

확인:

- 실제 사용자가 들어간 route
- 실제 렌더된 component
- 최근 수정한 TemplateInputModal이
  정말 이 화면에 사용되는지
- 동일/유사 TemplateInputModal이
  다른 파일에 또 존재하는지
- admin / teacher / shared 영역에
  템플릿 입력 구현이 중복되어 있는지

파일명을 추측하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 최근 OTA 코드 실제 포함 여부
━━━━━━━━━━━━━━━━━━━━━━

최근 수정 SHA:

14563b2e4a5f0f2c20d01d8e5ab5fded5b929a35

에서 적용한:

maxHeight: 200
scrollEnabled={true}

가 현재 production-v2에 실제 존재하는지 확인하십시오.

확인:

- 현재 main/work tree
- 현재 production-v2 OTA 대상 코드
- 해당 component 실제 source
- revert/overwrite 여부

단 OTA를 다시 배포하지 마십시오.

보고만.

━━━━━━━━━━━━━━━━━━━━━━
3. 템플릿 입력 구조적 중복 확인
━━━━━━━━━━━━━━━━━━━━━━

SWIMNOTE APP 내에서
“내 템플릿 추가/수정” 기능을 수행하는 구현만 검색하십시오.

전체 APP TextInput audit 금지.

확인:

- 컴포넌트 수
- 실제 route별 사용처
- 각각의 multiline props
- keyboard handling
- scroll handling

만약 같은 기능이 2개 이상 구현되어 있다면:

각 파일과 실제 사용 경로를 보고.

이번 단계에서는 통합하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 키보드 문제 정확히 분해
━━━━━━━━━━━━━━━━━━━━━━

실제 현재 component 기준으로 다음을 확인:

- TextInput 내부 scroll 가능 여부
- sheet 자체가 화면보다 커지는지
- KeyboardAvoidingView behavior
- keyboardVerticalOffset
- dismiss 방식 존재 여부
- 바깥 영역 탭 시 Keyboard.dismiss 여부
- return key / blurOnSubmit
- 키보드가 열린 상태에서 저장/취소 접근 여부

“scrollEnabled=true가 있으니 정상일 것”
이라고 추정하지 마십시오.

현재 구조에서
왜 스크린샷처럼 화면 대부분을 TextInput이 차지하는지
레이아웃 계산 기준으로 설명하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 좋아요 — 실제 SOURCE 확인
━━━━━━━━━━━━━━━━━━━━━━

선생님:

반응 & 댓글

화면을 실제 렌더하는 파일을 찾으십시오.

검색:

- 반응 & 댓글
- 학부모 반응
- 좋아요
- reactions
- comments

보고:

- screen/component
- API endpoint
- response mapper
- 표시 문자열 생성 코드

특히:

문지은(이연우)

라는 형태가

어떤 두 필드를 조합해서 생성되는지
정확히 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 좋아요 데이터 모델 의미 확인
━━━━━━━━━━━━━━━━━━━━━━

reaction 1건에 대해
다음 identity를 분리하십시오.

- reaction.id
- diary_id
- parent user/account id
- parent display name
- student_id
- student name
- diary owner student_id
- parent-student relation

중요:

“누가 버튼을 눌렀는가”
=
parent account

와

“어느 학생의 일지에 대한 반응인가”
=
student

는 다른 개념입니다.

현재 DB/API가 이 둘을
어떻게 저장/조회하는지 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 복수 자녀 계정에서 student 귀속 방식 확인
━━━━━━━━━━━━━━━━━━━━━━

한 parent user가:

Child A
Child B

두 명 이상과 연결되어 있을 때

Child B의 diary에 좋아요를 누르면
reaction의 student_id가 어떻게 결정되는지 확인하십시오.

다음 가능성을 실제 코드로 확인:

A.
diary.student_id를 사용

B.
request payload student_id를 사용

C.
parent의 첫 번째 child를 사용

D.
parent_student 관계 join에서 LIMIT 1 사용

E.
user display_name + diary student name을 조합

F.
student_id 없이 parent_id만 저장한 뒤
조회 시 자녀를 추론

G.
기타

특히 LIMIT 1,
첫 번째 relation,
ORDER BY 없는 child lookup
여부를 반드시 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. 문제 reaction 1건 DB READ ONLY 확인
━━━━━━━━━━━━━━━━━━━━━━

가능하면 스크린샷에 해당하는
이연우 diary의 좋아요 reaction 1건만
Production DB READ ONLY로 확인하십시오.

수정/삭제 금지.

확인:

- diary id
- diary student_id
- diary student name
- reaction parent/user id
- reaction student_id 존재 여부
- parent와 연결된 child 목록
- API 조회 결과에서 사용된 student row

개인정보 원문은 보고에 최소화하십시오.

핵심은:

왜 이연우 diary의 reaction이
문지은(이연우)로 표시되는지

데이터 흐름을 증명하는 것입니다.

━━━━━━━━━━━━━━━━━━━━━━
9. reaction WRITE와 READ를 분리 확인
━━━━━━━━━━━━━━━━━━━━━━

반드시 구분하십시오.

WRITE 단계에서 이미 잘못된 student_id가 저장되는지

또는

DB 저장은 정상인데
teacher reaction 조회 시 잘못 join하는지

둘 중 어느 단계인지 확정하십시오.

예:

Parent APP
→ POST reaction
→ DB row

Teacher APP
→ GET reactions
→ API JOIN
→ 표시

각 단계별 student identity를 추적하십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. parent mode 정상 화면과 비교
━━━━━━━━━━━━━━━━━━━━━━

사용자 보고상
학부모 모드에서는 해당 diary가 정상적으로 보입니다.

동일 diary를 기준으로:

- parent diary endpoint
- teacher reactions endpoint

가 student identity를 가져오는 방식을 비교하십시오.

정상 parent 경로가
diary.student_id를 기준으로 한다면
teacher reaction 경로와 차이를 보고하십시오.

전체 parent API audit 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. 반복 발생의 구조적 원인 조사
━━━━━━━━━━━━━━━━━━━━━━

이 두 버그를 각각 고치는 것을 넘어서
다음 항목을 조사하십시오.

[A] UI duplication

동일 사용자 기능이
role 또는 route별로 복제되어
한 곳만 수정되고 다른 곳은 남는 구조가 있는지.

[B] API contract duplication

같은 데이터 의미를
화면마다 다른 endpoint/mapper로 해석하는지.

[C] identity ambiguity

user_id
parent_id
student_id
diary student_id

의 의미가
response field 이름에서 명확히 분리되어 있는지.

예:

name
display_name
owner_name
student_name

같이 모호한 필드 때문에
잘못 조합될 가능성이 있는지.

[D] legacy path

현재 production에서
새 구현과 구 구현이 동시에 살아 있고
navigation에 따라 서로 다른 코드가 실행되는지.

실제 존재하는 문제만 보고.

━━━━━━━━━━━━━━━━━━━━━━
12. 재발 방지 원칙 제안
━━━━━━━━━━━━━━━━━━━━━━

아직 구현하지 말고
최소한의 구조적 예방책만 제안하십시오.

예를 들어 실제 조사 결과에 따라:

- 템플릿 입력 component canonical 1개
- 장문 입력 공통 wrapper
- reaction response에
  parent_user_name / student_name 명시 분리
- reaction student identity는
  반드시 diary.student_id에서 결정
- multi-child parent에서 child 추론 금지
- 화면별 자체 문자열 조합 금지
- API response schema test
- 최소 regression test

등.

하지만 실제 root cause가 확인된 항목만
제안하십시오.

대규모 refactor 제안 금지.

━━━━━━━━━━━━━━━━━━━━━━
13. 이번 단계 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

- APP 코드 수정 금지
- API 코드 수정 금지
- DB 수정 금지
- migration 금지
- OTA 금지
- Render deploy 금지
- 기존 reaction 수정 금지
- 기존 diary 수정 금지

조사만.

━━━━━━━━━━━━━━━━━━━━━━
14. 최종 보고 형식
━━━━━━━━━━━━━━━━━━━━━━

[A. TEMPLATE ACTUAL SOURCE]

- 실제 route
- 실제 component
- 최근 수정 component와 동일 여부
- duplicate implementation 존재 여부

[B. TEMPLATE CURRENT CODE]

- maxHeight
- scrollEnabled
- KeyboardAvoidingView
- keyboard dismiss
- 실제 레이아웃 문제

[C. TEMPLATE ROOT CAUSE]

왜 최근 수정 후에도
실제 사용자가 같은 유형의 오류를 보는지
한 문장으로 확정

[D. REACTION SOURCE]

- teacher reaction screen
- endpoint
- 표시 문자열 생성 위치

[E. REACTION WRITE]

- 좋아요 생성 시 저장되는 identity
- student_id 결정 방식

[F. REACTION READ]

- teacher 조회 JOIN
- parent/student name 결정 방식

[G. MULTI-CHILD CASE]

한 parent에 2명 이상 자녀가 있을 때
현재 로직이 어떻게 동작하는지

[H. PRODUCTION SAMPLE]

문제 reaction 1건:
- diary student
- reaction identity
- API 표시 identity

READ ONLY 결과

[I. REACTION ROOT CAUSE]

왜
문지은(이연우)
형태로 잘못 표시됐는지
WRITE/READ 중 어느 단계 문제인지 확정

[J. STRUCTURAL ROOT CAUSE]

왜 이런 오류가 반복되는지:

- duplicate UI
- duplicate API meaning
- identity ambiguity
- legacy path
- 기타

중 실제 확인된 것만 보고

[K. PERMANENT PREVENTION PLAN]

아직 수정하지 말고:

1. 직접 버그 최소 수정
2. 재발 방지 최소 구조 개선
3. regression test 항목

을 구분하여 제안.

APP/API/DB 중
어디를 수정해야 하는지도 명시.

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은
‘확인하지 못함’이라고 명시하고
추측해서 정상이라고 보고하지 마십시오.
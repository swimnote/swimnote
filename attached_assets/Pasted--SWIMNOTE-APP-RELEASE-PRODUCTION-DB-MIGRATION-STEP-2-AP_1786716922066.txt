[SWIMNOTE APP RELEASE]
[PRODUCTION DB MIGRATION — STEP 2]
[APPLY GR3 ONLY]

현재 완료:

GR1 Production Migration ✅

확인:
growth_report_cycles EXISTS
growth_report_questions EXISTS
growth_report_answers EXISTS
growth_reports GR1 columns 24/24 EXISTS

이번 단계에서는:

GR3 migration 하나만 Production에 적용한다.

GR5 실행 금지.
Group 7/8 실행 금지.
Render deploy 금지.
API/Worker deploy 금지.
OTA 금지.
EAS build 금지.
코드 수정 금지.

==================================================
1. TARGET DB
==================================================

반드시 동일 SWIMNOTE APP Production Supabase.

사용:
SUPABASE_DATABASE_URL
+
현재 repository canonical buildConfig() 방식

AI ENGINE PROD_DATABASE_URL 사용 금지.

실행 직전 read-only 확인:

current_database()
swimming_pools count
growth_report_cycles EXISTS
growth_reports.product_status EXISTS

이전 GR1 적용 DB와 동일하지 않으면 STOP.

==================================================
2. MIGRATION TARGET
==================================================

오직:

initGrowthReportGR3Schema()

또는 repository의 GR3 전용 canonical entrypoint.

대상 파일:

src/migrations/growth-report-gr3-engine-init.ts

다른 migration 같이 실행 금지.

==================================================
3. EXPECTED CHANGE
==================================================

GR3 migration 실제 파일 기준으로
정확한 schema 변경만 적용.

현재 알려진 핵심:

growth_reports.analysis_retry_count

추가.

실제 migration에 다른 additive column/index가 있으면
파일을 source of truth로 사용.

==================================================
4. SAFETY
==================================================

migration 파일 확인:

DROP TABLE 없음
DROP COLUMN 없음
DELETE 없음
TRUNCATE 없음
기존 unrelated data 변경 없음

additive-only가 아니면 STOP.

==================================================
5. EXECUTION
==================================================

가능하면 fail-fast / transaction 방식.

GR3 성공 확인 전
GR5 실행 금지.

==================================================
6. POST CHECK
==================================================

성공 후 read-only 확인:

growth_reports.analysis_retry_count EXISTS

type
default
nullable
constraint

를 migration contract와 비교.

추가 index/constraint가 있다면 전부 확인.

==================================================
7. DATA SAFETY
==================================================

migration 전후:

growth_reports row count 비교.

현재 예상:
0 → 0

신규 Growth Report row 생성 금지.
Cycle/Question/Answer row 생성 금지.

schema only.

==================================================
8. FAILURE POLICY
==================================================

오류 발생 시 즉시 STOP.

GR5 실행 금지.
임의 DROP/rollback SQL 금지.

실제 적용 상태만 보고.

==================================================
9. NO DEPLOY
==================================================

성공 후에도:

Render deploy 금지.
Worker deploy 금지.
OTA/build 금지.

==================================================
10. REPORT
==================================================

다음만 보고:

1. TARGET DB VERIFIED
2. GR3 MIGRATION ENTRYPOINT
3. ADDITIVE ONLY? YES/NO
4. EXECUTION RESULT
5. analysis_retry_count STATUS
6. TYPE / DEFAULT / NULLABILITY
7. INDEX / CONSTRAINT STATUS
8. BEFORE growth_reports ROW COUNT
9. AFTER growth_reports ROW COUNT
10. DESTRUCTIVE OPERATION
11. PRODUCTION DATA CREATED
12. GR5 EXECUTED? YES/NO
13. SERVER DEPLOY STATUS

성공:

SWIMNOTE_APP_GR3_PRODUCTION_MIGRATION_APPLIED ✅

실패:

SWIMNOTE_APP_GR3_PRODUCTION_MIGRATION_BLOCKED ❌
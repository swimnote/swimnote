지금부터 AI 엔진 Replit은 건드리지 마십시오.

현재 문제는 스윔노트 앱 내부의 AI 일지 모달 런타임 크래시입니다.

현재 작업 브랜치:
deploy-photo-clone

현재 HEAD:
1140cf5433cdd5fc4efd8a9742d0c4aa3f4ed1a9

현재 증상:
- 일지 작성 화면 정상 진입
- 반 공통 일지 카드 우측 하단에 “AI 작성” 버튼 정상 표시
- AI 작성 버튼을 누르면 BaseAIModal이 열리는 듯하다가 앱이 튕김
- AI 엔진 DB 서버와 실제 API는 아직 연결하지 않은 상태
- 현재 AI 생성은 더미 데이터 기준

이번 작업 목표:
실제 Expo Go에서 AI 작성 버튼을 눌렀을 때 앱이 튕기지 않고 모달이 정상적으로 열리도록 크래시 원인을 찾고 수정하는 것

중요:
추측으로 수정하지 마십시오.
실제 실행 로그와 Stack Trace를 기준으로 최초 예외 지점을 찾으십시오.

확인 대상:

1. DiaryWriteView
2. DiaryEditView
3. DiaryAIButton
4. BaseAIModal
5. DiaryAIContent
6. DiaryAIActionBar
7. useDiaryAI
8. AIContext / AIProvider
9. AIStateMachine
10. Reanimated
11. Gesture Handler
12. SafeArea
13. Keyboard Controller
14. Modal 내부 Portal 또는 Navigation 의존성

먼저 아래 흐름을 실제로 추적하십시오.

DiaryWriteView 또는 DiaryEditView
→ textareaFooter
→ DiaryAIButton
→ BaseAIModal
→ DiaryAIContent
→ useDiaryAI
→ 첫 렌더링

반드시 확인할 것:

- 버튼 클릭 직후 발생하는 최초 에러 메시지
- Metro Console 로그
- Expo Runtime Error
- JS Stack Trace
- Native Crash 여부
- 에러 파일명
- 에러 줄 번호
- 최초 예외를 발생시킨 컴포넌트
- Provider 누락 여부
- Hook이 Provider 밖에서 호출되는지
- undefined/null 접근 여부
- Reanimated worklet 오류 여부
- GestureHandlerRootView 누락 여부
- 컴포넌트가 렌더링 중 state를 변경하는지
- useEffect 무한 반복 여부
- Modal open 직후 잘못된 상태 전환이 발생하는지
- COMPLETE 또는 RESULT 상태 초기화 오류 여부

작업 순서:

1.
현재 코드를 수정하지 말고 실제 크래시 로그를 먼저 확보하십시오.

2.
최초 예외 지점과 원인을 보고하십시오.

3.
원인이 확인된 뒤 최소 수정만 수행하십시오.

4.
AI 엔진 API, DB, 크레딧, 검색 로직은 절대 연결하지 마십시오.

5.
기존 일지 저장, 사진, 영상, 앨범, 스크롤, 키보드 기능을 수정하지 마십시오.

6.
임시 stub, 가짜 Provider, 임시 파일로 덮어쓰는 방식은 금지합니다.

7.
다른 브랜치로 전환하거나 merge하지 마십시오.

8.
현재 deploy-photo-clone 브랜치에서만 작업하십시오.

완료 조건:

- Expo Go에서 실제 일지 작성 화면을 직접 실행
- AI 작성 버튼을 직접 누름
- 모달이 튕기지 않고 정상 열림
- INPUT 화면 정상 표시
- 모달 닫기 정상 작동
- 더미 AI 생성 후 RESULT 상태 정상 표시
- “일지에 삽입” 클릭 시 textarea 내용이 실제로 변경됨
- COMPLETE 애니메이션 후 모달 정상 종료
- 일지 작성 화면이 그대로 유지됨
- 기존 일지 기능 회귀 없음
- DiaryEditView에서도 동일하게 정상 작동
- TypeScript 검사 통과
- 실제 앱 화면 스크린샷 제출

완료 보고서에는 아래를 포함하십시오.

1. 실제 크래시 원인
2. 최초 에러 메시지 원문
3. 에러 발생 파일과 줄 번호
4. 수정한 파일 목록
5. 각 파일별 수정 내용
6. 왜 이 수정으로 해결되는지
7. Expo Go 실제 검증 결과
8. DiaryWriteView 검증 결과
9. DiaryEditView 검증 결과
10. 삽입 동작 검증 결과
11. 기존 기능 회귀 여부
12. 실제 앱 스크린샷

코드 수정이나 배포만으로 완료 처리하지 마십시오.

실제 Production 앱 흐름과 동일한 Expo Go 화면에서 기능을 끝까지 직접 조작하고 검증한 뒤에만 완료로 보고하십시오.
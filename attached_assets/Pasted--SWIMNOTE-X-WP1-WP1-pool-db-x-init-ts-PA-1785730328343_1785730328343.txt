# SWIMNOTE X — WP1 최종 회귀검증 지시서

WP1 정정 완료 보고서를 검수했습니다.

다음 항목은 승인합니다.

* pool-db-x-init.ts의 PART 1·PART 2 Migration 분리
* Curriculum 테이블 5개 구조
* Growth Event FK 2개
* index.ts 필수 DB Migration 실패 처리
* 운영 DB에 적용된 Curriculum 구조
* 기존 데이터 무변경
* PART 2 테이블 미사용 상태
* 현재 Rollback 불필요 판단

그러나 WP1 전체 완료 승인은 아직 하지 않습니다.

남은 작업은 코드 수정이 아니라 일반모드 핵심 회귀검증과 실패 경로 검증입니다.

━━━━━━━━━━━━━━━━━━━━━━

1. 이번 작업 범위
   ━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서는 테스트와 증거 제출만 진행하십시오.

허용:

* 기존 테스트 코드 실행
* 읽기 전용 DB 조회
* 개발환경 또는 안전한 테스트 계정으로 API 검증
* Mock 기반 초기화 실패 테스트
* 로그 확인

금지:

* 기능 코드 수정
* Migration 수정
* DB 스키마 변경
* 샘플 수영장 생성
* 샘플 학생 생성
* 운영 회원 데이터 수정
* X모드 활성화
* Commit
* Push
* OTA
* Render 배포
* RevenueCat 변경
* WP2 작업

테스트 과정에서 코드 수정이 필요하면 작업을 중단하고 먼저 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 현재 상태 확정
━━━━━━━━━━━━━━━━━━━━━━

현재 상태는 다음과 같이 기록하십시오.

```text
Production Migration 실행: 실행됨
운영 DB 변경: 실행됨
Curriculum 테이블 5개: 생성 완료
Growth Event FK 2개: 추가 완료
운영 DB 구조: 승인 설계와 일치
서버 재시작: 미실행
자동 Commit: 발생함
Push: 미실행
배포: 미실행
전체 Repository 타입체크: FAIL
WP1 수정 파일 오류: 오류 목록에 없음
일반모드 핵심 회귀검증: 미완료
WP1 최종 승인: 대기
```

이 상태를 다시 다르게 표현하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 안전한 테스트 계정 조사
━━━━━━━━━━━━━━━━━━━━━━

먼저 Repository와 현재 개발환경에서 기존 테스트 계정 사용 방법만 조사하십시오.

확인 대상:

* integration-test.ts
* validate-api-endpoints.ts
* wp10-final-evidence-test.ts
* gen-super-token.ts
* 기존 테스트 fixture
* 환경변수 기반 테스트 계정
* 이미 존재하는 개발용 pool_admin 계정
* 이미 존재하는 개발용 teacher 계정
* 이미 존재하는 개발용 parent 계정

제출:

* 파일 경로
* 사용되는 계정 종류
* 운영 실사용 계정인지 테스트 전용인지
* 데이터 쓰기 여부
* 테스트가 생성·수정·삭제하는 데이터
* 실행 후 정리 방법
* 안전하게 실행 가능한지

운영 실사용 회원이나 학생 정보를 임의로 사용하지 마십시오.

안전한 기존 테스트 계정이 없으면 인증 API 테스트는 `미실행`으로 유지하십시오.

테스트를 위해 새 계정을 생성하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 정상 로그인 검증
━━━━━━━━━━━━━━━━━━━━━━

안전한 기존 테스트 계정이 확인된 경우에만 실행하십시오.

역할:

* pool_admin
* teacher
* parent

각 역할에 대해 제출:

* 요청 경로
* 요청 데이터 마스킹본
* HTTP 상태
* 응답의 핵심 필드
* JWT 또는 민감정보 전체 출력 금지
* 로그인 후 조회 가능한 pool_id가 기존 테스트 수영장인지 확인

통과 기준:

* 정상 자격증명으로 로그인 성공
* 기존 응답 Contract 변화 없음
* X 컬럼 추가로 로그인 응답 오류 없음

안전한 계정이 없으면 실행하지 말고 그 이유를 제출하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 일반 AI 일지 생성 검증
━━━━━━━━━━━━━━━━━━━━━━

안전한 teacher 테스트 계정과 기존 테스트 학생이 확인된 경우에만 실행하십시오.

검증 대상:

```text
POST /api/v1/teacher-diary/generate
```

확인:

* 기존 Contract 요청
* 일반모드 수영장 사용
* 기존 템플릿 검색
* 정상 AI 응답
* result.common 또는 현재 실제 Contract의 공통 필드
* 학생별 student_ref/content
* curriculum_matches가 일반모드에서 강제로 생성되지 않는지
* Growth Event가 생성되지 않는지
* Curriculum 테이블에 데이터가 추가되지 않는지

전후 row count:

* growth_events
* curriculum_versions
* curriculum_items
* student_curriculum_assignments

통과 기준:

```text
일반 AI 일지 정상
+
Growth Event 0건 유지
+
Curriculum 데이터 0건 유지
```

실제 API 비용이 발생한다면 1회만 실행하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 기존 Diary Save 검증
━━━━━━━━━━━━━━━━━━━━━━

안전한 기존 테스트 일지를 사용할 수 있을 때만 실행하십시오.

검증:

* 기존 일반모드 Diary Save API
* 저장 응답
* 기존 일지 필드
* 학생별 일지 저장
* 일반모드에서 Growth Event 미생성
* 기존 저장 흐름 변화 없음

주의:

* 실사용 학생 일지 수정 금지
* 기존 운영 일지 덮어쓰기 금지
* 신규 테스트 일지 생성이 필요하면 먼저 중단하고 승인 요청

읽기 전용으로 검증할 수 없다면 `미실행`으로 유지하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 기존 Template 검색 검증
━━━━━━━━━━━━━━━━━━━━━━

일반모드 수영장의 기존 템플릿 검색을 확인하십시오.

확인:

* searchTemplates(poolId, meaning)
* swimming_pool_id 조건
* scope='global'
* 기존 fallback 동작
* x_global이 일반모드 결과에 포함되지 않는지
* 결과 개수와 최종 선택 로직 변화 없음

가능하면 기존 자동 테스트 또는 함수 단위 테스트를 사용하십시오.

DB에 템플릿을 INSERT하거나 수정하지 마십시오.

통과 기준:

```text
기존 pool 템플릿 검색 정상
+
일반모드 x_global 노출 0건
```

━━━━━━━━━━━━━━━━━━━━━━
8. Migration 실패 경로 Mock 검증
━━━━━━━━━━━━━━━━━━━━━━

운영 DB 연결을 끊거나 실제 Migration 오류를 발생시키지 마십시오.

Mock 또는 단위 테스트로만 검증하십시오.

확인할 경로:

1. initPoolDb() reject
2. initSuperDb() reject
3. setServerReady() 미호출
4. process.exit(1) 호출
5. 오류 로그 호출

가능한 방식:

* 의존 함수 Mock
* process.exit Mock
* setServerReady spy
* Promise reject 단위 테스트

새로운 운영 로직을 추가하지 마십시오.

현재 구조가 테스트하기 어려워 코드 변경이 필요하면 테스트를 중단하고 다음을 보고하십시오.

```text
현재 구조상 Mock 불가
필요한 코드 변경
영향 범위
```

사용자 승인 없이 테스트 편의를 위한 리팩터링을 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
9. 자동 Commit 상태 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 자동 Commit:

```text
9a3cc63e
Initialize database schema and update API server configuration
```

읽기 전용으로 다음을 제출하십시오.

* `git show --stat 9a3cc63e`
* 해당 Commit에 포함된 전체 파일
* WP1 외 파일 포함 여부
* 예상하지 못한 변경 포함 여부
* Commit이 로컬에만 있는지
* remote branch에 Push됐는지

Push하지 마십시오.

WP1 외 파일이 포함돼 있으면 즉시 보고하고 추가 조치를 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. 타입체크 상태
━━━━━━━━━━━━━━━━━━━━━━

전체 타입체크는 이미 FAIL로 확인됐습니다.

다시 `PASS`라고 표현하지 마십시오.

최종 표기:

```text
전체 Repository 타입체크: FAIL
WP1 수정 파일이 오류 목록에 포함됨: NO
변경 전 기준선: 없음
기존 오류 여부: pre-existing로 추정
```

이번 작업에서 기존 타입 오류를 수정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
11. 완료 판정 기준
━━━━━━━━━━━━━━━━━━━━━━

다음 두 가지 경우를 구분하십시오.

## 경우 A — 안전한 테스트 가능

아래를 실제 실행:

* 정상 로그인
* 일반 AI 일지
* 기존 Template 검색
* 안전한 Diary Save
* 실패 경로 Mock

모두 통과하면 WP1 최종 승인 요청 가능.

## 경우 B — 안전한 테스트 불가능

다음을 명시:

* 실행하지 못한 테스트
* 이유
* 테스트 계정 또는 테스트 데이터 부재
* 운영 데이터 위험
* 코드 수정 없이는 Mock 불가능한 항목

이 경우에도 임의 테스트 데이터를 만들지 말고 멈추십시오.

━━━━━━━━━━━━━━━━━━━━━━
12. 최종 제출 형식
━━━━━━━━━━━━━━━━━━━━━━

1. 안전한 테스트 계정 조사 결과
2. 정상 로그인 결과
3. 일반 AI 일지 결과
4. Diary Save 결과
5. Template 검색 결과
6. Growth Event 전후 건수
7. Curriculum 테이블 전후 건수
8. Migration 실패 Mock 결과
9. 자동 Commit 전체 파일 목록
10. 예상 외 변경 여부
11. 전체 타입체크 상태
12. 미실행 테스트와 이유
13. 코드 추가 수정 여부
14. DB 추가 변경 여부
15. Commit 추가 발생 여부
16. Push 여부
17. 배포 여부
18. WP1 최종 승인 가능 여부 자체 판단

━━━━━━━━━━━━━━━━━━━━━━
13. 제출 후 중단
━━━━━━━━━━━━━━━━━━━━━━

테스트 결과만 제출하고 멈추십시오.

WP2로 넘어가지 마십시오.

금지:

* 코드 수정
* DB 변경
* 추가 Migration 실행
* 신규 테스트 데이터 생성
* 서버 재시작
* Commit
* Push
* 배포
* X모드 활성화
* 다음 WP 작업

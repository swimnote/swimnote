[SWIMNOTE 보강 기능 회귀 복구 — 증거 기반 1회 조사 지시서]

목적:

보강 기능을 새로 만들지 않는다.
이미 검증 완료된 보강 기능을 기준으로 현재 Production에서 발생한 회귀 지점만 찾는다.

이번 작업은 "조사 전용"이다.

코드 수정 금지.
DB 변경 금지.
Commit / Push / Render / OTA 금지.

불필요한 재설계, 광범위 탐색, 같은 파일 반복 분석도 금지한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0. 가장 중요한 원칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 증거 수준:

DB 조건 일치 3건 = CONFIRMED

서버 handler:
DB rows
→ map(is_expired 추가)
→ res.json(result)

코드 흐름상 res.json 직전 3건 예상
= INFERRED

실제 HTTP JSON
= UNKNOWN

실기기 waitingList
= UNKNOWN

실제 rendered cards
= UNKNOWN

따라서 현재 최초 UNKNOWN 지점은:

HTTP RESPONSE

이다.

이 지점부터 순서대로 조사한다.

금지:

"DB가 3건이므로 API도 3건"
"코드상 filter가 없으므로 정상"
"아래로 스크롤하면 있을 것"
"ScrollView 문제 같다"
"캐시 문제 같다"

모두 증거 없이는 금지한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 기존 정상 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

보강 기능 정상 기준:

branch 계열:
hotfix/makeup-date-range-clean

기준 SHA:

1c0df48aa7345ac976876712b00602bf452e7045

보강 기능은 이미 구현 및 검증 완료된 기능이다.

새 설계 금지.

정상 기능:

- waiting 보강
- expired 보강
- 기간 지난 보강 separator
- 보강반 배정
- 날짜/회차 선택
- 직접 완료
- 인계
- 관리자 보강 관리
- 선생님 보강 관리
- 공용 날짜 범위 정책
- eligible-occurrences

특히 정상 UX:

일반 보강
↓
────────────
기간 지난 보강 (N건)
────────────
↓
expired 보강

expired N건이면 separator 정확히 1개가 보여야 한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. 조사 비용 제한
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 조사에서 필요한 파일만 읽는다.

최소 대상:

artifacts/swim-app/app/(teacher)/makeups.tsx

artifacts/swim-app/app/(admin)/makeups.tsx

artifacts/api-server/src/routes/teachers.ts

artifacts/api-server/src/routes/admin.ts

artifacts/api-server/src/lib/makeup-date-range.ts

그리고 apiRequest 구현 파일은
캐시 동작 확인에 필요한 정확한 파일 1개만 추가로 찾는다.

최근 메신저/알림 작업과의 diff 확인이 필요하면
git diff로 위 보강 파일 변경 여부만 확인한다.

Repository 전체를 반복 grep하지 않는다.

node_modules 검색 금지.
build 결과물 검색 금지.
generated 검색 금지.
attached_assets 검색 금지.
memory 검색 금지.

같은 내용을 확인하기 위한 반복 명령 금지.

한 번 확인한 사실은 다시 검색하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. STEP 1 — Git 신원 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

먼저 정확히 실행:

git branch --show-current
git rev-parse HEAD
git status --short
git rev-parse origin/hotfix/makeup-date-range-clean
git rev-parse origin/release/v1.2-phase1-clean

보고:

Current Branch:
Current HEAD:
Working Tree:
hotfix reference SHA:
release branch SHA:

여기서 코드 수정하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. STEP 2 — 보강 회귀 Diff
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기준 SHA:

1c0df48aa7345ac976876712b00602bf452e7045

현재 HEAD와 아래 파일만 비교한다.

git diff 1c0df48aa7345ac976876712b00602bf452e7045..HEAD -- \
'artifacts/swim-app/app/(teacher)/makeups.tsx' \
'artifacts/swim-app/app/(admin)/makeups.tsx' \
'artifacts/api-server/src/routes/teachers.ts' \
'artifacts/api-server/src/routes/admin.ts' \
'artifacts/api-server/src/lib/makeup-date-range.ts'

각 파일:

UNCHANGED
또는
CHANGED

로 판정.

CHANGED라면 정확히 어떤 함수/조건이 바뀌었는지만 제출.

전체 프로젝트 diff를 장황하게 분석하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. STEP 3 — Production 실행 경로 고정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Preview 금지.

Production API는:

https://swimnote.kr/api

이다.

다른 URL을 사용하지 않는다.

현재 Production 앱이 호출하는 API가 위 주소인지 기존 EAS production 설정으로 확인한다.

환경변수 수정 금지.

Render branch 이름만 보고 Production 코드라고 판단하지 않는다.

실제 Render Live SHA가 필요하면 기존 확인된 Live deployment 정보를 사용하거나
READ-ONLY 조회만 한다.

배포 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. STEP 4 — GET waiting 실제 HTTP 증거
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

조사 대상:

GET /teacher/makeups?status=waiting

가장 먼저 필요한 것은 실제 HTTP 응답이다.

확인해야 할 것:

HTTP status
Content-Type
JSON total count
waiting count
expired count
각 record ID
각 record status
각 record absence_date
각 record expire_at
각 record is_expired

단:

실제 Production teacher JWT가 없다면
인증 우회 금지.
로그인 로직 수정 금지.
임의 계정 생성 금지.
DB에서 token 생성 금지.

JWT가 없어 실제 호출 불가능하면:

HTTP RESPONSE = UNKNOWN

으로 유지한다.

절대로 DB 결과를 HTTP 결과로 대신하지 않는다.

그 경우 다음 단계의 최소 runtime 계측안을 작성하되
아직 코드에 넣지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. STEP 5 — API cache 정확히 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

apiRequest 구현에서 정확히 확인:

1. GET cache key
2. TTL
3. /teacher/makeups?status=waiting 캐시 대상 여부
4. pull-to-refresh가 cache를 bypass하는지
5. clearApiCache 호출 여부
6. 보강 배정 후 invalidation 여부
7. 보강 완료 후 invalidation 여부
8. 보강 인계 후 invalidation 여부
9. 직접 완료 후 invalidation 여부
10. HTTP 실패 시 기존 waitingList가 유지되는지

현재 보고에서 알려진 후보:

token + "::" + URL cache key
TTL 약 30초

그러나 다시 코드로 검증한다.

판정은:

CONFIRMED / INFERRED / UNKNOWN

중 하나만 사용.

캐시가 존재한다는 사실만으로
현재 장애 원인을 "캐시"라고 결론 내리지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. STEP 6 — loadWaiting 전체 추적
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

makeups.tsx에서 실제 함수 기준으로:

loadWaiting()
↓
apiRequest()
↓
res.ok
↓
res.json()
↓
setWaitingList()
↓
정렬
↓
expired 계산
↓
render

순서대로 추적한다.

특히 확인:

res.ok=false일 때 동작
catch일 때 동작
기존 state 유지 여부
refresh 시 호출 함수
mutation 성공 후 재조회 함수

각 단계에 filter / slice / pagination이 있는지도 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. STEP 7 — expired separator 복구 여부 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

makeups.tsx에서 다음 로직을 직접 확인:

firstExpiredIdx
expiredCount

또는 동일 역할 코드.

정상 기준:

expiredCount === 0
→ separator 없음

expiredCount > 0
→ 첫 expired 카드 바로 앞에

"기간 지난 보강 (N건)"

separator 정확히 1회.

현재 코드에 없다면:

CONFIRMED UI REGRESSION

으로 판정.

"expired 카드 자체는 렌더링되므로 정상"

이라고 판정하면 안 된다.

기존 검증 UX가 사라졌으면 회귀다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. STEP 8 — Scroll 구조 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ScrollView 자체를 원인으로 단정하지 않는다.

정적 확인만 한다.

확인:

부모 flex:1
부모 minHeight:0
ScrollView/FlatList style
contentContainerStyle
paddingBottom
Safe Area
removeClippedSubviews
목록 내부 조건부 렌더링

정적 코드만으로 실제 마지막 카드가 보인다고 확정하지 않는다.

실기기 증거가 없으면:

RUNTIME SCROLL = UNKNOWN

이다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. STEP 9 — eligible-occurrences
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기준 endpoint:

GET /teacher/makeups/:makeupId/eligible-occurrences

기준 hotfix와 현재 코드를 비교한다.

특히:

startDate
endDate
getMakeupDateRange
validateMakeupDateRange

사용 여부 확인.

공용 helper:

artifacts/api-server/src/lib/makeup-date-range.ts

확인:

addDateDays
dayOfWeekFromDateStr
isValidDateFormat
isValidCalendarDate
getMakeupDateRange
validateMakeupDateRange

teacher/admin이 별도 날짜 계산을 다시 만들었는지 확인.

실제 eligible-occurrences HTTP 응답을 확인하지 못하면
runtime 결과는 UNKNOWN으로 남긴다.

waiting 목록 문제와 eligible-occurrences 문제를
하나의 원인으로 합치지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. STEP 10 — 최근 PHASE 1 영향 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최근 학부모 요청/알림 작업이
보강 관련 파일을 변경했는지 Git 기준으로 확인한다.

특히:

parent_student_requests

와

makeup_sessions

를 혼동하지 않는다.

link-result는 기존 보강 상태 변경 기능이 아니다.

확인:

link-result가 makeup_sessions UPDATE 수행하는지.

정상:

수행하지 않음.

parent request에:

status
processed_result_type
processed_result_id
processed_by
processed_at

등을 연결하는 역할만 해야 한다.

최근 PHASE 1 때문에
makeup_sessions 기존 로직을 수정했다면 정확히 보고한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. 최초 장애 지점 결정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

아래 표를 반드시 작성:

DB COUNT:
Server query result:
Server res.json 직전:
HTTP JSON:
App received JSON:
waitingList state:
Sorted list:
expiredCount:
firstExpiredIdx:
Rendered IDs:
Device visible cards:

각 항목:

CONFIRMED
INFERRED
UNKNOWN

표시.

그리고 위에서 아래로 내려가면서

마지막 CONFIRMED 정상 단계

와

최초 CONFIRMED 장애 단계

를 구분한다.

UNKNOWN이 사이에 있으면
그 뒤 단계를 원인으로 확정하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. HTTP 이후가 UNKNOWN이면 최소 계측안만 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 JWT 때문에 HTTP 응답을 확인할 수 없고
정적 분석으로 원인 확정이 안 되면
딱 다음 3개 계측만 제안한다.

API_RESPONSE_COUNT + IDs
WAITING_LIST_COUNT + IDs
RENDERED_IDS

대상:

artifacts/swim-app/app/(teacher)/makeups.tsx

단,

이번 조사에서는 삽입하지 않는다.

console.log를 실제 넣지 않는다.
DEV UI를 실제 넣지 않는다.
Commit하지 않는다.
OTA하지 않는다.

어디에 무엇을 넣으면 되는지만
최소 diff 형태로 보고한다.

사용자 승인 후 별도 실행한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. 수정안 작성 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

원인이 CONFIRMED되기 전:

수정 금지.

원인이 CONFIRMED되면:

새 구조 설계 금지.

기준 SHA/hotfix의 정상 코드를
가장 작은 diff로 복구하는 방법을 우선 제안한다.

반드시 두 안을 검토:

A. 현재 구조를 새로 고치는 방법
B. 검증 완료 hotfix 동작을 최소 diff로 복원하는 방법

특별한 이유가 없으면 B를 우선한다.

수정 파일 수를 최소화한다.

DB UPDATE로 코드 문제를 덮지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. 작업 즉시 중단 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 상황이면 즉시 멈추고 보고:

- Production API가 https://swimnote.kr/api가 아님
- 기준 SHA 불일치
- 보강 hotfix 기준을 찾을 수 없음
- 실제 Render Live SHA와 조사 코드 SHA가 다름
- 인증 수정이 필요해 보임
- DB UPDATE/DELETE가 필요해 보임
- 기존 보강 데이터 변경이 필요해 보임
- 2개 이상의 서로 다른 원인이 발견되어 수정 범위가 커짐
- 보강 이외 파일 수정이 필요해 보임

임의 해결 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
17. 절대 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 절대 실행 금지:

코드 Write/Edit
DB UPDATE
DB DELETE
DB INSERT
Migration
Commit
Push
Render deploy
OTA
Preview OTA
환경변수 변경
eas.json 변경
package.json 변경
auth 변경
JWT 변경
SessionContext 변경
로그인 변경
AI 변경
메신저 변경
결제 변경
자동 생성 파일 수정

attached_assets를 commit에 포함하지 않는다.

Replit auto-commit이 발생했으면
그 사실을 즉시 보고하고
추가 Git 조작도 하지 말고 멈춘다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
18. 불필요한 작업 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 조사는 좁고 짧게 수행한다.

금지:

- 이미 확인한 내용을 다른 명령으로 반복 검증
- 전체 Repository 무차별 검색
- 관계없는 파일 분석
- 새로운 architecture 제안
- UI 재설계
- 보강 시스템 전체 리팩터링
- 테스트용 새 서버 생성
- Preview 환경 생성
- 임시 endpoint 생성
- 디버그용 인증 우회
- 원인 확정 전 코드 작성
- "혹시 모르니" 식 추가 수정

필요한 증거를 얻었으면 즉시 다음 단계로 넘어간다.

같은 사실을 2~3번 확인하면서 작업량을 늘리지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
19. 최종 보고 형식
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 15개만 제출:

1. Current Branch / HEAD / Status
2. 기준 SHA
3. 보강 관련 5개 파일 Diff
4. Production API URL
5. GET waiting 실제 HTTP 증거
6. API cache 구조
7. loadWaiting 흐름
8. waitingList / expiredCount / firstExpiredIdx 상태
9. expired separator 존재 여부
10. Scroll 구조
11. eligible-occurrences 상태
12. PHASE 1이 보강 코드에 미친 diff
13. 마지막 CONFIRMED 정상 지점 / 최초 장애 또는 UNKNOWN 지점
14. CONFIRMED 직접 원인 또는 "아직 원인 미확정"
15. 최소 복구안 + 수정 예상 파일

마지막에 반드시:

DB 변경 필요 여부:
코드 수정 실행 여부:
Commit:
Push:
Render:
OTA:

를 기재한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
20. 완료 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 단계의 성공 조건은
"보강이 고쳐졌다"가 아니다.

성공 조건:

기존 검증 완료 hotfix
→ 현재 Production

사이에서 어디가 달라졌는지 증명하고,

DB
→ Server
→ HTTP
→ App
→ State
→ Sort
→ Render
→ Device

중 최초로 깨지는 지점을
증거 수준에 맞게 특정하는 것이다.

원인을 CONFIRMED하지 못하면
UNKNOWN 지점과 최소 계측안만 제출한다.

보고 후 멈춘다.

절대 수정하지 않는다.
최종 설계도 확인했습니다.

이전 설계보다 구조가 훨씬 명확해졌습니다.

특히 아래 사항은 방향이 좋습니다.

- Service와 Hook 분리
- React 의존 제거
- SEARCHING 상태 추가
- PERMISSION 상태 유지
- Modal → Hook → Service → API → Engine → DB 단방향 구조
- 기존 API 유지
- 기존 기능 보존
- 기존 모달 삭제 없이 V2 병행

이 방향으로 진행하십시오.

다만 구현 전에 아래 사항을 마지막으로 반영하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. Hook는 상태관리만 담당하십시오.
━━━━━━━━━━━━━━━━━━━━━━

현재 useDiaryAIV2가

- 상태관리
- retry
- requestId
- Abort
- handleSubmit
- handleVoicePress
- handleInsert
- handleClose

까지 모두 가지고 있습니다.

가능하면

상태관리

↓

Controller

↓

Service

구조로 한 단계 더 분리할 수 있는지 검토하십시오.

다만 작업량이 크게 증가한다면 이번에는 Hook 구조를 유지해도 됩니다.

이번 목표는 구조를 완벽하게 만드는 것이 아니라
안정적인 V2를 만드는 것입니다.

━━━━━━━━━━━━━━━━━━━━━━
2. SEARCHING은 실제 서버 이벤트를 기다리지 말고 UI를 먼저 준비하십시오.
━━━━━━━━━━━━━━━━━━━━━━

현재 SEARCHING은

"확장 대비"

로 되어 있습니다.

하지만 앞으로 SWIMNOTE AI는

의도분석
↓

템플릿 확인
↓

DB 검색
↓

내용 보충
↓

GPT 생성

순서로 동작합니다.

따라서 SEARCHING 화면과 메시지는 지금부터 구현하십시오.

현재 서버가 진행 이벤트를 보내지 않더라도

SEARCHING

↓

GENERATING

구조를 사용할 수 있도록 설계하십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 진행 메시지는 하드코딩하지 마십시오.
━━━━━━━━━━━━━━━━━━━━━━

로딩 문구는

"관련 정보를 검색하고 있습니다."

"일지를 작성하고 있습니다."

등을 컴포넌트 내부에 직접 작성하지 말고

ProgressMessage

또는

DiaryProgressPhase

구조에서 관리하십시오.

향후

학부모 AI

성장리포트

영상분석

에서도 재사용할 수 있도록 하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. Service는 SWIMNOTE AI Engine 전용 규칙을 유지하십시오.
━━━━━━━━━━━━━━━━━━━━━━

Service가 GPT를 직접 호출하거나

DB를 직접 검색하거나

프롬프트를 생성해서는 안 됩니다.

Service는

API 요청

응답 검증

정규화

오류 변환

까지만 담당하십시오.

SWIMNOTE AI Engine의 역할을 Service가 가져오면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
5. V2 구현 중 기존 기능을 절대 삭제하지 마십시오.
━━━━━━━━━━━━━━━━━━━━━━

다음 기능은 그대로 유지되어야 합니다.

- requestId
- AbortController
- stale response 무시
- retry
- insert 중복 방지
- Whisper STT
- normalizeDiaryResponse
- validateDiaryRequest
- 결과 수정
- 전체 일지
- 학생별 일지
- 기존 저장 흐름

기능 삭제 없이 구조만 단순화하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 구현 순서를 지키십시오.
━━━━━━━━━━━━━━━━━━━━━━

1.
DiaryAIService

↓

2.
useDiaryAIV2

↓

3.
DiaryAIActionBarV2

↓

4.
DiaryAIModalV2

↓

5.
DiaryAIButton 연결

↓

6.
실제 기기 테스트

↓

7.
기존 모달 제거 여부 결정

기존 모달은 검증 완료 전까지 삭제하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 구현 완료 후 제출할 내용
━━━━━━━━━━━━━━━━━━━━━━

코드 작성 후에는 반드시 아래 내용을 함께 제출하십시오.

- 수정된 파일 목록
- 신규 파일 목록
- 기존 파일 중 미사용 파일
- API 변경 여부
- DB 변경 여부
- 기존 기능 유지 여부 체크리스트
- 실제 기기 테스트 결과
- 알려진 제한 사항

승인 후 구현을 시작하십시오.
[SWIMNOTE WEB — P0 MEGA MENU DISMISS 실제 동작 수정 / 이전 PASS 무효]

대표가 실제 Preview에서 직접 검증했습니다.

이전 완료보고의:

- 메뉴 밖으로 pointer 이동 → CLOSE PASS
- outside click → CLOSE PASS
- navigation → CLOSE PASS

판정은 실제 Preview 동작과 일치하지 않습니다.

현재 실제 증상:

1. ⋯ 메뉴 OPEN
2. 다른 페이지로 이동
3. 새 페이지 본문으로 마우스 이동
4. 본문 텍스트를 클릭/드래그하여 선택까지 가능
5. 그런데 Mega Menu는 계속 OPEN
6. ⋯ 버튼을 다시 눌러야만 사라짐

따라서 이전 PASS 보고는 무효입니다.

이번 작업에서는 추정이나 코드상 PASS가 아니라
실제 Preview 동작을 기준으로 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 먼저 실제 원인을 찾을 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SiteHeader / Expanded Menu의 DOM 구조를 실제로 확인하십시오.

특히 다음을 확인:

- panelRef가 실제 보이는 메뉴 박스에 붙어 있는지
- panelRef가 full-screen wrapper에 붙어 있지 않은지
- invisible backdrop/scrim이 viewport 전체를 덮고 있지 않은지
- fixed wrapper의 height가 화면 전체인지
- pointer-events를 받는 투명 element가 본문 위에 남아 있지 않은지
- Desktop에서도 Mobile용 backdrop이 살아 있는지
- document outside-pointer listener가 실제로 등록되는지
- route change effect가 실제 pathname 변경을 감지하는지

현재 대표가 본문 텍스트를 선택할 수 있는데
메뉴가 닫히지 않는다는 것은
기존 outside detection이 실동작하지 않는 상태입니다.

원인을 정확히 확인한 뒤 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. DESKTOP 메뉴를 MODAL처럼 만들지 말 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Desktop의 ⋯ Mega Menu는
Modal이 아니라 Navigation Dropdown입니다.

Desktop에서는:

- full-screen backdrop 금지
- viewport 전체 transparent overlay 금지
- body scroll lock 금지
- invisible full-screen click layer 금지

Mega Menu element는
실제로 보이는 메뉴 영역만 차지해야 합니다.

예:

Header
높이 약 52px

Expanded Panel
Header 바로 아래부터
실제 메뉴 콘텐츠 높이까지만

그 아래 실제 페이지 영역은
진짜 page body여야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. POINTER LEAVE 판정 방식 교체
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 mouseleave/relatedTarget 구현이 신뢰할 수 없으므로
Desktop에서는 좌표 기반 global pointer tracking으로 변경하십시오.

메뉴가 OPEN인 동안에만
document의 pointermove를 감시합니다.

단 mouse pointer에만 적용:

event.pointerType === "mouse"

또는 mouse 환경에서만 실행.

매 pointer move 시:

headerRef.current.getBoundingClientRect()
panelRef.current.getBoundingClientRect()

를 가져와서

현재 clientX/clientY가:

A. Header 실제 rect 내부
또는
B. Visible Expanded Panel 실제 rect 내부

이면 메뉴 유지.

둘 다 아니면:
100ms 정도 dismiss timer 시작.

100ms 이내 다시 A/B 영역으로 들어오면 timer cancel.

즉 개념적으로:

const insideHeader = pointInside(headerRect, x, y)
const insidePanel = pointInside(panelRect, x, y)

if (insideHeader || insidePanel) {
  cancelCloseTimer()
} else {
  scheduleClose(100)
}

중요:

DOM contains(target)만 믿지 마십시오.

Full-screen wrapper/backdrop가 존재할 경우
contains()는 잘못된 결과를 낼 수 있습니다.

반드시 "실제로 보이는 Header와 Panel의 bounding rect"
기준으로 판정하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. panelRef 위치 절대 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

panelRef는 반드시:

"사용자 눈에 실제 보이는 흰색 Mega Menu panel"

그 element 자체에 붙이십시오.

다음에 붙이면 FAIL:

- viewport 전체 wrapper
- backdrop
- AnimatePresence outer container
- fixed inset-0 container
- page overlay

panelRef.getBoundingClientRect()의 bottom은
스크린샷상 메뉴가 끝나는 실제 위치 부근이어야 합니다.

화면 하단까지 내려가 있으면 잘못된 ref입니다.

개발 중 실제 rect 값을 확인하고 수정하십시오.

최종 코드에는 debug log 제거 가능.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. OUTSIDE POINTERDOWN은 별도로 강제
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

pointermove와 별개로
document pointerdown도 사용하십시오.

메뉴 OPEN 상태에서 pointerdown 좌표가:

Header rect 외부
AND
Panel rect 외부

이면:

setMenuOpen(false)

즉 대표가 현재처럼
새 페이지 본문 텍스트를 클릭하거나 드래그하기 시작하는 순간
무조건 메뉴가 닫혀야 합니다.

본문에서 text selection을 시작해도:

pointerdown
→ menu CLOSE
→ drag selection 계속

이어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. NAVIGATION은 클릭 순간 즉시 CLOSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Mega Menu의 모든 navigation item은:

onClick / navigation handler에서
route 이동보다 먼저:

closeExpandedMenu()

를 실행하십시오.

순서:

메뉴 item 선택
→ setOpen(false)
→ navigate

입니다.

Route 이동 후 useEffect만 믿지 마십시오.

둘 다 적용:

1. Item click immediate close
2. Route/location change safety close

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. ROUTE CHANGE SAFETY RESET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Wouter의 실제 현재 location API를 확인하여
pathname 변경 시:

setOpen(false)

가 반드시 실행되게 하십시오.

추가로 필요한 경우:

- popstate
- hashchange

도 처리하십시오.

특히:

/technology#investment

같은 hash navigation에서도
기존 Mega Menu가 남으면 안 됩니다.

최종 원칙:

PATH CHANGE → CLOSED
HASH CHANGE → CLOSED
BACK/FORWARD → CLOSED

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. HEADER ↔ PANEL 이동은 유지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

자동 close가 너무 민감해서
메뉴 사용 자체가 어려워지면 안 됩니다.

다음은 유지:

Header → Panel
= OPEN 유지

Panel → Header
= OPEN 유지

Panel → 실제 Page Body
= 약 100ms 후 CLOSE

Header → 실제 Page Body
= 약 100ms 후 CLOSE

100ms는 flicker 방지용.

300ms 이상 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. MOBILE은 별도 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Mobile/Touch에서는 pointer hover close를 적용하지 않습니다.

Mobile:

⋯ tap → OPEN

Menu item tap
→ 즉시 CLOSE
→ navigation

Outside tap
→ CLOSE

ESC 가능한 환경
→ CLOSE

Route change
→ CLOSE

Mobile에서는 기존 full-screen menu가 필요하면 유지 가능하나
Desktop DOM/behavior와 혼동되지 않게 분기하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 전역 UX 헌법 교체
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이전 TRANSIENT UI DISMISSAL CONSTITUTION을
이번 규칙으로 교체하십시오.

이번 버전이 최종 헌법입니다.

핵심:

"Transient UI는 사용자가 다음 작업을 시작하면
자동으로 사라져야 한다."

필수 dismiss triggers:

NAVIGATION
→ CLOSE

ROUTE/HASH CHANGE
→ CLOSE

OUTSIDE POINTER DOWN
→ CLOSE

DESKTOP POINTER LEAVES VISIBLE INTERACTION REGION
→ CLOSE

ESC
→ CLOSE

ACTION COMPLETE
→ CLOSE

향후 PC Dashboard 제작에서도 동일 적용.

특히 PC Dashboard의:

- dropdown
- profile menu
- filter
- sort
- more menu
- notification panel
- member action menu
- class action menu
- context menu

에 그대로 적용하십시오.

단 persistent sidebar/modal처럼
명시적으로 유지되어야 하는 UI는 예외.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 이번엔 "코드상 PASS" 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이전 허위 PASS가 있었으므로
이번에는 다음 실동작을 Preview에서 직접 수행하십시오.

TEST A

⋯ OPEN
→ 마우스를 메뉴에서 아래 Page Body로 이동
→ 손대지 않고 0.2초 대기
→ 메뉴가 실제 화면에서 사라져야 함

PASS/FAIL

TEST B

⋯ OPEN
→ Page Body 텍스트에서 mouse down
→ drag selection

mouse down 순간 메뉴 CLOSE되어야 함.

PASS/FAIL

TEST C

⋯ OPEN
→ "특허 / IP" 클릭

새 페이지가 보이는 순간
Mega Menu 없어야 함.

PASS/FAIL

TEST D

⋯ OPEN
→ "AI 및 기술 투자" 클릭
→ #investment 이동

Mega Menu 없어야 함.

PASS/FAIL

TEST E

⋯ OPEN
→ Header에서 Panel로 마우스 이동

Mega Menu가 닫히면 안 됨.

PASS/FAIL

TEST F

Panel → Header

유지되어야 함.

PASS/FAIL

TEST G

⋯ OPEN
→ ESC

CLOSE + ⋯ focus 복귀.

PASS/FAIL

TEST H

⋯ OPEN
→ Browser Back

CLOSE.

PASS/FAIL

TEST I

Mobile
→ Menu item tap

CLOSE + navigation.

PASS/FAIL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 실제 검증 증거를 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 완료보고에는 단순히:

"PASS"

만 쓰지 마십시오.

반드시 다음을 포함:

1. 기존 root cause
2. panelRef가 붙은 실제 DOM element
3. Desktop panel의 실제 boundingRect:
   - top
   - bottom
   - height

4. pointer가 Page Body에 있을 때:
   - clientY
   - panelRect.bottom
   - insidePanel = false
   확인

5. TEST A~I 실제 결과

즉:

"코드상 동작 예상"

금지.

실제 Preview에서 interaction 수행 후 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. 작업 범위 제한
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번에는 이것만 수정:

- SiteHeader Mega Menu dismiss behavior
- 관련 공통 Transient UI UX Constitution

변경 금지:

HOME
제품 카드
로고
제품 상세
Login/Auth
PC Dashboard
DB/API
AI ENGINE
Production Deploy

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. 종료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Build PASS 후 중단.

Production 배포 금지.

대표가 Preview에서 직접:

⋯ OPEN
→ 마우스를 아래 본문으로 이동

을 다시 검증한 뒤 승인합니다.
[보낼 곳: APP REPLIT]

[P0 FINAL — AI Diary 실제 사용 경로 추적 → 최소 수정 → Production E2E → iOS OTA]

중요 전제:

- Render 최신 배포는 이미 완료된 상태로 간주한다.
- 기존 CurriculumEvidenceResolver / Manual Diary / growth_events / CPO / SCP 구조는 현재 정상 연결 상태다.
- 기존 수동일지 backfill/precision guard/direct match 수정은 절대 되돌리지 않는다.
- 이번 작업은 실제 사용 중인 AI Diary가 왜 DB에서 ai_generated=false / curriculum_matches=0으로 남는지 확인하고,
  정확한 최소 수정만 한 뒤 최종 Production 검증과 OTA까지 끝내는 작업이다.

━━━━━━━━━━━━━━━━━━━━━━
0. 절대 보호조건
━━━━━━━━━━━━━━━━━━━━━━

절대 금지:

- 기존 Manual Diary resolver 삭제/대체
- 기존 AI Diary curriculum_matches 생성 로직 삭제
- MatchToken 검증 삭제
- growth_events schema 변경
- CPO/SCP schema 변경
- curriculum_versions/items 재생성
- active curriculum version 변경
- Toykids curriculum 데이터 수정
- AI Engine Teacher Diary 핵심 pipeline 재작성
- 기존 APP contract 임의 변경
- 새로운 일반 GPT classifier 추가
- 기존 precision guard/direct match 제거
- backfill 재실행으로 기존 evidence 덮어쓰기

이번 작업은
"AI Diary provenance / curriculum_matches 유실 지점"
만 정확히 찾아 최소 수정한다.

━━━━━━━━━━━━━━━━━━━━━━
1. 실제 Production AI Diary UI 경로 전수 추적
━━━━━━━━━━━━━━━━━━━━━━

사용자는 Toykids에서 현재 AI 일지를 실제로 사용 중이다.

따라서 이전 보고의:

"AI Diary generate 사용 이력 0"
"adoption 문제"

결론은 폐기.

현재 Production 앱에서 선생님이 실제 사용하는 AI 일지 흐름을 코드 기준으로 끝까지 추적:

AI 일지 버튼
→ Modal
→ Hook
→ Service
→ Client
→ 실제 HTTP endpoint
→ Response
→ Draft 적용
→ Diary Save
→ Server Route
→ DB INSERT

각 단계 파일/함수명 제출.

특히 실제 endpoint를 정확히 확인:

- SWIMNOTE AI Engine 직접 호출인지
- APP API proxy인지
- legacy 일반 GPT endpoint인지
- /v1/teacher-diary/generate인지
- 다른 endpoint인지

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 실제 AI 요청 Contract 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 실제 AI Diary 호출 1건 기준으로 확인:

- schema_version / contractVersion
- feature
- pool_id
- class_id
- lesson_date
- student_refs
- poolMode
- endpoint host/path

중요:

과거 조사에서 curriculum_matches 생성 조건이
contractVersion='1.3' AND poolMode='x'
라고 보고됐다.

현재 프로젝트 공식 Teacher Diary contract는 1.0 기준으로 운영되어 왔으므로,
실제 Production 호출이:

A. 1.0
B. 1.3
C. 다른 값
D. legacy contract

중 무엇인지 반드시 실측.

임의로 contractVersion을 바꾸지 말고
먼저 실제값과 server 조건을 비교.

━━━━━━━━━━━━━━━━━━━━━━
3. 실제 AI Response provenance 추적
━━━━━━━━━━━━━━━━━━━━━━

AI 응답 직후 앱 state에서 확인:

- request_id
- pipeline_mode
- result.common
- result.students
- template_ids
- knowledge_ids
- curriculum_matches
- isAiGenerated 또는 equivalent state
- generation source / provenance field

curriculum_matches가 response에:

- 존재
- 빈 배열
- field 자체 없음

중 무엇인지 확인.

━━━━━━━━━━━━━━━━━━━━━━
4. Draft 적용 후 값 유실 여부 추적
━━━━━━━━━━━━━━━━━━━━━━

AI 응답을 기존 Diary 입력란에 적용한 직후 확인:

- aiGenerated state
- curriculumMatches state
- request_id/provenance state

그리고 최종 저장 직전:

POST /diaries body에 실제로:

- ai_generated
- curriculum_matches
- ai request/provenance
- note content

중 무엇이 들어가는지 확인.

단계별 비교:

AI response
→ Hook state
→ Diary screen state
→ Save handler
→ HTTP body
→ Server request
→ DB row

어느 단계에서 값이 사라지는지 정확히 특정.

━━━━━━━━━━━━━━━━━━━━━━
5. class_diaries.ai_generated=false 원인 확정
━━━━━━━━━━━━━━━━━━━━━━

class_diaries.ai_generated가 어디에서 설정되는지 확인.

가능성:

A. client가 보내지 않아 DB default=false
B. Save handler가 false로 덮음
C. server가 curriculum_matches 존재 여부로 판단
D. AI 전용 save endpoint에서만 true
E. Draft 적용 후 일반 save route를 타면서 provenance 유실
F. 실제 AI 기능이 legacy path라 flag 자체가 없음

실제 AI Diary 1건 기준:

AI 생성 직후:
aiGenerated = ?

POST /diaries 직전:
ai_generated = ?

server 수신:
ai_generated = ?

DB INSERT:
ai_generated = ?

반드시 실측.

━━━━━━━━━━━━━━━━━━━━━━
6. curriculum_matches=0 원인 확정
━━━━━━━━━━━━━━━━━━━━━━

실제 Production AI Diary 1건에서 확인:

- Phase 8 실행 여부
- searchCurriculumCandidates 실행 여부
- active curriculum version 조회 여부
- candidates 수
- curriculum_matches 수
- match_token 존재 여부

Toykids curriculum 자체는 이미 정상임:

- active version 정상
- curriculum_items 정상
- source_trace/stroke/domain/atomic_skill 정상

따라서 curriculum data provisioning 재조사 금지.

원인은 AI 호출 경로/contract/save 유실 중에서 찾는다.

━━━━━━━━━━━━━━━━━━━━━━
7. 아래 CASE 중 하나로 판정
━━━━━━━━━━━━━━━━━━━━━━

CASE A
AI 기능은 사용 중이나
save 시 ai_generated flag만 유실.

CASE B
AI 기능은 사용 중이나
legacy AI endpoint를 타서 curriculum_matches 미생성.

CASE C
새 AI Engine 사용 중이나
contractVersion / poolMode 조건 때문에
curriculum Phase 8 미실행.

CASE D
curriculum_matches는 생성되지만
Draft → Diary Save 사이에서 유실.

CASE E
복합 원인.

CASE F
기타 — 실제 evidence와 함께 설명.

━━━━━━━━━━━━━━━━━━━━━━
8. 최소 수정 원칙
━━━━━━━━━━━━━━━━━━━━━━

CASE 확정 후 필요한 최소 수정만 한다.

예:

CASE A:
save body에 ai_generated provenance 정상 전달.

CASE B:
현재 사용 중인 AI Diary path가 legacy라면
기존 UI/UX 유지한 채
정식 Teacher Diary Engine 경로로 연결하거나
현재 legacy path가 curriculum evidence를 받을 수 있도록
기존 Engine adapter와 연결.

단 legacy fallback을 조용히 유지하거나
새 일반 GPT 경로를 추가하지 말 것.

CASE C:
contract mismatch라면
공식 contract를 깨지 않는 방식으로
server-side 호환/adapter 수정.

1.0 contract를 강제로 1.3으로 바꾸는 식의
breaking change 금지.

CASE D:
curriculum_matches state가 save까지 보존되도록
최소 수정.

중요:
기존 AI Diary 정상 생성/문장 품질/학생별 분리 기능을
절대 훼손하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
9. 기존 경로 Regression
━━━━━━━━━━━━━━━━━━━━━━

수정 후 반드시:

A. 기존 Manual Diary
→ resolver
→ growth_event
→ CPO
→ SCP

정상 유지.

B. AI Diary
→ generate
→ curriculum_matches
→ save
→ growth_event
→ CPO
→ SCP

정상.

C. 기존 Parent/Admin/X progress API contract
변화 없음.

D. 기존 backfill 결과 보존.

━━━━━━━━━━━━━━━━━━━━━━
10. 실제 Production AI Diary E2E
━━━━━━━━━━━━━━━━━━━━━━

실제 안전한 test student 1명으로 AI Diary 1건 실행.

확인:

1. AI generate 실제 성공
2. request_id 생성
3. 실제 endpoint 기록
4. 실제 contractVersion 기록
5. curriculum_matches > 0 또는
   내용상 후보 없음이면 0이 정상인 이유 명시
6. match_token 존재 여부
7. POST /diaries에 ai_generated=true
8. POST /diaries에 curriculum_matches 전달
9. DB class_diaries.ai_generated=true
10. growth_event source=match_token 생성
11. CPO 생성/갱신
12. SCP 재계산

내용상 curriculum candidate가 확실히 나오는
예: 평영 팔동작 / 접영 발차기 등
테스트 문장을 사용해
curriculum_matches가 실제 생성되는 케이스로 검증.

━━━━━━━━━━━━━━━━━━━━━━
11. Manual Diary Production E2E 재확인
━━━━━━━━━━━━━━━━━━━━━━

같은 최신 서버에서 수동 Diary 1건:

Diary
→ CurriculumEvidenceResolver
→ growth_event teacher_manual
→ CPO
→ SCP

정상 유지 확인.

AI 수정 때문에 manual pipeline 깨지면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
12. Admin / Parent / X 실제 검증
━━━━━━━━━━━━━━━━━━━━━━

실제 학생 3명:

- diary 많음
- 중간
- 적음

각각:

Admin progress
Parent progress

동일값 확인.

X Dashboard:

progress_students_total
progress_students_confirmed
progress_avg_pct

Production API 실제값 == UI 실제값 확인.

회원상세도 실제 카드 클릭 정상 확인.

━━━━━━━━━━━━━━━━━━━━━━
13. Render 최신 SHA
━━━━━━━━━━━━━━━━━━━━━━

수정이 발생했다면:

main push
→ Render가 정확히 최신 main SHA LIVE

확인:

/health commit
==
최종 main HEAD

필수.

코드 수정이 없었다면
현재 LIVE SHA 그대로 기록.

"push 완료"만으로 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
14. 테스트
━━━━━━━━━━━━━━━━━━━━━━

필수:

- AI Diary provenance test
- curriculum_matches preservation test
- ai_generated save test
- MatchToken path test
- Manual Diary regression
- curriculum progress mapper regression

이번 변경 신규 FAIL = 0.

기존 unrelated 실패는
이번 작업에서 수정하지 말고 목록만 유지.

━━━━━━━━━━━━━━━━━━━━━━
15. iOS OTA 사전조건
━━━━━━━━━━━━━━━━━━━━━━

OTA 전 실제 확인:

Production installed build:
255

runtimeVersion:
2.1.0

app version:
2.0.1

Git canonical source:
release/v2.0.0

EAS channel:
production-v2

production-v2가 실제로 어느 EAS update branch에 매핑되는지
EAS 실제 조회 결과 사용.

문서/메모리 추정값 사용 금지.

또한:

- release/v2.0.0 clean
- origin/release/v2.0.0 exact
- native incompatible change 없음
- Android 제외

확인.

━━━━━━━━━━━━━━━━━━━━━━
16. iOS Production OTA
━━━━━━━━━━━━━━━━━━━━━━

위 전부 PASS 후 OTA 1회만 발행.

iOS only.
Android OTA 금지.
중복 OTA 금지.

최신 APP 변경이
canonical release/v2.0.0에 정상 포함된 상태에서만 발행.

━━━━━━━━━━━━━━━━━━━━━━
17. OTA 재시작 모달 실제 확인
━━━━━━━━━━━━━━━━━━━━━━

build 255 실제 기기에서:

앱 실행/foreground
→ checkForUpdateAsync()
→ update available
→ fetchUpdateAsync()
→ "업데이트 적용을 위해 재시작" 모달
→ 확인
→ reloadAsync()
→ 최신 코드 적용

모달이 안 나오면 OTA 재발행 금지.

먼저:

- channel
- mapped branch
- runtime
- update availability
- check trigger

trace.

━━━━━━━━━━━━━━━━━━━━━━
18. 최종 완료보고
━━━━━━━━━━━━━━━━━━━━━━

아래 순서 그대로 보고:

1. 실제 AI Diary UI 경로
2. 실제 endpoint
3. 실제 contractVersion
4. 실제 poolMode
5. AI response curriculum_matches
6. Draft state curriculum_matches
7. POST /diaries ai_generated
8. POST /diaries curriculum_matches
9. DB class_diaries.ai_generated
10. growth_event 생성 결과
11. 정확한 CASE A/B/C/D/E/F
12. 수정 파일
13. Manual Diary regression
14. AI Diary Production E2E
15. Admin/Parent 학생 3명 progress
16. X Dashboard KPI
17. 회원상세 실기기 결과
18. 최종 main SHA
19. Render LIVE SHA
20. /health commit
21. 이번 변경 신규 테스트 FAIL
22. production-v2 실제 mapped EAS branch
23. release/v2.0.0 clean/origin exact 여부
24. OTA ID
25. build 255 update detected 여부
26. 재시작 모달 실제 표시 여부
27. reload 후 적용 확인
28. 잔존 FAIL

잔존 FAIL 0개일 때만 PASS.

완료 후 STOP.
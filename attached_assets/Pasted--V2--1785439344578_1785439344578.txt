설계도 확인했습니다.

전체 방향은 기존 모달을 계속 수정하는 것이 아니라,
기존 기능을 유지한 채 새 모달(V2)로 재구성하는 방향이라 좋습니다.

다만 구현 전에 아래 내용을 반드시 반영해서 설계도를 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. useDiaryAI는 통째로 복사하거나 이식하지 마십시오.
━━━━━━━━━━━━━━━━━━━━━━

현재 useDiaryAI에는

- UI 상태관리
- API 호출
- AbortController
- Retry
- requestId 관리

등이 모두 섞여 있습니다.

이 상태를 그대로 복사하면 현재 구조와 버그까지 같이 가져오게 됩니다.

반드시 아래 구조로 분리하십시오.

DiaryAIModalV2
↓

useDiaryAIService (또는 DiaryAIService)

↓

generateDiary()
processVoice()
normalizeDiaryResponse()
validateDiaryRequest()
handleInsert()

↓

API

↓

SWIMNOTE AI Engine

↓

DB

UI와 Service를 완전히 분리하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. SEARCHING 상태를 지금부터 포함하십시오.
━━━━━━━━━━━━━━━━━━━━━━

현재 설계에서는

INPUT
→ GENERATING
→ RESULT

인데,

SWIMNOTE AI 엔진은 앞으로

교사 입력
→ 의도 분석
→ 템플릿 확인
→ DB 검색
→ 내용 보충
→ GPT 생성

순서로 동작합니다.

따라서 UI도 이 구조를 반영할 수 있도록

INPUT
→ SEARCHING
→ GENERATING
→ RESULT

상태를 사용할 수 있도록 설계하십시오.

현재 서버가 SEARCHING 이벤트를 보내지 않더라도
상태 확장이 가능하도록 구조를 미리 준비하십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. PERMISSION 상태는 유지하십시오.
━━━━━━━━━━━━━━━━━━━━━━

권한 거부는 일반 오류(ERROR)가 아닙니다.

PERMISSION 상태를 유지하고
기존 AIPermissionView를 그대로 사용하십시오.

최종 상태는

INPUT
RECORDING
TRANSCRIBING
PERMISSION
SEARCHING
GENERATING
RESULT
ERROR

으로 유지하는 방향으로 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. Hook과 Service는 모달 전용 구조가 되면 안 됩니다.
━━━━━━━━━━━━━━━━━━━━━━

앞으로

- AI 일지
- 학부모 AI
- 성장리포트
- 영상분석
- 기타 AI 기능

에서 동일한 구조를 사용할 예정입니다.

따라서

DiaryAIModalV2
↓

Diary 전용 Hook

↓

DiaryAIService

처럼 계층을 분리하고,

Service는 React UI를 전혀 알지 못하도록 작성하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 계층 의존성을 단방향으로 유지하십시오.
━━━━━━━━━━━━━━━━━━━━━━

최종 구조는 아래 원칙을 반드시 지키십시오.

모달

↓

Hook

↓

Service

↓

API

↓

AI Engine

↓

DB

모달은 AI 처리 로직을 가지지 않습니다.

Hook은 레이아웃을 가지지 않습니다.

Service는 UI를 참조하지 않습니다.

API는 모달 존재를 알지 못합니다.

AI Engine은 React 구조를 알지 못합니다.

DB는 React와 API를 알지 못합니다.

역방향 의존성은 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 기존 기능은 절대 누락하지 마십시오.
━━━━━━━━━━━━━━━━━━━━━━

현재 설계에 정리한 기능은 모두 유지하십시오.

특히 아래 기능은 빠지면 안 됩니다.

- requestId 관리
- AbortController
- stale response 무시
- 자동 retry
- 입력 중 닫기 Alert
- 생성 중 dismiss 차단
- Android Back 처리
- insert 중복 방지
- 결과 수정
- 전체 일지
- 학생별 일지
- 기존 저장 흐름
- Whisper STT
- normalizeDiaryResponse
- validateDiaryRequest

기능 삭제 없이 구조만 단순화하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 서버와 엔진은 수정 대상이 아닙니다.
━━━━━━━━━━━━━━━━━━━━━━

이번 작업은

모달 구조와 연결 구조를 재정리하는 작업입니다.

SWIMNOTE AI Engine

DB

API

GPT 처리

템플릿 처리

커리큘럼 처리

는 변경하지 마십시오.

기존 API를 그대로 사용하는 것을 원칙으로 합니다.

━━━━━━━━━━━━━━━━━━━━━━
8. 구현 전에 수정된 설계도만 다시 제출하십시오.
━━━━━━━━━━━━━━━━━━━━━━

아직

- 코드 작성
- 커밋
- OTA 배포
- 기존 모달 삭제

는 진행하지 마십시오.

위 수정사항을 반영한 최종 설계도를 먼저 제출한 뒤,
승인 후 구현을 시작하십시오.
SWIMNOTE 보강 정원초과 허용 — 배포 전 최종 Gate 검증

현재 구현 diff는 승인한다.
아직 Commit/Push/Render/OTA 하지 않는다.

이번 단계에서는 코드 수정하지 말고
현재 working tree 그대로 핵심 변경이 실제 운영 코드 경로에서 동작하는지만 검증한다.

목적:
기존 88개 회귀 테스트 통과와 별도로
이번 "정원 마감 반 보강 허용" 변경 자체를 직접 검증한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Teacher 미래 assign 직접 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 teachers.ts Route를 사용하는 테스트로 확인:

조건:
- makeup status = waiting
- 대상 반 정원 = 5
- 현재 인원 = 5
- occurrence = 미래
- 날짜는 today -14 / +28 범위 안
- 요일 정상
- 휴일 아님
- pool 일치

기대:
HTTP 성공
CLASS_FULL 발생하지 않음
assign 처리 경로 진입

가능하면 6/5 상태도 추가 확인.

중요:
teachers.ts 로직 복사본 테스트 금지.
실제 Route 또는 실제 운영 함수 연결 테스트만 인정.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. Teacher 오늘/과거 complete-direct 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정원 5/5 상태에서:

A. 오늘
B. 과거지만 -14일 범위 내

각각 complete-direct가
CLASS_FULL 때문에 차단되지 않는지 확인.

기존 날짜/요일/휴일/state 검증은 그대로 통과해야 함.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. eligible-classes 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

teacher:
정원 5/5 반이 eligible-classes 응답에서 사라지지 않고 포함되는지 확인.

admin:
정원 5/5 반이 응답에서 filter로 제거되지 않고 포함되는지 확인.

그리고 capacity/current_members/is_full/is_eligible 등의
표시용 데이터는 계속 반환되는지 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 기존 차단 규칙 생존 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정원이 차 있어도 보강은 허용하지만 아래는 그대로 차단:

- -15일
- +29일
- 잘못된 달력 날짜
- 다른 pool 반
- 수업 요일 불일치
- 휴일
- completed
- cancelled
- expired + allow_expired 미승인
- 미래 날짜를 complete-direct로 요청
- 오늘/과거를 미래 assign 방식으로 요청

기존 테스트가 실제 운영 Route를 검증하고 있다면
그 테스트 결과를 재사용해도 됨.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. UI 정적 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

makeups.tsx:
- 정원마감/정원초과 뱃지 유지
- is_full 때문에 disabled 되지 않음
- is_full 때문에 onPress return 하지 않음

ClassDetailSheet.tsx:
- disabled={isSaving} 유지
- is_full 때문에 disabled 되지 않음
- 정원마감/정원초과 뱃지 유지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음만 제출:

1. 사용한 테스트 파일
2. 운영 Route 직접 연결 여부
3. 5/5 미래 assign 결과
4. 5/5 오늘 complete-direct 결과
5. 5/5 과거 complete-direct 결과
6. teacher 5/5 eligible-classes 노출 결과
7. admin 5/5 eligible-classes 노출 결과
8. 기존 날짜/state/요일/휴일 차단 회귀 결과
9. 전체 테스트 PASS/FAIL
10. 현재 git diff --name-status
11. 코드 추가 수정 발생 여부
12. 최종 배포 가능 여부

핵심 테스트가 실제 Route 연결로 PASS하면
그때 Commit → Push → Render 배포 → Health Check → OTA 순서로 진행한다.

검증 결과만 제출하고 중단.
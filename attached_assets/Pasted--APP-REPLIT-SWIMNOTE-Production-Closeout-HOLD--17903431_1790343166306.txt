[보낼 곳: APP REPLIT]

[SWIMNOTE — 회원 일괄등록 최종 Production Closeout]

현재 상태는 HOLD입니다.

기능 수정 내용은 승인합니다.

다만 완료보고에 따르면:

WEB BulkMembersPage.tsx
= “개발 서버 반영”

으로만 되어 있어
Production WEB 배포가 확인되지 않았습니다.

또한 API/APP 배포 식별자가 축약되어 있고
이전에 요구한 1,000명 synthetic regression 결과가
최종 보고에서 빠져 있습니다.

새 기능 추가나 리팩터링은 하지 마십시오.

아래 항목만 최종 확인/배포하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. WEB PRODUCTION 배포
━━━━━━━━━━━━━━━━━━━━━━

현재 개발 서버에만 반영된:

BulkMembersPage.tsx

최종 변경을
실제 Production dashboard에 배포하십시오.

대상:

https://swimnote.kr

회원 엑셀 일괄등록 화면에서 실제로 다음이 보여야 합니다.

- 기본 다운로드 양식 = 이름 + 보호자 연락처 2컬럼
- “이름과 보호자 연락처만 있으면 등록”
- 동명이인 허용
- 반 이름 등 선택사항
- blocking / warning 구분
- 행별 오류 표시
- 오류 CSV 다운로드
- 최대 1,000명

개발 서버 화면만 확인하고
Production 정상이라고 보고하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. API 실제 Production 증빙
━━━━━━━━━━━━━━━━━━━━━━

현재 보고:

SHA 4699183a
Render LIVE

축약값만 있습니다.

실제 다음을 조회하십시오.

- Work HEAD full 40-character SHA
- Production LIVE full 40-character SHA
- Render Deploy ID
- status = live

Work SHA와 LIVE SHA가
이번 bulk 변경을 포함한 동일 commit인지 확인.

추측해서 40자를 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. APP OTA 실제 증빙
━━━━━━━━━━━━━━━━━━━━━━

현재 보고:

iOS 01a0d8b0

은 축약값입니다.

실제 다음을 보고하십시오.

- full 40-character OTA Git SHA
- EAS Update Group ID 전체
- iOS Update ID 전체
- branch
- channel
- runtimeVersion

기준:

branch/channel = production-v2
runtimeVersion = 2.2.0

추가 OTA는
현재 OTA가 정상 배포된 것이 확인되면 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 1,000명 SYNTHETIC TEST
━━━━━━━━━━━━━━━━━━━━━━

이전에 요청한 TEST A~J가
실제로 실행됐다면 결과만 증빙하십시오.

실행하지 않았다면
Production 회원 데이터를 생성하지 말고
test/local/rollback 가능한 환경에서만 실행하십시오.

A.
정상 1,000명
→ validate 성공
→ commit 전체 성공

B.
1,000명 중 이름 누락 1명
→ 정확한 Excel 행 번호
→ 0명 INSERT

C.
보호자 연락처 누락 1명
→ 정확한 row/name/reason
→ 0명 INSERT

D.
동명이인 다수
→ blocking 없음

E.
같은 보호자 연락처 형제 다수
→ blocking 없음

F.
없는 반 20명
→ warning
→ 회원 등록
→ class_group_id=null

G.
기존 회원과 이름+보호자 연락처 동일
→ duplicate warning
→ 등록 가능

H.
1,001명
→ ROW_LIMIT_EXCEEDED
→ 0명 INSERT

I.
기존 7컬럼 파일
→ 정상 parse

J.
신규 2컬럼 파일
→ 정상 parse

가능하면 함께 보고:

- 1,000명 validate 소요시간
- commit 소요시간
- payload 크기
- 413 발생 없음
- timeout 발생 없음

테스트를 수행하지 못한 항목은
PASS라고 쓰지 말고
“확인하지 못함”이라고 쓰십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 정책 최종 확인
━━━━━━━━━━━━━━━━━━━━━━

Production 코드에서 다음이 실제 유지되는지만 확인하십시오.

필수:
- 이름
- 보호자 연락처

허용:
- 동명이인
- 동일 보호자 연락처
- 이름+전화 동일도 warning 후 등록 가능

WARNING:
- duplicate suspected
- class not found
- optional field 문제

BLOCKING:
- 이름 없음
- 보호자 연락처 없음
- 명백히 사용할 수 없는 연락처
- 필수 header 없음
- 1,001명 이상
- 파일/시스템 오류

반 이름 미발견:
회원 등록 허용
class_group_id = null

partial success:
금지

blocking error 존재:
0명 INSERT

━━━━━━━━━━━━━━━━━━━━━━
6. DB / 기존 데이터
━━━━━━━━━━━━━━━━━━━━━━

확인:

- DB migration 없음
- 기존 회원 데이터 수정 없음
- Production synthetic 1,000명 INSERT 없음
- 기존 parent auto-approval 로직 의미 변경 없음

━━━━━━━━━━━━━━━━━━━━━━
7. 최종 보고 형식
━━━━━━━━━━━━━━━━━━━━━━

[A. PRODUCTION WEB]

- Production Git SHA
- deploy ID/status
- production domain
- 실제 Production 화면에서 새 정책 확인 여부

[B. API]

- Work full SHA
- Production LIVE full SHA
- Render Deploy ID
- status

[C. APP]

- OTA full Git SHA
- EAS Update Group ID
- iOS Update ID
- branch/channel
- runtimeVersion

[D. TEST A~J]

각각:
PASS / FAIL / 확인하지 못함

[E. 1000 PERFORMANCE]

- payload size
- validate time
- commit time
- 413 여부
- timeout 여부

[F. FINAL POLICY]

- 필수 2개
- duplicate warning
- class warning
- all-or-nothing

[G. UNCHANGED]

- DB migration 없음
- 기존 회원 데이터 변경 없음
- Production test insert 없음
- Native build 없음

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은 ‘확인하지 못함’이라고 명시하고
축약 SHA나 추측값으로 Production 증빙을 대신하지 마십시오.
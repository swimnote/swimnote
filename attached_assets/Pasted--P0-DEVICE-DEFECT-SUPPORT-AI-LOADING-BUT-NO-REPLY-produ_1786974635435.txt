[P0-DEVICE-DEFECT — SUPPORT AI LOADING BUT NO REPLY]

실제 production iPhone defect.

재현:

TestFlight Build 246
production OTA:
01a00fb3-b5c7-7478-842c-89066c0bb394

AI 문의 화면에서:

"x모드 알려줘"

전송.

현상:

사용자 메시지는 전송됨
→ loading 표시
→ AI 답변이 채팅에 올라오지 않음

WP-CS-08R FULL CLOSE 중단.

신규 기능 개발 금지.
추측 수정 금지.

실제 request 기준으로 ROOT CAUSE부터 확인.

==================================================
1. PRODUCTION REQUEST TRACE
==================================================

가장 최근:

"x모드 알려줘"

Support request를 실제 production logs에서 추적.

확인:

case_id
request_id
user role
pool_id
mode

POST /support/respond

STARTED =
COMPLETED =
HTTP_STATUS =
LATENCY =

어디에서 종료/실패/대기하는지 확인.

raw 사용자 메시지를 보고서에 복사하지 말고
실제 디버깅에만 사용.

==================================================
2. SERVER FLOW TRACE
==================================================

해당 request가 아래 어디까지 갔는지 확인:

A.
user message 저장

B.
AI_PROCESSING transition

C.
runResolutionChain()

D.
resolution result

E.
gatherEvidence()

F.
OpenAI/provider call if any

G.
AI response generation

H.
insertSupportMessage(author_role=ai)

I.
AI_RESPONDED / HUMAN_REQUIRED transition

J.
HTTP response returned

각 단계:

PASS
FAIL
NOT_REACHED

보고.

==================================================
3. RESOLUTION RESULT
==================================================

"x모드 알려줘"가 실제로 어떤 결과였는지 확인:

resolution_status =
source_type =
source_id =
confidence =
evidence_count =
llm_used =
model =

중요:

현재 Knowledge PENDING=20인 것은
원인이라고 단정하지 않는다.

Frontend Map / DB_STATE / no_evidence 모두
정상적으로 HTTP response를 끝내야 한다.

Knowledge가 없어도 무한 loading은 절대 정상 아님.

==================================================
4. OPENAI PATH
==================================================

실제 provider call이 있었다면 확인:

OPENAI_CALLED =
REQUEST_STARTED =
REQUEST_COMPLETED =
PROVIDER_STATUS =
PROVIDER_ERROR =
TIMEOUT =
TOKENS =

API key 값 출력 금지.

OpenAI call이 없었다면:

OPENAI_CALLED = NO

로 명확히 보고.

==================================================
5. MESSAGE STORAGE
==================================================

실제 DB에서 해당 case 확인.

user message =
EXISTS

AI message =
EXISTS / MISSING

AI message가 EXISTS인데 앱에 안 보이면
server generation 문제가 아니라
client refresh/state 문제로 분류.

AI message가 MISSING이면
server flow 어디서 끊겼는지 확인.

==================================================
6. CASE STATE
==================================================

해당 case 실제 state 확인:

BEFORE =
AFTER =

특히:

AI_PROCESSING 상태에 멈췄는지 확인.

요청 실패 후에도
영구 AI_PROCESSING에 stuck 되는 구조면 defect.

==================================================
7. HTTP RESPONSE
==================================================

서버가 실제 client에 반환한 body 확인.

민감 내용 제외하고 구조만 보고:

success =
case_id =
answer_present =
resolution_source =
confidence =
requires_human =
llm_used =
error_code =

HTTP request가 응답 없이 hanging이면
정확한 await 지점을 찾는다.

==================================================
8. MOBILE FLOW
==================================================

SupportChatScreen.tsx handleSend 감사.

실제 순서 확인:

set loading
→ POST /support/respond
→ response parse
→ append/refresh message
→ loading false

확인:

finally에서 loading=false 보장되는가
non-2xx 처리
[보낼 곳: APP REPLIT]

[승인 — gauge-01 Production migration 실행 + 커리큘럼 E2E 완성]

gauge-01-progress-tables.ts Production migration 실행 승인한다.

단, migration 실행만으로 PASS 금지.

현재 보고서에 논리 모순이 하나 있다.

ROOT CAUSE 표에서는:

parent curriculum-chat "준비중"
= curriculum_items < 300
= WPB-02 별도 policy

라고 보고했다.

그런데 FINAL checklist에서는:

migration + backfill 후
Parent "준비중" 제거

라고 적었다.

따라서 아래 순서로 끝까지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━
1. gauge-01 Production migration 실행
━━━━━━━━━━━━━━━━━━━━━━

생성:

- curriculum_progress_observations
- student_curriculum_progress

Production DB에 실제 생성 확인.

schema/index/unique/check constraint가
migration 정의와 정확히 일치하는지 확인.

migration 재실행 가능한 멱등성 확인.

━━━━━━━━━━━━━━━━━━━━━━
2. 신규 Diary write path 실제 검증
━━━━━━━━━━━━━━━━━━━━━━

실제 학생 1명으로 신규 일지 저장.

확인:

Teacher Diary
→ growth_events
→ CPO mapper
→ curriculum_progress_observations
→ computeConfirmedProgress
→ student_curriculum_progress

각 단계 실제 record 확인.

기존처럼 CPO write가 fail-safe catch에서
조용히 사라지는 상태가 없어야 한다.

━━━━━━━━━━━━━━━━━━━━━━
3. 과거 Diary backfill 실행
━━━━━━━━━━━━━━━━━━━━━━

기존 diary → growth_events → CPO 변환.

중복 생성 금지.

학생별 과거 evidence를 반영한 뒤
student_curriculum_progress 재계산.

backfill 전/후:

- 대상 diary 수
- growth_event 수
- 생성 CPO 수
- SCP 학생 수
- duplicate 0

보고.

━━━━━━━━━━━━━━━━━━━━━━
4. WPB-02 "준비중" gate 별도 조사
━━━━━━━━━━━━━━━━━━━━━━

매우 중요.

보고서상 Parent "준비중"은
curriculum_items < 300 조건의 WPB-02 policy가 별도 원인이다.

따라서 migration과 별개로 실제 코드 추적:

- "준비중" 문자열 위치
- curriculum_items < 300 조건 위치
- 이 조건이 어떤 기능을 막는지
- curriculum-chat만 막는지
- parent curriculum progress/gauge까지 막는지

정확히 확인.

원래 제품 요구는:

학부모가 실제 자녀의
커리큘럼 진행률/게이지를 보는 것.

따라서 item 300개 미만이라는 이유로
실제 진도 데이터가 있는데도
전체 커리큘럼 화면을 "준비중"으로 막으면 안 된다.

300개 조건이 AI curriculum-chat의
지식량 준비 기준이라면:

AI 채팅 기능과
학생 진도 게이지를 분리.

Progress 데이터가 존재하면
학부모 진도 화면은 정상 표시.

AI curriculum-chat만 준비가 안 됐다면
그 기능만 별도 unavailable 처리.

"준비중" 하나로 전체 화면 차단 금지.

━━━━━━━━━━━━━━━━━━━━━━
5. Parent gauge 실제 연결
━━━━━━━━━━━━━━━━━━━━━━

Parent 화면이 반드시
student_curriculum_progress의 canonical 결과를 사용하도록 확인.

실제 표시:

- progress %
- 게이지
- completed count
- total count
- current/in-progress item
- recent update

placeholder 값 금지.

Admin progress와 동일 학생 비교:

Admin %
==
Parent %

필수.

━━━━━━━━━━━━━━━━━━━━━━
6. 완료 판정 검증
━━━━━━━━━━━━━━━━━━━━━━

단순 CPO existence를 COMPLETED로 간주하지 말 것.

현재 curriculum-confirmation-engine의
실제 완료 판정 규칙을 보고.

- IN_PROGRESS
- COMPLETED
- display_confirmed_pct

가 어떤 evidence에서 결정되는지 확인.

특히 display_confirmed_pct가
"절대 감소 금지"인데
근거 없는 완료율 누적이 없는지도 확인.

━━━━━━━━━━━━━━━━━━━━━━
7. X Dashboard curriculum KPI
━━━━━━━━━━━━━━━━━━━━━━

migration/backfill 완료 후
X Dashboard curriculum KPI도
SCP/CPO 기반 실제 값으로 연결.

student_curriculum_assignments 기반:
"배정 / 미배정"

다시 사용 금지.

실제 progress 기반 KPI만 표시.

━━━━━━━━━━━━━━━━━━━━━━
8. Member detail도 Production 실제 확인
━━━━━━━━━━━━━━━━━━━━━━

보고서에 "코드 정상"이라고 되어 있지만
사용자 실기기에서는 회원상세가 안 열렸다.

따라서 코드 검증으로 닫지 말 것.

실제 Production 회원 3명:

GET /admin/students/:id/detail

실제 authenticated HTTP 200 확인.

- 일반 재원
- 출결 많은 회원
- 출결 거의 없는 회원

실기기에서 카드 → 상세 정상 표시까지 확인.

━━━━━━━━━━━━━━━━━━━━━━
9. Render
━━━━━━━━━━━━━━━━━━━━━━

필요 server 변경 전부 main push.

Render가 반드시
해당 최신 main SHA로 LIVE인지 확인.

구버전 Render 상태에서 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. OTA
━━━━━━━━━━━━━━━━━━━━━━

서버/DB/E2E 전부 성공 후에만
iOS Production OTA 1회.

현재 실제 Production binary 기준 runtime/channel 사용.

중간 OTA 금지.
Android OTA 금지.

━━━━━━━━━━━━━━━━━━━━━━
완료보고
━━━━━━━━━━━━━━━━━━━━━━

1. migration 실행 결과
2. CPO table 실제 존재
3. SCP table 실제 존재
4. 신규 diary → CPO → SCP E2E
5. backfill 대상/처리 건수
6. duplicate 여부
7. WPB-02 <300 gate 정확한 역할
8. Parent "준비중" 정확한 root cause
9. progress 화면과 AI chat 분리 여부
10. 실제 학생 3명 Admin progress
11. 동일 학생 Parent progress
12. Admin == Parent
13. X Dashboard progress KPI
14. 회원상세 Production 3명 HTTP 결과
15. 회원카드 실기기 결과
16. main SHA
17. Render LIVE SHA
18. OTA ID
19. 잔존 FAIL

PASS / HOLD / FAIL

완료 후 STOP.
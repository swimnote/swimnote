[SWIMNOTE EAS BUILD 중단 및 기존 정상 빌드 방식 복원 조사]

현재 iOS/Android EAS Build가 반복 실패하고 있다.

지금부터 eas.json 및 프로젝트 설정을 추가 수정하지 않는다.

CODE CHANGE 금지.
eas.json 추가 변경 금지.
app.json 변경 금지.
package 변경 금지.
Commit/Push 금지.

현재 문제는 Instagram 기능 구현이 아니라
EAS Build 실행 환경 / monorepo project root 문제를 먼저 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 현재 빌드 즉시 중단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 실행 중인 실패/재시도 build process 중단.

새 EAS build 시작 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. 기존 성공 Production Build 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최근 iOS/Android에서 실제 성공했던 Native Build를 찾는다.

각각 제출:

- Build ID
- Commit SHA
- build profile
- working directory
- 실행 명령
- EAS projectId
- app root
- eas.json 위치
- package.json 위치
- pnpm workspace 기준 위치

특히 과거 성공 build가:

repo root

에서 실행됐는지,

artifacts/swim-app

에서 실행됐는지 정확히 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 현재 Android workspace 오류 원인 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 보고:

Android:
EAS에 `workspace` 프로젝트로 잘못 등록
→ Prebuild 실패

반드시 확인:

- pwd
- eas.json 실제 경로
- app.json 실제 경로
- package.json 실제 경로
- expo config가 어느 app을 반환하는지
- `npx expo config --json`의 project slug / owner / projectId
- `eas project:info` 결과

SWIMNOTE app이 아닌 workspace root를
EAS project로 인식한 이유를 확정한다.

projectId를 새로 만들거나
새 EAS project를 생성하지 않는다.

기존 SWIMNOTE EAS project를 그대로 사용해야 한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. iOS distribution 수정 중단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 목적은 테스트 build다.

그러나 임의로:

internal → store
store → internal

을 반복 변경하지 않는다.

기존 성공 iOS build의 credentials/profile을 먼저 확인한다.

만약 iPhone에 직접 설치할 preview가 필요하고
Ad Hoc credential이 없다면:

Ad Hoc 설정이 필요한 상태라고 보고.

만약 기존 TestFlight 방식으로 검증하는 것이
더 안전하다면 store build + TestFlight를 사용.

결정 전에 eas.json 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 현재 eas.json 변경 diff 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Instagram 작업 시작 전 기준과
현재 eas.json diff를 제출한다.

특히:

- preview distribution
- ios distribution
- android distribution
- env

어떤 줄이 빌드 오류 대응 중 변경됐는지 제출.

Instagram 기능에 필요하지 않은
임시 build workaround 변경은 별도로 표시.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 완료 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음만 제출:

1. 최근 성공 iOS Build ID/profile
2. 최근 성공 Android Build ID/profile
3. 성공 build 실행 directory
4. 현재 실행 directory
5. 기존 EAS projectId
6. 현재 EAS가 인식한 projectId
7. Android workspace 오류 직접 원인
8. iOS credential 상태
9. eas.json 원래 상태
10. eas.json 현재 상태
11. 빌드 위해 변경된 임시 설정 목록
12. 기존 정상 방식으로 돌아가기 위한 최소 조치

보고 후 멈춘다.

새 build 금지.
추가 commit/push 금지.
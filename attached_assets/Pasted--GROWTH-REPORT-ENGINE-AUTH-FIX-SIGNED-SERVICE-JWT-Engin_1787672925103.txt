[앱] GROWTH REPORT ENGINE AUTH FIX — SIGNED SERVICE JWT

현재 확정된 실제 Engine auth contract:

Endpoint:
POST /api/v1/growth-report/analyze

Engine middleware:
aiJwtAuthMiddleware

Expected header:
Authorization: Bearer <HS256 JWT>

Engine signing secret:
JWT_SECRET

필수 claims:
- userId: non-empty string
- role: allowed role
- poolId: string | null
- tv: 1
- iat
- exp

현재 APP/API 구현 오류:

Authorization: Bearer <GROWTH_REPORT_ENGINE_SECRET>

즉 raw secret을 token으로 전송 중.

이 방식은 Engine auth contract와 호환되지 않아
HTTP 401 발생.

목적:

GROWTH_REPORT_ENGINE_SECRET을
raw Bearer token이 아니라
Engine service JWT 서명 secret으로 사용하도록 수정한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 기존 Engine client 최소 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Growth Report Engine 호출 client/function만 확인.

확인:

- 실제 파일
- fetch/request 함수
- Authorization header 생성 위치
- GROWTH_REPORT_ENGINE_SECRET 사용 위치

관련 파일만 조사.
repo-wide audit 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. SERVICE JWT 생성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Engine 호출 직전에
HS256 JWT를 생성.

signing secret:

process.env.GROWTH_REPORT_ENGINE_SECRET

중요:

Render의 이 ENV 값은
AI Engine Production의 JWT_SECRET과 동일한 값이어야 함.

ENV 이름 자체를 양쪽에서 같게 만들 필요 없음.

APP/API:
GROWTH_REPORT_ENGINE_SECRET

AI Engine:
JWT_SECRET

값만 동일하면 됨.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. JWT CLAIM CONTRACT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

service token payload는 Engine middleware가 허용하는
최소 claims만 사용.

예:

{
  userId: "service:growth-report-worker",
  role: "platform_admin",
  poolId: <현재 report의 swimming_pool_id 또는 null>,
  tv: 1
}

iat / exp는 JWT library가 정상 부여.

token lifetime:
짧게.

예:
5분 이내.

장기 고정 JWT 저장 금지.

Engine 호출마다 또는 짧은 TTL로 생성.

중요:

role은 실제 Engine allowed role 중
해당 endpoint에서 필요한 최소 권한을 사용.

현재 endpoint가 role-specific 권한을 요구하지 않는다면
불필요하게 super_admin을 사용하지 말 것.

실제 middleware/route 기준으로
platform_admin이 정상 허용되는지 확인 후 사용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. HEADER 수정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before:

Authorization:
Bearer ${GROWTH_REPORT_ENGINE_SECRET}

After:

Authorization:
Bearer ${signedServiceJwt}

raw secret이 HTTP request에 직접 노출되면 안 됨.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. SECRET SAFETY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

절대 로그 금지:

- GROWTH_REPORT_ENGINE_SECRET 값
- Engine JWT_SECRET 값
- 생성된 full JWT token

로그에는 필요하면:

- auth_mode = signed_service_jwt
- token_created = true
- exp
- role

정도만 허용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. ENV VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GROWTH_REPORT_ENGINE_SECRET 없으면:

현재처럼 Engine 호출 시
명확한 configuration error 반환.

secret을 fallback 값으로 생성 금지.

JWT_SECRET이라는 이름의 새 APP env를
불필요하게 추가하지 말 것.

기존:
GROWTH_REPORT_ENGINE_SECRET

을 Engine signing secret 용도로 재사용.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. UNIT TEST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 외부 AI 호출 없이 검증.

TC1 signed JWT 생성
TC2 algorithm HS256
TC3 userId 존재
TC4 allowed role
TC5 tv=1
TC6 iat 존재
TC7 exp 존재
TC8 short expiry
TC9 raw secret이 Authorization에 직접 사용되지 않음
TC10 missing secret config error
TC11 secret/token 로그 없음

Engine의 동일 test secret으로
jwt.verify() 가능한지 unit에서 검증.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. SINGLE REPORT E2E 유지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

수정 후에도:

GROWTH_REPORT_ANALYSIS_AUTO_ENABLED=false

유지.

나머지 253건 자동 분석 금지.

대상:

gr_fe41fb43e105462a86a2eee2bdc4abf0

만 정상 super_admin single trigger로 실행.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 배포
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

수정 commit/push 후
최종 SHA만 보고.

사용자가 Render Dashboard에서
직접 Manual Deploy할 예정.

RENDER_API_KEY 사용 금지.

중간 SHA 반복 deploy 요청 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 완료 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[앱] ENGINE SIGNED JWT AUTH FIX REPORT

- engine client file/function
- previous auth behavior
- final auth behavior
- signing algorithm
- service userId
- service role
- token TTL
- poolId binding
- raw secret transmitted: NO
- secret/token logged: NO
- tests/pass
- changed files
- commit SHA

ENV requirement:
- GROWTH_REPORT_ENGINE_SECRET must equal Engine JWT_SECRET
- value must NOT be printed

DEPLOY:
- USER MANUAL DEPLOY REQUIRED

AI calls during implementation:
0

Final Verdict:

ENGINE SERVICE JWT AUTH READY FOR DEPLOY

STOP.
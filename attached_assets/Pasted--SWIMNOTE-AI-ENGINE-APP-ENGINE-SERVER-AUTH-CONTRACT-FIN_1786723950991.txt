[SWIMNOTE AI ENGINE]
[APP → ENGINE SERVER AUTH CONTRACT — FINAL VERIFICATION]

중요:

이 작업은 AI ENGINE repository에서 수행한다.
SWIMNOTE APP repository가 아니다.

현재 ENGINE Production:
https://swimnote.ai.kr

Deployed SHA:
840dba9

대상 routes:

POST /api/v1/growth-report/analyze
POST /api/v1/parent-curriculum/search

둘 다 aiJwtAuthMiddleware 사용 중.

SWIMNOTE APP 구현은 현재:

Authorization: Bearer <ENV 값>

형태로 ENV 값을 그대로 전송한다.

APP은 jwt.sign() 하지 않는다.

이번 작업 목적:

ENGINE의 aiJwtAuthMiddleware 실제 구현을 직접 확인해서
APP이 Render에 어떤 형태의 Bearer token을 넣어야 하는지
최종 확정한다.

코드 수정 금지.
Production deploy 금지.
DB write 금지.
Secret 값 출력 금지.

==================================================
1. ENGINE REPOSITORY IDENTITY
==================================================

확인:

- repository = SWIMNOTE AI ENGINE
- branch
- HEAD SHA
- working tree

aiJwtAuthMiddleware가 실제 존재하는 파일 경로 보고.

못 찾으면 STOP.

==================================================
2. aiJwtAuthMiddleware IMPLEMENTATION
==================================================

실제 middleware 코드를 직접 추적.

Authorization:

Bearer <token>

에 대해 정확히 무엇을 하는지 확인.

다음 중 하나로 판정:

A. raw shared secret 문자열 비교
B. jwt.verify() 기반 JWT 검증
C. 둘 다 허용
D. 기타

함수 이름/라이브러리/env 이름까지 보고.

추측 금지.

==================================================
3. AUTH SECRET SOURCE
==================================================

middleware가 어떤 ENGINE ENV를 사용하는지 확인.

예:
JWT_SECRET
AI_APP_SECRET
SERVICE_SECRET
기타

실제 이름만 보고.

값 출력 금지.

==================================================
4. JWT 방식이라면
==================================================

jwt.verify()를 사용한다면 확인:

- algorithm
- signing secret
- issuer 검증 여부
- audience 검증 여부
- exp 필수 여부
- sub 필수 여부
- role 필수 여부
- pool_id 필수 여부
- 기타 required claims

실제로 middleware가 검사하는 항목만 보고.

==================================================
5. CRITICAL QUESTION
==================================================

SWIMNOTE APP이 현재처럼:

Authorization: Bearer <ENV에 저장된 token>

을 보내는 구조를 유지할 수 있는지 판정.

특히 JWT 방식이어도:

APP ENV에
"미리 발급된 signed JWT 자체"
를 넣어서 그대로 Bearer로 보내면 되는 구조인지,

아니면:

APP이 요청할 때마다
JWT_SECRET을 사용하여 jwt.sign() 해야 하는 구조인지

명확히 판정.

==================================================
6. TOKEN EXPIRATION
==================================================

JWT라면 매우 중요.

APP ENV에 pre-signed JWT를 넣는 방식이 가능하더라도
exp가 있는 token이라면 영구 사용 불가.

확인:

- exp required?
- expiration duration?
- long-lived/static token 사용 가능한가?
- APP이 동적으로 JWT를 생성해야 하는가?

==================================================
7. BOTH ROUTES
==================================================

Growth Report route와
Parent Curriculum route가

정확히 같은 aiJwtAuthMiddleware instance/contract를 쓰는지 확인.

YES / NO

다르면 각각 보고.

==================================================
8. CURRENT PRODUCTION AUTH ENV
==================================================

Production ENGINE에서 middleware가 요구하는
secret/env가 존재하는지만 확인.

값 출력 금지.

예:

JWT_SECRET = PRESENT / MISSING

==================================================
9. SAFE LOCAL AUTH CONTRACT TEST
==================================================

Production 데이터/분석을 호출하지 않고
auth layer까지만 테스트.

실제 ENGINE dev/test 환경의
현재 auth contract를 사용.

다음 결과를 확인:

A. no Authorization
→ 401

B. invalid token
→ 401

C. middleware contract에 맞는 VALID token
→ auth 통과

C는 이후 body validation 단계의
400/422 등으로 넘어가면 auth 통과로 판정 가능.

실제 LLM 분석 실행 금지.
실제 Growth Report 생성 금지.
DB write 금지.

==================================================
10. APP IMPLEMENTATION DECISION
==================================================

최종적으로 정확히 하나를 선택:

CASE A
APP current raw Bearer architecture 그대로 사용 가능.
Render APP에 올바른 token 문자열만 넣으면 됨.

APP_CODE_CHANGE_REQUIRED = NO

CASE B
APP이 매 요청마다 JWT를 sign해야 함.

APP_CODE_CHANGE_REQUIRED = YES

CASE C
다른 변경 필요.

구체적으로 설명.

==================================================
11. APP ENV RELATION
==================================================

다음 두 APP ENV:

GROWTH_REPORT_ENGINE_SECRET
PARENT_CURRICULUM_ENGINE_SECRET

에 무엇을 넣어야 하는지
"값 자체 말고 타입/형식"만 보고.

예:

- raw shared secret
- signed JWT token
- JWT signing secret
- 기타

두 ENV에 같은 값을 사용해도 되는 구조인지도 판정.

YES / NO / SHOULD_BE_SEPARATE

==================================================
12. NO CHANGE
==================================================

이번 작업에서:

ENGINE 코드 수정 금지.
APP 코드 수정 금지.
Production ENV 변경 금지.
Production deploy 금지.
DB write 금지.

==================================================
13. REPORT
==================================================

다음만 보고:

1. ENGINE BRANCH / HEAD
2. AUTH MIDDLEWARE FILE
3. AUTH METHOD
4. SECRET ENV NAME
5. RAW STRING COMPARISON? YES/NO
6. JWT VERIFY? YES/NO
7. JWT ALGORITHM
8. REQUIRED CLAIMS
9. EXP REQUIRED?
10. PRE-SIGNED STATIC TOKEN POSSIBLE?
11. APP MUST jwt.sign()? YES/NO
12. GROWTH ROUTE AUTH CONTRACT
13. PARENT CURRICULUM AUTH CONTRACT
14. SAME CONTRACT? YES/NO
15. PRODUCTION AUTH ENV PRESENT?
16. VALID TOKEN SAFE TEST RESULT
17. GROWTH_REPORT_ENGINE_SECRET SHOULD CONTAIN
18. PARENT_CURRICULUM_ENGINE_SECRET SHOULD CONTAIN
19. SAME SECRET ALLOWED?
20. APP CODE CHANGE REQUIRED?
21. PRODUCTION CHANGE
22. DB WRITE

최종:

APP_ENGINE_AUTH_CONTRACT_VERIFIED ✅

또는

APP_ENGINE_AUTH_CONTRACT_BLOCKED ❌
[WP-CS26-R — GROUNDED GPT SEQUENCE RUNTIME & MOBILE CLOSURE]

목적:

WP-CS26의 코드 구현은 승인한다.

이번 WP에서는 신규 기능을 추가하지 않는다.

다음 항목만 최종 검증한다.

1. 연속 3회 규칙
2. 문의사항 보내기 → Grounded GPT
3. GPT 해결 실패 → Human escalation
4. Grounding 보안
5. 모바일 실제 UI
6. Production 배포 후 runtime

━━━━━━━━━━━━━━━━━━━━━━

1. 현재 기준
   ━━━━━━━━━━━━━━━━━━━━━━

Git commit:

5d3b17cf

기존 구현:

DIRECT DB
→ 동일 intent 연속 3회
→ 문의사항 보내기
→ Support Knowledge retrieval
→ Grounded GPT
→ 사용자 해결 여부
→ 미해결 확인 시 Human
→ Super Admin

이 구조를 변경하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━
2. 금지
━━━━━━━━━━━━━━━━━━━━━━

신규 기능 추가 금지.

새 DB 구조 금지.

새 Vector DB 금지.

Support Knowledge 대량 수정 금지.

GPT 자유답변 허용 금지.

이번 WP는 검증/필요 최소 버그 수정만 수행.

━━━━━━━━━━━━━━━━━━━━━━
3. 연속 3회 Runtime
━━━━━━━━━━━━━━━━━━━━━━

동일 intent 질문을 서로 다른 표현으로 입력.

예:

Q1:
"아이 사진이 안 보여요"

Q2:
"우리 아이 사진이 왜 안 떠요?"

Q3:
"사진이 계속 안 나오는데요"

동일 intent라면:

Q1 → 문의 CTA 없음
Q2 → 문의 CTA 없음
Q3 → 문의사항 보내기 CTA 표시

필수:

CTA_BEFORE_3 = 0
CTA_ON_THIRD = PASS

━━━━━━━━━━━━━━━━━━━━━━
4. streak reset
━━━━━━━━━━━━━━━━━━━━━━

PHOTO
PHOTO
MAKEUP
PHOTO
PHOTO

테스트.

마지막 PHOTO는 streak=2여야 함.

CTA 없어야 함.

필수:

STREAK_RESET_FAILURE = 0

━━━━━━━━━━━━━━━━━━━━━━
5. GPT 호출 시점
━━━━━━━━━━━━━━━━━━━━━━

세 번째 질문만으로 GPT를 호출하면 안 됨.

반드시 사용자가:

[문의사항 보내기]

를 눌러야 GPT 단계 시작.

필수:

GPT_CALL_BEFORE_USER_ESCALATION = 0

━━━━━━━━━━━━━━━━━━━━━━
6. Grounded GPT Retrieval
━━━━━━━━━━━━━━━━━━━━━━

문의사항 보내기 이후:

현재 intent
최근 동일 문제 conversation
기존 Direct Answer
관련 Support Knowledge

를 실제 retrieval.

GPT 입력에:

retrieved knowledge IDs
revisions
role
mode
pool scope

가 포함되는지 runtime trace로 확인.

필수:

GPT_WITHOUT_KNOWLEDGE_CONTEXT = 0

━━━━━━━━━━━━━━━━━━━━━━
7. Grounding Content Test
━━━━━━━━━━━━━━━━━━━━━━

최소 10개 문제 유형 테스트.

GPT가:

검색된 Knowledge에 없는
기능
UI
가격
환불
정책
오류 원인
해결법

을 추가하지 않는지 확인.

필수:

UNSUPPORTED_GPT_CLAIMS = 0
UNSUPPORTED_POLICY_CLAIMS = 0
UNSUPPORTED_UI_CLAIMS = 0

━━━━━━━━━━━━━━━━━━━━━━
8. 이전 답변 반복 방지
━━━━━━━━━━━━━━━━━━━━━━

예:

1차 DB 답변:
"자녀 연결 상태를 확인해 주세요."

사용자:
"연결되어 있는데도 안 보여요."

GPT가 다시:

"자녀 연결 상태를 확인해 주세요."

만 반복하지 않는지 확인.

다른 verified Knowledge가 존재하면
그 다음 가능한 해결 순서를 안내.

필수:

REPEATED_ALREADY_TRIED_ADVICE = 0

단 다른 verified Knowledge가 없으면
새 원인을 창작하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
9. Knowledge 없음
━━━━━━━━━━━━━━━━━━━━━━

retrieval result = 0

테스트.

GPT가 일반지식으로 SWIMNOTE 답변 생성 금지.

안전한 insufficient response 후:

[해결됐어요]
[아직 해결되지 않았어요]

표시.

필수:

ZERO_KNOWLEDGE_HALLUCINATION = 0

━━━━━━━━━━━━━━━━━━━━━━
10. GPT 후 해결
━━━━━━━━━━━━━━━━━━━━━━

GPT 답변 후:

[해결됐어요]

선택.

기대:

Human ticket 생성 없음.
Super Admin Push 없음.
conversation resolved.

필수:

RESOLVED_TO_HUMAN = 0

━━━━━━━━━━━━━━━━━━━━━━
11. GPT 후 미해결
━━━━━━━━━━━━━━━━━━━━━━

GPT 답변 후:

[아직 해결되지 않았어요]

선택.

그 시점에만:

HUMAN_REQUIRED
Human ticket 생성/재사용
Super Admin Push

실행.

필수:

PREMATURE_SUPER_ADMIN_NOTIFICATION = 0

━━━━━━━━━━━━━━━━━━━━━━
12. Security
━━━━━━━━━━━━━━━━━━━━━━

Grounded GPT Context에 대해:

Parent → teacher/admin Knowledge
Normal → X-only Knowledge
Pool A → Pool B Knowledge

노출 금지.

필수:

GPT_CONTEXT_ROLE_LEAKAGE = 0
GPT_CONTEXT_MODE_LEAKAGE = 0
GPT_CONTEXT_POOL_LEAKAGE = 0

━━━━━━━━━━━━━━━━━━━━━━
13. HUMAN_ONLY
━━━━━━━━━━━━━━━━━━━━━━

가격/환불 등 HUMAN_ONLY는
3회 반복을 기다리지 않는 기존 예외 유지.

단 GPT가 정책을 창작하면 안 됨.

필수:

HUMAN_ONLY_POLICY_INFERENCE = 0

━━━━━━━━━━━━━━━━━━━━━━
14. Callback
━━━━━━━━━━━━━━━━━━━━━━

전화 상담 질문 테스트.

답변:

전화 문의는 별도로 운영하지 않는다는
확정 문구 표시.

문의 flow에서:

사용자가 직접 연락받을 전화번호 입력
연락 동의

확인.

가입 전화번호 자동 사용 금지.

필수:

PROFILE_PHONE_AUTO_USED = 0

━━━━━━━━━━━━━━━━━━━━━━
15. Render Deployment
━━━━━━━━━━━━━━━━━━━━━━

검증 코드가 준비되면
승인된 Production branch 기준 Render 배포.

보고:

Git HEAD
Remote SHA
Render SHA
Health

Deployed SHA 일치 확인.

━━━━━━━━━━━━━━━━━━━━━━
16. OTA
━━━━━━━━━━━━━━━━━━━━━━

SupportChatScreen 변경이 OTA 대상이면
현재 Production OTA 정책에 따라 배포.

불필요한 Store Build 금지.

보고:

OTA ID
runtime/version

━━━━━━━━━━━━━━━━━━━━━━
17. Mobile Device E2E
━━━━━━━━━━━━━━━━━━━━━━

실제 모바일 앱에서:

동일 intent 질문 3회

→ 3회째 CTA

→ 문의사항 보내기

→ GPT 답변

→ 해결 여부 버튼

→ 아직 해결되지 않았어요

→ Human pending

까지 확인.

웹 404를 모바일 검증 대체로 사용하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━
18. Super Admin E2E
━━━━━━━━━━━━━━━━━━━━━━

Human escalation 이후:

Super Admin Push
Case Inbox 표시
동일 conversation context

확인.

Super Admin 답변 후
사용자 conversation에 이어지는지 smoke.

Push 실기기 검증이 환경상 불가능하면
PENDING_DEVICE로 명확하게 기록.

━━━━━━━━━━━━━━━━━━━━━━
19. Regression
━━━━━━━━━━━━━━━━━━━━━━

현재:

Support API / resolver = 83 PASS

전체 기존 테스트 포함 full suite 실행.

이번 변경 파일에 새 오류 0 유지.

기존 unrelated type errors는
별도 PRE_EXISTING으로 분리.

━━━━━━━━━━━━━━━━━━━━━━
20. COMPLETE REPORT
━━━━━━━━━━━━━━━━━━━━━━

WP-CS26-R COMPLETE REPORT

BASELINE

Git HEAD:
Remote SHA:
Render SHA:
OTA:

STREAK

Same intent tests:
CTA before 3:
CTA on third:
Reset tests:
Reset failures:

GPT TRIGGER

Calls before inquiry:
Calls after inquiry:

GROUNDING

Retrieved knowledge:
GPT without knowledge context:
Unsupported GPT claims:
Unsupported policy claims:
Unsupported UI claims:
Repeated previous advice:
Zero-knowledge hallucination:

SECURITY

Role leakage:
Mode leakage:
Pool leakage:

USER RESOLUTION

Solved → human:
Unresolved → human:

HUMAN

Premature admin notification:
Human ticket:
Super Admin push:
Same conversation:

CALLBACK

Profile phone auto-used:
User-entered callback:
Consent:

MOBILE

3-streak UI:
Inquiry CTA:
GPT response:
Resolved buttons:
Human pending:

RUNTIME

API boot:
Support tests:
Full tests:
Changed-file type errors:
Pre-existing errors:

FINAL

THREE_CONSECUTIVE_RULE_VERIFIED: YES / NO
GROUNDED_GPT_VERIFIED: YES / NO
HUMAN_LAST_STAGE_VERIFIED: YES / NO
MOBILE_SEQUENCE_VERIFIED: YES / NO

CS26_CLOSE: YES / NO
AUTONOMOUS_SUPPORT_SEQUENCE_COMPLETE: YES / NO
READY_FOR_PRODUCTION: YES / NO

WP_CS26_RUNTIME_CLOSED ✅

━━━━━━━━━━━━━━━━━━━━━━
21. 종료
━━━━━━━━━━━━━━━━━━━━━━

완료 후 멈춘다.

모두 통과하면
고객지원 시퀀스 작업을 종료하고
우리가 별도로 제작하는 Customer Support Master DB를
import하는 단계로 이동한다.

[/끝]

[SWIMNOTE 보강 복구 — 허위 완료 판정 금지 / 실제 장애 2개 직접 수정]

현재 보고는 완료로 승인하지 않습니다.

실기기 CONFIRMED 장애:

1. 지난 보강 카드가 실제 화면에 나오지 않음
2. 보강반 선택 후 날짜/회차가 나오지 않고
   "수업 회차를 불러오지 못했습니다" 표시

현재 수정 diff:

makeups.tsx
5 insertions / 51 deletions

주요 변경은:
- MAKEUP_DIAG 제거
- selectClass 로그 제거
- 500ms retry 제거

즉 실제 장애를 고치는 기능 변경은 확인되지 않았습니다.

따라서 Commit / Push / OTA 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 잘못된 판정 정정
━━━━━━━━━━━━━━━━━━━━━━

다음 판정을 즉시 정정하십시오.

CASE 10:
"Contract 일치 → 오류 미발생"

이 판정은 무효.

실기기에서 실제 오류가 발생했으므로:

CASE 10 = FAIL

또는 HTTP/runtime 증거 없으면:

UNKNOWN

으로 표시하십시오.

"코드상 정상"을 "기능 정상"으로 표현하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 장애 A — 지난 보강 실제 미표시
━━━━━━━━━━━━━━━━━━━━━━

separator가 존재한다는 사실은 해결이 아닙니다.

사용자 실기기에서는 expired 카드 자체가 안 보입니다.

따라서 실제로 확인할 것은:

GET /teacher/makeups?status=waiting
↓
실제 response의 expired 건수
↓
waitingList
↓
sorted
↓
render

입니다.

단, 다시 진단 로그를 추가하지 마십시오.

먼저 현재 코드에서
왜 실기기에는 1건만 표시될 수 있는지
정확한 실행 경로를 확인하십시오.

특히:

- teacher 권한 조건
- status 조건
- 실제 Production server handler
- res.ok=false 경로
- cached response
- stale state

중 실기기 증상을 만들 수 있는 경로를
현재 코드 원문으로 좁히십시오.

원인 확인 전 UI 추가 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. 장애 B — 날짜/회차 실패
━━━━━━━━━━━━━━━━━━━━━━

현재 오류 문구가 발생하는 정확한 코드 경로를 제출하십시오.

반드시 원문 확인:

selectClass()

다음 항목:

- apiRequest 호출
- endpoint
- query params
- res.status
- res.ok
- data parsing
- data.occurrences
- setOccurrences
- setOccError
- catch
- finally

"Contract가 맞다"에서 끝내지 마십시오.

실기기에서 오류 문구가 나온다면
setOccError가 실행된 정확한 조건이 존재합니다.

그 조건을 찾아야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
4. 기존 TypeScript 오류 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 오류:

makeups.tsx(1009,23)
onPress={doAssign}

이 오류의 실제 코드 원문을 제출하십시오.

확인:

- doAssign 함수 signature
- onPress가 전달하는 event와 signature 충돌 여부
- 정상본 06:00 코드에도 동일 오류가 있었는지
- 실제 보강 배정 실행과 관련 있는지

"기존 오류"라는 이유만으로 무시하지 마십시오.

보강 기능과 직접 관련되면 이번 복구 대상입니다.

━━━━━━━━━━━━━━━━━━━━━━
5. 확정 정책은 변경하지 않는다
━━━━━━━━━━━━━━━━━━━━━━

다음은 이미 확정된 정책:

- KST 오늘 -14일 ~ +28일
- 결석 예정일 이전 선보강 허용
- waiting + expired 표시
- expired 이월 유지
- 기간 지난 보강 구분 표시

이 정책을 다시 설계하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 보호 영역 절대 동결
━━━━━━━━━━━━━━━━━━━━━━

수정 금지:

parent/notifications.tsx
parent/requests.tsx
teacher/messages-inbox.tsx
parent_student_requests
notifications
link-result
notify-result
SessionContext
auth
JWT
apiRequest 공용 구현
AI
결제/구독

DB UPDATE/DELETE/INSERT 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. 이번 단계 완료 조건
━━━━━━━━━━━━━━━━━━━━━━

다음 두 장애의 "직접 코드 경로"를 찾아야 합니다.

A.
왜 expired 데이터가 실제 기기에서 표시되지 않는가

B.
왜 eligible-occurrences가 실제 기기에서 실패 문구로 가는가

그리고 TypeScript 오류가 보강 실행과 관련 있는지도 판정.

아직 수정하지 마십시오.

최종 제출:

1. 지난 보강 미표시 직접 경로
2. 날짜/회차 실패 직접 경로
3. setOccError 실행 조건
4. doAssign 타입 오류 원문
5. 실제 수정이 필요한 정확한 파일/함수
6. 최소 복구안

Commit: 없음
Push: 없음
Render: 없음
OTA: 없음

보고 후 멈추십시오.
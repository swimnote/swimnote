[PHASE 0/1 IMPLEMENT 승인]

현재 제출한 FINAL CONTRACT CORRECTION 및 PHASE 0/1 계획 승인.

단 아래 2개 조건을 반드시 적용하고 바로 구현하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. PHASE 0 + PHASE 1 원자적 구현
━━━━━━━━━━━━━━━━━━━━━━

PHASE 0과 PHASE 1을 별도 배포/별도 중간상태로 만들지 말 것.

한 작업에서 동시에 완료:

- dashboard 4개 label 변경
- dashboard 4개 route 변경
- report-hub.tsx 생성
- diary-hub.tsx 생성
- curriculum-hub.tsx 생성
- x-hub.tsx 생성
- admin layout route 등록

즉 route가 신규 화면을 가리키는 순간
해당 shell 파일도 반드시 존재해야 함.

중간 broken navigation 상태 금지.

한 commit으로 묶을 것.

━━━━━━━━━━━━━━━━━━━━━━
2. _layout navigator 실제 구조 확인
━━━━━━━━━━━━━━━━━━━━━━

보고서의:

<Stack.Screen ... />

코드를 그대로 가정해서 사용하지 말 것.

현재 repository의:

artifacts/swim-app/app/(admin)/_layout.tsx

를 확인하고
실제 사용 중인 navigator:

- Tabs
- Stack
- 기타 Expo Router 구조

중 현재 패턴을 그대로 따를 것.

기존 hidden admin screen 등록 방식을 찾아
동일 방식으로 4개 hub를 등록.

navigator 구조 변경 금지.
새 navigation architecture 도입 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. 구현 범위
━━━━━━━━━━━━━━━━━━━━━━

이번 구현은 PHASE 0/1 ONLY.

버튼 최종 label:

1. AI 학생리포트
2. AI 일지피드
3. AI 커리큘럼
4. SWIMNOTE X 관리

최종 route:

1. /(admin)/report-hub
2. /(admin)/diary-hub
3. /(admin)/curriculum-hub
4. /(admin)/x-hub

shell은 placeholder only.

아직:

- KPI
- API
- SQL
- 학생목록
- INDEX
- filter
- 실제 운영 데이터

구현 금지.

PHASE 2 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
4. 기존 화면 보존
━━━━━━━━━━━━━━━━━━━━━━

삭제/변경 금지:

- x-growth.tsx
- x-mode-hub.tsx
- x-subscription.tsx
- x-setup.tsx

기존 직접 route 정상 유지.

Normal 관리자 dashboard 영향 금지.

━━━━━━━━━━━━━━━━━━━━━━
5. TARGETED TEST
━━━━━━━━━━━━━━━━━━━━━━

반드시 검증:

T1. 신규/수정 파일 TS error 0

T2. X 관리자 홈
AI 학생리포트 tap
→ report-hub 정상 진입

T3. AI 일지피드 tap
→ diary-hub 정상 진입
→ 기존 diary-list broken route 완전 제거 확인

T4. AI 커리큘럼 tap
→ curriculum-hub 정상 진입

T5. SWIMNOTE X 관리 tap
→ x-hub 정상 진입

T6. 각 shell Back
→ X 관리자 dashboard 정상 복귀

T7. 기존:
x-growth
x-mode-hub
x-subscription
x-setup
직접 진입 정상

T8. Normal 관리자 홈 영향 없음

T9. 앱 완전 종료/재실행 후 OTA에서도 동일

━━━━━━━━━━━━━━━━━━━━━━
6. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[CODE]
- SHA
- 수정 파일
- 신규 파일
- 실제 _layout navigator 종류
- hidden route 등록 방식

[ROUTES]
4개 버튼 각각
label / route / screen 존재 여부

[TEST]
T1~T9 PASS/FAIL

[OTA]
rollback target
new OTA ID
runtimeVersion

[REGRESSION]
Normal 관리자 영향 여부
기존 X route 영향 여부

완료 후 STOP.

PHASE 2 시작 금지.
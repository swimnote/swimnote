[보낼 곳: APP REPLIT]

# SWIMNOTE 2.0 FINAL MASTER WP V5 — REPOSITORY VALIDATION
# MODE: REVIEW ONLY
# 구현 금지 / DB 변경 금지 / Render 금지 / OTA 금지
#
# 목적:
# DB/migration 및 신규 plan architecture 정비 이후
# SWIMNOTE 2.0 출시까지 사용할 최종 WP 계획을 확정하려고 합니다.
#
# 아래 WP를 현재 실제 release/v2.0.0 repository와 대조하여
# 누락 / 중복 / 잘못된 순서 / 불필요한 작업 / 더 먼저 해야 할 작업을
# 최종 검수하십시오.
#
# 단순 동의하지 말고 실제 코드/DB/API/job 구조를 조사해서 반박할 부분은 반박하십시오.

============================================================
0. SCALE / PRODUCT ASSUMPTIONS
============================================================

목표 규모:

- 500+ swimming pools
- 약 100,000 users
- ADMIN / TEACHER / PARENT
- SWIMNOTE BASE
- X300 / X500 / X1000
- DATA100 / DATA300
- Super Admin
- RevenueCat
- Push
- R2
- AI Engine
- Growth Report

중요 원칙:

- Client pool_id를 authorization source로 신뢰하지 않음.
- Cross-pool leak = Release Blocking P0.
- Production runtime DDL 금지.
- Heavy monthly/background work는 request thread에서 장시간 동기 실행 금지.
- Legacy historical billing/data는 신규 구조 전환 때문에 삭제하지 않음.
- 1.6.3 backward compatibility 때문에 2.0.1 출시를 지연하지 않음.

============================================================
WP0 — PRODUCTION BASELINE LOCK
============================================================

목표:

신규 plan architecture와 Production 기준선을 먼저 고정.

확인 대상:

SWIMNOTE 9,900
X300 119,000 / 300
X500 189,000 / 500
X1000 349,000 / 1000
DATA100 7,900 / +100GB
DATA300 22,900 / +300GB

완료 조건:

- Production DB catalog 일치
- API catalog 일치
- Super Admin catalog 일치
- Render 최신 intended code 실행
- Legacy Coach/Premier 신규 선택 불가
- Toykids를 manual X1000 기준 tenant로 설정
- paid=false
- manual=true
- force_disabled=false
- effective=true
- x_plan_key=x1000
- member_limit=1000

검수 질문:

1. 현재 실제 repository/Production 상태에서 WP0에 빠진 것이 있는가?
2. Toykids manual X1000을 개발 기준 tenant로 사용하는 데 architecture 문제가 있는가?

============================================================
WP1 — X MEMBER LIMIT ENFORCEMENT
============================================================

X300 = active member max 300
X500 = 500
X1000 = 1000

active member 정의는 현재 repository의 공식 회원 lifecycle을 재사용.

Warning:

limit - 10
limit - 5
limit

한도 도달:

기존 회원/기존 데이터 정상.
신규 회원 생성만 BLOCK.

Upgrade 후 즉시 unlock.

검수:

- 실제 회원 생성 endpoint 전체에서 한도를 우회할 다른 create/import/bulk route가 있는지 찾기.
- CSV/import/재등록/복원 등 다른 회원증가 경로도 확인.
- count query performance와 race condition 검토.
- 동시에 두 명 생성 시 300→302가 되는 race 방지 여부 제안.

============================================================
WP2 — UNIFIED NOTICE / BANNER / PUSH MODEL
============================================================

최종 UX:

공지함:

1. SWIMNOTE 공지
2. 우리 수영장 공지

하나의 Notice entity가:

- title
- message/body
- image
- send_push
- show_banner
- target_roles[]
- target_pools[]
- starts_at
- ends_at
- deep_link optional

을 가짐.

Banner는 별도 콘텐츠가 아니라 Notice presentation mode.

앱 실행/로그인 시 해당 사용자에게 최초 1회 노출.

사용자 action:

- 확인
- 다시 보지 않기

dismiss 후 banner 재노출 금지.
Notice는 공지함에 계속 남음.

Super Admin:

- 전체/단일/복수 pool
- ADMIN/TEACHER/PARENT 복수 선택

Pool Admin:

- 자기 pool
- TEACHER/PARENT만

사진은 기존 R2 활용.

검수:

- 현재 notices/platform_banners를 어떻게 최소 migration으로 통합하는 것이 가장 안전한지.
- 별도 테이블 유지가 더 안전하다면 근거 제시.
- server-side per-user dismissal의 최소 schema.
- 100K users에서 dismissal row 증가가 문제인지.
- 공지 조회 index/pagination 필요사항.

============================================================
WP3 — PUSH FAN-OUT SCALE
============================================================

현재 recipient-by-recipient 직렬 Push 제거.

목표:

- FCM batch/multicast
- bounded batch size
- async worker
- partial failure 처리
- invalid token 정리
- idempotency
- large audience safe

검수:

현재 실제 push implementation 기준으로
가장 작은 변경으로 10K~100K 대상 발송을 안전하게 만들 방법 제안.

FCM API/Expo Push 등 실제 현재 provider 구조를 정확히 확인.

============================================================
WP4 — AUTH / RATE LIMIT / NOTICE SECURITY
============================================================

출시 전 필수:

- login rate limit
- signup rate limit
- password/auth-sensitive routes
- abuse-prone public endpoints

Notice/banner read:

authenticated user
→ membership
→ role
→ pool/target filter

Super Admin mutation은 Super Admin only.

Pool Admin mutation은 자기 pool only.

검수:

현재 공개 endpoint / auth middleware 누락을 repository 전체에서 조사.

P0만 보고.

============================================================
WP5 — PAGINATION / DB CONNECTION / PERFORMANCE
============================================================

Pagination 우선:

- students
- diary
- parent
- notifications
- notices
- payments
- audit
- jobs
- Super Admin large lists

목표:

무제한 list response 제거.

DB:

- connection max
- PgBouncer transaction pooler
- slow query
- indexes
- N+1
- 500-pool loops

검수:

각 endpoint를 P0/P1/P2로 분류.

출시 전에 반드시 고쳐야 하는 endpoint만 추려서 보고.

============================================================
WP6 — REVENUECAT NEW PRODUCTS + WEBHOOK SAFETY
============================================================

Products:

SWIMNOTE
X300
X500
X1000
DATA100
DATA300

Paid vs Manual entitlement 분리 유지.

Webhook:

receive
→ persist/dedupe
→ process
→ result

event_id unique/idempotent.

RC event log 보존.

검수:

- 현재 webhook 실제 처리구조
- 중복처리 가능성
- 신규 products mapping
- restore/cancel/expiry/resubscribe/billing issue
- DATA100/DATA300 additive policy
- RevenueCat product/entitlement/offering에서 추가로 필요한 항목

============================================================
WP7 — OPERATIONS / INCIDENT DETECTION
============================================================

최소 deep health:

- API
- DB
- worker
- R2
- core dependencies

최소 alert:

- service down
- API 5xx spike
- DB errors/connections
- worker repeated failure
- queue backlog
- Growth batch mass failure

초기에는 Email 또는 Slack 한 채널이면 충분.

검수:

- 현재 logs/health/errors infrastructure 최대 재사용.
- Datadog/PagerDuty 등 과도한 도입은 권고하지 말 것 unless truly necessary.
- 100K 기준 최소운영안 제시.

============================================================
WP8 — SUPER ADMIN OPERATIONS CENTER
============================================================

기존 Super Admin 재사용/보강.

Pool Health:

- active members
- plan/member limit
- storage
- last admin activity
- billing
- push failures
- AI failures
- Growth failures
- recent operational errors

상태:

NORMAL
WARNING
CRITICAL

문제 pool 우선 정렬.

검수:

현재 이미 구현된 health data와 중복되는 것 제거.
추가 query가 500 pools에서 N+1을 만들지 않도록 설계 검토.

============================================================
WP9 — ADMIN ACTION AUDIT
============================================================

Audit 대상:

- X manual grant/revoke
- plan change
- billing adjustment
- role/permission change
- important member deletion/change
- notice send
- feature override

필드:

actor
action
target
before
after
reason optional
timestamp

검수:

현재 WP8 Audit/Support 구조와 정확히 무엇을 재사용할 수 있는지.
새 audit system을 중복 구현하지 않는 최소 설계 제안.

============================================================
WP10 — MEMBER CRM MVP
============================================================

출시 전 최소:

A. Admin Notes
- 일반
- 상담
- 결제
- 수업
- 차량
- 주의사항
- author/time

B. Absence / Retention Warning
- 일정 기간 출석/수업 없음
- 관리자 표시

출시 후:

tags
duplicate detector
merge
full timeline
consultation/trial CRM

검수:

현재 student schema/routes/UI를 기준으로
Admin Notes와 absence warning을 가장 적게 침범하여 넣는 방법.

정말 출시 전에 필요한지 다시 평가.

============================================================
WP11 — SUPER ADMIN MARKETING MVP
============================================================

거대한 Campaign system은 출시 전 만들지 않음.

Notice targeting만 확장:

Pool:
all / selected / multiple

Plan:
SWIMNOTE / X300 / X500 / X1000

Role:
ADMIN / TEACHER / PARENT

Channel:
Notice / Push / Banner

지원 시나리오:

- 신규 기능
- X upgrade
- 결제 안내
- 정책 공지
- 서비스 장애 공지

검수:

현재 notice/push 구조 위에서 구현 가능한 최소 변경.
Campaign analytics는 launch blocker가 아님.

============================================================
WP12 — GROWTH REPORT AUTO ISSUE RECOVERY
============================================================

기존 batch/analysis worker 재구현 금지.

목표:

2026-09-05 자동발급 미실행 원인 확정 및 Production 자동발급 복구.

검수:

- scheduler
- feature flag
- eligibility
- enqueue
- batch
- analysis
- retry
- push
- idempotency

100K 대비:

pool/member-level work distribution
bounded concurrency
backlog visibility

현재 worker에서 실제 부족한 부분만 제안.

============================================================
WP13 — DATA INTEGRITY CHECKER
============================================================

초기 read-only.

탐지:

- X entitlement without valid x_plan_key
- invalid entitlement combinations
- broken pool/class references
- orphan media
- duplicate membership
- broken guardian relation
- active member > plan limit
- subscription inconsistencies

자동수정은 launch requirement 아님.

검수:

현재 schema에서 가장 실질적인 integrity checks 우선순위 제안.

============================================================
WP14 — CROSS-POOL / ROLE SECURITY FINAL
============================================================

최소 두 pool:

A. Toykids X1000
B. Normal SWIMNOTE pool

Roles:

Super Admin
Pool Admin
Teacher
Parent

Resources:

members
classes
attendance
diary
media
notice
billing
AI
Growth
subscription

Cross-pool leak:
0 required.

검수:

현재 repository에서 아직 의심되는 P0 endpoint를 우선 리스트업.

============================================================
WP15 — PRODUCTION X/NORMAL E2E
============================================================

Toykids X1000:

Admin:
plan/member/curriculum/settings/notices

Teacher:
schedule/attendance/diary/X AI

Parent:
feed/progress/growth

Normal SWIMNOTE pool도 regression.

검수:

실제 screen/routes 기준 최종 E2E checklist 보완.

============================================================
WP16 — BACKUP / DISASTER RECOVERY
============================================================

운영 검증:

- Supabase backup
- PITR availability
- restore procedure
- staging restore test
- R2 recovery/delete policy
- migration rollback procedure

내부 RPO/RTO 결정.

검수:

코드 구현이 정말 필요한지,
운영 체크리스트만으로 충분한 부분과
자동화해야 할 부분을 분리.

============================================================
WP17 — LEGACY / UI CLEANUP
============================================================

정리:

- Coach/Premier 신규 UI
- legacy X
- debug/test
- mock
- dummy
- dead route/component
- stale feature flags

Historical data 삭제 금지.

검수:

현재 repo에서 실제 cleanup 후보를 category별로 보고.
동작 중인 rollback path를 무리하게 삭제하지 말 것.

============================================================
WP18 — 2.0.1 RELEASE CANDIDATE
============================================================

기능 추가 freeze.

검증:

Normal
X
Super Admin
RevenueCat
Push
Notice
R2
AI Engine
Growth
Member limit
Deep link
Security
Monitoring

Version reconciliation:

marketing/app version
runtimeVersion
iOS buildNumber
Android versionCode

특히 현재 개발 history상
runtime 2.1.0과 user intended native 2.0.1이 혼재할 가능성이 있으므로
실제 config를 조사하여 충돌 없이 release versioning proposal을 제시.

============================================================
WP19 — STORE RELEASE
============================================================

iOS:
TestFlight → App Store

Android:
internal/closed → Production

1.6.3 backward compatibility is NOT a launch blocker.

============================================================
WP20 — POST-LAUNCH P1
============================================================

후속 후보:

- RevenueCat reconciliation
- DLQ
- stronger health
- member tags
- duplicate detector
- member merge
- detailed CRM
- delivery metrics
- data integrity dashboard

검수:

실제 traffic가 적을 때도 미루면 위험한 항목이 있는지 확인.

============================================================
WP21 — SCALE P2
============================================================

성장 후:

- Campaign Analytics
- Member Timeline
- Full CRM
- pg-boss or more durable queue
- advanced APM
- advanced R2 retry
- AI concurrency tuning
- data warehouse

검수:

100K 이전 반드시 승격해야 할 것이 있는지 확인.

============================================================
FINAL VALIDATION QUESTIONS
============================================================

반드시 아래에 답변:

1. WP0~WP21 순서가 dependency 관점에서 맞는가?
2. 서로 순서를 바꿔야 하는 WP는 무엇인가?
3. 합쳐야 하는 WP는 무엇인가?
4. 삭제 가능한 중복 WP는 무엇인가?
5. 현재 이미 IMPLEMENTED라 작업량이 거의 없는 WP는 무엇인가?
6. 출시 전 P0인데 빠진 것이 있는가?
7. 우리가 P0로 잡았지만 출시 후로 미뤄도 되는 것이 있는가?
8. 100K user scale에서 가장 먼저 터질 가능성이 높은 5가지는 무엇인가?
9. DB schema/migration을 다시 크게 흔들 가능성이 있는 WP가 있는가?
10. 지금 먼저 schema를 확정해야 뒤에서 재작업이 없는 영역은 무엇인가?
11. Render worker/API 분리 또는 별도 worker service가 출시 전에 필요한가?
12. 현재 cron/DB polling 구조로 어느 정도 규모까지 합리적으로 버틸 것으로 보는가?
13. 현재 Supabase/Render/R2 구조에서 single point of failure는 무엇인가?
14. Super Admin에서 운영자가 반드시 봐야 하지만 현재 계획에서 빠진 지표가 있는가?
15. Pool Admin이 300~1,000명의 회원을 관리할 때 현재 CRM MVP 외에 출시 전 반드시 필요한 기능이 있는가?
16. 마케팅 MVP가 과한가/부족한가?
17. Notice/Push/Banner 통합 설계가 현재 repo에서 최선인가?
18. Toykids를 manual X1000 기준 tenant로 고정하는 것이 최종 E2E에 적절한가?
19. Growth Report 5일 자동발급을 언제 검증하는 것이 가장 안전한가?
20. 2.0.1 release 전에 반드시 실제 Production에서 확인해야 하는 작업 목록은 무엇인가?

============================================================
REQUIRED OUTPUT
============================================================

# SWIMNOTE 2.0 FINAL MASTER WP V5 VALIDATION REPORT

## 1. OVERALL VERDICT

READY TO FREEZE:
YES / NO

WHY:
...

## 2. WP TABLE

WP | STATUS | KEEP/MERGE/MOVE/DELETE | ACTUAL GAP | DEPENDENCY | PRIORITY
...

## 3. REQUIRED ORDER CHANGES

...

## 4. MISSING P0

...

## 5. DUPLICATE / ALREADY IMPLEMENTED

...

## 6. SCHEMA / MIGRATION RISKS

...

## 7. 100K SCALE TOP RISKS

1.
2.
3.
4.
5.

## 8. INCIDENT / RECOVERY REVIEW

...

## 9. MEMBER MANAGEMENT REVIEW

...

## 10. SUPER ADMIN REVIEW

...

## 11. MARKETING / NOTICE REVIEW

...

## 12. REVENUECAT REVIEW

...

## 13. GROWTH REPORT REVIEW

...

## 14. RELEASE VERSIONING REVIEW

...

## 15. FINAL RECOMMENDED WP ORDER

WP...
→ WP...
→ ...

## 16. FINAL ANSWERS 1~20

...

## 17. FINAL RECOMMENDATION

현재 설계를 그대로 FREEZE:
YES / NO

수정이 필요하면:
반드시 변경할 항목만 정확히 제시.

IMPORTANT:

이번 작업은 REVIEW ONLY.

CODE CHANGES:
0

DB CHANGES:
0

PRODUCTION DATA CHANGES:
0

RENDER:
NO

OTA:
NO

MIGRATION:
NO

STOP AFTER REPORT.
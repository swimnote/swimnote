SWIMNOTE 긴급 복구 — Production API_BASE 회귀 원인 확정 및 복원 준비

현재 실기기 최신 Production OTA 진단에서 결정적 불일치 발견.

실기기:

updateId:
019fdced-cfe6-7185-8050-b2713f4a1f85

runtimeVersion:
1.6.1

channel:
production

API_BASE:
https://swimnote.kr/api

하지만 최근 정상 서버 수정과 검증·Render 배포 대상은:

https://swimnote-api.onrender.com/api

조사 결과:

swimnote.kr/api
→ Google Frontend / GCP 계열 별도 서버

swimnote-api.onrender.com/api
→ Render 서버

Render custom domains에 swimnote.kr 없음.

따라서 지금부터 보강 로직 수정 금지.
서버 코드 수정 금지.

목표는 단 하나:

"왜 현재 Production OTA가 swimnote.kr/api를 사용하고 있는지 확인하고,
의도된 Production API_BASE를 확정한 뒤 안전하게 복원한다."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 현재 앱 API_BASE 결정 경로 전수 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음을 Repository 실제 코드에서 확인한다.

검색:

EXPO_PUBLIC_API_URL
API_BASE
swimnote.kr/api
swimnote-api.onrender.com/api

제출:

- API_BASE가 최종 결정되는 파일
- fallback 값
- process.env 사용 위치
- Constants.expoConfig 사용 여부
- eas.json env
- app.config / app.json
- OTA export/build script
- Production / Preview 각각의 env 주입 구조

어떤 우선순위로 최종 URL이 정해지는지 정확히 작성.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. EAS Production 환경변수 실제 값 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 EAS production 환경에서:

EXPO_PUBLIC_API_URL

실제 값을 확인한다.

원문 전체를 불필요하게 노출하지 말고 URL만 제출.

확인 대상:

production
preview

각각:

EXPO_PUBLIC_API_URL = ?

현재 실기기 진단값인

https://swimnote.kr/api

가 어디에서 주입됐는지 특정한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 정상 OTA와 현재 OTA 비교
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

과거 정상 배포 증거 중:

EXPO_PUBLIC_API_URL
=
https://swimnote-api.onrender.com/api

가 번들에 포함됐던 OTA를 찾는다.

비교:

정상 OTA:
- commit
- updateId
- API_BASE

현재 OTA:
- commit 40714ef3
- updateId 019fdced-cfe6-7185-8050-b2713f4a1f85
- API_BASE https://swimnote.kr/api

두 OTA 사이에서
API_BASE가 변경된 최초 commit / EAS env 변경 / export script 변경을 특정한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. Production의 의도된 API 서버 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 Repository와 최근 배포 운영 기록을 기준으로
Production 앱이 원래 어느 서버를 사용해야 하는지 확정한다.

후보 A:
https://swimnote-api.onrender.com/api

후보 B:
https://swimnote.kr/api

판단 근거 제출:

- 최근 Render 배포 구조
- eas.json
- EAS production env
- API health
- 최근 정상 OTA bundle 검사
- 서버 DB 연결 구조

임의 판단 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 아직 코드 수정하지 말 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

만약 의도된 Production endpoint가

https://swimnote-api.onrender.com/api

인데 EAS env만 잘못되어 있다면:

코드 수정 불필요.

그 경우 필요한 복구는:

1. EAS production EXPO_PUBLIC_API_URL 수정
2. preview도 동일 정책인지 확인
3. 새 Production OTA
4. 실기기 API_BASE 재확인

뿐이다.

서버 재배포도 필요 없음.

반대로 swimnote.kr/api가 의도된 endpoint라면
Render 서버를 수정했던 최근 작업들이 Production 앱에 적용되지 않는 구조이므로
GCP 서버 배포 구조를 먼저 찾아야 한다.

두 경우를 구분하기 전 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 최종 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음만 보고한다.

1. API_BASE 최종 결정 코드
2. 코드 fallback URL
3. EAS production EXPO_PUBLIC_API_URL
4. EAS preview EXPO_PUBLIC_API_URL
5. eas.json 관련 값
6. 현재 OTA가 swimnote.kr/api가 된 정확한 이유
7. 마지막 onrender.com/api 정상 OTA
8. API_BASE가 변경된 최초 시점
9. Production의 의도된 API endpoint
10. 최소 복구 방법
11. 코드 수정 필요 여부
12. Render 재배포 필요 여부
13. OTA만으로 복구 가능한지
14. 복구 승인 가능 여부

조사만 하고 중단.

코드 수정
Commit
Push
Render
OTA
DB 변경

전부 금지.
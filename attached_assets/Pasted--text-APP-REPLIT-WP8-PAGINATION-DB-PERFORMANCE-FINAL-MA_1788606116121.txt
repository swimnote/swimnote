```text
[보낼 곳: APP REPLIT]

# WP8 — PAGINATION / DB PERFORMANCE
# FINAL MASTER WP V6.1
# PLAN FREEZE — 계획 변경 금지

목적:

현재 대량 데이터에서 병목이 될 수 있는 주요 list API와 DB query를
Store 출시 전에 안전한 수준으로 정리합니다.

핵심:

1. unbounded / fixed-limit list 개선
2. backward-compatible cursor pagination
3. 우선순위 API부터 적용
4. N+1 / 반복 query 제거
5. DB pool / PgBouncer / index 상태 확인
6. 500-pool scale에서 명백한 full-scan / loop 병목 제거

이번 작업은 WP8만 수행합니다.

WP9 이후 작업 절대 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
0. 최상위 원칙
━━━━━━━━━━━━━━━━━━━━━━

FINAL MASTER WP는 Freeze 상태입니다.

이번 WP는:

"전체 API를 새 pagination contract로 갈아엎는 작업"

이 아닙니다.

우선순위가 높은 list/query에
backward-compatible하게 최소 적용합니다.

금지:

- API 전체 rewrite
- GraphQL 도입
- ORM 교체
- DB architecture 교체
- Redis cache 신규 도입
- WP9 Monitoring
- WP10 Growth
- 이후 WP 선행작업
- unrelated refactor

1.6.3 compatibility 때문에
2.0.1 출시를 지연시키지 않습니다.

단 active 2.0.x client contract는
불필요하게 깨뜨리지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. Branch
━━━━━━━━━━━━━━━━━━━━━━

Branch:

release/v2.0.0

Required previous commit:

823ff644

시작 전 최소 확인:

- local release HEAD
- origin/release HEAD
- working tree clean
- 823ff644 ancestry 포함

WP1~WP7 재감사 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 이전 검수에서 이미 확인된 우선 위험
━━━━━━━━━━━━━━━━━━━━━━

이전 Replit review에서 확인된 실제 상태:

- GET /diary
  LIMIT 50/100
  plain array response

- GET /notifications
  plain array response

- parent diary messages
  LIMIT 100

따라서 우선순위:

P1:
1. diary
2. parent diary/feed/messages
3. notifications

P2:
4. students/members
5. notices
6. audit logs
7. payments/billing history
8. jobs/queues
9. Super Admin list endpoints

이 순서로 확인하십시오.

repo 전체 list endpoint를 무차별 조사하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. Pagination 기본 원칙
━━━━━━━━━━━━━━━━━━━━━━

가능하면 cursor pagination 사용.

권장 cursor source:

- created_at + id
또는
- sortable immutable id

현재 query ordering convention에 맞게.

offset pagination을 새 canonical 방식으로 확대하지 마십시오.

단 기존 endpoint가 이미 offset/page 기반이고
안전하게 bounded되어 있다면
이번 WP에서 억지로 cursor로 재작성할 필요 없음.

핵심은:

- unbounded 제거
- stable order
- duplicate/missing row 방지
- bounded response

━━━━━━━━━━━━━━━━━━━━━━
4. Backward Compatibility
━━━━━━━━━━━━━━━━━━━━━━

기존 client가 plain array를 기대하는 endpoint를
갑자기:

{
  items: [],
  next_cursor: ...
}

로 바꾸면 안 됩니다.

먼저 실제 current client usage를 최소 확인하십시오.

가능한 전략:

A. 기존 endpoint:
기존 array contract 유지
+ query param cursor/limit 지원

B. additive metadata header

C. `/v2` 또는 현재 repository convention의 additive paginated route

D. existing response object가 이미 wrapper이면
next_cursor만 additive

가장 작은 backward-compatible 방식을 선택하십시오.

중요:

1.6.3 compatibility는 release blocker가 아니지만
현재 활성 2.0.x 기능을 깨뜨리는 변경은 피하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. Limit 정책
━━━━━━━━━━━━━━━━━━━━━━

각 endpoint:

default limit:
합리적인 bounded 값

max limit:
server-side cap

예:
50 / max 100

정확한 값은 현재 UX와 query 특성 기준으로 결정.

client가:

limit=100000

을 보내도 max cap 적용.

limit <=0 / invalid:
validation.

━━━━━━━━━━━━━━━━━━━━━━
6. Diary
━━━━━━━━━━━━━━━━━━━━━━

최우선.

확인:

- admin diary list
- teacher diary list
- parent diary/feed list
- diary message/history 관련 list

필수:

- stable ordering
- bounded query
- next page 가능
- same timestamp row 누락/중복 방지

권장:

(created_at, id)

composite cursor.

중요:

Teacher/Parent authorization scope는 WP6 결과를 그대로 유지.

pagination 때문에 pool/class/student scope guard가 약해지면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
7. Notifications
━━━━━━━━━━━━━━━━━━━━━━

Notification inbox/list:

- bounded
- newest first
- cursor
- read/unread state 유지
- deep link 유지

연속 pagination 시:

duplicate notification 0
missing notification 0

WP4 Notice와 notifications를
새 시스템으로 합치지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. Students / Members
━━━━━━━━━━━━━━━━━━━━━━

Admin member list 등에서
수백~1000명 규모에도 안정적이어야 합니다.

확인:

- search
- status filter
- class filter
- ordering
- pagination

WP2 active member count logic은 수정하지 마십시오.

Member limit enforcement와 list pagination은 별개.

━━━━━━━━━━━━━━━━━━━━━━
9. Notices
━━━━━━━━━━━━━━━━━━━━━━

WP4 Notice inbox/query에서
무제한 전체 loading이 있으면 bounded 처리.

target filtering / dismissal / reads semantics 유지.

WP4 schema 재설계 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. Super Admin Lists
━━━━━━━━━━━━━━━━━━━━━━

500 pools 기준으로 확인:

- pool/operator lists
- support cases
- audit logs
- health lists
- billing lists

특히:

for each pool
→ query

식 N+1이 있으면 최소 수정.

가능하면:

JOIN
GROUP BY
batch query

기존 repository style 재사용.

대형 analytics materialization 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. DB Connection Pool
━━━━━━━━━━━━━━━━━━━━━━

현재 API server DB pool configuration을 확인하십시오.

보고:

- driver
- max connections
- idle timeout
- connection timeout
- Production PgBouncer 사용 여부 / connection string mode
- worker와 API가 pool을 어떻게 공유하는지

이번 WP에서
근거 없이 pool max를 크게 늘리지 마십시오.

Production connection limit을 모르는 상태에서
"100으로 올리면 빠르다" 같은 변경 금지.

명백한 misconfiguration이 있을 때만 최소 수정.

━━━━━━━━━━━━━━━━━━━━━━
12. PgBouncer Compatibility
━━━━━━━━━━━━━━━━━━━━━━

Production Supabase/Postgres connection 방식에서
PgBouncer / transaction pooling을 사용하는 경우:

- prepared statement issue
- long transaction
- connection pinning
- worker polling

등 현재 ORM/driver와 충돌 없는지 최소 확인.

새 DB proxy 도입 금지.

현재 Production connection mode를 확인할 근거가 없으면
추측하지 말고 UNKNOWN으로 보고.

━━━━━━━━━━━━━━━━━━━━━━
13. Index Audit — 최소 범위
━━━━━━━━━━━━━━━━━━━━━━

새 index를 무차별 추가하지 마십시오.

이번 WP에서 실제 pagination/query에 필요한 index만 확인.

예:

diaries:
(pool_id, created_at, id)
student-related:
(student_id, created_at, id)

notifications:
(user_id, created_at, id)

notices:
target / starts_at / ends_at 관련 현재 query

정확한 index는 실제 WHERE + ORDER BY 기준.

이미 equivalent index가 있으면 추가 금지.

━━━━━━━━━━━━━━━━━━━━━━
14. New Index 필요 시 Migration
━━━━━━━━━━━━━━━━━━━━━━

실제 missing index가 병목 원인으로 확인되면:

canonical migration file 작성.

신규 runtime DDL 금지.

절차:

1. migration file
2. Staging exact 확인
3. Staging 적용
4. index verify
5. EXPLAIN/EXPLAIN ANALYZE 가능하면 Staging에서 확인
6. second-run/idempotency
7. Production 적용 금지
8. 보고

Production DB migration:
사용자 승인 전 NO.

━━━━━━━━━━━━━━━━━━━━━━
15. Query Performance
━━━━━━━━━━━━━━━━━━━━━━

Staging에서 현실적인 fixture/data로 가능하면
주요 query latency를 확인.

최소:

- diary first page
- diary next page
- notifications
- student list
- Super Admin pool list

정확한 대규모 Production SLA를 주장하지 마십시오.

목표:

명백한 full table scan/N+1/unbounded response 제거.

━━━━━━━━━━━━━━━━━━━━━━
16. N+1
━━━━━━━━━━━━━━━━━━━━━━

특히 다음 패턴 탐지:

rows.map(async row => db.query(...))

for pool of pools:
  await query(...)

for student:
  fetch class
  fetch parent
  fetch status

실제 hot list path에서 발견된 경우만 수정.

repo 전체 N+1 hunting 금지.

━━━━━━━━━━━━━━━━━━━━━━
17. 500 Pool Loop
━━━━━━━━━━━━━━━━━━━━━━

Super Admin endpoint에서:

전체 Pool 불러옴
→ Pool마다 health/billing/storage/member query

형태라면 명백한 병목.

가능한 최소 batch/aggregate로 수정.

단 WP7 control-center 단일 Pool summary는
500-pool bulk endpoint와 혼동하지 마십시오.

단일 pool detail은 N+1이 아님.

━━━━━━━━━━━━━━━━━━━━━━
18. Queue / Job Lists
━━━━━━━━━━━━━━━━━━━━━━

WP5 push_fanout_jobs/deliveries
Growth jobs 등 운영 list가 있다면:

- unbounded 전체 read 금지
- default bounded
- failed/pending filter 가능
- ordering/index 확인

Worker processing logic 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━
19. Cursor Security
━━━━━━━━━━━━━━━━━━━━━━

Cursor는 pagination position일 뿐
authorization token이 아닙니다.

cursor에 pool/user/resource ID가 들어가더라도
server auth scope 검증은 매 request마다 다시 수행.

client cursor를 신뢰해서
다른 pool query로 이동하면 안 됩니다.

Cursor decode invalid:
400 또는 repository convention.

━━━━━━━━━━━━━━━━━━━━━━
20. Cursor Encoding
━━━━━━━━━━━━━━━━━━━━━━

가능하면 opaque cursor.

예:

base64url(JSON {
  created_at,
  id
})

단 secret/encryption 필요 없음.

민감정보 넣지 마십시오.

새 crypto system 금지.

현재 pagination helper가 있으면 재사용.

━━━━━━━━━━━━━━━━━━━━━━
21. Stable Pagination 테스트
━━━━━━━━━━━━━━━━━━━━━━

중요:

같은 created_at을 가진 10개 row가 있어도
pagination 중:

duplicate 0
missing 0

이어야 합니다.

따라서 timestamp만 cursor로 쓰는 구조 금지.

secondary id tie-breaker 필수.

━━━━━━━━━━━━━━━━━━━━━━
22. Concurrent Insert
━━━━━━━━━━━━━━━━━━━━━━

페이지 1 조회 후
새 row가 삽입돼도
다음 cursor 요청에서 기존 row가 과도하게 중복되거나 누락되지 않는
현재 ordering semantics를 검증.

완전 snapshot isolation pagination까지 만들 필요 없음.

일반적인 keyset pagination 안정성만.

━━━━━━━━━━━━━━━━━━━━━━
23. Search + Pagination
━━━━━━━━━━━━━━━━━━━━━━

학생 검색 등에서:

search keyword
+ cursor
+ filter

가 함께 사용될 경우
cursor next query에도 동일 filter가 적용되어야 합니다.

client가 다른 filter로 cursor 재사용하면
safe behavior.

복잡한 signed cursor까지 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
24. Response Size
━━━━━━━━━━━━━━━━━━━━━━

대형 JSON payload가 있는 endpoint는
목록에서 필요 없는 heavy field를 모두 포함하는지 확인.

특히:

- diary full content/media metadata
- audit raw payload
- large nested student relations

List에는 summary,
detail endpoint에는 full data

라는 기존 pattern이 있으면 재사용.

단 API contract를 깨뜨리는 필드 제거 금지.

새 v2 paginated response에서만 lightweight projection 적용 가능.

━━━━━━━━━━━━━━━━━━━━━━
25. 필수 테스트
━━━━━━━━━━━━━━━━━━━━━━

전체 suite 금지.

WP8 관련 테스트만.

A. Diary first page bounded

B. Diary next cursor

C. Same timestamp multiple rows
→ duplicate 0 / missing 0

D. Parent diary authorization 유지

E. Teacher diary authorization 유지

F. Notifications first/next page

G. Notifications read/deep link preserved

H. Students pagination

I. Search + pagination

J. max limit enforcement

K. invalid cursor safe error

L. cross-pool cursor spoof BLOCK

M. Notices bounded

N. Super Admin pools bounded

O. audit logs bounded if current hot path

P. jobs/queue list bounded if exposed

Q. N+1 fixed endpoint regression

R. 500-pool simulation/query-count evidence

S. concurrent insert pagination behavior

T. legacy/current client compatibility test

━━━━━━━━━━━━━━━━━━━━━━
26. DB Performance Evidence
━━━━━━━━━━━━━━━━━━━━━━

완료보고에서 최소:

Endpoint:
before query count
after query count

before limit behavior
after limit behavior

index used:
...

Staging EXPLAIN:
...

가능한 것만 실제 evidence로 제출.

숫자 추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
27. DB / Migration
━━━━━━━━━━━━━━━━━━━━━━

기본:

Schema changes:
NO

Index migration:
ONLY IF ACTUALLY NEEDED

Production DB:
NO WRITE

Staging DB:
index migration이 있을 때만.

신규 runtime DDL:
NO.

━━━━━━━━━━━━━━━━━━━━━━
28. 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

- WP9 monitoring/alerts
- WP10 Growth logic
- WP11 Admin Notes
- WP12 Marketing
- WP13 Integrity Checker
- WP14 Legacy cleanup
- WP15 Version/runtime
- WP16 E2E
- RevenueCat
- Push worker architecture
- Notice schema redesign
- Manual X Grant
- AI Engine
- DB architecture replacement
- Redis/cache platform
- unrelated refactor

━━━━━━━━━━━━━━━━━━━━━━
29. Render / OTA
━━━━━━━━━━━━━━━━━━━━━━

Server code 변경 예상:

Render required:
YES/NO 보고.

App API consumption 변경 시:

iOS OTA required:
YES/NO 보고.

Replit OTA publish 금지.

Android OTA:
NO.

Native:
NO.

Production deploy:
하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
30. PASS 완료조건
━━━━━━━━━━━━━━━━━━━━━━

WP8 PASS:

1. diary major list bounded
2. parent diary/feed bounded
3. notifications bounded
4. stable cursor pagination
5. backward-compatible contract
6. students/member major list bounded
7. notices bounded
8. Super Admin major list bounded
9. audit/job lists bounded where exposed/hot
10. cross-pool cursor leak 0
11. max limit enforced
12. same timestamp duplicate/missing 0
13. major N+1 eliminated
14. obvious 500-pool loop eliminated
15. DB pool config reviewed
16. required indexes verified
17. new index only if evidence
18. migration if needed → Staging PASS
19. Production DB changes 0
20. related tests PASS
21. Commit SHA

━━━━━━━━━━━━━━━━━━━━━━
31. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

# WP8 PAGINATION / DB PERFORMANCE REPORT

Target:
APP

Branch:
...

HEAD before:
823ff644

HEAD after:
...

origin/release HEAD:
...

Commit SHA:
...

Commit message:
...

Files changed:
...

Pagination helper:
...

Compatibility strategy:
...

DIARY:
before:
...
after:
...
default limit:
...
max limit:
...
cursor:
...
same timestamp stable:
PASS / FAIL

PARENT DIARY/FEED:
...

NOTIFICATIONS:
...

STUDENTS/MEMBERS:
...

NOTICES:
...

SUPER ADMIN LISTS:
...

AUDIT LOGS:
...

JOBS/QUEUES:
...

Cross-pool cursor spoof:
BLOCKED / FAIL

Legacy/current client compatibility:
PASS / FAIL

N+1 issues found:
...

N+1 fixes:
...

500-pool loops found:
...

500-pool fixes:
...

DB driver:
...

DB pool max:
...

Idle timeout:
...

Connection timeout:
...

PgBouncer:
USED / NOT USED / UNKNOWN

Index audit:
...

New indexes:
...

Migration:
YES / NO

Migration file:
...

Staging migration:
PASS / FAIL / NOT REQUIRED

Production migration:
NOT RUN

EXPLAIN evidence:
...

Query count before/after:
...

Tests:
...

Test result:
PASS / FAIL

DB schema changes:
YES / NO

Production DB changes:
NO

Render required:
YES / NO

iOS OTA required:
YES / NO

Android OTA:
NO

Native binary required:
NO

Remaining blocker:
NONE / ...

Final:
PASS / HOLD / FAIL

중요:

FINAL MASTER WP는 Freeze 상태입니다.

이번 WP 완료 후
추가 database/cache architecture를 제안하거나 구현하지 마십시오.

WP9 이후 작업 선행 금지.

Production migration/deploy는 Replit이 실행하지 마십시오.

완료조건 충족 후 즉시 STOP하십시오.
```

[보낼 곳: APP REPLIT]

# WP16 P0 — COMMON + STUDENT VIDEO UPLOAD PARITY FIX
# 사진 업로드는 정상 → 수정 금지
# 공통 일지와 학생별 개별 일지의 VIDEO 기능을 동일 수준으로 맞춤
# 영상 선택 → 업로드 → 상태 표시 → 저장 → 재진입까지 실제 E2E 확인
# 수정 완료 시 release/v2.0.0 반영 + iOS production-v2 OTA 자동 수행
# Render Production deploy 금지

현재 실기기 증상:

1. 반 공통 일지
- 영상 업로드 선택 가능
- "업로드 중" 표시는 보임
- 실제 업로드/저장 완료 여부는 아직 실기기에서 확실하지 않음

2. 학생별 개별 일지
- 사진은 정상
- 영상 업로드가 안 됨
- 개별 일지에도 영상 첨부/업로드 기능이 반드시 있어야 함

최종 요구:

공통 일지와 학생별 개별 일지 모두 아래 기능이 동일해야 함.

영상 선택
→ 업로드 시작
→ 업로드 중 표시
→ 업로드 성공
→ preview 유지
→ Diary 저장
→ 다시 Edit 열기
→ 영상 그대로 존재

━━━━━━━━━━━━━━━━━━━━━━
1. 범위 제한
━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 금지:

- 사진 업로드 수정
- Parent Feed
- AI Engine
- Diary navigation
- Growth Report
- Notification
- Admin
- RevenueCat
- 전체 repo audit

오직 Diary VIDEO:

공통 video
학생별 video

두 경로만 비교/수정.

━━━━━━━━━━━━━━━━━━━━━━
2. 공통 VIDEO 현재 실제 경로 확인
━━━━━━━━━━━━━━━━━━━━━━

공통 일지 영상:

button:
...

picker:
...

handler:
...

upload function:
...

endpoint:
...

returned video.id:
...

state:
...

save payload field:
...

attach API:
...

edit reopen mapping:
...

이 경로를 기준 implementation으로 사용.

공통 경로가 실제로 끝까지 연결되어 있는지 확인:

select:
PASS / FAIL

HTTP 201:
PASS / FAIL

video.id 저장:
PASS / FAIL

save payload:
PASS / FAIL

diary attach:
PASS / FAIL

reopen:
PASS / FAIL

"업로드 중 배너 표시"만으로 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. 학생별 VIDEO 경로 전체 확인
━━━━━━━━━━━━━━━━━━━━━━

학생별 추가 일지 카드에서:

video button 존재:
YES / NO

onPress handler:
...

student argument:
...

picker:
...

upload function:
...

endpoint:
...

returned video.id:
...

student-specific state:
...

save payload:
...

note/student diary attach:
...

edit reopen:
...

어디서 공통 경로와 달라지는지 exact 확인.

━━━━━━━━━━━━━━━━━━━━━━
4. 학생별 영상 UI 반드시 추가/복구
━━━━━━━━━━━━━━━━━━━━━━

개별 학생 카드에도 현재 사진과 동일하게
영상 선택 기능이 보여야 함.

예:

[앨범에서 선택]
[내 사진앨범]
[내 영상앨범]

현재 앱의 기존 media UI 패턴에 맞춰
공통 일지와 같은 video action 제공.

새 디자인 시스템 만들지 말 것.

학생별:
"내 영상앨범"
버튼이 없으면 추가.

버튼은 정확한 student context를 전달해야 함.

━━━━━━━━━━━━━━━━━━━━━━
5. 학생별 영상 state 분리
━━━━━━━━━━━━━━━━━━━━━━

학생별 video는 반드시
해당 학생에게 귀속.

예:

서태웅 video
→ 서태웅 media state

학생 A 영상이
학생 B에게 붙으면 P0.

공통 video state와
student video state 섞이면 FAIL.

확인:

studentId/ref key:
...

student video IDs:
...

student video preview:
...

━━━━━━━━━━━━━━━━━━━━━━
6. 공통/개별 동일 upload state UX
━━━━━━━━━━━━━━━━━━━━━━

두 경로 모두:

UPLOAD START:
"영상 업로드 중... 잠시 기다려주세요"

UPLOAD SUCCESS:
배너 사라짐
preview 유지
uploaded 상태 표시

UPLOAD FAIL:
배너 사라짐
명확한 실패 표시
재시도 가능

영상 업로드 중:
Save 차단

안내:
"영상 업로드가 완료될 때까지 잠시 기다려주세요."

공통만 되고 개별은 표시 안 되면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
7. 실제 HTTP 검증
━━━━━━━━━━━━━━━━━━━━━━

공통 video 1개:
endpoint:
POST /videos/group

HTTP:
201 필수

returned:
video.id
video.file_url

학생별 video 1개:
endpoint:
POST /videos/private

HTTP:
201 필수

returned:
video.id
video.file_url

실제 request/response evidence 필요.

코드상 함수 존재만으로 PASS 금지.

━━━━━━━━━━━━━━━━━━━━━━
8. save payload 검증
━━━━━━━━━━━━━━━━━━━━━━

공통:

selectedAlbumVideos 또는 실제 current state
→ common video_ids
→ save/attach

학생별:

studentAlbumVideos[studentId] 또는 실제 current state
→ 해당 student video_ids
→ note/student-specific attach

반드시 확인:

Common video IDs:
present

Student video IDs:
present

학생별 video IDs가
save 전에 drop/filter되지 않는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
9. 서버 attach 결과 확인
━━━━━━━━━━━━━━━━━━━━━━

공통 video:

Diary save
→ /videos/diary-attach
→ relation saved

학생별 video:

Student/note save
→ /videos/note-attach
또는 실제 student-specific attach endpoint
→ relation saved

HTTP:
2xx

relation:
YES

서버/API 자체가 이미 정상이라면
수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. EDIT 화면에서도 동일 기능
━━━━━━━━━━━━━━━━━━━━━━

DiaryEditView에서도:

반 공통:
기존 video 표시
새 video 추가
video 제거

학생별:
기존 video 표시
새 video 추가
video 제거

모두 가능해야 함.

특히 현재 학생별 Edit 카드에
사진만 있고 영상 버튼이 없다면
영상도 동일하게 추가.

━━━━━━━━━━━━━━━━━━━━━━
11. 재진입 검증
━━━━━━━━━━━━━━━━━━━━━━

최종 PASS 조건:

A. 공통
video 선택
→ upload
→ save
→ Diary Hub
→ 다시 Edit
→ video 존재

B. 학생별
서태웅 video 선택
→ upload
→ save
→ 다시 Edit
→ 서태웅 카드에 video 존재

둘 다 실제 확인.

━━━━━━━━━━━━━━━━━━━━━━
12. 실패 분류
━━━━━━━━━━━━━━━━━━━━━━

학생별 video 실패를 아래 중 하나로 확정:

SV1
video button 없음

SV2
handler 연결 안 됨

SV3
student context 전달 안 됨

SV4
upload 함수 호출 안 됨

SV5
HTTP 실패

SV6
video.id state 미저장

SV7
student save payload에서 누락

SV8
attach API 누락

SV9
read/edit mapping 누락

SV10
기타 — exact evidence 필요

━━━━━━━━━━━━━━━━━━━━━━
13. 사진 regression
━━━━━━━━━━━━━━━━━━━━━━

사진 path 수정 금지.

최종 확인만:

Common photo:
PASS

Student photo:
PASS

영상 수정으로 사진 깨지면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
14. 수정 정책
━━━━━━━━━━━━━━━━━━━━━━

Client-only로 해결 가능하면:
release/v2.0.0 수정.

Server 변경이 실제로 필요한 경우:
main commit/push만.

Render deploy:
NO

DB migration:
NO

Native dependency:
NO

━━━━━━━━━━━━━━━━━━━━━━
15. OTA 자동 수행
━━━━━━━━━━━━━━━━━━━━━━

Client 수정 완료 후:

release/v2.0.0
commit
push

Preflight:

branch =
release/v2.0.0

working tree =
clean

HEAD =
origin/release/v2.0.0

APP_CONTRACT_VERSION =
1.0

Teacher Diary Engine =
https://swimnote-professional-engine.onrender.com

App Version =
2.0.1

RuntimeVersion =
2.1.0

Native incompatible =
NO

Android =
NO

모두 PASS면
추가 사용자 승인 없이

iOS production-v2 OTA 완료.

main에서 OTA 절대 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

# WP16 COMMON + STUDENT VIDEO PARITY REPORT

APP release before:
APP release after:
Client commit:

SERVER_CHANGED:
YES / NO

━━━━━━━━━━
COMMON VIDEO
━━━━━━━━━━

Video button:
YES / NO

Picker:
PASS / FAIL

Upload function:
...

HTTP:
...

video.id:
YES / NO

State stored:
YES / NO

Save payload:
YES / NO

Attach:
PASS / FAIL

Reopen:
PASS / FAIL

━━━━━━━━━━
STUDENT VIDEO
━━━━━━━━━━

Video button:
YES / NO

Picker:
PASS / FAIL

Student context:
PASS / FAIL

Upload function:
...

HTTP:
...

video.id:
YES / NO

Student state stored:
YES / NO

Save payload:
YES / NO

Attach:
PASS / FAIL

Reopen:
PASS / FAIL

Failure classification:
SV1 / SV2 / SV3 / SV4 / SV5 / SV6 / SV7 / SV8 / SV9 / SV10

Exact root cause:
...

━━━━━━━━━━
UX
━━━━━━━━━━

Common uploading banner:
PASS / FAIL

Student uploading banner:
PASS / FAIL

Save blocked during upload:
PASS / FAIL

Failure message:
PASS / FAIL

━━━━━━━━━━
PHOTO REGRESSION
━━━━━━━━━━

Common photo:
PASS / FAIL

Student photo:
PASS / FAIL

━━━━━━━━━━
OTA
━━━━━━━━━━

Source branch:
release/v2.0.0

Source SHA:
...

origin match:
YES / NO

Preflight:
PASS / FAIL

Published:
YES / NO

Channel:
production-v2

Platform:
iOS ONLY

Group ID:
...

Update ID:
...

Android OTA:
NO

main OTA:
NO

━━━━━━━━━━
FINAL
━━━━━━━━━━

Common video E2E:
PASS / FAIL

Student video E2E:
PASS / FAIL

Remaining video defects:
NONE / ...

Final:
READY_FOR_REAL_DEVICE
/
HOLD_RENDER_DEPLOY
/
FAIL

STOP.
WP17 선행 금지.
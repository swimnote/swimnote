[보낼 곳: APP REPLIT]

[SWIMNOTE 정산 V2 — weekly_count 실제 등록/수정 흐름 추가 조사]

WP-SETTLE-V2-1 구현 전에
students.weekly_count가 실제 운영에서
어떻게 생성되고 변경되는지 추가 확인하십시오.

아직 코드 수정 / migration / 배포하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. weekly_count 입력 Source 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 Production APP에서
회원 등록 또는 반 등록 시

선생님/관리자가:

- 주1회
- 주2회
- 주3회

를 선택하는 실제 화면을 찾으십시오.

반드시 실제 코드 기준으로 확인:

A. 화면 파일
B. 선택 UI
C. 저장 handler
D. 호출 endpoint
E. request payload field
F. 서버 route
G. DB UPDATE/INSERT 대상 column

최종적으로:

사용자가 화면에서 “주2회” 선택
→ students.weekly_count = 2

가 실제로 연결되어 있는지 확인하십시오.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 회원 신규등록 시 반영
━━━━━━━━━━━━━━━━━━━━━━

신규 회원 등록 시:

주1 선택
→ weekly_count=1

주2 선택
→ weekly_count=2

주3 선택
→ weekly_count=3

으로 실제 저장되는지 확인.

등록 후:

GET 학생 상세/목록
또는 실제 사용하는 조회 endpoint에서

weekly_count가 즉시 동일하게 반환되는지 확인하십시오.

즉:

입력
→ 저장
→ 재조회

까지 확인.

━━━━━━━━━━━━━━━━━━━━━━
3. 기존 회원 weekly_count 변경 경로
━━━━━━━━━━━━━━━━━━━━━━

기존 회원의:

주1 → 주2
주2 → 주3
주3 → 주1

변경이 가능한 실제 UI를 찾으십시오.

누가 변경 가능한지 확인:

- pool_admin
- teacher
- super_admin

각 역할별 현재 권한.

그리고 변경 시:

students.weekly_count

가 실제 UPDATE 되는지 확인.

별도 class_type이나 다른 field에 저장되는
중복 구조가 있는지도 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 정산 calculator 실시간 반영 여부
━━━━━━━━━━━━━━━━━━━━━━

정산 V2에서 weekly_count는:

students.weekly_count

를 직접 Source of Truth로 사용할 예정입니다.

따라서 다음 흐름이 가능한지 확인:

학생 A:
weekly_count=1

calculator 조회
→ weekly_1 pricing 사용

학생 수정:
weekly_count=2 저장

calculator 재조회
→ 즉시 weekly_2 pricing 사용

APP 재로그인
서버 재시작
별도 batch job

등이 필요하면 안 됩니다.

즉 API 재조회만으로
즉시 반영되는 구조인지 확인하십시오.

캐시가 있다면
weekly_count 변경 후 stale 가능성도 확인하십시오.

불필요한 캐시 조사 확대 금지.
해당 calculator 경로만 확인.

━━━━━━━━━━━━━━━━━━━━━━
5. 시간표와 weekly_count 불일치 가능성
━━━━━━━━━━━━━━━━━━━━━━

중요.

weekly_count=2인데
실제 student_class_history상 반이
주1회 schedule_days인 경우가 가능한지 확인하십시오.

또는:

weekly_count=1인데
다중 반 배정 때문에 실제 시간표가 주2회 이상인 경우.

이 두 값이 다를 수 있다면
정산에서 어느 값을 무엇에 쓰는지 명확히 구분합니다.

최종 원칙 후보:

weekly_count
→ 가격정책 선택
→ weekly_1/2/3
→ monthly_fee / sessions_per_month

시간표 등록상태
→ 실제 해당월 scheduled_regular_count

즉:

weekly_count는 “가격 기준”

schedule/history는 “수업 횟수 기준”

으로 역할을 분리합니다.

두 값이 다르다고
서로 덮어쓰거나 자동 보정하지 마십시오.

불일치는 관리자에게 표시할 수 있는지만 제안.

━━━━━━━━━━━━━━━━━━━━━━
6. 월 중간 weekly_count 변경
━━━━━━━━━━━━━━━━━━━━━━

반드시 확인할 중요한 케이스:

9월 1~15일 주1
9월 16일부터 주2

처럼 월 중간 weekly_count가 변경되는 경우.

현재 DB가:

students.weekly_count 현재값 1개만 보존하는지

또는

weekly_count 변경 이력이 별도로 존재하는지

확인하십시오.

A.
변경 이력이 존재한다면:
→ 실제 구조와 필드 보고.

B.
현재값만 존재한다면:
→ 복잡한 신규 이력 시스템을 만들지 마십시오.

그 경우 SIMPLE V2 기본 정책은:

현재 AUTO 계산
→ 현재 students.weekly_count 사용

선생님이 이미 저장한 월:
→ auto_amount_snapshot / total_revenue 유지

월 중간 변경으로 실제 결제금액과 차이가 있으면:
→ manual adjustment

로 처리하는 방향이 가능한지 검토하십시오.

이번 단계에서
weekly_count history 신규 테이블 제안 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. 과거 월 영향
━━━━━━━━━━━━━━━━━━━━━━

weekly_count가 현재값 하나라면

주1 → 주2 변경 후
과거 미저장 월 calculator를 열었을 때

과거 월도 주2 가격으로 계산될 가능성이 있습니다.

이 현상이 실제로 발생하는지 확인하십시오.

그리고 복잡한 이력 시스템 없이
현재 V2 원칙으로 어느 수준까지 보호 가능한지 보고하십시오.

우선 원칙:

SAVED / CONFIRMED 월
→ 저장된 total_revenue와 auto_amount_snapshot 보호

AUTO 상태 과거 월
→ 현재값 기반 계산이 될 수 있음

이 정도 단순화가 가능한지 보고.

정확한 과거 가격 이력 시스템은
이번 V2 범위에 넣지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. 등록/수정 저장 오류도 같이 확인
━━━━━━━━━━━━━━━━━━━━━━

최근 수영장 설정 저장 API가 깨져 있었던 이력이 있으므로

weekly_count 등록/수정 경로도
실제 endpoint/DB write가 정상인지 확인하십시오.

단 전체 회원관리 audit 금지.

딱:

신규등록 1건 흐름
기존회원 weekly_count 수정 1건 흐름

만 확인.

━━━━━━━━━━━━━━━━━━━━━━
9. 최종 보고
━━━━━━━━━━━━━━━━━━━━━━

아래만 보고하십시오.

[A. CREATE FLOW]
- 주1/주2/주3 선택 화면
- 저장 field
- endpoint
- students.weekly_count 실제 저장 여부

[B. UPDATE FLOW]
- 기존 회원 weekly_count 변경 화면
- 권한
- endpoint
- DB UPDATE 여부

[C. REALTIME]
- 변경 후 calculator 재조회 즉시 반영 여부
- 별도 cache/batch 필요 여부

[D. ROLE SEPARATION]
- weekly_count = 가격정책 선택 기준
- schedule/history = 실제 수업횟수 기준
으로 분리 가능한지

[E. MID-MONTH CHANGE]
- weekly_count 이력 존재 여부
- 현재값만 존재하면 V2에서 어떻게 단순 처리할지

[F. PAST MONTH]
- 현재값 변경이 과거 AUTO 월에 영향을 주는지
- SAVED/CONFIRMED 보호 가능 여부

[G. CURRENT BUG]
- 신규등록/수정 저장 경로 정상 PASS/FAIL

아직 코드 수정하지 마십시오.
migration 하지 마십시오.
배포하지 마십시오.

이번 조사 목적은:

students.weekly_count를
정산의 가격 기준 Source of Truth로 사용해도 되는지

그리고 사용자가 회원 등록/수정에서 변경한 값이
정산에 즉시 반영되는지

확정하는 것입니다.
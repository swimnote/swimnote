```text
[보낼 곳: APP REPLIT]

# WP13 — DATA INTEGRITY CHECKER
# FINAL MASTER WP V6.1
# READ-ONLY ONLY
# PLAN FREEZE — 중간 계획 변경 금지

목적:

출시 전 DB의 주요 데이터 관계가 깨져 있는지
Super Admin이 안전하게 검사할 수 있는
READ-ONLY Data Integrity Checker를 구현합니다.

핵심:

- orphan relation
- cross-pool relation
- invalid status/state
- entitlement inconsistency
- member limit inconsistency
- storage inconsistency
- notice/push relation
- Growth relation
- duplicate logical data

를 탐지하고 보고합니다.

절대 자동 수정하지 않습니다.

이번 작업은 WP13만 수행합니다.

WP14 이후 작업 절대 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
0. 절대 원칙
━━━━━━━━━━━━━━━━━━━━━━

이번 WP의 가장 중요한 규칙:

READ ONLY.

금지:

- UPDATE
- DELETE
- INSERT를 통한 자동 repair
- orphan 자동 삭제
- status 자동 변경
- entitlement 자동 보정
- member_limit 자동 보정
- billing 자동 보정
- Growth report 자동 재생성
- Production data mutation

발견:

→ issue report
→ evidence
→ severity
→ suggested action

까지만.

사용자 승인 없이 어떤 데이터도 고치지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. Branch
━━━━━━━━━━━━━━━━━━━━━━

Branch:

release/v2.0.0

Required previous commit:

dc768326

시작 전 최소 확인:

- local release HEAD
- origin/release HEAD
- working tree clean
- dc768326 ancestry 포함

WP1~WP12 재감사 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 기존 시스템 재사용
━━━━━━━━━━━━━━━━━━━━━━

기존:

- system-health
- Pool Health
- audit_logs
- Support Cases
- ops alerts
- Super Admin control center

재사용.

새 data-quality platform 만들지 마십시오.

가능하면 기존 Super Admin operations 영역에
read-only integrity summary를 additive하게 추가.

━━━━━━━━━━━━━━━━━━━━━━
3. 검사 범위
━━━━━━━━━━━━━━━━━━━━━━

이번 WP는 다음 핵심 데이터만 검사.

A. Pool / User / Role
B. Students / Members
C. Parents / parent-child links
D. Classes / membership
E. Attendance
F. Diary / media relations
G. X entitlement / plan / member limit
H. Billing / RevenueCat logical state
I. Storage quota metadata
J. Notices / dismissals / reads
K. Push fanout jobs/deliveries
L. Growth batch/report relations

이 외 repository 전체 데이터 품질 분석 금지.

━━━━━━━━━━━━━━━━━━━━━━
4. Pool / User Integrity
━━━━━━━━━━━━━━━━━━━━━━

탐지:

- user가 참조하는 swimming_pool_id가 없음
- pool-scoped role인데 pool 없음
- invalid role/state combination
- 동일 user의 명백히 모순된 pool ownership

단 현재 intentional global/super_admin 구조는
오류로 잡지 마십시오.

existing role semantics 우선.

━━━━━━━━━━━━━━━━━━━━━━
5. Student / Member Integrity
━━━━━━━━━━━━━━━━━━━━━━

탐지:

- student/member의 pool 없음
- class assignment pool과 member pool 불일치
- active entity가 deleted/archived relation과 동시에 존재하는 명백한 모순
- canonical lifecycle 밖 status
- duplicate logical member 가능성은
  확실한 unique key 기준으로만 탐지

이름이 같다는 이유만으로 duplicate 취급 금지.

━━━━━━━━━━━━━━━━━━━━━━
6. Parent / Child Integrity
━━━━━━━━━━━━━━━━━━━━━━

탐지:

parent_students relation에서:

- parent 없음
- student 없음
- parent pool과 student pool 불일치
- approved relation인데 canonical pool ownership 불일치
- duplicate identical link

WP6 security semantics를 기준으로 검사.

자동 unlink 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. Class Integrity
━━━━━━━━━━━━━━━━━━━━━━

탐지:

- class pool 없음
- class_members member 없음
- class member와 class의 pool 불일치
- assigned teacher와 class pool 불일치
- deleted class에 active relation이 남아 있는 명백한 경우

현재 historical relation 정책이 있으면
그것을 오류로 잡지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. Attendance Integrity
━━━━━━━━━━━━━━━━━━━━━━

WP6에서 발견한 cross-pool issue 재발 탐지.

검사:

attendance pool
student pool
class pool

간 불일치.

또한:

- missing student
- missing class/resource
- impossible duplicate key

현재 canonical attendance schema 기준.

━━━━━━━━━━━━━━━━━━━━━━
9. Diary / Media Integrity
━━━━━━━━━━━━━━━━━━━━━━

탐지:

- diary pool과 student/class pool 불일치
- missing student/class reference
- media metadata가 존재하지만 parent diary/resource 없음
- media resource pool mismatch
- deleted diary에 active attachment가 남는 것이 현재 정책상 invalid인 경우

R2 실제 object existence를
이번 WP에서 전체 HEAD scan하지 마십시오.

DB metadata 관계 검사 우선.

R2 physical integrity full scan은 범위 아님.

━━━━━━━━━━━━━━━━━━━━━━
10. X Entitlement Integrity
━━━━━━━━━━━━━━━━━━━━━━

중요.

현재 canonical semantics:

paid
manual
management_override
force_disabled
x_plan_key
member_limit
effective resolver

기준으로 검사.

예:

x_plan_key=x300인데 member_limit != 300
x500인데 !=500
x1000인데 !=1000

→ integrity issue.

단 legacy/non-X plan의 기존 member_limit semantics를
잘못 오류 처리하지 마십시오.

검사:

- invalid x_plan_key
- effective X인데 required prerequisite 없음
- force_disabled=true인데 effective resolver가 true라면 contradiction
- manual/paid/management source와 resolver output 불일치
- mode=x 자체를 paid로 판단하지 않음

절대 자동 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. Billing / RevenueCat Integrity
━━━━━━━━━━━━━━━━━━━━━━

WP3 semantics 유지.

검사:

- duplicate revenuecat event_id
  (DB UNIQUE 때문에 정상적으로 0 예상)
- processed=false/stuck event
- X paid entitlement와 명백히 모순되는 current RC state
- unsupported product mapping
- DATA purchase logical duplicate evidence

중요:

RC reconciliation worker 만들지 마십시오.

read-only inconsistency detection만.

Historical billing 보존.

━━━━━━━━━━━━━━━━━━━━━━
12. Storage Integrity
━━━━━━━━━━━━━━━━━━━━━━

검사:

- negative quota
- negative usage
- addon quota invalid
- effective quota 계산 모순
- DB metadata상 usage > quota

usage > quota 자체는
"데이터 삭제 대상"이 아닙니다.

특히 DATA cancellation 정책은 아직 미확정.

따라서:

OVER_QUOTA

상태는 issue/warning으로만 표시.

삭제/차단 정책 자동 적용 금지.

━━━━━━━━━━━━━━━━━━━━━━
13. Notice Integrity
━━━━━━━━━━━━━━━━━━━━━━

검사:

- notice_dismissals notice 없음
- notice_dismissals user 없음
- notice_reads notice/user 없음
- invalid target role
- invalid plan target
- starts_at > ends_at
- pool-scoped notice target이 모순된 경우

legacy platform_banners는 존재 자체를 오류로 잡지 마십시오.

DEPRECATED historical compatibility 상태 유지.

━━━━━━━━━━━━━━━━━━━━━━
14. Push Integrity
━━━━━━━━━━━━━━━━━━━━━━

WP5 queue 기준.

검사:

push_fanout_deliveries:

- job_ref가 존재하지 않음
- push_token_id 없음
- impossible status
- SENT인데 sent_at 없음 등 명백한 state contradiction
- 동일 logical unique key duplicate

push_fanout_jobs:

- COMPLETED인데 pending delivery 존재
- FAILED인데 모든 delivery SENT 같은 명백한 모순
- 오래된 PROCESSING은 WP9 stuck monitoring과 중복될 수 있으므로
  integrity에서는 structural inconsistency에 집중.

Worker 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. Growth Integrity
━━━━━━━━━━━━━━━━━━━━━━

검사:

- report가 참조하는 student 없음
- cycle 없음
- pool mismatch
- same student + cycle duplicate
- batch completed인데 명백한 child report structural inconsistency
- published report에 required canonical output 없는 경우
- invalid status transition evidence

WP10 recovery logic은 수정하지 마십시오.

REVIEW_REQUIRED는 정상 상태일 수 있으므로
오류로 취급 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. Severity
━━━━━━━━━━━━━━━━━━━━━━

간단하게:

CRITICAL:
- cross-pool data relation
- billing/entitlement destructive contradiction
- orphan causing user data exposure risk

WARNING:
- invalid lifecycle/state
- over quota
- stale logical relation
- mismatch that does not immediately leak data

INFO:
- cleanup candidate
- deprecated/historical condition

현재 ops severity convention이 있으면 재사용.

새 scoring engine 금지.

━━━━━━━━━━━━━━━━━━━━━━
17. Issue Format
━━━━━━━━━━━━━━━━━━━━━━

각 issue 최소:

- code
- severity
- entity_type
- entity_id
- pool_id if applicable
- summary
- evidence
- detected_at
- suggested_action

민감정보 전체 dump 금지.

예:

{
  code: "CLASS_MEMBER_CROSS_POOL",
  severity: "CRITICAL",
  entity_type: "class_member",
  entity_id: "...",
  pool_id: "...",
  summary: "...",
  evidence: {
    class_pool_id: "...",
    member_pool_id: "..."
  }
}

student name/phone 등 unnecessary PII 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. Checker 실행 방식
━━━━━━━━━━━━━━━━━━━━━━

Super Admin only.

최소 두 가지:

A. single pool scan

B. global scan

단 global scan은
500 pools / large data 고려.

한 request에서 무한 scan 금지.

가능하면:

- bounded batches
- per-check LIMIT
- summary counts
- cursor/page

WP8 pagination helper 재사용 가능.

━━━━━━━━━━━━━━━━━━━━━━
19. Global Scan Performance
━━━━━━━━━━━━━━━━━━━━━━

금지:

for each pool:
  20 queries

식 500-pool N+1.

가능하면 set-based SQL.

예:

JOIN
NOT EXISTS
GROUP BY

등.

Integrity checker가 Production 부하 원인이 되면 안 됩니다.

대형 full table COUNT 반복 금지.

필요한 query만.

━━━━━━━━━━━━━━━━━━━━━━
20. Read-only Enforcement
━━━━━━━━━━━━━━━━━━━━━━

구현 자체에 repair endpoint를 만들지 마십시오.

금지 route 예:

POST /integrity/fix
PATCH /integrity/repair
DELETE /integrity/orphans

없어야 합니다.

API는 GET 또는
scan 의미의 POST여도
DB mutation이 없어야 합니다.

scan result를 DB에 저장할 필요도 없습니다.

가능하면 on-demand computed result.

━━━━━━━━━━━━━━━━━━━━━━
21. Super Admin API
━━━━━━━━━━━━━━━━━━━━━━

기존 Super Admin route convention 사용.

최소:

GET /super/integrity/summary

GET /super/integrity/issues

optional:

GET /super/integrity/pools/:poolId

또는 equivalent.

새 API hierarchy 전체 설계 금지.

━━━━━━━━━━━━━━━━━━━━━━
22. UI
━━━━━━━━━━━━━━━━━━━━━━

대형 dashboard 만들지 않습니다.

기존 Super Admin operations/health 영역에:

Data Integrity

최소 표시:

Overall:
OK / WARNING / CRITICAL

Critical count
Warning count

카테고리별 count

최근 scan 결과 또는 on-demand scan

Issue list:
code / severity / pool / summary

수정 버튼:
없음.

"자동 수정":
절대 없음.

━━━━━━━━━━━━━━━━━━━━━━
23. Audit
━━━━━━━━━━━━━━━━━━━━━━

Read-only scan 자체를
모든 query마다 audit할 필요 없음.

Super Admin이 manual global integrity scan을 실행한 경우
기존 audit_logs에:

DATA_INTEGRITY_SCAN

정도 1건 기록 가능.

단 audit 때문에 대량 row 생성 금지.

Scan 결과 전체를 audit payload에 복제 금지.

━━━━━━━━━━━━━━━━━━━━━━
24. DB Schema / Migration
━━━━━━━━━━━━━━━━━━━━━━

기본:

Schema changes:
NO

Migration:
NO

on-demand checker 우선.

새 integrity_issues table 만들지 마십시오.

이번 MVP에서는 결과 persistence 필요 없음.

신규 runtime DDL:
NO.

━━━━━━━━━━━━━━━━━━━━━━
25. Production Safety
━━━━━━━━━━━━━━━━━━━━━━

Production에서 read-only checker가 향후 실행될 수 있지만
Replit 작업 중:

Production mutation:
0

Production repair:
0

대량 destructive test:
0

Staging/test fixtures로 기능 검증.

Production full scan을 Replit이 임의 실행하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
26. Required Integrity Checks
━━━━━━━━━━━━━━━━━━━━━━

최소 issue code 의미:

USER_ORPHAN_POOL
MEMBER_ORPHAN_POOL
PARENT_CHILD_ORPHAN
PARENT_CHILD_CROSS_POOL
CLASS_MEMBER_ORPHAN
CLASS_MEMBER_CROSS_POOL
ATTENDANCE_CROSS_POOL
DIARY_CROSS_POOL
MEDIA_ORPHAN_RESOURCE
MEDIA_CROSS_POOL
X_PLAN_LIMIT_MISMATCH
X_INVALID_PLAN
X_RESOLVER_INCONSISTENCY
RC_STUCK_EVENT
STORAGE_INVALID_QUOTA
STORAGE_OVER_QUOTA
NOTICE_ORPHAN_DISMISSAL
NOTICE_INVALID_PERIOD
PUSH_ORPHAN_DELIVERY
PUSH_JOB_STATE_INCONSISTENT
GROWTH_ORPHAN_REPORT
GROWTH_DUPLICATE_REPORT

정확한 key naming은 repository convention에 맞게 조정 가능.

━━━━━━━━━━━━━━━━━━━━━━
27. False Positive 방지
━━━━━━━━━━━━━━━━━━━━━━

중요.

검사기는 틀린 경고를 많이 만들면 운영에 쓸 수 없습니다.

각 check는 현재 canonical product semantics 근거로만.

예:

- withdrawn member가 존재함
  → 오류 아님

- REVIEW_REQUIRED report
  → 오류 아님

- platform_banners legacy row
  → 오류 아님

- DATA usage > current quota
  → WARNING, 자동 삭제 대상 아님

- mode=x paid=false
  → 오류 아님

이 원칙 테스트.

━━━━━━━━━━━━━━━━━━━━━━
28. Security
━━━━━━━━━━━━━━━━━━━━━━

Super Admin only.

Pool Admin:
global integrity API 접근 금지.

Teacher:
금지.

Parent:
금지.

Issue evidence가 다른 Pool data를
일반 사용자에게 노출하지 않도록.

client supplied role 신뢰 금지.

━━━━━━━━━━━━━━━━━━━━━━
29. Required Tests
━━━━━━━━━━━━━━━━━━━━━━

전체 suite 금지.

WP13 관련 tests만.

A.
clean fixtures
→ CRITICAL 0

B.
orphan user pool
→ USER_ORPHAN_POOL

C.
parent-child cross pool
→ CRITICAL

D.
class-member cross pool
→ CRITICAL

E.
attendance cross pool
→ CRITICAL

F.
diary cross pool
→ CRITICAL

G.
media orphan
→ detected

H.
X300 limit 500
→ X_PLAN_LIMIT_MISMATCH

I.
mode=x paid=false manual=true
→ NO false positive

J.
management override X
→ NO false positive

K.
force disabled semantics
→ resolver consistency

L.
RC duplicate/event constraints
→ appropriate result

M.
storage negative quota
→ detected

N.
usage > quota
→ WARNING only

O.
notice orphan dismissal
→ detected

P.
starts_at > ends_at
→ detected

Q.
push orphan delivery
→ detected

R.
push completed + pending deliveries
→ inconsistency

S.
growth orphan report
→ detected

T.
REVIEW_REQUIRED
→ NO false positive

U.
withdrawn member
→ NO false positive

V.
legacy platform_banners
→ NO false positive

W.
Pool Admin global scan
→ 403

X.
Teacher
→ 403

Y.
Parent
→ 403

Z.
client role spoof
→ BLOCK

AA.
checker executes UPDATE
→ 0

AB.
checker executes DELETE
→ 0

AC.
checker executes INSERT repair
→ 0

AD.
500-pool simulation
→ no per-pool N+1

AE.
issue evidence PII-safe

AF.
bounded result list

AG.
Production data mutation
→ 0

━━━━━━━━━━━━━━━━━━━━━━
30. Query Evidence
━━━━━━━━━━━━━━━━━━━━━━

완료보고:

checks count
queries count

500 pool simulation query count

N+1:
NONE / ...

각 주요 check가 set-based인지 제출.

실제 EXPLAIN이 가능한 Staging query는
필요한 핵심 2~3개만 확인 가능.

과도한 DB benchmarking 금지.

━━━━━━━━━━━━━━━━━━━━━━
31. 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

- WP14 Legacy Cleanup
- WP15 Version/runtime
- WP16 Production E2E
- WP17 RC
- auto repair
- cleanup worker
- data migration
- CRM
- Monitoring redesign
- Growth 수정
- RevenueCat 수정
- Push 수정
- Notice schema 수정
- Admin Notes 수정
- Marketing 수정
- AI Engine
- unrelated refactor

━━━━━━━━━━━━━━━━━━━━━━
32. Render / OTA
━━━━━━━━━━━━━━━━━━━━━━

Server checker 추가 예상:

Render required:
YES/NO

Super Admin JS UI 추가 시:

iOS OTA required:
YES/NO

Replit OTA publish 금지.

Android OTA:
NO

Native:
NO 예상.

Production deploy:
하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
33. PASS 완료조건
━━━━━━━━━━━━━━━━━━━━━━

WP13 PASS:

1. read-only integrity checker
2. Pool/User integrity
3. member/parent/class relations
4. attendance/diary/media cross-pool detection
5. X entitlement/limit checks
6. RC checks
7. storage checks
8. notice checks
9. push checks
10. Growth checks
11. false-positive protections
12. severity
13. Super Admin only
14. bounded global scan
15. 500-pool N+1 없음
16. repair endpoint 0
17. DB mutation 0
18. schema/migration 없음
19. related tests PASS
20. Commit SHA

━━━━━━━━━━━━━━━━━━━━━━
34. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

# WP13 DATA INTEGRITY CHECKER REPORT

Target:
APP

Branch:
...

HEAD before:
dc768326

HEAD after:
...

origin/release HEAD:
...

Commit SHA:
...

Commit message:
...

Files changed:
...

Checker:
...

Read-only:
PASS / FAIL

Repair endpoints:
0 / ...

Integrity checks implemented:
...

Total check types:
...

Severity:
...

Pool/User:
PASS / FAIL

Students/Members:
PASS / FAIL

Parents:
PASS / FAIL

Classes:
PASS / FAIL

Attendance:
PASS / FAIL

Diary:
PASS / FAIL

Media:
PASS / FAIL

X entitlement:
PASS / FAIL

RevenueCat:
PASS / FAIL

Storage:
PASS / FAIL

Notices:
PASS / FAIL

Push:
PASS / FAIL

Growth:
PASS / FAIL

False-positive protections:

mode=x paid=false:
PASS / FAIL

withdrawn:
PASS / FAIL

REVIEW_REQUIRED:
PASS / FAIL

legacy platform_banners:
PASS / FAIL

over quota warning only:
PASS / FAIL

Super Admin only:
PASS / FAIL

Pool Admin denied:
PASS / FAIL

Teacher denied:
PASS / FAIL

Parent denied:
PASS / FAIL

Issue evidence PII safe:
PASS / FAIL

Bounded:
PASS / FAIL

500-pool N+1:
NONE / ...

Query count:
...

DB mutation statements:
UPDATE: 0 / ...
DELETE: 0 / ...
repair INSERT: 0 / ...

DB schema changes:
NO

Migration:
NO

Production data changes:
NO

Production full scan:
NO

Tests:
...

Test result:
PASS / FAIL

Render required:
YES / NO

iOS OTA required:
YES / NO

Android OTA:
NO

Native binary required:
NO

Remaining blocker:
NONE / ...

Final:
PASS / HOLD / FAIL

중요:

FINAL MASTER WP는 Freeze 상태입니다.

WP13은 진단기입니다.
자동 수정기는 절대 만들지 마십시오.

WP14 이후 작업 선행 금지.

완료조건 충족 후 즉시 STOP하십시오.
```

[보낼 곳: APP REPLIT]

조사보고 검수 결과: HOLD

조사 자체는 승인합니다.
하지만 제시한 F 수정계획을 그대로 실행하지 마십시오.

이번 작업의 최종 목표는:

Super Admin
→ 수영장 관리
→ 특정 수영장 상세

에서

해당 수영장의 실제 SWIMNOTE 운영정보가
APP / Pool Admin Dashboard와 동일한 Production source of truth에서
정확하고 실시간으로 조회되는 상태

입니다.

임시 컬럼 추가,
없는 테이블을 슈퍼관리자 화면 때문에 새로 만드는 것,
catch 후 0/null로 위장,
30초 polling

으로 마감하면 안 됩니다.

아래 순서대로 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. class_groups.active 우회 제거
━━━━━━━━━━━━━━━━━━━━━━

확정사항:

canonical:
class_groups.is_deleted = false

잘못된 Super Admin query:
class_groups.active = true

따라서:

1. super.ts의 해당 query를
   is_deleted = false
   기준으로 수정

2. 오늘 Production에 임의로 추가한
   class_groups.active GENERATED ALWAYS 컬럼 제거

3. APP/API 기존 class 조회와 동일한 의미인지 확인

새 active 컬럼을 canonical로 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. X canonical fields 정식 정리
━━━━━━━━━━━━━━━━━━━━━━

다음 필드는 실제 APP/API canonical 사용이 확인된 것으로 보고되었습니다.

- x_paid_entitlement
- x_manual_entitlement
- x_force_disabled
- x_management_override
- x_plan_key
- xmode_config_status

이 중:

x_management_override는
정식 migration 파일 존재.

나머지는 일부 migration 파일이 없는 상태입니다.

이번에 할 일:

A.
현재 Production에 실제 존재하는 값/type/default/nullability 확인

B.
APP/API 코드가 기대하는 canonical schema 확인

C.
오늘 수동으로 추가한 필드가
canonical definition과 정확히 동일한지 비교

D.
동일하다면 현재 Production column은 유지 가능하지만,
Repository에 정식 idempotent migration을 추가하여
향후 환경 재구축 시 동일 schema가 만들어지게 하십시오.

E.
정의가 다르면 무작정 drop/recreate하지 말고
데이터 손실 없는 ALTER 계획으로 수정.

Production 값을 임의 삭제하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. 확인하지 못한 subscription/storage fields 먼저 조사
━━━━━━━━━━━━━━━━━━━━━━

다음은 아직 확인하지 못했다고 보고했습니다.

- base_manual_entitlement
- member_limit
- base_storage_bytes
- addon_storage_bytes
- subscription_expires_at
- auto_renew_status

이것들을 Super Admin 페이지 때문에
임의 추가하지 마십시오.

각각:

- APP/API 실제 사용 여부
- canonical schema
- 기존 migration
- Production 존재 여부
- 대체 field 존재 여부

를 확인하고

MIGRATION MISSING
또는
SUPER QUERY BUG

로 분류한 뒤 처리하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 존재하지 않는 테이블을 함부로 만들지 말 것
━━━━━━━━━━━━━━━━━━━━━━

현재 Super Admin summary가 참조하지만
Production에 없다고 보고한:

- growth_reports
- support_cases
- x_monthly_operational_snapshots
- push_fanout_jobs
- push_fanout_deliveries
- event_logs.level

을 전부 새 migration으로 만드는 것은 금지합니다.

각 항목마다 먼저
APP / 기존 Pool Admin / Worker가 실제 어떤 source를 쓰는지 확인하십시오.

중요:

Super Admin 화면에 숫자 하나 보여주기 위해
새 도메인 테이블을 만드는 것이 아닙니다.

원칙:

기존 canonical source 존재
→ 그 source 재사용

정식 migration이 존재하고 Production 적용만 누락
→ migration 적용 검토

도메인 자체가 아직 Production에 없음
→ Super Admin에서 가짜 0/null로 정상처럼 보이지 않게 처리

예:
"데이터 없음"
"미구현"
"집계 source 없음"

등 실제 상태를 표시.

없는 데이터를 0이라고 표시하면 안 됩니다.

0은 실제 값이 0일 때만 사용하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. Growth Report source 정확히 재확인
━━━━━━━━━━━━━━━━━━━━━━

특히 growth_reports는 중요합니다.

SWIMNOTE Production에는 이미
Growth Report 기능이 실제 운영 중입니다.

따라서 Super Admin query가
"growth_reports 테이블 없음"
이라고 나왔다면:

- 실제 Growth Report canonical table 이름
- 실제 API route
- 실제 APP/Pool Admin query

를 먼저 확인하십시오.

Super Admin용 growth_reports 테이블을 새로 만들지 마십시오.

실제 운영 Growth Report source에 연결하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. Super Admin Summary SQL 복제 최소화
━━━━━━━━━━━━━━━━━━━━━━

현재 문제의 본질 중 하나가:

Super Admin용 summary SQL이
APP/Pool Admin canonical logic과 별개로 작성되어
schema drift가 발생한 것인지 확인하십시오.

가능하면 같은 domain 의미는:

existing service/helper/query

를 재사용하십시오.

예:

member count
class count
teacher count
X mode
storage
subscription
growth report status

를 Super Admin에서 다시 별도 schema 가정으로 작성하지 마십시오.

대규모 리팩터링은 금지.

최소한의 공통 helper 추출 또는 기존 helper 재사용만 허용.

━━━━━━━━━━━━━━━━━━━━━━
7. Realtime — polling 금지
━━━━━━━━━━━━━━━━━━━━━━

사용자 요구는 "실시간 연동"입니다.

30초 polling은 최종안으로 승인하지 않습니다.

현재 이미 존재하는:

PostgreSQL LISTEN/NOTIFY
→ API SSE
→ React Query invalidate/refetch

구조를 재사용하십시오.

목표:

Super Admin이 Pool A 상세를 열고 있을 때

APP / Pool Admin에서 Pool A의:

- 회원
- 반
- 선생님
- X 상태
- 구독/설정
- 기타 해당 summary에 영향 주는 정보

가 변경되면

Super Admin 상세도
수동 새로고침 없이 자동 갱신.

구현 원칙:

- super_admin 인증 검증
- 선택한 pool_id 범위만 구독
- 기존 pool isolation 훼손 금지
- JWT query string 금지
- 개인정보 SSE payload 금지
- event에는 최소 identifier만 전달
- event 수신 후 React Query invalidate/refetch

기존 pool_admin SSE를 권한검증 없이
super_admin에게 그대로 열지 마십시오.

필요하면 동일 infrastructure 위에
super_admin authorized stream을 추가하십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. Super Admin 상세 화면의 "실제 연동" 검증
━━━━━━━━━━━━━━━━━━━━━━

이번 작업은 API 200만 확인하면 완료가 아닙니다.

Production의 실제 수영장 1곳을 기준으로 E2E 확인하십시오.

최소:

A. 회원
APP/Pool Admin에서 회원 상태 변경
→ Super Admin 상세 숫자/상태 자동 반영

B. 반
반 추가/상태 변경
→ Super Admin 자동 반영

C. 선생님
teacher 상태 변경
→ Super Admin 자동 반영

D. X 상태
실제 canonical X 상태 변경
→ Super Admin 자동 반영

E. 새로고침 후에도 동일 값

F. 다른 수영장의 이벤트가
현재 보고 있는 수영장 상세를 잘못 invalidate하지 않는지 확인

━━━━━━━━━━━━━━━━━━━━━━
9. 오류 숨김 금지
━━━━━━━━━━━━━━━━━━━━━━

현재처럼:

table 없음
column 없음
query 실패

를 catch해서

0
null
[]

로 정상 응답하는 구조는 수정하십시오.

운영상 optional section이라면
페이지 전체를 죽이지 않는 것은 맞습니다.

하지만 응답에는 section별 상태를 구분할 수 있게:

available
unavailable
error

등 기존 contract에 맞는 최소 상태를 사용하십시오.

UI도:

실제 0
과
데이터 source unavailable

을 구분해야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
10. 배포 전 정리
━━━━━━━━━━━━━━━━━━━━━━

오늘 Production에 직접 적용한 모든 ALTER를 목록화하십시오.

각각:

- 유지
- 정식 migration으로 소급
- rollback/drop

중 하나로 분류.

특히:

class_groups.active
→ 올바른 query 수정 후 DROP

x canonical fields
→ 실제 canonical과 일치하면 유지 + migration 정식화

로 처리.

Production 데이터 삭제 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

완료 후 아래만 보고하십시오.

A. SCHEMA

1. 오늘 수동 추가한 Production 변경 전체 목록
2. 유지한 column
3. 제거한 column
4. 새 정식 migration 파일
5. Production schema와 repo canonical schema 일치 여부

B. SUPER ADMIN QUERY

6. 수정된 endpoint
7. class_groups canonical query
8. X mode canonical source
9. subscription/storage canonical source
10. Growth Report 실제 canonical source
11. 없는 table/field를 잘못 참조하던 query 전체 목록

C. REALTIME

12. Super Admin SSE endpoint
13. 인증 방식
14. pool filtering 방식
15. event type
16. React Query invalidate 대상

D. PRODUCTION E2E

17. 회원 변경 자동 반영 PASS/FAIL
18. 반 변경 자동 반영 PASS/FAIL
19. 선생님 변경 자동 반영 PASS/FAIL
20. X 상태 자동 반영 PASS/FAIL
21. 다른 pool isolation PASS/FAIL

E. DEPLOY

22. 수정 파일
23. full work commit SHA
24. full Production LIVE SHA
25. Render API deploy ID
26. swimnote-web deploy ID
27. migration 적용 목록

완료했다고만 보고하지 마십시오.

확인하지 못한 항목은
"확인하지 못함"
이라고 명시하십시오.

추측해서 정상이라고 보고하지 마십시오.
[보낼 곳: APP REPLIT]

[P0 SUPER ADMIN FINAL TRUTH-FIX — FALSE PASS CORRECTION]

이전 Super Admin 완료보고는 HOLD로 되돌린다.

사용자가 직접 검증해서 아래 허위/오류 2건 확인됨.

1.
operational_errors 테이블 존재하지 않음.

실제 operational warning/error source:
event_logs
level = 'WARNING' | 'ERROR'

2.
swimnote-web Production은 최신 commit이 배포되지 않았음.

https://swimnote.kr
last-modified 기준 기존 2026-08-28 build 서비스 중.

이전:
"Web deployed commit = 7e58e070"
보고는 무효.

이번 작업은 새 기능 개발 금지.

오직 아래 2개를 실제 Production 기준으로 수정/검증해서
Super Admin MASTER를 진짜로 닫는다.

━━━━━━━━━━━━━━━━━━━━━━
1. EVENT LOG SOURCE 수정
━━━━━━━━━━━━━━━━━━━━━━

Super Admin Overview / Pool Health / System Health에서

operational_errors

참조 전부 검색.

존재하지 않는 table을 query하는 코드 전부 제거.

실제 canonical source:

event_logs

사용.

Warning/Error 기준은
현재 event_logs schema/code의 실제 level 값을 확인해서 사용.

예:

level = 'WARNING'
level = 'ERROR'

단:
문자열 대소문자/enum은 실제 schema 기준.

임의 컬럼 생성 금지.
새 테이블 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 관련 쿼리 전수 확인
━━━━━━━━━━━━━━━━━━━━━━

Super Admin Web/API에서:

- Overview warning/error
- Pool Control Health
- AI/System error
- recent error count
- system-status aggregation

중 operational_errors를 참조하는 곳 전부 확인.

event_logs 기반으로 교체.

같은 종류의 stale table name이 더 있는지도
이번 수정 범위 안에서 바로 확인.

별도 audit 보고 금지.
발견 즉시 수정.

━━━━━━━━━━━━━━━━━━━━━━
3. SERVER TEST
━━━━━━━━━━━━━━━━━━━━━━

반드시 실제 endpoint 호출:

- Super Overview
- Pool List
- Pool Detail
- System Health

전부 200.

존재하지 않는 table 오류 0.

실제 WARNING/ERROR 집계가
event_logs 데이터와 일치하는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
4. RENDER DEPLOY
━━━━━━━━━━━━━━━━━━━━━━

현재 push commit:

61541d11

또는 수정 후 더 최신 commit

기준으로:

main push
→ Render Production deploy
→ LIVE 대기
→ /health commit 확인

PASS 조건:

Render LIVE commit == main HEAD

"deploy triggered"
"update_in_progress"

상태로 완료보고 금지.

━━━━━━━━━━━━━━━━━━━━━━
5. SWIMNOTE-WEB 실제 Production 배포
━━━━━━━━━━━━━━━━━━━━━━

가장 중요.

현재 swimnote-web 최신 코드가
https://swimnote.kr 에 실제 반영되지 않음.

이번에는:

build success만 보고 금지.
hasSuccessfulBuild만 보고 금지.

실제 Production publish 완료 필요.

현재 Replit deployment 구조를 확인해서
코드에서 자동 publish가 불가능하고
사용자 Publish 클릭이 실제로 필요한 경우:

그 시점에서만 정확히 사용자 액션 요청.

요청 내용은:

- 어느 artifact/project에서
- 어느 버튼을
- 어떤 deployment target으로
- 눌러야 하는지

정확히 한 번에 설명.

사용자 action 완료 후에는
다시 실제 Production 검증까지 계속.

━━━━━━━━━━━━━━━━━━━━━━
6. WEB PRODUCTION VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━

배포 후 반드시 실제:

https://swimnote.kr/super

확인.

검증:

- 최신 Super Admin UI 존재
- Overview 최신 KPI 존재
- Pool table 존재
- Pool detail/control dashboard 존재
- 공지 관리 존재
- 광고 관리 존재
- 최신 pricing 존재

HTTP header / asset hash / build marker / deployed commit 등
현재 환경에서 검증 가능한 방식으로
최신 코드가 실제 Production에 올라갔음을 증명.

단순 git SHA 보고만 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. 실제 WEB AUTH 검증
━━━━━━━━━━━━━━━━━━━━━━

Production에서:

비로그인
→ /super 접근 차단

일반 role
→ 차단

Super Admin
→ 접근 가능

실제 Production route 기준 확인.

━━━━━━━━━━━━━━━━━━━━━━
8. SUPER ADMIN 핵심 실제 SMOKE
━━━━━━━━━━━━━━━━━━━━━━

Production Web에서 최소:

A.
Overview

B.
Pool List

C.
Pool Detail

D.
Subscription/next renewal

E.
Curriculum

F.
AI/System Health

G.
공지 관리

H.
광고 관리

화면/API 정상.

특히 Health 화면에서
event_logs 기반 warning/error 정상.

━━━━━━━━━━━━━━━━━━━━━━
9. 허위완료 방지 규칙
━━━━━━━━━━━━━━━━━━━━━━

이번부터:

Git push = 완료 아님
Build success = 완료 아님
Deploy triggered = 완료 아님
hasSuccessfulBuild = 현재 배포 증거 아님

Production 실제 URL에
해당 commit 기능이 반영된 것까지 확인해야 완료.

확인 불가능한 항목은
PASS로 작성 금지.

반드시:

VERIFIED
NOT VERIFIED
BLOCKED

중 하나로 명시.

━━━━━━━━━━━━━━━━━━━━━━
10. 최종 완료보고
━━━━━━━━━━━━━━━━━━━━━━

1. operational_errors 참조 제거 위치
2. canonical event source
3. event_logs 실제 schema/level
4. Overview warning/error 결과
5. Pool Health 결과
6. 관련 API smoke
7. main HEAD
8. Render LIVE commit
9. swimnote-web 실제 배포 방법
10. 사용자 Publish action 필요 여부
11. 실제 Production URL
12. Production 최신 배포 검증 증거
13. /super auth 검증
14. Overview Production 결과
15. Pool List Production 결과
16. Pool Detail Production 결과
17. Notice Production 결과
18. Ads Production 결과
19. pricing Production 결과
20. 잔존 blocker

PASS 조건:

- nonexistent operational_errors query = 0
- event_logs 기반 health 정상
- Render LIVE == main HEAD
- 최신 swimnote-web 실제 Production 반영
- /super 실제 Production 작동
- build success가 아니라 실제 배포 확인
- blocker 0

완료 후 STOP.
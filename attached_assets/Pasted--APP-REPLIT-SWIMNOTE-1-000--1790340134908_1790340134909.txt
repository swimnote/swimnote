[보낼 곳: APP REPLIT]

[SWIMNOTE — 회원 엑셀 일괄등록 정책 회귀 원인 + 1,000명 대량등록 구조 전면 조사]

중요:
이번 단계는 조사만 합니다.

코드 수정 금지.
APP OTA 금지.
WEB 배포 금지.
API 배포 금지.
DB migration 금지.
Production 데이터 수정 금지.

현재 SWIMNOTE의
회원 엑셀 일괄등록 정책이
기존에 확정했던 운영방식과 달라졌습니다.

현재 실제 화면에는:

회원 엑셀 일괄등록

헤더:
이름*, 연락처, 생년(YYYY),
보호자 이름, 보호자 연락처,
반 이름, 메모

그리고:

- 이름만 필수처럼 표시
- 서버 검증
- 중복/반 이름 오류
- 오류 하나라도 있으면 등록 불가
- ALL OR NOTHING

등의 설명이 존재합니다.

하지만 기존에 확정한 제품 정책은
훨씬 느슨한 대량등록 방식이었습니다.

━━━━━━━━━━━━━━━━━━━━━━
0. 최종 제품 정책 재확정
━━━━━━━━━━━━━━━━━━━━━━

이번 조사 기준은 다음입니다.

회원 일괄등록의 필수값:

1. 이름
2. 연락처

딱 두 개.

나머지는 모두 선택값:

- 생년
- 보호자 이름
- 보호자 연락처
- 반 이름
- 메모
- 기타 선택 정보

동명이인:

허용.

같은 이름 때문에 등록 차단 금지.

전화번호는
학부모/회원 연결 및 자동승인에 활용되는
핵심 식별정보로 사용.

단,
현재 SWIMNOTE에서 실제 자동승인에 사용하는
canonical 전화번호 field가 무엇인지
반드시 코드로 확인하십시오.

예:

student.phone
contact
guardian_phone
parent phone
기타

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 왜 정책이 바뀌었는지 Git HISTORY 조사
━━━━━━━━━━━━━━━━━━━━━━

현재의 엑셀 등록 화면/API가
언제 어떤 변경으로 현재 정책이 됐는지 추적하십시오.

검색:

- 회원 엑셀 일괄등록
- 등록 양식
- ALL OR NOTHING
- 중복
- validate
- bulk
- excel
- xlsx
- csv
- import
- 회원 등록

git log
git blame
git log -S
git log -G

등을 필요한 범위에서 사용.

보고:

- 현재 구현이 들어온 commit SHA
- 날짜
- 변경 파일
- 이전 구현과의 차이
- 필수항목이 어떻게 바뀌었는지
- duplicate validation이 언제 들어왔는지
- 반 이름 검증이 언제 들어왔는지
- UI 설명이 언제 변경됐는지
- APP/WEB/API 중 어디에서 정책이 바뀌었는지

“아마 리팩터링하면서”
같은 추측 금지.

Git 증거로 설명하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 과거 ‘느슨한 등록 정책’ 흔적 확인
━━━━━━━━━━━━━━━━━━━━━━

Git history에서 과거에 다음 의미가
실제로 존재했는지 확인하십시오.

- 이름 + 연락처만 있으면 등록
- 동명이인 허용
- 선택정보 없어도 등록
- 반 미지정 허용
- 전화번호 기반 parent/member 연결
- 대량 import 중심

과거 코드/commit이 있으면:

- SHA
- 파일
- validation 조건

보고.

과거 코드가 확인되지 않으면
“확인하지 못함”이라고 보고.

사용자의 현재 정책을
과거 코드였다고 추측해서 보고 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. APP + WEB 실제 ENTRY POINT 전부 특정
━━━━━━━━━━━━━━━━━━━━━━

현재:

A. SWIMNOTE APP
B. swimnote.kr / Office / Admin WEB

에서 회원 엑셀 일괄등록을
실제로 수행하는 route/component를 찾으십시오.

각각 보고:

- 화면 파일
- 파일 선택 handler
- Excel parser
- preview
- validation 호출
- final import 호출
- API endpoint

중복 구현 여부도 확인.

특히:

APP와 WEB가
같은 API를 사용하는지

아니면
각자 별도 validation을 하는지

반드시 구분.

━━━━━━━━━━━━━━━━━━━━━━
4. 엑셀 양식 생성 위치 확인
━━━━━━━━━━━━━━━━━━━━━━

“등록 양식 다운로드” 파일이
어디에서 만들어지는지 확인.

확인:

- frontend에서 직접 생성
- 서버에서 다운로드
- static xlsx
- CSV template
- 기타

현재 헤더:

이름
연락처
생년
보호자 이름
보호자 연락처
반 이름
메모

각 컬럼이:

- required
- optional
- validation rule

어디에서 정의되는지 확인.

UI 문구와 실제 서버 정책이
다른지도 확인.

━━━━━━━━━━━━━━━━━━━━━━
5. 현재 REQUIRED VALIDATION 전부 정리
━━━━━━━━━━━━━━━━━━━━━━

현재 한 row가 등록되기 위해
실제로 통과해야 하는 조건을
APP/WEB/API/DB 순서로 전부 정리하십시오.

예:

- 이름 필수
- 연락처 필수/선택
- 전화번호 format
- 생년 format
- class existence
- 중복 이름
- 중복 전화
- guardian 필수
- 기타

실제 존재하는 것만 보고.

각 rule마다:

[APP]
[WEB]
[API]
[DB constraint]

중 어디에서 강제되는지도 표시.

━━━━━━━━━━━━━━━━━━━━━━
6. DUPLICATE 정책 전면 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 어떤 조건으로
“중복 회원”이라고 판단하는지 확인하십시오.

예:

- 이름
- 이름 + 생년
- 이름 + 전화번호
- 전화번호
- 보호자 전화번호
- 동일 반 + 동일 이름
- 기타

그리고 실제 DB에:

- students.name UNIQUE
- phone UNIQUE
- 기타 unique constraint

가 존재하는지 확인.

중요:

최종 제품 정책은
동명이인을 허용합니다.

따라서 이름만 같다는 이유로
등록 차단하면 안 됩니다.

이번 단계에서는 수정하지 말고
현재 차단 위치만 특정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 전화번호와 자동승인 구조 확인
━━━━━━━━━━━━━━━━━━━━━━

사용자의 기존 운영 정책:

회원/학부모가 APP 가입 또는 연결될 때
전화번호가 핵심 matching 정보로 사용되고
자동승인/자동연결이 가능해야 합니다.

현재 실제 구현을 추적하십시오.

Excel import에서 저장한 연락처가:

→ 어느 DB column에 저장되고
→ parent/student/account matching에서
→ 어느 API가 읽고
→ 어떤 normalization을 거쳐
→ 어떤 조건으로 자동승인되는지

끝까지 확인하십시오.

특히:

01012345678
010-1234-5678
+82 10...
공백 포함

같은 값이
동일 번호로 normalization되는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
8. 동일 전화번호 허용 여부
━━━━━━━━━━━━━━━━━━━━━━

이름 중복만 조사하지 말고
전화번호 중복도 확인하십시오.

어린이 수영장에서는
형제/자매가 동일 보호자 전화번호를
사용할 수 있습니다.

따라서 현재 시스템에서:

학생 A
학생 B

가 같은 parent/contact phone을
가질 수 있는지 확인.

엑셀 import가 이를
중복 오류로 막는지도 확인.

제품 의미를 임의 변경하지 말고
현재 parent linking 구조와 함께 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
9. 반 이름 선택값 처리 확인
━━━━━━━━━━━━━━━━━━━━━━

최종 정책에서는:

반 이름이 없어도
회원 자체 등록은 가능해야 합니다.

현재:

반 이름 없음
반 이름 오타
존재하지 않는 반

각 경우에 어떻게 동작하는지 확인.

특히 현재 설명의:

“중복·반 이름 오류”

가 회원 자체 등록까지 막는 구조인지 확인.

최종 설계 후보는 조사만:

A.
반 이름 공란
→ 미배정 회원으로 정상 등록

B.
반 이름 존재 + 정확히 일치
→ 해당 반 자동 배정

C.
반 이름 존재 + 찾을 수 없음
→ 해당 row 명확한 오류 표시

또는

회원은 등록하고 반 배정만 보류

중 현재 시스템에서
어떤 방식이 더 안전한지 보고.

아직 정책 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. 1,000명 FILE 실제 처리 한계 조사
━━━━━━━━━━━━━━━━━━━━━━

목표:

한 번에 회원 1,000명 업로드.

현재 시스템이
1,000 rows를 안정적으로 처리 가능한지 조사하십시오.

확인:

FRONTEND:

- xlsx/csv parser
- 파일 크기 제한
- row count 제한
- 메모리 사용
- preview 1,000행 렌더링 방식
- 브라우저/UI freeze 가능성

API:

- request body size limit
- JSON payload 크기
- endpoint timeout
- Render timeout 영향
- validation loop
- import loop
- transaction 범위

DB:

- 1,000회 개별 INSERT인지
- bulk INSERT인지
- transaction인지
- N+1 SELECT 존재 여부
- 반 조회를 row마다 하는지
- duplicate query를 row마다 하는지
- 1,000명에서 query 수가 몇 회가 되는지

측정 가능한 것은
실제 코드 기준으로 계산하십시오.

━━━━━━━━━━━━━━━━━━━━━━
11. 1,000명 등록의 목표 구조
━━━━━━━━━━━━━━━━━━━━━━

아직 구현하지 말고
현재 구조와 비교해
가장 안전한 최소 구조를 제안하십시오.

목표:

Excel 1회 upload

→ client parse

→ 전체 row 구조 검증

→ server validation

→ 전체 오류 반환

→ 오류 0건일 때만 transaction 시작

→ bulk insert

→ commit

즉 1,000명을
1명씩 API 1,000회 호출하는 방식은 금지.

가능하면:

1 request
+
set-based validation
+
bulk INSERT
+
1 transaction

방향을 검토.

현재 DB/ORM 구조상
실제로 가능한 수준만 제안.

━━━━━━━━━━━━━━━━━━━━━━
12. ALL OR NOTHING 정책 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 ALL OR NOTHING은
그 자체가 문제라는 뜻이 아닙니다.

사용자 요구:

1,000명 중 한 명이라도
실제 등록 불가 오류가 있으면

그냥:

“업로드 실패”

라고 끝내면 안 됩니다.

오류 row를 정확하게 보여주고
사용자가 파일을 수정하여
다시 업로드할 수 있어야 합니다.

따라서 현재:

- validation 단계
- insert 단계

중 어느 단계에서 오류가 발생하고

일부 데이터가 이미 저장된 뒤
실패할 가능성이 있는지 확인.

Atomic transaction 여부도 확인.

━━━━━━━━━━━━━━━━━━━━━━
13. ERROR REPORT 현재 구조 조사
━━━━━━━━━━━━━━━━━━━━━━

현재 실패 응답이:

- generic error
- first error only
- errors[]
- row index
- field
- reason

중 어떤 구조인지 확인.

APP와 WEB에서
서버 오류 detail을 버리고:

“업로드 실패”

하나로 치환하는 코드가 있는지도 확인.

예:

catch {
  alert("업로드 실패")
}

같은 구조 검색.

━━━━━━━━━━━━━━━━━━━━━━
14. 최종 ERROR UX 계약 제안
━━━━━━━━━━━━━━━━━━━━━━

아직 구현하지 말고
다음 UX가 가능한지 설계하십시오.

예:

985번째 행 — 김민수
연락처 없음
→ 연락처를 입력해주세요.

991번째 행 — 박지우
연락처 형식 오류
→ 010-1234-5678 형식으로 수정해주세요.

997번째 행 — 이서준
반 이름 "돌고래A"를 찾을 수 없음
→ 반 이름을 수정하거나 비워주세요.

상단:

총 1,000명
정상 997명
오류 3명

오류가 있으므로
아직 아무 회원도 등록하지 않았습니다.

파일을 수정한 뒤 다시 업로드하세요.

필요하면:

[오류 목록 다운로드]

CSV/XLSX 형태 제공 가능성도 조사.

이번 단계에서는 구현 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. 오류는 가능한 한 전부 한 번에 반환
━━━━━━━━━━━━━━━━━━━━━━

현재 validation이
첫 번째 오류에서 return하는지 확인.

최종 목표는:

1,000 rows 전체를 검사하고

가능하면 한 번에:

errors: [
  {
    row: 985,
    name: "김민수",
    field: "contact",
    code: "CONTACT_REQUIRED",
    message: "연락처가 없습니다."
  },
  ...
]

형태로 반환.

사용자가:

하나 수정
→ 다시 올림
→ 또 다른 오류 발견

을 반복하게 만들지 않는 것이 목표.

현재 구조에서 가능한지 보고.

━━━━━━━━━━━━━━━━━━━━━━
16. GENERIC 500 / 업로드 실패 원인 노출
━━━━━━━━━━━━━━━━━━━━━━

Excel 자체 오류와
시스템 장애를 구분해야 합니다.

예:

VALIDATION ERROR:
사용자가 고칠 수 있음.

SYSTEM ERROR:
서버/DB 장애.

현재 둘이
같은 “업로드 실패”로 표시되는지 확인.

최종 계약 제안:

VALIDATION_ERROR
→ row별 오류 표시

FILE_FORMAT_ERROR
→ 파일/시트/헤더 문제 표시

IMPORT_SYSTEM_ERROR
→ 시스템 오류로 별도 안내

NETWORK_ERROR
→ 재시도 안내

중 실제 API 구조에 맞게 제안.

━━━━━━━━━━━━━━━━━━━━━━
17. HEADER 정책 조사
━━━━━━━━━━━━━━━━━━━━━━

최종 필수 header는:

이름
연락처

두 개.

나머지는 선택.

현재 parser가:

- header 순서 고정인지
- 정확한 한글 문자열 일치가 필요한지
- 추가 컬럼을 허용하는지
- 없는 optional column을 허용하는지
- 빈 optional cell을 허용하는지

확인.

1,000명 실무 업로드를 위해
최대한 느슨하게 만들 수 있는 부분을 보고.

예:

컬럼 순서 자유
추가 컬럼 무시
optional header 없어도 허용

등.

아직 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. 빈 행 / 공백 / 엑셀 실무 데이터
━━━━━━━━━━━━━━━━━━━━━━

1,000명짜리 실제 Excel에는 흔히:

- 마지막 빈 행
- 중간 빈 행
- 이름 앞뒤 공백
- 전화번호 숫자 cell
- 전화번호 문자열 cell
- 하이픈
- Excel scientific notation
- 생년 숫자/string
- 빈 메모
- 숨은 row

등이 존재할 수 있습니다.

현재 parser가
어떻게 처리하는지 확인.

최종적으로
사용자가 일일이 정리하지 않아도 되는
“느슨한 normalization” 범위를 제안하십시오.

━━━━━━━━━━━━━━━━━━━━━━
19. 이름 + 연락처 최소 등록 시 DB 필수값 확인
━━━━━━━━━━━━━━━━━━━━━━

중요.

UI/API에서 느슨하게 해도
DB에서 다른 NOT NULL 값이 필요하면
실제 등록이 실패할 수 있습니다.

students 또는 관련 테이블의
INSERT 필수 컬럼을 확인.

“이름 + 연락처만 주어진 회원”을
현재 정상적인 default 값으로
생성할 수 있는지 확인.

필요한 server-generated field:

- id
- pool_id
- status
- created_at
- class_group_id null
- birth_year null
- memo null
- 기타

를 정리.

DB 변경 없이 가능한지 보고.

━━━━━━━━━━━━━━━━━━━━━━
20. APP / WEB 정책 단일화 여부
━━━━━━━━━━━━━━━━━━━━━━

이번 문제처럼
APP와 WEB에서 정책이 다시 달라지는 것을 막기 위해

validation의 canonical source가
어디여야 하는지 조사하십시오.

목표는:

APP
WEB

둘 다 UI에서는 preview만 하고

최종 validation rule은
동일 API/server contract를 사용.

예:

bulk-members/validate
bulk-members/import

또는 현재 endpoint를 재사용.

현재 구조에서
최소 변경으로 단일화할 방법을 제안.

━━━━━━━━━━━━━━━━━━━━━━
21. 이번 조사에서 절대 하지 말 것
━━━━━━━━━━━━━━━━━━━━━━

- 필수값 변경 금지
- duplicate check 삭제 금지
- Excel template 변경 금지
- APP UI 변경 금지
- WEB UI 변경 금지
- API 변경 금지
- DB 변경 금지
- 1,000명 Production 테스트 삽입 금지
- 실제 회원 데이터 수정 금지
- deploy 금지

조사만 합니다.

━━━━━━━━━━━━━━━━━━━━━━
22. 완료보고 형식
━━━━━━━━━━━━━━━━━━━━━━

[A. POLICY REGRESSION HISTORY]

- 원래 구현 확인 여부
- 현재 정책이 들어온 commit
- 변경된 validation
- APP/WEB/API 각각 변화

[B. CURRENT ENTRY POINTS]

APP:
- route
- component
- parser
- endpoint

WEB:
- route
- component
- parser
- endpoint

[C. CURRENT TEMPLATE]

- 생성 위치
- headers
- required/optional
- 현재 UI 설명

[D. CURRENT VALIDATION MATRIX]

컬럼별:

- APP
- WEB
- API
- DB

required/validation 조건 표

[E. DUPLICATE RULE]

- 이름
- 연락처
- 보호자 연락처
- 이름+반
- DB unique

현재 실제 차단 조건

[F. PHONE / AUTO APPROVAL]

Excel 연락처
→ DB
→ parent/member matching
→ 자동승인

전체 흐름

[G. CLASS]

- 반 공란
- 정상 반
- 존재하지 않는 반

현재 동작

[H. 1000 ROW CAPACITY]

- frontend
- API
- DB
- query count
- transaction
- timeout/body limit

실제 병목

[I. ERROR PIPELINE]

현재:

row error
→ API
→ APP/WEB

어디에서 상세 오류가 사라지는지

[J. ALL OR NOTHING]

- 현재 atomic 여부
- 일부 insert 후 실패 가능성
- 최종 권장 구조

[K. TARGET PERMISSIVE CONTRACT]

사용자 요구 기준:

필수:
이름
연락처

선택:
나머지

동명이인:
허용

기타 어떤 검증만 남겨야 하는지 제안

[L. ERROR UX CONTRACT]

1,000명 중 오류가 있을 때:

- row 번호
- 이름
- field
- reason
- 총 오류 수
- 파일 수정 후 재업로드

구조 제안

[M. MINIMUM FIX PLAN]

아직 실행하지 말고:

APP
WEB
API
DB
template
validation
error UI

각각 무엇을 최소 수정하면 되는지 정리.

[N. DEPLOY IMPACT]

수정 시 예상:

- APP OTA 필요 여부
- WEB deploy 필요 여부
- API Render deploy 필요 여부
- DB migration 필요 여부
- Native build 필요 여부

완료했다고만 보고하지 마십시오.

특히
“기존 정책이 왜 바뀌었는지”는
추측하지 말고 Git history/diff로 증명하십시오.

1,000명 처리 가능성도
“될 것 같다”가 아니라
현재 parser/API/query/transaction 구조를 기준으로
병목을 구체적으로 보고하십시오.

확인하지 못한 항목은
‘확인하지 못함’이라고 명시하고
추측해서 정상이라고 보고하지 마십시오.
[보낼 곳: APP REPLIT]

[SWIMNOTE — 관리자 수업일지 XMODE_NOT_ENTITLED 원인 분석 + 수정 계획 수립]

아직 코드 수정하지 마십시오.

사용자 실제 기기에서:

관리자
→ 수업관리
→ 수업일지

진입 시:

XMODE_NOT_ENTITLED

오류가 발생하고 있습니다.

제품 정책상:

수업일지 조회 자체는
일반 SWIMNOTE 기본 기능입니다.

X모드 미가입 관리자도
자기 수영장의 수업일지를
조회할 수 있어야 합니다.

이번 단계에서는:

1. 실제 원인 확인
2. 현재 권한 구조 확인
3. 어디를 어떻게 수정할지 계획 수립

까지만 진행하십시오.

코드 수정 금지.
배포 금지.
migration 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 실제 화면 SOURCE 확인
━━━━━━━━━━━━━━━━━━━━━━

스크린샷의:

관리자 → 수업일지

화면을 실제 렌더링하는
정확한 파일 경로를 확인하십시오.

화면 제목:

“수업일지”

및:

전체
AI
일반

필터와 검색 UI가 있는
실제 source를 찾으십시오.

파일명을 추측하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 실제 API 흐름 추적
━━━━━━━━━━━━━━━━━━━━━━

이 화면 진입 시 호출되는
실제 API를 확인하십시오.

흐름:

APP screen
→ API endpoint
→ route
→ middleware
→ service/query

까지 추적.

보고:

- endpoint
- HTTP method
- role
- 호출 순서
- 어떤 요청에서 XMODE_NOT_ENTITLED가 발생하는지

전체 diary API audit 금지.

이 화면 진입에 필요한 요청만 확인.

━━━━━━━━━━━━━━━━━━━━━━
3. XMODE_NOT_ENTITLED 발생 위치
━━━━━━━━━━━━━━━━━━━━━━

다음 중 실제 원인을 코드 근거로 특정하십시오.

A.
APP 자체의 X entitlement guard

B.
API route middleware

C.
공통 requireXMode / entitlement helper

D.
Diary service 내부 검사

E.
일반 SWIMNOTE 화면인데
X 전용 endpoint를 잘못 호출

F.
기타

정확한:

파일
함수
조건문

까지 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 현재 권한 구조 확인
━━━━━━━━━━━━━━━━━━━━━━

수업일지 READ에 현재 적용되는 권한을 분리해서 보고하십시오.

예:

- 인증
- role
- pool ownership
- teacher assignment
- student access
- diary visibility
- X entitlement

중 실제로 어떤 검사가 있는지.

특히:

pool_admin이 자기 pool 전체 일지를
어떤 기준으로 읽도록 설계되어 있는지 확인하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 제품 정책과 현재 코드 차이
━━━━━━━━━━━━━━━━━━━━━━

최종 제품 정책은 다음입니다.

[기본 SWIMNOTE 기능]

X모드와 무관하게:

pool_admin
→ 자기 수영장 수업일지 조회 가능

teacher
→ 기존 담당범위 조회

parent
→ 자기 자녀 공개 일지 조회

가능해야 합니다.

다음 READ 기능은
X entitlement 대상이 아닙니다.

- 수업일지 목록
- 수업일지 상세
- 날짜 조회
- 검색
- 전체/AI/일반 필터
- 학생 노트 조회
- 이미 저장된 AI 일지 조회

단:

AI 일지 “생성”
X 전용 curriculum
X 전용 AI 기능

등은 별개입니다.

“AI로 만들어진 일지”
라는 이유만으로
읽기까지 X로 잠그면 안 됩니다.

현재 코드가 이 정책과
어디서 충돌하는지 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 영향범위 확인
━━━━━━━━━━━━━━━━━━━━━━

수정 예정 guard가
다른 기능에서도 재사용되는지 확인하십시오.

예:

requireXMode를
수업일지뿐 아니라

- AI 생성
- X curriculum
- X report
- 기타 X 기능

에서도 공유한다면

helper 자체를 통째로 완화하는 방식은 금지 예정입니다.

수업일지 READ만
정확히 분리할 수 있는 최소 수정 위치를 찾으십시오.

즉:

X guard 제거
≠
전체 X entitlement 완화

입니다.

━━━━━━━━━━━━━━━━━━━━━━
7. AI 필터 의미 확인
━━━━━━━━━━━━━━━━━━━━━━

화면의:

전체
AI
일반

필터에서

“AI”

가 무엇을 의미하는지 확인하십시오.

의도:

AI로 생성되어 이미 저장된 일지 조회.

이 필터를 선택하는 것 자체가
새 AI 생성 요청인지,
단순 저장 데이터 filtering인지
실제 코드로 확인.

단순 filtering이면
X entitlement를 요구하면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
8. RAW ERROR 노출 원인
━━━━━━━━━━━━━━━━━━━━━━

현재 사용자 화면에:

XMODE_NOT_ENTITLED

가 그대로 표시됩니다.

이 내부 error code가
어떤 error handling 경로를 통해
그대로 노출되는지도 확인하십시오.

이번 계획에는:

A.
잘못된 X 차단 제거

가 우선이며,

B.
향후 실제 API 오류 시
내부 code를 사용자에게 그대로 보여주지 않도록
기존 error mapping을 재사용

할 수 있는지 포함하십시오.

단 이번 단계에서 수정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
9. 수정 계획 수립
━━━━━━━━━━━━━━━━━━━━━━

실제 원인 확인 후
가장 작은 수정계획을 작성하십시오.

계획에는 반드시:

1.
수정할 파일

2.
수정할 route/function

3.
제거/변경할 X entitlement 조건

4.
반드시 유지할:
- auth
- role
- pool access
- teacher/student access
- diary visibility

5.
AI 생성 등 X 기능에는
entitlement를 그대로 유지하는 방법

6.
APP 수정이 필요한지
API만 수정하면 되는지

7.
배포 대상:
- API
- APP OTA
중 무엇인지

를 포함.

아직 실행 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. 대표 QA 계획
━━━━━━━━━━━━━━━━━━━━━━

구현 후 검증할 QA도 계획으로 제시하십시오.

최소:

A.
X 미가입 pool_admin
→ 수업일지 목록 조회 가능

B.
전체 / AI / 일반 필터
→ 모두 조회 가능

C.
X 가입 pool_admin
→ 기존 정상 유지

D.
teacher
→ 기존 담당범위 유지

E.
parent
→ 기존 자녀 범위 유지

F.
X 전용 AI 생성 기능
→ 비가입자는 기존대로 차단

G.
타 pool 데이터
→ 접근 불가 유지

H.
내부 XMODE_NOT_ENTITLED
→ 정상 일지 조회 화면에서 더 이상 노출되지 않음

━━━━━━━━━━━━━━━━━━━━━━
11. 금지
━━━━━━━━━━━━━━━━━━━━━━

이번 단계에서는 절대 하지 마십시오.

- 코드 수정
- requireXMode 제거
- API 변경
- APP 수정
- DB 변경
- migration
- 배포
- OTA
- 전체 diary refactor
- 전체 X 권한 audit

원인 확인 + 수정 계획만 제출.

━━━━━━━━━━━━━━━━━━━━━━
12. 최종 보고 형식
━━━━━━━━━━━━━━━━━━━━━━

[A. ACTUAL SCREEN]

- 실제 화면 파일

[B. API FLOW]

- 호출 endpoint
- route
- middleware/service 흐름

[C. ROOT CAUSE]

- XMODE_NOT_ENTITLED 발생 파일
- 함수/guard
- 실제 조건
- 왜 관리자 기본 일지 조회까지 막히는지

[D. CURRENT ACCESS]

현재 diary READ의:
- auth
- role
- pool
- teacher/student
- X entitlement

각 검사 위치

[E. X BOUNDARY]

수업일지 READ와
X 전용 기능의 현재 경계

[F. IMPACT]

해당 X guard를 수정할 경우
영향받을 수 있는 다른 기능

[G. PROPOSED FIX]

최소 수정계획:
- 수정 파일
- 수정 위치
- 변경할 조건
- 유지할 권한검사
- APP/API 배포 필요 여부

[H. QA PLAN]

A~H 검증방법

[I. RISK]

이번 수정으로
잘못 풀릴 수 있는 권한이 있는지

이번 단계는 조사와 계획만입니다.

코드 수정하지 마십시오.
배포하지 마십시오.

확인하지 못한 항목은
‘확인하지 못함’이라고 명시하십시오.
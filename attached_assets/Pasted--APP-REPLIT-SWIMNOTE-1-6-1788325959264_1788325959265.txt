[보낼 곳: APP REPLIT]

# ============================================================
# SWIMNOTE 1.6.3 / 2.0
# PRODUCTION API DIAGNOSTIC DEPLOY ONLY
#
# APPROVED:
# SERVER-SIDE LOGIN DIAGNOSTICS DEPLOY
#
# SOURCE COMMIT:
# 5a2d3dba
#
# IMPORTANT:
# CLIENT OTA IS NOT APPROVED IN THIS TASK
# ============================================================

Repository:
swimnote/swimnote

Branch:
release/v2.0.0

Approved source commit:
5a2d3dba

Target production service:
swimnote-api
(Render Production API)

이번 작업은
로그인 장애 원인 추적을 위한
SERVER-SIDE 진단코드만 Production에 반영하는 승인 작업입니다.

============================================================
1. 승인 범위
============================================================

승인됨:

- api-server 변경 배포
- [SERVER_BOOT] 로그
- [AUTH_TRACE] 로그
- [PROCESS_ERROR] 로그
- [SERVER_HEALTH] / keep-alive 진단 로그
- /api/healthz additive metadata
  - boot_id
  - uptime_seconds
  - commit/version if available

승인 안 됨:

- 1.6.3 OTA
- 2.0 OTA
- iOS OTA
- Android OTA
- 앱 Build
- App Store
- Google Play
- RevenueCat 외부 설정
- Pricing 변경
- Subscription 추가작업
- DB migration
- Paid Insight migration
- 일반 앱 기능 수정

============================================================
2. 배포 전 PRE-FLIGHT
============================================================

실제 배포 전에 반드시 확인:

Repository:
swimnote/swimnote

Branch:
release/v2.0.0

Source commit contains diagnostics:
5a2d3dba

확인:

git status:
clean

현재 source에 다음 server-side 변경이 존재:

- boot_id 생성
- SERVER_BOOT log
- AUTH_TRACE
- PROCESS_ERROR
- healthz additive metadata
- 기존 login response schema 불변

Client SessionContext 변경은
이번 Production server 배포 대상과 분리.

============================================================
3. DEPLOY SOURCE 주의
============================================================

현재 Render Production service가
어느 branch/revision을 실제 배포하는지 먼저 확인.

과거 production server branch가
deploy-photo-clone 등 별도 branch라면

release/v2.0.0 전체를
그대로 Production branch로 교체하지 마십시오.

반드시:

현재 Production server HEAD
→ 로그인 diagnostics server 변경만
최소 backport/cherry-pick

형태로 반영.

일반 2.0 앱/서버 미배포 기능이
Production에 섞이면 FAIL.

============================================================
4. SERVER-ONLY CHANGESET 추출
============================================================

commit 5a2d3dba에서
이번 배포에 필요한 server-side 파일만 식별.

포함 가능:

artifacts/api-server/...
server logging/health/auth diagnostics 관련 파일

제외:

artifacts/swim-app/context/auth/SessionContext.tsx
및 기타 client-side 변경

이번 배포는 Server only.

============================================================
5. CLIENT 변경 절대 배포 금지
============================================================

SessionContext.tsx의:

login_request_id
client AUTH_TRACE category
POST_LOGIN_ERROR 분리

등은 이번 작업에서 OTA하지 않습니다.

Production app binary/update:
변경 없음.

============================================================
6. PRODUCTION BRANCH BACKPORT
============================================================

현재 Render production branch를 확인한 뒤:

A.
Production branch에 이미 diagnostics server code가 있으면
중복 적용 금지.

B.
없으면
5a2d3dba의 SERVER 부분만
최소 backport.

새 commit 생성 시:

명확한 message 사용.

예:

fix(prod): add login availability diagnostics

정확한 SHA 보고.

============================================================
7. 배포 전 SECURITY 확인
============================================================

로그에 절대 포함 금지:

- identifier
- email
- phone
- password
- JWT
- refresh token
- full request body
- user name
- pool name
- parent/student PII

AUTH_TRACE 허용:

request_id
event
route
status
elapsed_ms
boot_id
safe error class/code

============================================================
8. HEALTHZ BACKWARD COMPATIBILITY
============================================================

기존 /api/healthz 응답 필드는 유지.

새 metadata는 additive only.

1.6.3 old parser:
영향 0.

Response key 삭제/rename:
NO.

============================================================
9. 배포
============================================================

위 검증 통과 후
swimnote-api Production Render service에
SERVER diagnostics 변경만 배포.

DB migration:
NO

Schema mutation:
NO

============================================================
10. 배포 직후 HEALTH CHECK
============================================================

배포 완료 후 확인:

GET /api/healthz

Expected:
HTTP 200

확인 필드:

기존 health fields
+
boot_id
+
uptime_seconds
+
commit/service_version if available

boot_id:
non-empty

uptime:
배포 직후 작은 값부터 증가

============================================================
11. LOGIN ROUTE SAFE CHECK
============================================================

실제 사용자 credential 사용 금지.

safe invalid credential/request로:

POST /api/auth/unified-login

확인:

- 서버 도달
- 정상 JSON auth error
- 5xx 아님
- AUTH_TRACE received
- AUTH_TRACE response
- 동일 request_id 가능하면 correlation
- response body contract 기존 유지

Expected:
401/정상 canonical auth error

500:
FAIL

timeout:
FAIL

============================================================
12. SERVER BOOT LOG 확인
============================================================

Render logs에서:

[SERVER_BOOT]

1회 이상 확인.

기록 확인:

boot_id
started_at
commit/version if available

같은 process에서 반복 SERVER_BOOT:
비정상.

============================================================
13. AUTH TRACE 확인
============================================================

safe login test 후 Render logs에서:

[AUTH_TRACE]
event=received

+
[AUTH_TRACE]
event=response

확인.

elapsed_ms 포함.

PII 없음 확인.

============================================================
14. PROCESS ERROR 확인
============================================================

정상 배포 직후:

[PROCESS_ERROR]

반복 발생:
FAIL.

unhandled rejection / uncaught exception이
반복되면 정상 배포로 간주하지 말 것.

============================================================
15. KEEP-ALIVE
============================================================

기존 localhost self-ping 유지.

배포 때문에 동작 변경 금지.

가능하면 logs에서
health ping 실패가 즉시 반복되지 않는지 확인.

============================================================
16. ROLLBACK 조건
============================================================

다음 중 하나면 즉시 rollback:

- /api/healthz 5xx
- server boot loop
- login route 5xx
- 기존 login response contract 깨짐
- auth middleware crash
- PROCESS_ERROR 반복
- PII가 logs에 노출
- unrelated production regression

Rollback target:

배포 직전 Production server SHA

정확한 SHA를 배포 전에 기록.

============================================================
17. 1.6.3 실제 로그인
============================================================

서버 배포 정상 확인 후
사용자가 직접 1.6.3 앱에서 로그인 확인할 예정.

Replit이 실제 사용자 credential로 로그인하지 마십시오.

이번 작업에서
1.6.3 OTA:
NO.

============================================================
18. REGRESSION
============================================================

Production 배포 전
server diagnostics 관련 tests 확인.

기존 stable regression을
불필요하게 전부 반복할 필요는 없지만

변경 파일 관련 test는 반드시 PASS.

가능하면 현재 same-command 결과도 기록.

New failures:
0.

============================================================
19. STAGE 8 상태
============================================================

Stage 8 code는 유지.

외부 Store / RevenueCat 설정:
이번 작업에서 진행 금지.

Login diagnostic deploy 완료 후
다시 Stage 8로 복귀.

============================================================
20. 완료보고
============================================================

# SWIMNOTE PRODUCTION LOGIN DIAGNOSTIC DEPLOY REPORT

Repository:
Production branch:
Production service:

Source diagnostic commit:
5a2d3dba

## PRE-DEPLOY

Production HEAD Before:
...

Working tree:
...

Server-only backport:
YES / NO

Client files included:
YES / NO

Expected:
NO

## DEPLOY COMMIT

Production HEAD After:
...

Files changed:
...

## HEALTH

/api/healthz:
...

HTTP:
...

boot_id:
...

uptime_seconds:
...

commit/version:
...

## LOGIN ROUTE

/api/auth/unified-login safe test:
...

Status:
...

5xx:
YES / NO

Expected:
NO

## LOGS

SERVER_BOOT:
FOUND / NOT FOUND

AUTH_TRACE received:
FOUND / NOT FOUND

AUTH_TRACE response:
FOUND / NOT FOUND

PROCESS_ERROR:
NONE / ...

PII exposure:
YES / NO

Expected:
NO

## KEEP-ALIVE

Immediate failures:
...

## ROLLBACK

Previous Production SHA:
...

Rollback needed:
YES / NO

If YES:
reason:
...

## DATABASE

Migration executed:
NO

Expected:
NO

## CLIENT

1.6.3 OTA:
NO

2.0 OTA:
NO

Expected:
NO / NO

## STAGE 8

External config:
UNCHANGED / NOT TOUCHED

## FINAL

PRODUCTION LOGIN DIAGNOSTICS DEPLOYED — HEALTHY

or

ROLLED BACK

or

BLOCKED
[앱]

UNWRITTEN-SLOTS PRODUCTION TRACE
MANUAL DEPLOY + LIVE LOG CAPTURE

현재 상태:

RENDER LIVE SHA:
9e98b41a

NEW PUSHED SHA:
a9e0e763

중요:

a9e0e763에 포함된
curriculum route poolId null guard 3곳은
다른 작업에서 의도적으로 반영한 정상 변경이다.

이번 작업과 직접 관련 없더라도
REVERT 하지 않는다.

그대로 포함한 상태로
a9e0e763 전체를 배포 대상으로 사용한다.

==================================================
1. DEPLOY TARGET
==================================================

최종 배포 SHA:

a9e0e763

swimnote-api
Render Manual Deploy

사용자가 직접 수행.

AI Engine Render 건드리지 않음.

배포 후 확인:

STATUS = Live
DEPLOYED_SHA = a9e0e763

==================================================
2. DO NOT MODIFY CODE YET
==================================================

현재 추가된:

[unwritten-slots]

stage diagnostic logging 유지.

이번 Step에서는
추가 수정/리팩터링 금지.

먼저 production에서
실제 실패 stage와 exception만 확정한다.

==================================================
3. LIVE DEVICE TRACE — HOME
==================================================

Render가 Live 된 뒤
실제 로그인된 iPhone에서:

HOME
→ 일지작성
→ 작성할 수업 선택

딱 1회 실행.

Render logs에서
해당 요청의:

[unwritten-slots]

로그 블록 확인.

기록:

URL
includeWritten
HTTP status
stage
teacher_id 존재 여부
pool_id 존재 여부
class_group_count
slot_count
error_name
error_message
stack top

개인정보/토큰/secret 출력 금지.

==================================================
4. LIVE DEVICE TRACE — DIARY SELECTOR
==================================================

같은 production 상태에서:

DIARY
→ 상단 수업명 탭
→ 수업 변경

딱 1회 실행.

대상 요청:

GET /api/diaries/unwritten-slots?includeWritten=true

동일하게 확인:

HTTP status
stage
teacher_id 존재 여부
pool_id 존재 여부
class_group_count
slot_count
error_name
error_message
stack top

==================================================
5. ROOT CAUSE CLASSIFICATION
==================================================

두 요청의 실제 로그를 기준으로
원인을 확정.

다음 중 어디인지 분류:

- DB query failure
- production schema mismatch
- teacher/pool resolution failure
- schedule_days parse failure
- date normalization failure
- missing column
- response serialization failure
- includeWritten-specific failure
- 기타

추측 금지.

두 selector가
같은 root cause인지
서로 다른 root cause인지도 명시.

==================================================
6. IMPORTANT
==================================================

로그 확인 후에도
이번 Step에서 바로 수정하지 않는다.

먼저 정확한 원인만 보고.

새 endpoint 생성 금지.
frontend fallback 추가 금지.
빈 배열 반환으로 오류 숨기기 금지.
try/catch로 500 삼키기 금지.

==================================================
7. CURRICULUM CHANGE
==================================================

curriculum route poolId null guard 3곳:

KEEP

이번 작업에서 revert 금지.

이 변경은
a9e0e763 배포에 그대로 포함.

==================================================
8. FINAL REPORT
==================================================

DEPLOYED_SHA:

RENDER_STATUS:
Live / Fail

HOME_REQUEST:
URL:
STATUS:
STAGE:
ERROR_NAME:
ERROR_MESSAGE:

DIARY_SELECTOR_REQUEST:
URL:
STATUS:
STAGE:
ERROR_NAME:
ERROR_MESSAGE:

ROOT_CAUSE:

COMMON_ROOT_CAUSE:
YES / NO

CURRICULUM_NULL_GUARD:
KEPT

CODE_FIX_DONE:
NO

NEXT_FIX_SCOPE:
<정확히 수정해야 할 함수/쿼리/라인>

완료 후 STOP.
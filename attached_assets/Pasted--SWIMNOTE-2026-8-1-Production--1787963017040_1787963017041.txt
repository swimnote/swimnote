[SWIMNOTE — 토이키즈스윔클럽 서태웅 2026년 8월 성장리포트 1건 단일 발행]

목표:
Production 공개버전에서 전체 성장리포트 발급은 절대 실행하지 않고,
"토이키즈스윔클럽" 소속 "서태웅" 학생 1명에게만
2026년 8월 성장리포트 1건을 생성한다.

중요:
- 전체 발급 금지
- monthly batch/cron 실행 금지
- 다른 학생 report 생성 금지
- 다른 학생 notification/push 발송 금지
- bulk insert 금지
- 기존 단일학생 생성 경로만 사용
- 코드 수정 금지
- 대상이 1명으로 확정되지 않으면 즉시 STOP

==================================================
1. 대상 학생 특정
==================================================

Production DB에서 다음 조건으로 조회:

pool_name = "토이키즈스윔클럽"
student_name = "서태웅"

반드시 1명으로 특정한다.

확정 후 보고:
- student_id
- student_name
- pool_id
- pool_name
- class_id
- class_name
- 현재 재원 상태

같은 수영장 안에서도 2명 이상이면 임의 선택 금지하고 STOP.

==================================================
2. 발급 기간
==================================================

리포트 대상:
2026년 8월

기간:
2026-08-01 ~ 2026-08-31

해당 기간의 실제 수업/일지/성장 데이터만 사용한다.

==================================================
3. 생성 전 데이터 확인
==================================================

서태웅 1명 기준으로 확인:

- 8월 수업 데이터 존재
- 8월 일지 데이터 존재
- Growth Report 생성에 필요한 source/evidence 존재
- 이미 동일 기간 리포트가 발급되어 있는지 확인

이미 2026년 8월 리포트가 존재하면 중복 생성 금지하고 STOP.

==================================================
4. 단일학생 생성 경로 사용
==================================================

기존 Growth Report 시스템에서
student_id 1명만 대상으로 생성 가능한
service / endpoint / internal function을 사용한다.

금지:
- eligible students 전체 조회
- 전체 학생 loop
- 전체 재등록자 조회 후 필터
- monthly bulk issue
- 전체 report generation job

target count는 반드시 1.

==================================================
5. 성장리포트 생성
==================================================

토이키즈스윔클럽 서태웅 1명에 대해서만
현재 Production Growth Report pipeline을 그대로 실행한다.

가능하면 실제 운영 발급과 동일한 pipeline 사용:

8월 수업/일지/성장 데이터
→ 근거/evidence 추출
→ Curriculum Grounding
→ Growth Report 분석/구성
→ 저장
→ 앱 조회 가능 상태

==================================================
6. 알림 / Push
==================================================

이번 목적은 샘플 확인이므로
push notification은 발송하지 않는다.

현재 구조상 report 저장과 동시에 push가 강제된다면
실행하지 말고 STOP 후 보고.

다른 학생 notification 생성은 절대 금지.

==================================================
7. 발급 후 확인
==================================================

반드시 확인:

- 생성 report count = 1
- 대상 student_id = 서태웅
- pool = 토이키즈스윔클럽
- report period = 2026-08
- 다른 학생 신규 report count = 0
- report_id
- generation status
- 앱에서 조회 가능한지
- report 내용 생성 정상 여부
- Curriculum Grounding 정상 여부

==================================================
8. 삭제 기능 확인
==================================================

기존 Growth Report 삭제 기능으로
이번에 생성된 report_id를 삭제할 수 있는지 확인만 한다.

사용자 지시 전에는 삭제하지 않는다.

==================================================
9. 완료 보고
==================================================

TARGET
- student_id
- pool_id
- class_id

SOURCE DATA
- 8월 수업/일지 데이터 건수

REPORT
- report_id
- period
- status
- generation result

SAFETY
- target count
- 다른 학생 생성 count
- push 발송 여부

VIEW
- 앱 조회 가능 여부

DELETE
- 삭제 가능 여부

CODE
- 코드 수정 없음

DEPLOY
- Render 변경 없음
- OTA 변경 없음

서태웅 1건 생성 후 STOP.
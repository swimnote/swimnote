SWIMNOTE X — WP5 테스트 대상 및 계정 최종 확정 조사 지시

운영 DB 재검증 결과를 승인합니다.

확정 사항:

운영 DB: SUPABASE_DATABASE_URL의 Supabase PostgreSQL
WP1 Migration: 이미 적용 완료
xmode 컬럼 5개: 존재
X 관련 테이블: 존재
현재 X 활성 수영장: 0개
POOL_B 후보:
pool_1784310621737_qryl1x79s
수영장명: 샘플수영장
학생 0명 / 학부모 0명 / 일지 0건
판정: SAFE_TEST_POOL_CONFIRMED

이번 단계에서는 POOL_A와 테스트 계정만 확정합니다.

아직 DB 수정·계정 생성·X 상태 변경은 금지합니다.

━━━━━━━━━━━━━━━━━━━━━━

1. 절대 금지
    ━━━━━━━━━━━━━━━━━━━━━━

이번 조사 중 금지:

* 수영장 생성
* 사용자 생성
* 학생 생성
* 학부모 계정 생성
* 비밀번호 변경
* X 상태 PATCH
* DB INSERT·UPDATE·DELETE
* Migration
* 서버 재시작
* Render 배포
* 앱 코드 수정
* Commit
* Push
* OTA
* 실제 로그인 시도
* SMS 발송
* 토이키즈 데이터 접근·변경

운영 DB 읽기 전용 조회와 Repository 조사만 수행하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. POOL_B 최종 기준
━━━━━━━━━━━━━━━━━━━━━━

POOL_B 후보:

pool_id:
pool_1784310621737_qryl1x79s
수영장명:
샘플수영장

다음을 읽기 전용으로 다시 확인하십시오.

* swimming_pools 현재 X 상태
* 관리자 계정 수
* teacher 계정 수
* parent_account 수
* 학생 수
* 반 수
* 출결 수
* 일지 수
* 사진 수
* 영상 수
* 결제 기록 수
* 구독 상태
* 최근 활동 시각
* 실제 고객 데이터 존재 여부
* 개인정보 필드가 채워진 레코드 존재 여부

개인정보 값은 출력하지 말고 다음으로만 보고하십시오.

개인정보 레코드 없음
개인정보 필드 일부 존재
실제 고객 데이터 의심

POOL_B 최종 판정:

SAFE_TEST_POOL_CONFIRMED
UNSAFE_FOR_TEST
NEEDS_HUMAN_CONFIRMATION

━━━━━━━━━━━━━━━━━━━━━━
3. POOL_A 후보 조사
━━━━━━━━━━━━━━━━━━━━━━

POOL_A는 반드시 다음 조건을 만족해야 합니다.

xmode_entitlement = false
xmode_config_status = NOT_CONFIGURED
실제 운영 고객이 아님
테스트용 계정 존재
관리자·선생님·학부모 화면을 확인할 수 있음
개인정보 위험이 낮음
기존 일반모드 데이터가 최소한 존재

후보를 다음 순서로 조사하십시오.

후보 1

pool_1775118427405_xs80lcdmo
스윔노트 데모 수영장

후보 2

pool_1784865333802_mi7k4fsa4
스윔노트

울산 수영장과 토이키즈스윔클럽은 후보에서 제외하십시오.

각 후보별로 다음 건수만 제출하십시오.

* users
* pool_admin
* teacher
* parent_account
* students
* class_groups
* attendance
* diary
* photos
* videos
* payment 또는 결제 관련 기록
* 최근 로그인 또는 최근 활동
* 실제 전화번호 형식이 저장된 계정 수
* 실명으로 보이는 학생 이름 레코드 수

전화번호·이름·비밀번호 원문은 출력하지 마십시오.

각 후보를 다음 중 하나로 판정하십시오.

SAFE_TEST_POOL_CONFIRMED
POSSIBLE_REAL_DATA
UNSAFE_FOR_TEST
NEEDS_HUMAN_CONFIRMATION

━━━━━━━━━━━━━━━━━━━━━━
4. POOL_A 선택 원칙
━━━━━━━━━━━━━━━━━━━━━━

우선순위:

1. 이미 테스트 목적으로 만들어진 데모 수영장
2. 관리자·선생님·학부모 계정이 모두 존재
3. 실제 운영 결제 기록 없음
4. 개인정보가 없거나 명백한 테스트 데이터
5. 일반모드 기능을 확인할 최소 데이터 존재

POOL_A가 안전하게 확정되지 않으면 임의로 선택하지 마십시오.

그 경우:

SAFE_POOL_A_NOT_FOUND

로 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 기존 로그인 계정 구조 조사
━━━━━━━━━━━━━━━━━━━━━━

POOL_A와 POOL_B에서 역할별 계정 상태를 조사하십시오.

필요 역할:

pool_admin
teacher
parent_account

각 계정별 제출 항목:

* 내부 account ID
* role
* swimming_pool_id
* 활성 여부
* 로그인 방식
* 비밀번호 로그인 지원 여부
* SMS 인증 필요 여부
* 테스트용 계정으로 판단 가능한지
* 실제 사용자 계정으로 의심되는지

금지:

* username
* 전화번호
* 이메일
* 실명
* 비밀번호
* 인증번호

는 출력하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 계정 생성 필요성 최소화
━━━━━━━━━━━━━━━━━━━━━━

POOL_B에는 현재 pool_admin 1개만 존재한다고 보고됐습니다.

다음 대안을 비교하십시오.

방식 A

POOL_B에 teacher와 parent_account만 추가 생성

방식 B

기존 안전한 테스트 계정을 POOL_B로 이동

방식 C

pool_admin 역할만으로 서버·관리자 UI를 먼저 검증하고,
teacher·parent 검증은 다음 단계로 분리

방식 D

실제 DB 계정 없이 Mock 또는 Preview 환경에서 teacher·parent UI 검증

각 방식에 대해:

* DB 변경량
* 기존 데이터 영향
* 계정 이동 위험
* 로그인 가능성
* 개인정보 위험
* Rollback
* WP5 목표 충족도
* 예상 작업량

을 비교하십시오.

기존 계정을 다른 pool로 이동하는 방식은 기본적으로 권장하지 않습니다.

가장 안전하고 수정량이 적은 방식을 제안하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 인증 방식 Repository 조사
━━━━━━━━━━━━━━━━━━━━━━

다음 역할의 실제 로그인 Route와 인증 방식을 조사하십시오.

* pool_admin
* teacher
* parent_account

확인 항목:

* 로그인 API
* 필수 Body
* 비밀번호 기반 여부
* 전화번호 인증 여부
* 초대코드 필요 여부
* 계정 생성 Route
* 테스트 계정 생성에 사용할 수 있는 공식 Route
* DB 직접 INSERT가 필요한지
* 비밀번호 해시 생성 방식
* parent_account가 자녀 연결 없이 로그인 가능한지
* teacher가 반 배정 없이 로그인 가능한지

Repository에서 확인되지 않은 내용은 추정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. 실제 로그인 실행 전 검증 계획
━━━━━━━━━━━━━━━━━━━━━━

아직 로그인하지 말고 계획만 작성하십시오.

각 역할별:

계정 확인
→ 인증정보 별도 확보
→ 로그인 API 호출
→ 토큰의 role 확인
→ 토큰의 pool 귀속 확인
→ GET /pools/x-mode
→ 로그아웃 또는 토큰 폐기

토큰·비밀번호는 최종 보고서에 출력하지 않는 방식으로 설계하십시오.

━━━━━━━━━━━━━━━━━━━━━━
9. POOL_A·POOL_B 최종 구조 제안
━━━━━━━━━━━━━━━━━━━━━━

다음 표를 완성하십시오.

구분	pool_id	판정	관리자	선생님	학부모	데이터 상태	사용 가능
POOL_A normal							
POOL_B X sample	pool_1784310621737_qryl1x79s						

아직 X 상태를 변경하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. WP5 단계 재정리
━━━━━━━━━━━━━━━━━━━━━━

다음 순서로 정정하십시오.

WP5-A
POOL_A·POOL_B 및 계정 안전성 확정
WP5-B
읽기 전용 DB 기준선 캡처
WP5-C
필요한 테스트 계정 최소 생성
별도 사용자 승인 필수
WP5-D
POOL_B X 상태 PATCH
별도 사용자 승인 필수
WP5-E
Mode API 검증
WP5-F
개발 또는 실기기 앱 검증
WP5-G
계정·수영장 전환 격리 검증
WP5-H
일반모드 핵심 Smoke Test
WP5-I
DB·Audit 사후 검증
WP5-J
POOL_B 유지 또는 원복
별도 사용자 승인

Migration 단계는 제거하십시오.

━━━━━━━━━━━━━━━━━━━━━━
11. 필수 정정 사항
━━━━━━━━━━━━━━━━━━━━━━

향후 모든 문서에서 다음을 고정하십시오.

올바른 PATCH Body:

{
  "xmode_entitlement": true,
  "xmode_config_status": "READY",
  "reason": "WP5 sample activation"
}

날짜 필드는 변경하지 않을 경우 Body에서 생략하십시오.

Audit 조회:

WHERE entity_type = 'swimming_pool_xmode'
AND entity_id = '<POOL_B_ID>'

템플릿 조회:

WHERE scope = 'x_global'

운영 DB 조회는 반드시:

superAdminDb
SUPABASE_DATABASE_URL

기준으로 수행하십시오.

Replit executeSql의 heliumdb 결과를 운영 DB 증거로 사용하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
12. 제출 형식
━━━━━━━━━━━━━━━━━━━━━━

다음 목차로 제출하십시오.

1. POOL_B 재검증
2. POOL_A 후보별 데이터 건수
3. POOL_A 후보별 개인정보 위험
4. POOL_A 최종 판정
5. 역할별 기존 테스트 계정
6. 로그인 방식
7. 계정 생성 공식 경로
8. teacher·parent 추가 필요 여부
9. 계정 구성 대안 비교
10. 최종 POOL_A·POOL_B 구조
11. DB 변경 최소안
12. Rollback
13. WP5 정정 실행 단계
14. 남은 차단사항
15. 실행 승인 가능 여부

제출 후 중단하십시오.

코드 수정·DB 변경·계정 생성·X 상태 변경·로그인·Commit·Push·OTA는 아직 금지합니다.
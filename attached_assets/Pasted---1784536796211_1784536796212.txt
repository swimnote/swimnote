수정을 시작하기 전에 아래 항목을 먼저 모두 확인하고 보고하세요.

코드를 수정하기 전 최종 설계 검증 단계입니다.

==================================================
1. today-schedule.ts
==================================================

현재 studentCountMap은 students.status 기반으로 계산되고 있습니다.

학생 상세 목록과 동일한 기준으로 변경해야 합니다.

studentCountMap도 반드시 student_class_history 기준으로 계산하세요.

조건은 학생 목록과 완전히 동일해야 합니다.

필수 조건

- swimming_pool_id
- class_group_id
- enrolled_at <= 조회날짜
- left_at IS NULL OR left_at > 조회날짜
- students.deleted_at IS NULL

학생 상세 목록과 반 카드 인원이 항상 동일해야 합니다.

==================================================
2. today-schedule.ts DISTINCT ON 검증
==================================================

현재

DISTINCT ON (student_id, class_group_id)

ORDER BY

student_id,
class_group_id,
enrolled_at DESC

를 사용하고 있습니다.

동일 학생이 같은 반 history를 여러 번 가진 경우에도

항상 조회 날짜에 유효한 최신 history만 선택되는지

실제 코드 기준으로 다시 확인하세요.

중복 history에서도 과거 row가 선택되지 않는다는 근거를 보고하세요.

==================================================
3. attendance.ts
==================================================

현재 계획은

historyStudents에

- enrolled_at
- left_at

을 추가하고

attendance filter에서

등록일 이후

left_at 이전

조건으로 다시 필터링하는 것입니다.

하지만 이것만으로는 충분하지 않습니다.

반드시 다음 4가지를 모두 만족해야 합니다.

- student_id
- class_group_id
- attendance 날짜
- history 유효기간

attendance를 student_id만으로 매핑하면 안 됩니다.

반 이동한 학생은

이전 반과 현재 반에서

동일 attendance가 중복 계산될 수 있습니다.

attendance 테이블에 class_group_id가 저장되어 있는지 먼저 확인하세요.

있다면 반드시 함께 비교하세요.

없다면

왜 없는지,

현재 구조에서 어떻게 중복을 방지할 것인지

설명한 후 수정하세요.

weekly와 monthly 모두 동일하게 적용합니다.

==================================================
4. attendance 서버 보호
==================================================

현재 attendance 생성 API는

student_class_history를 검증하지 않고 INSERT합니다.

실운영에서는 today-schedule을 통해 생성되어 안전하더라도

서버 자체에는 보호가 없습니다.

다음 두 가지 중 어느 정책으로 갈지 설명하세요.

① API에서도 history 검증 추가

② today-schedule만 신뢰하는 구조 유지

선택 이유도 함께 보고하세요.

==================================================
5. parent.ts
==================================================

현재 학부모 일지는

현재 class_group_id와 assigned_class_ids만 조회합니다.

반 이동 후 과거 반 일지가 누락됩니다.

조회 대상 class_group_id는

student_class_history의

모든 반 이력으로 변경하세요.

lesson_date 기준 history JOIN은 그대로 유지하세요.

과거 교육기록은 모두 조회 가능해야 합니다.

==================================================
6. parent 출결 조회
==================================================

현재는 attendance.student_id만으로 조회합니다.

history 날짜 검증이 없습니다.

다음을 확인하세요.

- attendance가 이미 날짜 확정 데이터라 history JOIN이 불필요한지

또는

- history JOIN을 추가해야 하는지

둘 중 어느 방향인지

근거와 함께 설명하세요.

==================================================
7. diary.ts
==================================================

푸시 알림은

현재 반 기준으로 보내면 안 됩니다.

반드시

lesson_date 당시

student_class_history 기준으로

학부모를 조회하세요.

절대로

오늘 날짜를 fallback으로 사용하지 않습니다.

lesson_date가 전달되지 않는다면

함수 호출 구조를 변경해서

반드시 lesson_date를 전달하세요.

==================================================
8. attendance 테이블 구조 확인
==================================================

attendance 테이블 컬럼을 확인하세요.

특히

- class_group_id 존재 여부
- lesson_date
- student_id

를 보고하세요.

class_group_id가 없다면

반 이동 중복 문제를 어떻게 해결할 것인지

구체적으로 설명하세요.

==================================================
9. 수정 범위
==================================================

이번 작업은 아래 항목만 수정합니다.

1. today-schedule studentCountMap

2. attendance weekly

3. attendance monthly

4. parent 과거 반 일지 조회

5. diary sendDiaryPush

6. 필요한 최소 SQL

7. 필요한 최소 JS

기존 UI

모달

스크롤

레이아웃

탭

버튼

기존 기능은 절대 수정하지 않습니다.

==================================================
10. 수정 완료 후 보고
==================================================

수정 완료 후 아래 내용을 보고하세요.

1. 변경 파일 목록

2. 변경 SQL

3. 변경 JS

4. attendance 테이블 구조

5. class_group_id 존재 여부

6. 반 이동 테스트

7. 연기 테스트

8. 퇴원 테스트

9. 복귀 테스트

10. 과거 일지 조회 테스트

11. 푸시 알림 테스트

12. studentCountMap 검증

13. 회귀 테스트

모든 날짜 판정은 student_class_history 하나의 기준으로 통일합니다.

수정 중 날짜 시스템과 무관한 기능, UI, 레이아웃은 절대 변경하지 마세요.
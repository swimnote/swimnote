2단계 완료 내용 확인했습니다.

연결 구조, 음성 버튼 중복 제거, TEMP 타이머 격리, V1 롤백 경로 보존 모두 확인했습니다.

3단계 진행을 승인합니다.

다만 OTA 배포 후 바로 완료 처리하지 말고, 아래 순서대로 진행하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. OTA 배포 전 최종 확인
━━━━━━━━━━━━━━━━━━━━━━

아래 항목을 먼저 확인하고 결과를 남기십시오.

- 신규 파일 관련 타입 오류 0건
- DiaryAIButton에서 V2만 실제 렌더링되는지
- V1 코드는 실행되지 않고 롤백용으로만 남아 있는지
- Modal visible true 시 reset이 중복 호출되지 않는지
- 생성 요청 중 close가 차단되는지
- AbortController가 generate와 voice 요청별로 정확히 정리되는지
- TEMP 타이머가 요청 완료·오류·닫기·언마운트 시 모두 clear되는지
- retry 시 requestId와 stale response 판정이 충돌하지 않는지
- onInsert 완료 후 중복 삽입이 발생하지 않는지

위 항목에 문제가 없으면 OTA 배포하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. OTA 배포
━━━━━━━━━━━━━━━━━━━━━━

이번 OTA는 V2 실기기 검증용 배포입니다.

기존 V1 파일은 삭제하거나 수정하지 마십시오.

배포 후 아래 정보를 보고하십시오.

- 배포 채널
- 배포 ID 또는 업데이트 식별값
- 배포 시각
- 포함된 변경 파일 목록
- 배포 성공 여부

━━━━━━━━━━━━━━━━━━━━━━
3. 실기기 테스트
━━━━━━━━━━━━━━━━━━━━━━

가능한 한 실제 사용 흐름으로 테스트하십시오.

필수 테스트:

1. 모달 열기
- 첫 화면 즉시 INPUT 표시
- 깜빡임 없음
- 이전 결과·오류가 남지 않음

2. 텍스트 입력
- 입력
- AI 작성
- SEARCHING 표시
- GENERATING 표시
- RESULT 표시

3. 결과 확인
- 공통 일지 표시
- 학생별 일지 표시
- 공통 일지 수정
- 학생별 일지 수정
- 수정 내용 유지

4. 일지 삽입
- 공통 일지 기존 필드 삽입
- 학생별 일지 각 학생 필드에 정확히 삽입
- 중복 삽입 없음
- 기존 저장 흐름 정상

5. 음성
- 권한 요청
- 녹음 시작
- 녹음 중지
- TRANSCRIBING 표시
- transcript 입력 반영
- 권한 거부 시 PERMISSION 화면
- 재허용 후 정상 동작

6. 닫기
- 빈 INPUT 상태 닫기
- 입력 내용이 있을 때 확인 Alert
- SEARCHING 중 닫기 차단
- GENERATING 중 닫기 차단
- TRANSCRIBING 중 닫기 차단
- Android Back 동작
- 백드롭 터치 동작

7. 오류 및 복구
- 네트워크 오류
- timeout
- 자동 retry 1회
- ERROR 화면
- 다시 시도
- 닫고 재오픈 후 초기화

8. 반복 테스트
- 열기/닫기 10회
- 생성 5회 이상
- 음성 입력 3회 이상
- 오류 후 재시도 2회 이상
- 삽입 후 재오픈 3회 이상

━━━━━━━━━━━━━━━━━━━━━━
4. 특히 확인할 위험 항목
━━━━━━━━━━━━━━━━━━━━━━

AIResultArea를 RESULT 상태에서 레거시 EDITING으로 매핑한 부분은 반드시 확인하십시오.

확인할 내용:

- 결과가 처음부터 TextInput으로 표시돼도 레이아웃이 깨지지 않는지
- 키보드가 자동으로 올라오지 않는지
- 학생별 결과가 모두 편집 가능하게 표시되는지
- 긴 결과에서 스크롤과 ActionBar가 겹치지 않는지
- 수정 중 Android Back 또는 키보드 닫기가 정상인지

문제가 있으면 AIResultArea 자체를 크게 수정하지 말고,
V2 전용 표시 모드 또는 prop 분리를 먼저 제안하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 테스트 완료 후 보고 형식
━━━━━━━━━━━━━━━━━━━━━━

아래 형식으로 결과를 제출하십시오.

- 테스트 기기 및 OS
- OTA 배포 정보
- 성공 항목
- 실패 항목
- 재현 방법
- 콘솔 또는 서버 오류
- 수정된 파일
- V1 롤백 필요 여부
- 운영 배포 가능 여부 판단

실기기 검증 전에는 기존 V1 파일 삭제,
대규모 리팩터링,
운영 완료 선언을 하지 마십시오.

위 조건으로 3단계를 진행하십시오.
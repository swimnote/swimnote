[보낼 곳: APP REPLIT]

# ============================================================
# SWIMNOTE 2.0 FIRST-PASS FIX — WP1
# NAVIGATION + MEMBER DETAIL BLOCKER
#
# 계획 승인 완료.
#
# 이번 WP는 딱 2개만 실제 수정:
#
# D. 회원 상세 ClassPickerModal 제거 + 반 변경 flow 정상화
# I. X모드 전체 back-stack 실제 동작 복원
#
# 다른 항목은 건드리지 않는다.
# ============================================================

Repository:
swimnote/swimnote

Target branch:
release/v2.0.0

현재 실제 HEAD 확인 후 진행.

1.6.3:
NO TOUCH

Production:
NO TOUCH

RevenueCat:
NO TOUCH

Kakao:
NO TOUCH

Messenger:
NO TOUCH

============================================================
1. D — MEMBER DETAIL CLASS PICKER 제거
============================================================

사용자 실기기 문제:

회원 상세 진입 직후
`반 선택 (최대 1개)` 모달이 자동으로 뜸.

문제:
- X 버튼 동작 불가
- 전체 반이 길게 나열
- 상세 편집 방해

최종 정책:

member-detail에서는
ClassPickerModal을 더 이상 mount하지 않는다.

제거:

- ClassPickerModal import
- showPicker state
- onOpenPicker
- member-detail 내부 <ClassPickerModal ...>

단:

ClassPickerModal.tsx 파일 자체 삭제 금지.
members.tsx의 기존 반이동 flow가 아직 사용하므로 유지.

============================================================
2. 상세 내부 `반 변경` flow
============================================================

member-detail.tsx Section B(반 정보)에:

`반 변경`

명시적 action 추가.

탭 시에만 반 변경 UI 노출.

기존 infrastructure 재사용:

- classGroups
- assignedIds
- weeklyCount
- saveAssignment()
- PATCH /students/:id/assign

새 API:
NO

새 DB:
NO

============================================================
3. 반 변경 UI
============================================================

기존 ClassPickerModal의 핵심 selection 로직만 재사용.

하지만 member-detail에서는
Modal로 띄우지 않는다.

허용:

- detail 내부 조건부 section
또는
- 기존 프로젝트에 이미 있는 안전한 BottomSheet pattern

중요:
새 복잡한 picker architecture 만들지 말 것.

최소 동작:

반 변경 tap
→ 현재 선택 반 표시
→ 변경할 반 선택
→ 저장
→ saveAssignment()
→ UI 닫힘

취소/back:
→ 변경사항 폐기
→ 상세화면 그대로

============================================================
4. 전체 반 무한 나열 방지
============================================================

현재 classGroups가 pool 전체 반환되는 것은
관리자 상세에서 정상일 수 있음.

하지만 UI는 정돈.

최소:

- 반 이름/요일/시간이 식별 가능
- 정렬
- 제한된 높이 + 내부 scroll

가능하면 기존 데이터의
요일/시간/선생님 값을 그대로 표시.

검색 기능 등 신규 기능 추가:
NO

============================================================
5. D 테스트
============================================================

D01
회원 상세 진입
→ 반 선택 UI 자동 노출 0

D02
상세 기본정보 편집 가능

D03
반 변경 tap
→ 반 변경 UI 노출

D04
현재 반 표시

D05
새 반 선택

D06
저장
→ PATCH /students/:id/assign 정상

D07
저장 후 상세 반 정보 갱신

D08
취소
→ 데이터 변경 0

D09
back
→ 상세 유지

D10
members.tsx 기존 ClassPickerModal 반이동 회귀 0

============================================================
6. I — X MODE BACK STACK 전수 수정
============================================================

사용자 정책:

Back / X:
무조건 바로 이전 업무 화면

Home icon:
홈

Back에서 홈 점프:
금지

대상:

Settings
→ SWIMNOTE X모드

Settings
→ X모드 세팅하기

그 안의:
- 1차 메뉴
- 2차 메뉴
- 3차 상세
- modal
- bottom sheet

전부.

============================================================
7. 확정 버그 수정
============================================================

x-subscription.tsx 구매 완료 Alert confirm의:

router.replace("/(admin)/")

제거.

정상 target:

직전 x-mode-hub

가능하면:
router.back()

단 stack이 보장되지 않는 흐름이면
명시적 backTo/x-mode-hub fallback.

HOME landing으로 replace:
NO

============================================================
8. X route push/backTo 전수 확인
============================================================

계획 단계에서 잠재 원인으로 나온:

- X 관련 push 호출
- backTo param
- SubScreenHeader replace

전부 실제 코드 확인.

특히 금지:

backTo="/(admin)/"

또는 equivalent admin landing.

정상 target은 진입 경로에 따라:

settings
x-mode-hub
x-setup
x-hub
curriculum-hub
기타 직전 업무 화면

이어야 함.

============================================================
9. router.back() 코드 존재만으로 PASS 금지
============================================================

실제 stack sequence 기준 테스트.

아래를 직접 재현:

I01
settings
→ x-mode-hub
→ back
→ settings

I02
settings
→ x-setup
→ back
→ settings

I03
x-mode-hub
→ x-setup
→ back
→ x-mode-hub

I04
x-mode-hub
→ x-subscription
→ header back
→ x-mode-hub

I05
x-mode-hub
→ x-subscription
→ 구매 완료
→ x-mode-hub
→ HOME 이동 0

I06
x-hub
→ 각 x-info detail
→ back
→ x-hub

I07
settings
→ curriculum-hub
→ back
→ settings

I08
dashboard
→ curriculum-hub
→ back
→ dashboard

I09
modal X
→ 바로 직전 screen

I10
Home icon
→ dashboard

I11
Back action에서 admin landing/home fallback 0

============================================================
10. Shared navigation rule
============================================================

SubScreenHeader 기존 역할 유지:

Back:
이전 업무화면

Home:
dashboard

이 두 기능을 섞지 말 것.

공통 helper를 바꿀 필요가 있다면
현재 사용처 전체 영향 확인.

대규모 navigation refactor:
NO

============================================================
11. 범위 제한
============================================================

이번 WP에서 허용:

member-detail.tsx
필요한 member detail selection UI
x-subscription.tsx
X 관련 push/backTo 최소 파일
SubScreenHeader가 실제 원인일 경우 최소 수정
tests

금지:

members list UI 변경
MemberCard 변경
parent auto approval
messenger
home polish
teacher tabs
invite history
RevenueCat
1.6.3
Production server
DB

============================================================
12. REGRESSION
============================================================

최근 media cleanup fixes:
회귀 금지

Teacher scope:
회귀 금지

Kakao:
회귀 금지

Known failing tests:
변경 전/후 이름 비교.

New deterministic failures:
0

============================================================
13. COMMIT
============================================================

WP1 완료 후 1 commit 권장:

fix(v2.0): restore member detail class flow and x-mode back stack

============================================================
14. 완료보고
============================================================

# SWIMNOTE 2.0 WP1 COMPLETION REPORT

## SOURCE

Branch:
release/v2.0.0

HEAD Before:
...

HEAD After:
...

Commit:
...

## D — MEMBER DETAIL

ClassPickerModal mounted in detail:
NO

Auto-open modal:
0

Member detail editable:
PASS / FAIL

반 변경 action:
PASS / FAIL

Selection flow:
PASS / FAIL

PATCH /students/:id/assign:
PASS / FAIL

Cancel/back:
PASS / FAIL

members.tsx legacy transfer flow:
PASS / FAIL

D01-D10:
...

## I — X BACK STACK

x-subscription home jump:
FIXED / FAIL

backTo admin landing refs found:
...

Fixed:
...

Settings→X mode→Back:
PASS / FAIL

Settings→X setup→Back:
PASS / FAIL

X hub→detail→Back:
PASS / FAIL

Subscription purchase complete→X hub:
PASS / FAIL

Modal X→previous:
PASS / FAIL

Home icon→dashboard:
PASS / FAIL

Back→home accidental:
0 / ...

I01-I11:
...

## REGRESSION

Teacher scope:
PASS / FAIL

Media cleanup:
PASS / FAIL

Kakao:
PASS / FAIL

New deterministic failures:
0 / ...

## DEPLOY

OTA:
NO

Build:
NO

Render:
NO

Production:
NO CHANGE

## FINAL

WP1 COMPLETE — READY FOR WP2

또는

WP1 BLOCKED — EXACT ISSUE
SWIMNOTE 보강 회귀 — 문제 A 즉시 원인 확정

실기기 Production 진단 결과:

updateId:
019fdced-cfe6-7185-8050-b2713f4a1f85

runtimeVersion:
1.6.1

channel:
production

API_BASE:
https://swimnote.kr/api

waiting API:
HTTP 200
total=2
expired=0
julyExpired=0
sortedExpired=0

즉 현재 실기기에서 기간 지난 보강 2건은
UI 렌더 전에 waiting API 응답 단계에서 이미 사라져 있다.

이제 separator/UI 조사 금지.
먼저 waiting API 차이를 확정한다.

코드 수정 금지.
Commit/Push/Render/OTA/DB 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 동일 계정·동일 endpoint를 두 BASE에서 비교
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

동일한 공식 인증 토큰을 사용해서 아래 두 주소를 각각 호출한다.

A.
https://swimnote.kr/api/teacher/makeups

B.
https://swimnote-api.onrender.com/api/teacher/makeups

반드시 동일 Authorization token 사용.

각각 제출:

- HTTP status
- total
- expired count
- 배서준 7/25 존재 여부
- 배서준 7/31 존재 여부
- 배서준 8/1 존재 여부
- 배서준 8/7 존재 여부

전화번호/JWT 원문 출력 금지.

목표:

A와 B 응답이 다른지 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. swimnote.kr/api 실제 upstream 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Repository / Render / reverse proxy 설정에서

https://swimnote.kr/api

가 실제로 어디로 전달되는지 확인한다.

제출:

- proxy 설정 파일
- upstream 주소
- 현재 배포 서버와 동일한지
- cache가 존재하는지
- CDN/Cloudflare/Nginx 캐시 가능성
- `/teacher/makeups` GET 응답에 cache가 걸릴 가능성

"Render 운영 서버다"라고 추정하지 말고
실제 설정으로 증명한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. Production waiting Route 현재 코드 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 Render Live SHA에서
GET /teacher/makeups의 WHERE 조건 원문 제출.

특히:

status IN (...)
expire_at
absence_date
ownership
teacher_user_id
pool_id
assigned 상태
carryover 관련 조건

을 확인한다.

반드시 현재 Production SHA의 코드 기준.

기대 핵심:

status IN ('waiting', 'expired')

가 실제 Live 코드에 존재하는지 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 운영 DB와 API 결과 대조
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUPABASE_DATABASE_URL 운영 DB에서
배서준 4건을 SELECT한다.

각 건:

- makeup id
- absence_date
- status
- expire_at
- swimming_pool_id
- teacher/user ownership 관련 필드
- deleted/cancelled 관련 필드가 있다면 값

그리고 waiting Route의 WHERE 조건을
각 행에 하나씩 적용해서:

7/25 → 포함/제외 + 이유
7/31 → 포함/제외 + 이유
8/1 → 포함/제외 + 이유
8/7 → 포함/제외 + 이유

를 표로 제출한다.

이 단계에서
"DB에는 있다"로 끝내지 말고
왜 Route 결과가 2건인지 SQL 조건 수준에서 설명한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 결론 분기
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CASE A:
swimnote.kr/api = total 2
onrender.com/api = total 4

→ API_BASE/proxy/upstream 문제 확정.

CASE B:
둘 다 total 2

→ 현재 Live waiting Route 또는 DB 상태/ownership 조건 문제.

CASE C:
둘 다 total 4인데 실기기만 total 2

→ 앱 fetch/cache/응답 처리 문제.

이 세 경우 중 하나로 반드시 분류한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 문제 B는 잠시 보류
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

eligible-occurrences 오류는 별도 문제로 유지.

지금은 expired 2건 누락 원인을 먼저 확정한다.

문제 A 원인이 나온 뒤
문제 B 진단 캡처를 받는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 최종 제출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. swimnote.kr/api waiting 결과
2. onrender.com/api waiting 결과
3. 두 결과 동일 여부
4. swimnote.kr/api 실제 upstream
5. cache/proxy 여부
6. Live waiting Route WHERE 원문
7. 배서준 4건 운영 DB 상태
8. 4건 각각 Route 포함/제외 판정
9. total=2가 되는 정확한 이유
10. 원인 CASE A/B/C
11. 최소 수정안
12. 구현 전 승인 필요 여부

조사만 하고 중단.
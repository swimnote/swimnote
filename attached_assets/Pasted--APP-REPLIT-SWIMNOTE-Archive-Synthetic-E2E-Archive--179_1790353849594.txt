[보낼 곳: APP REPLIT]

[SWIMNOTE Archive — 최종 Synthetic E2E 검증만 수행]

현재 Archive 구현/배포는 승인합니다.

새 기능 개발 금지.
리팩터링 금지.
정책 변경 금지.
배포도 버그가 발견되지 않으면 금지.

남은 것은 TEST A~M의 실제 통합 검증뿐입니다.

Production 실제 회원 데이터 사용 금지.

기존 test pool / 격리 test tenant / synthetic fixture를 사용하고,
없다면 운영 회원과 충돌하지 않는 명시적 TEST 전용 데이터만 생성하십시오.

테스트 종료 후 생성한 test 데이터는 cleanup하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. TEST A — note 없는 diary
━━━━━━━━━━━━━━━━━━━━━━

synthetic student + class diary 생성.

common_content 존재
student_note 없음

퇴원 실행.

확인:

- withdrawn_member_archives 생성
- withdrawn_diary_archives 생성
- 해당 diary 포함
- student_note = NULL

실제 DB 결과로 확인.

━━━━━━━━━━━━━━━━━━━━━━
2. TEST B — 결석
━━━━━━━━━━━━━━━━━━━━━━

학생의 정상 수업 diary 생성 후
attendance='absent' 생성.

퇴원.

확인:

해당 diary가 Archive에 들어가지 않음.

━━━━━━━━━━━━━━━━━━━━━━
3. TEST C — completed makeup
━━━━━━━━━━━━━━━━━━━━━━

다른 반 diary +
해당 학생 completed makeup 생성.

퇴원.

확인:

- Archive 포함
- is_makeup_diary=true

━━━━━━━━━━━━━━━━━━━━━━
4. TEST D — 반 이동
━━━━━━━━━━━━━━━━━━━━━━

학생:

Class A
→ Class B
→ Class C

history 생성.

각 실제 재원기간에 diary 생성.

퇴원.

확인:

A/B/C 각 재원기간 diary가
모두 Archive에 존재.

재원기간 밖 diary는 미포함.

━━━━━━━━━━━━━━━━━━━━━━
5. TEST E — 장기연기 전/후 전체 보존
━━━━━━━━━━━━━━━━━━━━━━

교육구간 1 diary 생성

→ suspended
→ 장기연기 조건 충족
→ active
→ education_started_at 갱신
→ 교육구간 2 diary 생성
→ 퇴원

확인:

Archive에는

교육구간 1
+
교육구간 2

모두 존재.

education_started_at 이전 diary가
Archive에서 누락되면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
6. TEST F — Archive 실패 rollback
━━━━━━━━━━━━━━━━━━━━━━

격리 환경에서 Archive INSERT 실패를
의도적으로 발생시키십시오.

확인:

- withdraw API 성공 응답 금지
- student.status가 withdrawn으로 완료되지 않음
- 원본 diary/note/교육데이터 유지
- parent_students 유지
- purge 실행 안 됨

실패 유도 코드를 Production에 남기지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. TEST G — 재가입
━━━━━━━━━━━━━━━━━━━━━━

퇴원 완료 후
동일 이름/동일 연락처 조건으로 신규 회원 등록.

확인:

- 새 student_id
- Archive 자동 link 없음
- 과거 diary current feed에 없음
- 과거 Archive는 운영자 Archive에서만 존재

━━━━━━━━━━━━━━━━━━━━━━
8. TEST H — 관리자 수동 연결
━━━━━━━━━━━━━━━━━━━━━━

pool_admin으로:

Archive
→ 현재 student

수동 연결.

확인:

- withdrawn_archive_links active row 생성
- Parent archive API에서 지난 수강 기록 반환
- current diary/feed에는 Archive 미혼입
- Growth Report input에도 미혼입

━━━━━━━━━━━━━━━━━━━━━━
9. TEST I — 연결 해제
━━━━━━━━━━━━━━━━━━━━━━

동일 link 해제.

확인:

- unlinked_at 기록
- Parent archive API에서 더 이상 반환 안 됨
- Archive 원본은 그대로 존재
- current student 영향 없음

━━━━━━━━━━━━━━━━━━━━━━
10. TEST J — Teacher 권한
━━━━━━━━━━━━━━━━━━━━━━

같은 pool teacher:

- Archive 목록 조회 PASS
- Archive 상세/일지 조회 PASS
- link 생성 시 403
- unlink 시 403

확인.

━━━━━━━━━━━━━━━━━━━━━━
11. TEST K — Tenant isolation
━━━━━━━━━━━━━━━━━━━━━━

Pool A archive 생성.

Pool B teacher/admin으로 접근.

확인:

- 목록에서 안 보임
- 직접 archive ID로 접근해도 차단
- diary endpoint 차단
- link endpoint 차단

기존 API 의미에 따라 403/404 허용.

━━━━━━━━━━━━━━━━━━━━━━
12. TEST L — Current system 무영향
━━━━━━━━━━━━━━━━━━━━━━

Archive 연결 전/후 비교:

- current diary/feed
- current news
- Growth Report input
- previous usable report
- curriculum progress
- attendance
- member count

결과가 Archive 연결 때문에 달라지지 않는지 확인.

단 Parent의 별도
“지난 수강 기록”
영역만 추가되어야 함.

━━━━━━━━━━━━━━━━━━━━━━
13. TEST M — 다자녀
━━━━━━━━━━━━━━━━━━━━━━

Parent P:

Child A
Child B

둘 다 연결.

A만 퇴원.

확인:

- A parent_students relation 제거
- A Archive 생성
- B parent_students relation 유지
- Parent account 유지
- B 앱 접근 정상
- B 교육데이터 영향 없음

━━━━━━━━━━━━━━━━━━━━━━
14. TEST CLEANUP
━━━━━━━━━━━━━━━━━━━━━━

이번 검증에서 생성한:

- test students
- test parent
- test class
- test diaries
- test attendance
- test makeup
- Archive test rows
- Archive link rows

등 TEST 전용 데이터만 cleanup.

실제 운영 데이터 절대 수정 금지.

cleanup 후 잔존 test row도 확인.

━━━━━━━━━━━━━━━━━━━━━━
15. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

TEST A~M 각각:

- PASS
- FAIL
- 확인하지 못함

중 하나로 보고.

PASS인 경우
“코드상으로 보인다”가 아니라
실제 실행 결과/DB/API 응답 근거를 한 줄씩 보고.

추가로:

- test tenant/pool 종류
- Production 실회원 미사용 확인
- cleanup 완료 여부

보고.

버그가 발견되지 않았다면
코드 수정/재배포하지 마십시오.

버그가 발견된 경우에만
그 버그의 최소 수정 후
해당 TEST만 재검증하십시오.

완료했다고만 보고하지 마십시오.
확인하지 못한 것은 그대로
‘확인하지 못함’이라고 명시하십시오.
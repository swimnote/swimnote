[APP REPLIT — P0-2 Production 배포 마무리 / 코드 수정 금지]

P0-2 기능 코드는 최종 PASS 상태입니다.

최종 GitHub commit:
be24aa14

현재 문제는 코드가 아니라 배포 Source 불일치입니다.

현재 확인:
- GitHub main: be24aa14
- Render 추적 Repository: GitLab
  https://gitlab.com/swimnote-group/swimnote-project
- Render autoDeploy: OFF
- 현재 Production /health: ba8d6183
- 따라서 be24aa14는 아직 Production에 반영되지 않음

이번 작업에서는 기능 코드를 더 수정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. 먼저 GitLab 실제 HEAD 확인
━━━━━━━━━━━━━━━━━━━━━━

Render가 추적하는 GitLab main의 현재 HEAD SHA를 확인하십시오.

제출:

GitHub main HEAD =
GitLab main HEAD =
Render /health SHA =

세 개를 비교하십시오.

GitLab main에 be24aa14 내용이 실제 포함되어 있는지
commit ancestry/tree 기준으로 확인하십시오.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. Deploy latest commit 먼저 누르지 말 것
━━━━━━━━━━━━━━━━━━━━━━

GitLab main에 be24aa14가 반영되기 전에는
Render의 "Deploy latest commit"을 실행하지 마십시오.

Render는 GitLab을 Source로 사용하므로
GitLab이 옛 코드라면 옛 코드를 다시 배포하게 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
3. GitLab에 정확히 동기화
━━━━━━━━━━━━━━━━━━━━━━

Replit workspace에서 GitLab 인증정보/remote 사용 가능 여부를
먼저 확인하십시오.

토큰/비밀번호 값을 로그에 출력하지 마십시오.

가능하면:

- GitLab remote 추가/확인
- GitLab main fetch
- GitHub main과 GitLab main 차이 확인
- force push 금지
- GitLab의 독립 변경사항이 있는지 확인

GitLab main이 단순히 뒤처져 있고 충돌이 없다면
be24aa14까지의 GitHub main 변경을
GitLab main에 안전하게 반영하십시오.

방법은 repository history에 맞게
merge/cherry-pick/정상 push 중 가장 안전한 방법을 사용하십시오.

절대:
git push --force
git reset --hard 후 강제 push
금지.

GitLab에 별도 변경이 있어 충돌하거나
인증권한이 없다면 임의 처리하지 말고 STOP 후 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. GitLab 동기화 확인
━━━━━━━━━━━━━━━━━━━━━━

GitLab main에서 다음을 확인:

- parent-approval-hooks.ts에 full UUID 적용
- notif_bf_ 없음
- truncated UUID 없음
- 최신 P0-2 코드 포함

가능하면 GitLab HEAD가 be24aa14와 동일하면 가장 좋음.

history 때문에 SHA가 달라졌다면
tree/content가 동일한지 증명하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 그 다음 Render 배포
━━━━━━━━━━━━━━━━━━━━━━

GitLab 동기화가 완료된 뒤에만
Render Production을 수동 배포하십시오.

배포 대상은 반드시
P0-2 최종 코드가 포함된 GitLab commit.

배포 완료 후:

Render status = live

확인.

━━━━━━━━━━━━━━━━━━━━━━
6. Production 검증
━━━━━━━━━━━━━━━━━━━━━━

/health 호출:

기대:
commit = be24aa14

GitLab merge 때문에 SHA가 달라졌다면
그 정확한 GitLab SHA가 표시되어야 하며
be24aa14의 P0-2 변경사항이 모두 포함되어 있음을 증명하십시오.

추가 검증:

- 서버 boot 정상
- parent approval hook import/runtime error 없음
- DB migration 없음
- OTA 없음

━━━━━━━━━━━━━━━━━━━━━━
7. 이번 작업 금지
━━━━━━━━━━━━━━━━━━━━━━

- P0-2 로직 추가 수정
- DB UPDATE/backfill
- 기존 승인 보호자 일괄 소급 처리
- APP OTA
- Android/iOS 코드 변경
- Render Source 확인 없이 Deploy latest commit 실행
- GitLab force push

━━━━━━━━━━━━━━━━━━━━━━
8. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

다음만 보고하십시오.

1. GitHub HEAD
2. GitLab HEAD BEFORE
3. 두 repo 차이 여부
4. GitLab 동기화 방법
5. GitLab HEAD AFTER
6. Render deploy ID
7. Render live
8. /health SHA
9. P0-2 최종 코드 Production 포함 확인
10. DB 변경 없음
11. OTA 없음

/health에서 최종 코드가 확인되면
P0-2 CLOSED.
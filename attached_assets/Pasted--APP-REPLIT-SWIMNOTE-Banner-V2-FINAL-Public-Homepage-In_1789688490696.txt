[보낼 곳: APP REPLIT]

[SWIMNOTE — Banner V2 FINAL]
Public Homepage Integration + Production Deploy

Banner V2는 아직 CLOSED가 아닙니다.

현재 완료:

- Super Admin 가로 배너 관리 V2
- 1 row = 1 slide
- 최대 4개
- TEXT / IMAGE
- 16:5
- sort_order
- display_seconds
- R2 이미지 업로드
- display_url
- Parent APP 자동 슬라이드
- APP 실기기 정상 동작

까지 완료됐습니다.

이제 동일 Banner V2를
SWIMNOTE 홈페이지에도 연결하고
swimnote-web Production 배포까지 완료해야
Banner V2 전체 CLOSED입니다.

━━━━━━━━━━━━━━━━━━━━━━
1. 핵심 원칙
━━━━━━━━━━━━━━━━━━━━━━

홈페이지용 별도 배너 시스템을 만들지 마십시오.

반드시 기존:

platform_banners

데이터를 그대로 사용합니다.

즉 Super Admin에서 가로 배너를 한 번 등록/수정하면

APP
+
SWIMNOTE 홈페이지

가 같은 배너 데이터를 사용해야 합니다.

금지:

- website_banners 신규 테이블
- 홈페이지 전용 banner DB
- slides JSONB
- 홈페이지에 배너 내용 하드코딩
- APP/WEB 각각 별도의 sort_order
- APP/WEB 각각 별도의 이미지 업로드

DATA = SHARED
LAYOUT = PLATFORM SPECIFIC

원칙입니다.

━━━━━━━━━━━━━━━━━━━━━━
2. PUBLIC WEBSITE 인증 문제
━━━━━━━━━━━━━━━━━━━━━━

현재:

GET /platform/banners

는 requireAuth가 걸려 있습니다.

이 정책은 그대로 유지하십시오.

Parent APP은 로그인 사용자이므로
기존 authenticated endpoint를 계속 사용합니다.

하지만 SWIMNOTE 공개 홈페이지는
로그인하지 않은 방문자도 접근하므로

홈페이지 때문에:

GET /platform/banners

의 requireAuth를 제거하면 안 됩니다.

필요하다면 최소한의 공개 READ endpoint를 추가하십시오.

예시:

GET /public/banners?type=strip

실제 route naming은
현재 API convention에 맞게 정하십시오.

이 endpoint는 READ ONLY.

Super Admin CRUD와 절대 공유하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. PUBLIC BANNER ENDPOINT 보안
━━━━━━━━━━━━━━━━━━━━━━

Public endpoint는
노출 가능한 배너만 서버에서 필터링해야 합니다.

최소 조건:

banner_type = 'strip'

status = active

display_start / display_end
현재 노출 기간 충족

기존 target 정책상
공개 홈페이지에 노출 가능한 대상만 반환.

현재 target 값에:

all / parent 등

기존 의미가 있다면
그 의미를 먼저 확인해서 재사용하십시오.

새 target 체계를 임의로 만들지 마십시오.

예:

target='all'
→ APP + 홈페이지

target='parent'
→ Parent APP만

과 같은 현재 정책이 이미 가능하면 그대로 사용.

실제 현재 target enum/값이 다르면
기존 의미를 우선합니다.

━━━━━━━━━━━━━━━━━━━━━━
4. PUBLIC RESPONSE 최소화
━━━━━━━━━━━━━━━━━━━━━━

공개 홈페이지 endpoint에는
화면 표시용 데이터만 반환하십시오.

예:

id
title
description
color_theme
display_url
link_url
link_type
display_seconds
sort_order

등.

불필요한:

created_by
내부 관리자 정보
민감한 storage 설정

등은 public response에서 제외.

image_key도 display에 필요 없다면
굳이 공개할 필요 없습니다.

━━━━━━━━━━━━━━━━━━━━━━
5. 홈페이지 실제 SOURCE 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 swimnote.kr의 실제 공개 홈페이지를
렌더링하는 정확한 component/page부터 확인하십시오.

파일명을 추측하지 마십시오.

현재 홈페이지 route 및
Hero/상단 content 구조를 확인한 후
기존 디자인을 깨지 않는 위치에
Banner V2를 연결하십시오.

새 홈페이지를 만들지 마십시오.

기존 홈페이지에 통합합니다.

━━━━━━━━━━━━━━━━━━━━━━
6. 홈페이지 배너 디자인
━━━━━━━━━━━━━━━━━━━━━━

APP과 데이터/의미는 같되
웹 레이아웃에 맞게 표현하십시오.

기본 비율:

16 : 5

유지.

단 PC 브라우저에서
화면 전체 폭으로 무한정 늘리지 마십시오.

기존 홈페이지 content max-width가 있다면
그 폭을 따릅니다.

예:

width: 100%
max-width: 기존 content container
aspect-ratio: 16 / 5

모바일 웹에서도:

16:5

유지.

이미지:

object-fit: cover

TEXT:

기존 color_theme 반영.

홈페이지 전체 디자인과
이질감 없게 심플하게 처리하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 슬라이드 동작
━━━━━━━━━━━━━━━━━━━━━━

활성 strip banner:

1개
→ 단일 표시

2~4개
→ carousel

전환시간:

각 row의 display_seconds 사용.

값이 없으면:

15초

기본.

마지막 → 첫 번째 순환.

━━━━━━━━━━━━━━━━━━━━━━
8. 홈페이지 사용자 조작
━━━━━━━━━━━━━━━━━━━━━━

웹에서는 최소:

- 자동 전환
- 좌/우 이동
- dot indicator

지원.

모바일 웹에서는
현재 carousel library/구조상 안정적으로 가능하면
swipe/drag도 지원.

새 무거운 carousel dependency는
이 기능 하나 때문에 추가하지 마십시오.

React/CSS로 단순 구현 가능하면
기존 구조를 사용.

━━━━━━━━━━━━━━━━━━━━━━
9. TAB 비활성 상태
━━━━━━━━━━━━━━━━━━━━━━

브라우저 tab이 hidden이면
자동전환 timer를 불필요하게 계속 돌리지 않도록

Page Visibility API 또는
현재 프로젝트 기존 패턴이 있으면 재사용.

visible 복귀 시 정상 재개.

timer 중복 금지.

cleanup 필수.

과도한 구현은 필요 없습니다.

━━━━━━━━━━━━━━━━━━━━━━
10. TEXT / IMAGE 동일 지원
━━━━━━━━━━━━━━━━━━━━━━

TEXT banner:

title
description
color_theme

사용.

IMAGE banner:

서버가 반환하는:

display_url

사용.

홈페이지에서도 자체적으로:

API_BASE + image_key

등을 조합하지 마십시오.

APP과 마찬가지로
canonical display_url을 사용.

━━━━━━━━━━━━━━━━━━━━━━
11. 기존 R2 파이프라인 재사용
━━━━━━━━━━━━━━━━━━━━━━

이미 Production에서 확인된:

image upload
→ Cloudflare R2
→ display_url
→ HTTPS
→ HTTP 200 image/*

파이프라인을 그대로 사용.

홈페이지 때문에:

- R2 재업로드
- 이미지 복제
- website용 storage

만들지 마십시오.

한 이미지 object를
APP과 WEB이 함께 사용합니다.

━━━━━━━━━━━━━━━━━━━━━━
12. LINK 처리
━━━━━━━━━━━━━━━━━━━━━━

배너별 기존:

link_type
link_url

재사용.

홈페이지에서 external link:

https://

만 허용하는 기존 정책 유지.

javascript:
data:
file:
http:

등 금지.

링크 없는 배너는
그냥 표시만.

APP 내부 navigation용 값이 존재하더라도
홈페이지에서 임의로 해석하지 마십시오.

현재 Banner 정책에 맞게
웹에서 의미 있는 링크만 활성화.

━━━━━━━━━━━━━━━━━━━━━━
13. FALLBACK
━━━━━━━━━━━━━━━━━━━━━━

홈페이지에서도
활성 공개 strip banner가 0개라면

배너 영역 자체를 숨기는 것을 우선합니다.

APP의:

“스윔노트 — 우리 아이 수영 성장을 기록해보세요”

fallback을
홈페이지까지 반드시 복제할 필요는 없습니다.

홈페이지 기존 구조상
fallback이 자연스럽다면 사용할 수 있으나

빈 배너 때문에
큰 빈 공간이 생기면 안 됩니다.

핵심:

활성 배너 있음
→ 실제 배너 표시

활성 배너 없음
→ 기존 홈페이지 레이아웃 자연스럽게 유지

입니다.

━━━━━━━━━━━━━━━━━━━━━━
14. ADMIN 한 번 수정 → APP/WEB 동시 반영
━━━━━━━━━━━━━━━━━━━━━━

반드시 검증하십시오.

Super Admin에서:

제목 수정
이미지 변경
순서 변경
활성/비활성
display_seconds 변경

후

APP
+
홈페이지

가 각각 다음 조회 시
동일 platform_banners 원천데이터를 사용해야 합니다.

홈페이지 별도 저장/복사 과정 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. 최대 4개 정책
━━━━━━━━━━━━━━━━━━━━━━

기존 strip max 4 정책 그대로.

홈페이지가 별도로 4개를 관리하는 것이 아님.

같은 활성 strip rows를
웹에서도 순서대로 표시.

sort_order 동일.

━━━━━━━━━━━━━━━━━━━━━━
16. 홈페이지 CACHE 주의
━━━━━━━━━━━━━━━━━━━━━━

Super Admin에서 배너를 수정했는데
웹에서 오래된 내용이 계속 보이는 구조를 만들지 마십시오.

현재 swimnote-web / fetch 정책을 확인하여
배너 GET 요청이 적절하게 최신 데이터를 받을 수 있게 하십시오.

단 이를 이유로
전체 사이트 cache 정책을 변경하지 마십시오.

Banner 요청에 필요한 최소 처리만.

━━━━━━━━━━━━━━━━━━━━━━
17. 기존 홈페이지 성능 보호
━━━━━━━━━━━━━━━━━━━━━━

배너 때문에 홈페이지 첫 화면 전체가
API 응답을 기다리며 block되면 안 됩니다.

Banner fetch 실패 시:

홈페이지 자체는 정상 렌더링.

배너만 숨김/기존 fallback.

console infinite error
render loop

금지.

이미지도
정상 브라우저 loading 정책 사용.

━━━━━━━━━━━━━━━━━━━━━━
18. ACCESSIBILITY
━━━━━━━━━━━━━━━━━━━━━━

이미지 배너에
의미 있는 alt 처리가 가능하면
title 기반으로 사용.

좌/우 버튼은
접근 가능한 label 제공.

색상만으로 현재 slide 상태를
표현하지 마십시오.

과도한 접근성 리팩터링은 하지 말고
기본적인 웹 기준만 지키십시오.

━━━━━━━━━━━━━━━━━━━━━━
19. 이번 작업의 배포 범위
━━━━━━━━━━━━━━━━━━━━━━

API:

Public read endpoint가 필요한 경우에만
swimnote-api 수정/배포.

APP:

이미 정상 동작 확인 완료.

이번 홈페이지 통합 때문에
APP OTA 재배포하지 마십시오.

Dashboard/Public Web:

swimnote-web Production 배포.

DB:

migration 없음.

━━━━━━━━━━━━━━━━━━━━━━
20. 대표 QA
━━━━━━━━━━━━━━━━━━━━━━

전체 suite 금지.

아래만 확인.

A.

공개 홈페이지 비로그인 상태:

banner API HTTP 200

B.

inactive / 기간 외 banner
공개 endpoint에서 제외

C.

기존 TEXT banner
홈페이지 정상 표시

D.

IMAGE banner
display_url 사용
→ 홈페이지 실제 이미지 정상 표시

E.

2개 이상
→ 자동 전환

F.

display_seconds 적용
미설정 시 15초

G.

좌/우 이동

H.

dot indicator

I.

sort_order가 APP과 홈페이지에서 동일

J.

Super Admin에서 동일 row 수정
→ APP/WEB 동일 데이터 반영 구조

K.

배너 0개
→ 홈페이지 깨지지 않음

L.

API 오류
→ 홈페이지 본문 정상 유지

━━━━━━━━━━━━━━━━━━━━━━
21. 기존 APP AUTH 정책 유지
━━━━━━━━━━━━━━━━━━━━━━

중요:

Parent APP의:

GET /platform/banners

requireAuth 정책을
홈페이지 때문에 바꾸지 마십시오.

APP은 기존 authenticated endpoint.

WEB은 필요하면 public read-only endpoint.

책임을 분리합니다.

━━━━━━━━━━━━━━━━━━━━━━
22. 완료 조건
━━━━━━━━━━━━━━━━━━━━━━

Banner V2 최종 CLOSED 조건:

1.
Super Admin에서 배너 관리 가능

2.
Parent APP 실제기기 표시 정상

3.
이미지:
upload → R2 → display_url → APP 정상

4.
동일 배너가 공개 홈페이지에서도 정상 노출

5.
TEXT / IMAGE 모두 홈페이지 정상

6.
2~4개 carousel 정상

7.
APP/WEB sort_order 동일

8.
홈페이지 Production 배포 live

까지입니다.

━━━━━━━━━━━━━━━━━━━━━━
23. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[A. WEBSITE SOURCE]

- 실제 공개 홈페이지 route
- 수정한 component/file

[B. API]

- 기존 authenticated endpoint 유지 여부
- 홈페이지용 public endpoint 추가 여부
- 추가했다면 route 및 공개 field
- server active/schedule/target filtering 방식

[C. WEB UX]

- 16:5
- TEXT
- IMAGE
- 자동전환
- 좌우 이동
- indicator
- 모바일 대응

[D. SHARED DATA]

- APP/WEB 동일 platform_banners 사용 확인
- sort_order
- display_seconds
- display_url
재사용 확인

[E. QA]

A~L PASS/FAIL

[F. DEPLOY]

API 수정이 있었다면:
- full API LIVE SHA 40자
- API Render Deploy ID
- status=live

Dashboard/Public Web:
- full swimnote-web LIVE SHA 40자
- Render Deploy ID
- status=live

APP:
- 기존 OTA 유지 여부

Migration:
- 없음 확인

[G. ACTUAL PRODUCTION]

실제 공개 swimnote.kr에서:

- TEXT banner 실제 표시
- IMAGE banner 실제 표시
- carousel 실제 동작

을 확인했는지 각각 명시.

브라우저에서 실제 Production 확인을 못했다면
PASS라고 쓰지 말고
‘확인하지 못함’이라고 명시하십시오.

━━━━━━━━━━━━━━━━━━━━━━
24. 금지
━━━━━━━━━━━━━━━━━━━━━━

- 홈페이지 전용 배너 DB
- APP endpoint requireAuth 제거
- slides JSONB
- 신규 migration
- APP 재설계
- 배너 시스템 전면 재설계
- 홈페이지 전체 리팩터링
- unrelated 작업

구조상 실제 blocker가 없으면:

구현
→ 최소 QA
→ API 필요 시 Production 배포
→ swimnote-web Production 배포
→ LIVE 확인

까지 진행하십시오.

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은
‘확인하지 못함’이라고 명시하십시오.
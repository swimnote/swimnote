[보낼 곳: APP REPLIT]

[P0 — 서태웅 8월 성장리포트 재발급 전 차단요인 해결]

현재 조사 결과:

대상:
Toykids / 서태웅(active)
student_id:
student_1784491932941_n95eeew89

현재 8월 live report:
gr_eaf237b428de4a32907d8a5e53323d66

현재 report는 이미 최신 pipeline:
- composition_version = 1.0.0
- snapshot_version = 1
- grounding PASS
- framing PASS
- report_content JSONB
- legacy text report 아님

그러나 현재 재발급을 하면 안 되는 이유가 3개 확정됨:

1.
analysis_cutoff_at =
2026-08-24T15:00:00Z
(KST 2026-08-25 00:00)

→ 8/27 학생 일지 2건이 분석에서 제외됨
→ 8월 전체 수업기록이 반영되지 않음

2.
8개 section 중 3개 empty:
- teacher_guidance
- success_conditions
- behavioral_strengths

3.
PUBLISHED report인데 notifications = 0건
→ 성장리포트 발급 push 경로 검증 필요

이번 단계에서는 기존 8월 report 삭제/재발급 금지.
먼저 위 3개 차단요인의 정확한 원인을 확정하고
필요한 최소 수정만 수행한다.

━━━━━━━━━━━━━━━━━━━━━━
1. analysis_cutoff_at 원인 추적
━━━━━━━━━━━━━━━━━━━━━━

서태웅 2026-08 report의:

analysis_cutoff_at =
2026-08-24T15:00:00Z

이 값이 어디서 생성되었는지 exact trace.

확인:

- growth report cycle 생성
- scheduler
- snapshot builder
- eligibility
- report period 계산
- timezone 변환

반드시 보고:

A.
왜 2026-08 report가
8월 말(8/31 23:59:59 KST)이 아니라
8/25 00:00 KST에서 cutoff 되었는가

B.
현재 공식 무료 성장리포트 정책:
"전월 전체 수업기록 기반,
다음 달 5일 발급"

과 코드가 일치하는지 YES/NO

C.
정상 2026-08 report의 expected cutoff가
정확히 언제여야 하는지

중요:
임의로 cutoff만 DB UPDATE하지 말 것.
생성 로직 원인을 먼저 수정해야 함.

━━━━━━━━━━━━━━━━━━━━━━
2. 8/27 데이터 포함 시 Snapshot 재계산
━━━━━━━━━━━━━━━━━━━━━━

코드 수정 없이 dry-run 또는 비영구 테스트가 가능하면:

서태웅 8월 전체 데이터:

- student diary 4건
- class diary 2건

을 사용했을 때
snapshot/evidence가 어떻게 달라지는지 확인.

특히:

- fact count
- observation count
- claim count
- section input availability

을 비교.

기존:
cutoff 이전 student diary 2
class diary 1

전체 8월:
student diary 4
class diary 2

━━━━━━━━━━━━━━━━━━━━━━
3. 빈 3개 section 원인 분리
━━━━━━━━━━━━━━━━━━━━━━

현재 empty:

- teacher_guidance
- success_conditions
- behavioral_strengths

각 section마다 정확히 판정:

A.
source data 부족 때문에 empty

B.
snapshot/fact mapping 누락

C.
AI ENGINE composition 로직 누락

D.
output schema/persistence 누락

E.
UI parse 문제

각 section의:

source facts
→ engine input
→ engine output
→ persisted report_content

경로를 trace.

8/27 데이터까지 포함하면
실제로 채워질 수 있는지도 확인.

단:
근거 없는 내용으로 억지로 section을 채우는 방식 금지.

━━━━━━━━━━━━━━━━━━━━━━
4. notification 0건 원인 추적
━━━━━━━━━━━━━━━━━━━━━━

현재 report는:

product_status=PUBLISHED
published_at 존재

그런데 notifications row = 0.

확인:

notifyGrowthReportPublished()

가 실제 publish path에서 호출되는지 exact trace.

확인:

- 함수 호출 여부
- 호출 조건
- parent_user_id
- push token 유무
- notification INSERT 여부
- push provider 호출 여부
- 실패 시 error handling
- auto-publish path와 manual publish path 차이

판정:

N1. notification 함수 미호출
N2. parent/token 조건 불충족
N3. INSERT 실패
N4. push 실패
N5. auto-publish path 누락
N6. 기타

━━━━━━━━━━━━━━━━━━━━━━
5. PDF 상태
━━━━━━━━━━━━━━━━━━━━━━

현재 조사에서 PDF 미구현으로 보고됨.

코드 전체에서 성장리포트 PDF 저장/export 구현 여부를 다시 확인.

판정:

A.
실제 구현돼 있으나 현재 report에 artifact 없음

B.
UI만 있고 backend artifact 생성 없음

C.
완전 미구현

현재 공식 제품 정책:
AI 성장리포트는 앱 내 제공 + PDF 저장 가능.

출시 전 추가 구현이 필요한지 YES/NO 보고.

이번 단계에서 PDF 기능 신규 구현은 아직 하지 말 것.
상태만 확정.

━━━━━━━━━━━━━━━━━━━━━━
6. 수정 원칙
━━━━━━━━━━━━━━━━━━━━━━

이번 단계에서 수정이 필요한 경우:

- cutoff 계산 버그
- section mapping/persistence 명백한 버그
- notification publish path 누락

만 최소 수정 허용.

금지:

- 기존 서태웅 report 삭제
- 재발급 실행
- source diary 수정
- DB 수동 cutoff 변경
- 근거 없는 section 강제 생성
- eligibility 우회
- duplicate constraint 변경

━━━━━━━━━━━━━━━━━━━━━━
7. 회귀 테스트
━━━━━━━━━━━━━━━━━━━━━━

최소 확인:

- 8월 report → 8월 전체 데이터 포함
- 9월 report → 9월 전체 데이터 기준 계산
- timezone = Asia/Seoul 기준 정상
- 다음 달 5일 발급 정책과 일치
- 기존 다른 report period 깨지지 않음
- Grounding PASS 유지
- duplicate prevention 유지

━━━━━━━━━━━━━━━━━━━━━━
8. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

1. 8월 cutoff가 8/25로 잡힌 정확한 원인
2. 정상 expected cutoff
3. cutoff 로직 수정 필요 YES/NO
4. 수정 파일
5. 8/27 포함 후 usable source count
6. 3개 empty section별 원인 A/B/C/D/E
7. 전체 8월 데이터 사용 시 3개 section 생성 가능 여부
8. notification 0건 원인 N1~N6
9. notification 수정 필요 여부
10. PDF 상태 A/B/C
11. PDF 출시 전 구현 필요 여부
12. 테스트 결과
13. commit SHA (수정한 경우)
14. Production deploy/OTA 필요 여부
15. 서태웅 8월 report 삭제+재발급 진행 가능 YES/NO

완료 후 STOP.
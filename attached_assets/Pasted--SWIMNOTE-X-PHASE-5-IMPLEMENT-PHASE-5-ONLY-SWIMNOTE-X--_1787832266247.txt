[SWIMNOTE X 관리자 운영허브 — PHASE 5 IMPLEMENT]

승인된 전체 계획 중
이번 작업은 PHASE 5 ONLY.

대상:
SWIMNOTE X 관리
route:
`/(admin)/x-hub`

중요:
PHASE 6 시작 금지.
계획 변경 금지.
기능 추가 금지.
이번 PHASE 계약 범위만 구현.

━━━━━━━━━━━━━━━━━━━━━━
0. PHASE 5 고정 원칙
━━━━━━━━━━━━━━━━━━━━━━

이 화면은
X모드 전체 운영 관제판.

목표:

- X 활성/구독 상태
- 설정 완성도
- 오늘 확인할 것
- AI 일지 / 성장추적 / Parent AI / 리포트 / 커리큘럼 상태
- 학생 / 선생님 / 학부모 연결 현황
- AI 사용량 / 비용 / 저장공간
- 주요 X 기능 빠른 진입

중요:

관리자용 fake 상태 생성 금지.
실제 사용량과 무관한 feature flag만으로 "정상" 표시 금지.

모든 수치는
실제 X 기능이 사용하는 source of truth에서 가져올 것.

━━━━━━━━━━━━━━━━━━━━━━
1. 실제 SOURCE OF TRUTH
━━━━━━━━━━━━━━━━━━━━━━

X 상태:

- GET /billing/x-subscription-status
- GET /x-setup/status
- resolveEffectiveXEntitlement
- x_subscription_slots
- swimming_pools X 관련 컬럼

AI 일지:

- class_diaries
- PHASE 3에서 실제 구현한 diary-hub와 동일 기준

성장추적:

- growth_events

Parent AI:

- event_logs
- category='AI'
- metadata.feature='parent_curriculum_search'

리포트:

- growth_reports
- PHASE 2 report-hub와 동일 기준

커리큘럼:

- student_curriculum_assignments
- PHASE 4 curriculum-hub와 동일 기준

운영 인원:

- students
- users/teacher
- parent/student 실제 연결 구조

AI 사용량/비용:

- event_logs
- metadata.estimated_cost_usd
- 또는 현재 production에 실제 기록되는 동일 cost field

저장공간:

- 기존 GET /admin/storage

구현 전에 필요한 실제 컬럼/response key만 targeted 확인.

광범위한 재조사 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 매우 중요한 EQUALITY 원칙
━━━━━━━━━━━━━━━━━━━━━━

PHASE 5에서 표시하는 수치가
기존 허브와 따로 계산되면 안 됨.

반드시 아래 동일성 유지:

x-hub 리포트 수
=
report-hub 발행완료 수와 동일 기준

x-hub AI 일지 수
=
diary-hub 실제 일지 기준과 동일 기준

x-hub 커리큘럼 배정 수
=
curriculum-hub 배정 학생 수와 동일 기준

Parent AI 수
=
PHASE 4 Parent AI 이번 달 검색 건수와 동일 기준

가능하면 같은 SQL/helper/API aggregation 재사용.

복제된 서로 다른 계산식 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. X 상태 카드
━━━━━━━━━━━━━━━━━━━━━━

상단 최우선 영역.

표시:

- X 활성 상태
- 구독 상태/tier
- 사용 시작일
- 만료일
- 설정 완성도

사용 시작일 기준:

1순위:
x_subscription_slots.purchased_at

fallback:
swimming_pools.xmode_purchased_at

임의 날짜 생성 금지.

구독/entitlement 판정은
기존 resolver/API 그대로 사용.

━━━━━━━━━━━━━━━━━━━━━━
4. "오늘 확인할 것"
━━━━━━━━━━━━━━━━━━━━━━

실제 운영자가 바로 확인할 문제만 표시.

현재 정확히 계산 가능한 것만 사용.

후보:

1. 검토 대기 리포트
= growth_reports.product_status='REVIEW_REQUIRED'

2. 미배정 학생
= PHASE 4 curriculum-hub와 동일한 실제 기준

3. AI 일지 문제
현재 ai_status 영속 데이터가 없으므로
"AI 실패" 표시 금지.

4. 미작성 일지
PHASE 3에서 계산 불가 확정했으므로
표시 금지.

따라서 V1 "오늘 확인할 것"은:

- 검토 대기 리포트 N
- 미배정 학생 N

정확한 데이터만.

숫자 맞추려고 항목 추가 금지.

0건이면:
"현재 확인할 항목이 없습니다"

표시 가능.

━━━━━━━━━━━━━━━━━━━━━━
5. 기능 상태 5개
━━━━━━━━━━━━━━━━━━━━━━

섹션:

"X 기능 현황"

5개 row:

1. AI 일지
2. 성장추적
3. Parent AI
4. 리포트
5. 커리큘럼

중요:
"정상/오류" fake 판정 대신
실제 활동 수치를 표시.

━━━━━━━━━━━━━━━━━━━━━━
6. AI 일지 row
━━━━━━━━━━━━━━━━━━━━━━

현재 PHASE 3에서는
ai_status 영속 정보가 없음.

따라서 "AI 생성 N건" 표시 금지.

실제 가능한 값:

- 오늘 일지 N건
또는
- 이번 주 일지 N건

PHASE 3 diary-hub와 동일 기준 사용.

권장:

`오늘 일지 N건`

tap:
→ `/(admin)/diary-hub`

AI 여부를 추정하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
7. 성장추적 row
━━━━━━━━━━━━━━━━━━━━━━

실제:

growth_events

기준.

표시:

`이번 주 성장 이벤트 N건`

is_invalidated=false 기준 사용.

다른 KPI 추가 금지.

tap:

기존
`/(admin)/x-growth`

재사용.

━━━━━━━━━━━━━━━━━━━━━━
8. Parent AI row
━━━━━━━━━━━━━━━━━━━━━━

실제 PHASE 4 기준 그대로.

event_logs:

category='AI'
metadata.feature='parent_curriculum_search'

표시:

`이번 달 검색 N건`

tap:

현재 별도 관리자 Parent AI 상세 화면이 없으면
임의 신규 화면 생성 금지.

V1에서는:

- row tap 없음
또는
- 기존 event log / audit view가 실제 재사용 가능하면 연결

연결 가능한 화면이 없다면
disabled/non-chevron informational row로 구현.

━━━━━━━━━━━━━━━━━━━━━━
9. 리포트 row
━━━━━━━━━━━━━━━━━━━━━━

실제:

growth_reports

표시:

`이번 달 발행 N건`

기준:

product_status='PUBLISHED'
또는
published_at 기준

중요:
PHASE 2 report-hub와 같은 기준으로 맞출 것.

tap:

→ `/(admin)/report-hub`

━━━━━━━━━━━━━━━━━━━━━━
10. 커리큘럼 row
━━━━━━━━━━━━━━━━━━━━━━

실제:

student_curriculum_assignments

표시:

`배정 학생 N명`

기준:

active assignment
DISTINCT student_id

PHASE 4 curriculum-hub와 동일.

tap:

→ `/(admin)/curriculum-hub`

━━━━━━━━━━━━━━━━━━━━━━
11. 운영 현황
━━━━━━━━━━━━━━━━━━━━━━

섹션:

"운영 현황"

표시:

- 학생 N명
- 선생님 N명
- 학부모 연결 N명

중요:
단순 숫자만 표시.

tap 시:

학생:
기존 관리자 학생관리 route 재사용

선생님:
기존 관리자 선생님/권한/반 관리 route 중
실제 적합한 화면 재사용

학부모:
기존 학부모 연결/초대 관리 route가 있으면 재사용

새로운 Entity 관리 화면 생성 금지.

실제 route 없으면 해당 row는 정보형으로 유지.

━━━━━━━━━━━━━━━━━━━━━━
12. 학생 수 기준
━━━━━━━━━━━━━━━━━━━━━━

학생 수는
현재 재원 기준.

기존 실제 active student 판정 재사용.

예:

student_class_history.left_at IS NULL

단 PHASE 4에서 사용한 동일 기준 재사용.

다른 기준 새로 만들지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
13. 선생님 수 기준
━━━━━━━━━━━━━━━━━━━━━━

현재 수영장에 실제 연결된 teacher 수.

현재 users/teacher relation의
기존 admin 로직 재사용.

퇴사/비활성 교사가 있다면
기존 active 판정 사용.

임의 role='teacher' COUNT만 해서
다른 pool 교사 포함 금지.

━━━━━━━━━━━━━━━━━━━━━━
14. 학부모 연결 수 기준
━━━━━━━━━━━━━━━━━━━━━━

학생과 보호자가 실제 연결된
현재 repository의 parent-student relation 기준.

반드시 실제 table/column targeted 확인.

"학부모 계정 전체 수"와
"현재 수영장 연결 학부모 수"를 혼동하지 말 것.

이번 KPI는:

현재 pool 학생에 연결된
DISTINCT 보호자 수

권장.

정확한 relation이 없으면
임의 계산 금지하고 STOP 보고.

━━━━━━━━━━━━━━━━━━━━━━
15. AI 사용량 / 비용
━━━━━━━━━━━━━━━━━━━━━━

섹션:

"AI 사용량"

표시:

- 이번 달 AI 호출 N건
- 이번 달 AI 비용 $N

source:

event_logs

실제 production에서 확인된 cost field:

metadata.estimated_cost_usd

또는 현재 동일 trace에서 사용 중인
실제 production 필드.

반드시:

current pool
+
category='AI'
+
현재 월

기준.

비용 formatting:

소수점 과도하게 노출 금지.

예:
$0.42
$12.30

실제 USD 기준 유지.

원화 환산 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. 저장공간
━━━━━━━━━━━━━━━━━━━━━━

기존:

GET /admin/storage

재사용.

표시:

- 사용량
- 전체 할당량이 실제 API/구독 데이터로 존재하면 %
- 없으면 total_bytes만 표시

임의 quota 생성 금지.

tap:

기존 관리자 설정 저장공간 section으로
정상 route가 있으면 연결.

없으면 정보형.

━━━━━━━━━━━━━━━━━━━━━━
17. 빠른 진입
━━━━━━━━━━━━━━━━━━━━━━

섹션:

"빠른 관리"

버튼:

- X 세팅
- 구독 관리
- AI 학생리포트
- AI 일지피드
- AI 커리큘럼

route:

X 세팅
→ 기존 `/(admin)/x-setup`

구독 관리
→ 기존 `/(admin)/x-subscription`

AI 학생리포트
→ `/(admin)/report-hub`

AI 일지피드
→ `/(admin)/diary-hub`

AI 커리큘럼
→ `/(admin)/curriculum-hub`

새 route 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. 기존 X모드 세팅하기 관계
━━━━━━━━━━━━━━━━━━━━━━

설정 화면의:

"X모드 세팅하기"

는 그대로:

→ x-setup

유지.

x-hub는:

운영 현황 Dashboard

x-setup은:

상세 세팅

역할 분리 유지.

기존 설정 메뉴 삭제/변경 금지.

━━━━━━━━━━━━━━━━━━━━━━
19. SERVER API
━━━━━━━━━━━━━━━━━━━━━━

권장 신규 API:

GET /admin/x-hub/summary

실제 adminRouter가
`/api/admin` 아래 mount.

route 파일 내부 path:

`/x-hub/summary`

production:

`/api/admin/x-hub/summary`

이중 `/admin/admin` prefix 재발 금지.

가능하면 하나의 summary API에서
운영 대시보드 데이터를 반환.

단 기존:

- x-subscription-status
- x-setup/status
- admin/storage

는 그대로 재사용 가능.

서버에서 이 API들을 HTTP self-call 하지 말 것.

필요하면 동일 DB/helper 직접 재사용.

━━━━━━━━━━━━━━━━━━━━━━
20. RESPONSE CONTRACT
━━━━━━━━━━━━━━━━━━━━━━

권장:

{
  x_status: {
    enabled,
    subscription_status,
    tier,
    started_at,
    expires_at,
    setup_completion
  },

  attention: {
    review_required_reports,
    unassigned_students
  },

  features: {
    diaries_today,
    growth_events_week,
    parent_ai_searches_month,
    reports_published_month,
    curriculum_assigned_students
  },

  operations: {
    active_students,
    active_teachers,
    connected_parents
  },

  ai_usage: {
    calls_month,
    estimated_cost_usd_month
  },

  storage: {
    total_bytes,
    ...
  }
}

실제 기존 API 분리가 더 안전하면
client에서 2~3 API 병렬 조회 가능.

다만 같은 KPI를
서로 다른 로직으로 재계산 금지.

━━━━━━━━━━━━━━━━━━━━━━
21. UI 최종 구조
━━━━━━━━━━━━━━━━━━━━━━

`x-hub.tsx`

위에서 아래:

[SubScreenHeader]
SWIMNOTE X 관리

[X 상태]
활성
구독
시작일
만료일
설정 완성도

[오늘 확인할 것]
검토 대기 리포트
미배정 학생

[X 기능 현황]
AI 일지 — 오늘 일지 N
성장추적 — 이번 주 이벤트 N
Parent AI — 이번 달 검색 N
리포트 — 이번 달 발행 N
커리큘럼 — 배정 학생 N

[운영 현황]
학생
선생님
학부모 연결

[AI 사용량]
호출
예상 비용

[저장공간]

[빠른 관리]
X 세팅
구독 관리
AI 학생리포트
AI 일지피드
AI 커리큘럼

━━━━━━━━━━━━━━━━━━━━━━
22. 디자인
━━━━━━━━━━━━━━━━━━━━━━

X 관리자 현재 디자인 규칙 유지.

- Navy header
- White / Neutral body
- Cyan/Mint 최소 accent
- status card 과도하게 크지 않게
- shadow 최소
- hairline divider
- KPI 숫자 중심
- 모바일에서 핵심을 빠르게 스캔 가능

모든 기능을 큰 카드로 만들어
스크롤이 과도하게 길어지지 않도록.

"대시보드"답게 compact하게.

━━━━━━━━━━━━━━━━━━━━━━
23. LOADING / EMPTY / ERROR
━━━━━━━━━━━━━━━━━━━━━━

반드시:

LOADING
- 전체 blank 금지
- compact skeleton

ERROR
- 전체 X Hub 조회 실패
- 다시 시도

PARTIAL ERROR
가능하면:
구독/저장공간 등 일부 API 실패 시
전체 화면을 죽이지 말고 해당 section만 오류 표시.

기존 API 구조상 쉽게 가능할 경우에만.

과도한 error architecture 추가 금지.

━━━━━━━━━━━━━━━━━━━━━━
24. PERMISSION / SECURITY
━━━━━━━━━━━━━━━━━━━━━━

반드시:

- requireAuth
- pool_admin / super_admin
- requireXMode
- getAdminPoolId(req)
- cross-pool 차단

event_logs / students / growth_reports /
class_diaries / assignments / parent relation

모두 current pool 기준.

client pool_id 신뢰 금지.

━━━━━━━━━━━━━━━━━━━━━━
25. TARGETED PRE-CHECK
━━━━━━━━━━━━━━━━━━━━━━

구현 전 딱 아래만 확인:

C1.
X subscription status에서
실제 tier/status/start/end response key

C2.
x-setup status에서
설정 완성도 실제 key

C3.
PHASE 3 diary-hub의
"오늘 일지" 집계 query를 정확히 재사용 가능한지

C4.
PHASE 2 report-hub의
이번 달 published/review_required 기준

C5.
PHASE 4 curriculum-hub의
assigned/unassigned student query 기준

C6.
growth_events 이번 주 count에 필요한
pool/date/is_invalidated 컬럼

C7.
Parent AI event_logs 실제 동일 filter

C8.
active teacher count 실제 relation

C9.
parent-student 연결 실제 table/column

C10.
admin/storage response에서
quota 또는 limit 존재 여부

이 10개만 targeted 확인.

광범위한 재조사 금지.

blocker가 없으면 바로 구현.

정확한 데이터가 없는 항목은
추정하지 말고 해당 항목만 제외하고 보고.

━━━━━━━━━━━━━━━━━━━━━━
26. TEST
━━━━━━━━━━━━━━━━━━━━━━

T1.
X 관리자 → x-hub 정상

T2.
X 활성 상태 실제 entitlement와 동일

T3.
구독 상태/tier 정확

T4.
시작일 정확

T5.
만료일 정확

T6.
설정 완성도 정확

T7.
검토대기 리포트 count
= report-hub 동일 기준

T8.
미배정 학생 count
= curriculum-hub 동일 기준

T9.
오늘 일지 count
= diary-hub 동일 기준

T10.
이번 주 growth_events count 정확

T11.
Parent AI 이번 달 검색 count
= curriculum-hub 동일 기준

T12.
이번 달 발행 리포트
= report-hub 동일 기준

T13.
배정 학생
= curriculum-hub 동일 기준

T14.
학생 수 정확

T15.
선생님 수 정확

T16.
학부모 연결 수 정확

T17.
AI 호출 수 실제 event_logs 동일

T18.
AI 비용 SUM 정확

T19.
저장공간 API 값 동일

T20.
AI 일지 row → diary-hub 정상

T21.
성장추적 row → x-growth 정상

T22.
리포트 row → report-hub 정상

T23.
커리큘럼 row → curriculum-hub 정상

T24.
X 세팅 → x-setup 정상

T25.
구독관리 → x-subscription 정상

T26.
오늘 확인할 것 0건 empty 정상

T27.
cross-pool 차단

T28.
Normal 관리자 영향 없음

T29.
Normal Teacher/Parent 영향 없음

T30.
기존 X setup/subscription 영향 없음

T31.
loading/error 정상

T32.
TS error 0

T33.
production API route JSON 정상

T34.
OTA 정상

━━━━━━━━━━━━━━━━━━━━━━
27. PHASE 5 금지사항
━━━━━━━━━━━━━━━━━━━━━━

금지:

- X 기능별 ON/OFF 신규 구현
- 신규 feature flag
- 새로운 permission 구조
- AI status migration
- diary write 변경
- report generation 변경
- curriculum assignment 변경
- parent AI write 변경
- 새로운 활동로그 시스템
- 새로운 오류통합 시스템
- 새로운 AdminIndex 화면
- 상품화 기능 추가
- PHASE 6 시작

이번 PHASE는
"기존 X 기능의 실제 운영 현황을 한 화면에 정확히 모아 보여주는 것"
만 구현.

━━━━━━━━━━━━━━━━━━━━━━
28. DEPLOY
━━━━━━━━━━━━━━━━━━━━━━

신규 server API가 생기면:

- migration 없는지 확인
- Render production 수동 배포
- health
- `/api/admin/x-hub/summary`
  401/403 JSON 확인
- 가능하면 authenticated 200 contract 확인

앱 변경:

- production OTA
- runtimeVersion 1.6.3 유지

DB migration 필요 판단 시
임의 실행 금지.
STOP 후 보고.

━━━━━━━━━━━━━━━━━━━━━━
29. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[PHASE 5 RESULT]

[PRE-CHECK]
C1~C10

[SERVER]
- 신규/재사용 API
- query source
- permission
- pool isolation
- migration

[X STATUS]
- 활성
- 구독
- 시작일
- 만료일
- 설정완성도

[ATTENTION]
- 검토대기 리포트
- 미배정 학생

[FEATURE STATUS]
- diary
- growth
- Parent AI
- reports
- curriculum

[OPERATIONS]
- students
- teachers
- connected parents

[AI USAGE]
- calls
- cost

[STORAGE]
- API
- 표시값

[CONNECTION]
각 row 실제 route

[EQUALITY]
- x-hub ↔ report-hub
- x-hub ↔ diary-hub
- x-hub ↔ curriculum-hub

동일 query/기준 여부

[UI]
- 구조
- loading/error

[TEST]
T1~T34 PASS/FAIL

[CODE]
- SHA
- 수정 파일
- 신규 파일

[RENDER]
- deploy SHA
- build
- live
- health
- production route

[OTA]
- rollback target
- new OTA ID
- runtimeVersion

[REGRESSION]
- Normal
- 기존 X setup
- 기존 X subscription

완료 후 STOP.

PHASE 6 시작 금지.
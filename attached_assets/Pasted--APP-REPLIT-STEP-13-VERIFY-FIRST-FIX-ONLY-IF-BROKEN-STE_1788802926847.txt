[보낼 곳: APP REPLIT]

[STEP 13 — 수업정산 회귀검증 / VERIFY FIRST, FIX ONLY IF BROKEN]

STEP 12 PASS 완료.

이번 STEP 목표:
관리자 > 수업정산 기능이 기존 설계대로 실제 계산되는지만 빠르게 검증한다.

새 기능 추가 금지.
문제 없으면 코드 수정 없이 PASS.

━━━━━━━━━━━━━━━━━━━━━━
1. 확인 대상
━━━━━━━━━━━━━━━━━━━━━━

관리자 수업정산 화면 및 관련 API/service.

확인:

- 선생님별 수업 단가 설정 위치
- 월별 수업 건수 집계 방식
- 단가 × 인정 수업 수 계산
- 정산 상태
- 제출/확정 흐름
- 월 변경 시 독립 집계

━━━━━━━━━━━━━━━━━━━━━━
2. 핵심 계산 검증
━━━━━━━━━━━━━━━━━━━━━━

예시:

단가 30,000원
인정 수업 20회

→ 600,000원

정확히 계산되는지 확인.

최소 서로 다른 선생님 2~3명으로 검증 가능하면 확인.

실제 Production 데이터는 임의 변경하지 말고
read-only 계산 또는 안전한 test fixture 사용.

━━━━━━━━━━━━━━━━━━━━━━
3. 수업 건수 출처 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 인정 수업 수가 무엇을 기준으로 계산되는지 명확히 확인:

- 완료된 스케줄 자동 집계
- teacher 제출
- 관리자 입력
- 기타 기존 방식

기존 구현이 정상이라면 변경하지 말 것.

특히:

- 휴무일
- 취소 수업
- 보강 수업
- 다른 선생님 대체수업

이 기존 계산정책에 어떻게 반영되는지만 확인.

정책을 새로 만들지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
4. 매출 vs 정산 분리 확인
━━━━━━━━━━━━━━━━━━━━━━

홈의 "매출 확인"

과

관리자의 "수업정산"

이 같은 개념인지 확인.

원칙:

회원 수강료 매출
≠
선생님 지급 정산

현재 UI에서 혼동되는 문구가 있으면
레이블만 최소 수정.

계산 로직은 섞지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
5. 월별 독립성
━━━━━━━━━━━━━━━━━━━━━━

2026-08
2026-09

등 서로 다른 월을 조회했을 때

- 수업 횟수
- 단가
- 총 정산액
- 상태

가 각 월별로 독립적으로 계산되는지 확인.

전월 데이터가 다음달에 잘못 섞이면 수정.

━━━━━━━━━━━━━━━━━━━━━━
6. BASE/X + Teacher 영향
━━━━━━━━━━━━━━━━━━━━━━

Admin:
settlement.tsx

Teacher:
revenue.tsx 또는 별도 정산 화면

사전 감사 결과 별도 화면.

이번 admin 수정이 teacher에 자동 적용되지 않음.

따라서:

- admin 수업정산 검증
- teacher revenue/정산 화면 정상 여부 VERIFY

BASE/X는 공용 운영 기능이므로
둘 다 동일 계산 유지 확인.

━━━━━━━━━━━━━━━━━━━━━━
7. 수정 원칙
━━━━━━━━━━━━━━━━━━━━━━

정상:
→ 수정하지 말고 PASS.

버그 발견 시만 최소 수정.

허용:
- 계산식 오류
- 월 필터 오류
- 잘못된 API mapping
- 레이블 혼동

금지:
- 새 정산 시스템
- DB migration
- schema 변경
- 자동 급여 기능
- 회계 기능 확장
- RevenueCat
- AI ENGINE
- 다른 STEP

━━━━━━━━━━━━━━━━━━━━━━
8. 테스트
━━━━━━━━━━━━━━━━━━━━━━

최소:

1. 단가 설정값 조회
2. 인정 수업 수 조회
3. 단가 × 수업수 계산
4. 서로 다른 월 2개 독립성
5. 선생님 2~3명 계산 비교
6. 휴무/취소/보강 반영 방식 확인
7. BASE/X 회귀
8. teacher 별도 화면 회귀

━━━━━━━━━━━━━━━━━━━━━━
9. OTA
━━━━━━━━━━━━━━━━━━━━━━

코드 수정 없으면 OTA 없음.

앱 수정이 있으면
STEP 12 프론트 수정과 묶어서
iOS production-v2 OTA 1회 발행.

Android OTA 금지.

서버 수정 있으면 main 최소 반영 후 Render LIVE 확인.

━━━━━━━━━━━━━━━━━━━━━━
10. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

짧게:

1. 현재 계산 방식
2. 단가 설정 위치
3. 수업 수 집계 기준
4. 예시 계산 결과
5. 월별 독립성
6. 휴무/취소/보강 반영
7. 매출 vs 정산 역할
8. 수정 필요 여부
9. 수정 파일(있으면)
10. BASE/X 검증
11. teacher 회귀
12. 테스트 결과
13. app/server SHA
14. OTA 여부
15. DB migration 없음
16. STEP 14 미착수
17. PASS/HOLD/FAIL

완료 후 STOP.
```text
[보낼 곳: APP REPLIT]

# WP10 — GROWTH REPORT AUTO ISSUE RECOVERY
# FINAL MASTER WP V6.1
# PLAN FREEZE — 중간 계획 변경 금지

목적:

기존 Growth Report 자동 발급 파이프라인을 재구축하지 않고,
현재 존재하는 batch / analysis / scheduler / eligibility 구조를 재사용하여

- 매월 5일 자동 발급 실패
- scheduler 미실행
- eligibility 누락
- batch 실패
- analysis 실패
- REVIEW_REQUIRED 정체
- 중복 생성
- timezone/date boundary 문제

를 실제로 복구 가능한 상태로 만듭니다.

이번 작업은 WP10만 수행합니다.

WP11 이후 작업 절대 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
0. 최상위 원칙
━━━━━━━━━━━━━━━━━━━━━━

FINAL MASTER WP는 Freeze 상태입니다.

기존 Growth 시스템은 이미 존재합니다.

확인된 파일:

- growth-report-batch-worker.ts
- growth-report-analysis-worker.ts
- growth-report-scheduler.ts
- growth-report-eligibility.ts

절대 새 Growth Report 시스템을 만들지 마십시오.

금지:

- 새로운 worker architecture
- 새로운 Growth pipeline
- 새로운 report product
- AI Engine 재작성
- WP11 이후 작업
- unrelated refactor

작업 방식:

최소 조사
→ 실제 현재 failure path 확인
→ existing worker/scheduler/eligibility 재사용
→ recovery gap만 수정
→ Staging date/mock 검증
→ 완료보고
→ STOP

━━━━━━━━━━━━━━━━━━━━━━
1. Branch
━━━━━━━━━━━━━━━━━━━━━━

Branch:

release/v2.0.0

Required previous commit:

08b5b9a9

시작 전 최소 확인:

- local release HEAD
- origin/release HEAD
- working tree clean
- 08b5b9a9 ancestry 포함

WP1~WP9 재감사 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 공식 무료 성장리포트 정책
━━━━━━━━━━━━━━━━━━━━━━

공식 명칭:

AI 성장리포트

발급일:

매월 5일

대상:

전월 수업기록이 존재하고,
다음 달에도 재등록/계속 수강 상태인 회원.

즉:

지난달 수업내용
+
다음 달 재등록/continuing 확인

이 필요합니다.

퇴원/수강 종료 회원에게
마지막 무료 리포트를 자동 발급하지 않습니다.

유료 리포트:

AI 인사이트 전략 리포트
₩29,000

이번 WP에서 유료 리포트 상품 구조는 수정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. Timezone
━━━━━━━━━━━━━━━━━━━━━━

운영 기준 timezone:

Asia/Seoul

매월 5일 판단이 UTC 기준으로 밀려
4일/6일에 동작하면 안 됩니다.

확인:

- scheduler timezone
- target report month 계산
- previous month boundary
- enrollment/re-registration month 판단

예:

2026-09-05 Asia/Seoul 실행

target report period:
2026-08

eligibility:
2026-09 continuing/re-registered

형태.

Production 날짜 변경 금지.

Staging/test에서 date injection/mock 사용.

━━━━━━━━━━━━━━━━━━━━━━
4. Scheduler
━━━━━━━━━━━━━━━━━━━━━━

현재 growth-report-scheduler를 확인.

필수:

- 매월 5일 정확한 실행 조건
- 같은 달 중복 scheduler 실행 safe
- process restart 후 5일 발급을 완전히 놓치지 않는지
- scheduler heartbeat WP9와 호환

현재 scheduler가:

"5일 정확한 한 시점에만 실행"

해서 process downtime 시 한 달 전체를 놓칠 수 있다면
최소 recovery window를 추가하십시오.

예:

5일 이후 해당 월 batch가 아직 없으면
같은 달 내 재시도 가능.

단 정확한 설계는 현재 existing batch idempotency를 재사용.

새 scheduler system 금지.

━━━━━━━━━━━━━━━━━━━━━━
5. Monthly Batch Idempotency
━━━━━━━━━━━━━━━━━━━━━━

같은 Pool / 같은 report month에 대해
scheduler가 여러 번 실행되어도
동일 대상 Growth batch/report가 중복 생성되면 안 됩니다.

먼저 기존:

- batch unique key
- report unique key
- job status
- ON CONFLICT
- idempotency helper

를 확인.

이미 있으면 재사용.

없다면 현재 schema 안에서 가능한 최소 보완.

새 schema가 정말 필수일 경우에만
canonical migration → Staging → Production 미실행.

runtime DDL 금지.

━━━━━━━━━━━━━━━━━━━━━━
6. Eligibility
━━━━━━━━━━━━━━━━━━━━━━

growth-report-eligibility.ts를 canonical source로 유지.

새 eligibility engine 만들지 마십시오.

검증 대상:

A. 전월 수업기록 있음
+ 다음 달 재등록/continuing
→ ELIGIBLE

B. 전월 수업기록 있음
+ 다음 달 퇴원/종료
→ NOT ELIGIBLE

C. 전월 수업기록 없음
→ NOT ELIGIBLE

D. 새로 등록했지만 전월 수업 없음
→ NOT ELIGIBLE

E. 상태가 연기/휴회 등 현재 공식 lifecycle에서 continuing으로 인정되는지
→ 기존 제품 정책 그대로 사용

임의 lifecycle 재설계 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. Enrollment / Re-registration 기준
━━━━━━━━━━━━━━━━━━━━━━

중요:

"5일 현재 active인가"만 보는 단순 조건으로
공식 정책을 왜곡하지 마십시오.

현재 repository의 canonical enrollment/registration 데이터에서
다음 달 계속 수강/재등록 여부를 판단하십시오.

현재 실제 구조가:

- student status
- payment/enrollment month
- class registration
- monthly membership record

중 무엇을 사용하는지 최소 확인 후
existing canonical rule 재사용.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
8. Batch Worker Recovery
━━━━━━━━━━━━━━━━━━━━━━

기존 growth-report-batch-worker 재사용.

확인:

- FAILED batch 재시도 가능 여부
- RUNNING stale 상태 복구
- 일부 student만 실패한 partial state
- retry count
- duplicate enqueue

목표:

transient failure로 한 달 리포트가 영구 누락되지 않음.

Permanent data-quality failure는
무한 retry하지 않습니다.

━━━━━━━━━━━━━━━━━━━━━━
9. Analysis Worker Recovery
━━━━━━━━━━━━━━━━━━━━━━

기존 growth-report-analysis-worker 재사용.

확인:

- IN_PROGRESS stale
- FAILED
- retryable AI/API error
- permanent validation/data error
- duplicate analysis

Retryable:

bounded retry.

Permanent:

FAILED/REVIEW_REQUIRED 등 기존 상태로 이동.

새 AI reasoning logic 만들지 마십시오.

AI ENGINE 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. REVIEW_REQUIRED
━━━━━━━━━━━━━━━━━━━━━━

현재 REVIEW_REQUIRED 상태 의미를 확인.

이번 WP 목표:

REVIEW_REQUIRED가 영구적으로 묻혀
리포트 발급 누락이 되는지 확인.

필요 최소 운영:

- REVIEW_REQUIRED count
- 오래된 REVIEW_REQUIRED 감지
- Super Admin/ops에서 조회 가능
- legitimate retry/review action이 기존에 있으면 연결

자동으로 임의 승인하거나
AI 결과를 강제로 PASS시키지 마십시오.

Human review가 필요한 상태라면 유지.

WP9 monitoring과 연결 가능한 기존 metric 재사용.

━━━━━━━━━━━━━━━━━━━━━━
11. Failure Classification
━━━━━━━━━━━━━━━━━━━━━━

최소 구분:

RETRYABLE:
- temporary DB error
- temporary OpenAI/API failure
- timeout
- transient worker failure

PERMANENT / REVIEW:
- required source data missing
- invalid report input
- unrecoverable validation
- manual review required

무한 retry 금지.

현재 error code convention 재사용.

━━━━━━━━━━━━━━━━━━━━━━
12. Retry
━━━━━━━━━━━━━━━━━━━━━━

현재 worker에 retry 정책이 있으면 재사용.

없다면 최소:

- bounded attempts
- backoff
- last_error
- retry_at / equivalent

새 external queue 도입 금지.

WP5 push queue를 Growth에 재사용하지 마십시오.

Growth existing worker 유지.

━━━━━━━━━━━━━━━━━━━━━━
13. Stuck Job Recovery
━━━━━━━━━━━━━━━━━━━━━━

WP9에서 이미 감지하는:

- batch RUNNING stale
- analysis IN_PROGRESS stale

를 실제 recovery와 연결.

예:

lease/heartbeat semantics가 있으면
expired lease 후 재claim.

없다면 현재 status/timestamps를 사용해
safe stale reset/retry.

중요:

실제로 아직 정상 처리 중인 job을
두 worker가 동시에 재처리하지 않도록 해야 합니다.

현재 row lock / claim convention 재사용.

━━━━━━━━━━━━━━━━━━━━━━
14. Duplicate Report Prevention
━━━━━━━━━━━━━━━━━━━━━━

같은:

student
+ report_month
+ report_type

에 무료 AI 성장리포트가
2개 생기면 안 됩니다.

현재 unique constraint가 있으면 증명.

없다면:

기존 report lookup/idempotency로 완전 방지가 되는지 검증.

DB constraint 필요 시에만 canonical migration.

Production migration은 실행 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. Partial Batch
━━━━━━━━━━━━━━━━━━━━━━

예:

eligible 100명
→ 95 성공
→ 5 transient fail

상황에서:

95개 성공 리포트를 다시 중복 생성하지 않고
실패 5개만 재처리 가능해야 합니다.

batch 전체를 처음부터 재생성해서
성공 95개 중복 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. Notification / Feed Delivery
━━━━━━━━━━━━━━━━━━━━━━

무료 AI 성장리포트 발급 후:

앱 내 feed 형태 제공
+
기존 push/notification path

현재 구현이 있으면 그대로 재사용.

이번 WP에서 Notice/Push architecture 수정 금지.

발급 성공 후 알림이 duplicate되지 않는지도 최소 확인.

WP5 durable push와 현재 Growth 알림이 이미 연결되어 있다면 재사용.

새 notification path 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
17. PDF
━━━━━━━━━━━━━━━━━━━━━━

무료 성장리포트는 앱 내 피드 제공 + PDF 저장 가능.

현재 PDF generation/storage path가 있으면 regression만 확인.

PDF renderer 재설계 금지.

PPT/유료 전략리포트 작업 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. Manual Recovery Action
━━━━━━━━━━━━━━━━━━━━━━

운영상 Super Admin이 실패한 특정 month/pool batch를
다시 실행해야 할 수 있습니다.

기존 retry/requeue endpoint/action이 있으면 재사용.

없다면 최소 Super Admin-only recovery action 허용 가능.

조건:

- exact pool
- exact report month
- only failed/missing eligible reports
- duplicate-safe
- audit_logs 기록

대형 Growth Admin UI 만들지 마십시오.

Endpoint만으로 충분하면 UI 선행 금지.

━━━━━━━━━━━━━━━━━━━━━━
19. Audit
━━━━━━━━━━━━━━━━━━━━━━

기존 audit_logs 재사용.

Manual recovery가 추가되면:

- actor
- pool
- report_month
- action
- before/result

기록.

자동 scheduler 정상 실행을
모든 student 단위 audit row로 폭발시키지 마십시오.

운영 이벤트 수준으로 bounded.

━━━━━━━━━━━━━━━━━━━━━━
20. Production Safety
━━━━━━━━━━━━━━━━━━━━━━

Production에서:

- 날짜 변경
- scheduler 강제 실행
- Growth report 대량 생성
- report 삭제
- status 강제 변경

절대 금지.

Staging/test fixture로만 검증.

Production read-only evidence가 필요하면
안전한 existing mechanism 사용 가능.

━━━━━━━━━━━━━━━━━━━━━━
21. Date Mock
━━━━━━━━━━━━━━━━━━━━━━

테스트에서 clock/date injection을 사용.

최소:

2026-09-04
→ 아직 August report auto issue 안 함

2026-09-05
→ August report issue

2026-09-06
→ 이미 생성됐으면 duplicate 0
→ 5일 downtime으로 batch 미생성 상태면 recovery 실행 가능

2026-10-05
→ September report issue

Timezone:
Asia/Seoul

시스템 clock 전역 monkey patch로
다른 test를 오염시키지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
22. Required Test Matrix
━━━━━━━━━━━━━━━━━━━━━━

전체 suite 금지.

WP10 관련 tests만.

A.
Sep 4 KST
→ no Aug auto batch

B.
Sep 5 KST
→ Aug eligible batch 생성

C.
Sep 5 scheduler twice
→ duplicate batch/report 0

D.
Sep 6
+ Aug batch already exists
→ duplicate 0

E.
Sep 6
+ Aug batch missing
→ recovery creates missing batch

F.
eligible:
previous-month lessons + next-month continuing
→ included

G.
withdrawn/ended next month
→ excluded

H.
no previous-month lessons
→ excluded

I.
new student with no previous-month lesson
→ excluded

J.
100 eligible / 5 transient fail
→ retry 5 only
→ success 95 duplicate 0

K.
batch stale RUNNING
→ safe recovery

L.
analysis stale IN_PROGRESS
→ safe recovery

M.
transient AI failure
→ bounded retry

N.
permanent validation failure
→ infinite retry 0

O.
REVIEW_REQUIRED
→ preserved / visible

P.
same student+month
→ duplicate report 0

Q.
success notification duplicate 0

R.
manual recovery non-super-admin
→ 403

S.
manual recovery Super Admin
→ failed/missing only

T.
audit log for manual recovery

U.
Asia/Seoul month boundary

V.
Production writes
→ 0

━━━━━━━━━━━━━━━━━━━━━━
23. Existing Worker Reuse Evidence
━━━━━━━━━━━━━━━━━━━━━━

완료보고에 반드시:

growth-report-scheduler:
REUSED / REPLACED

growth-report-batch-worker:
REUSED / REPLACED

growth-report-analysis-worker:
REUSED / REPLACED

growth-report-eligibility:
REUSED / REPLACED

Expected:
모두 REUSED.

필요한 최소 patch는 허용.

완전 replacement는 FAIL unless actual P0 blocker evidence.

━━━━━━━━━━━━━━━━━━━━━━
24. DB / Migration
━━━━━━━━━━━━━━━━━━━━━━

기본 기대:

DB schema changes:
NO

Migration:
NO

기존 batch/report tables 재사용.

만약 unique/idempotency constraint가 실제 없고
코드만으로 안전 보장이 불가능하면:

canonical migration
→ Staging
→ second-run
→ Production 미실행

runtime DDL 금지.

━━━━━━━━━━━━━━━━━━━━━━
25. Monitoring Integration
━━━━━━━━━━━━━━━━━━━━━━

WP9 incident detection은 그대로 유지.

WP10 recovery 후:

condition resolved
→ WP9 current health/incident recovery가 정상 반영

되도록 기존 status를 정확히 갱신.

WP9 구조 재설계 금지.

━━━━━━━━━━━━━━━━━━━━━━
26. 수정 금지
━━━━━━━━━━━━━━━━━━━━━━

- WP11 Admin Notes
- WP12 Marketing
- WP13 Integrity
- WP14 Cleanup
- WP15 Version
- WP16 E2E
- New Growth pipeline
- New report product
- AI Engine 수정
- RevenueCat
- Push architecture
- Notice schema
- Pagination
- Monitoring architecture
- Production date manipulation
- Production report generation
- unrelated refactor

━━━━━━━━━━━━━━━━━━━━━━
27. PASS 완료조건
━━━━━━━━━━━━━━━━━━━━━━

WP10 PASS:

1. existing Growth workers reused
2. monthly 5th KST correct
3. downtime recovery
4. scheduler duplicate-safe
5. eligibility official policy correct
6. batch retry/recovery
7. analysis retry/recovery
8. stale job recovery
9. REVIEW_REQUIRED preserved/visible
10. partial batch retry only failed/missing
11. duplicate report 0
12. success notification duplicate 0
13. Super Admin manual recovery if needed
14. manual recovery audited
15. bounded retry
16. Production date/report mutation 0
17. related tests PASS
18. Commit SHA

━━━━━━━━━━━━━━━━━━━━━━
28. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

# WP10 GROWTH REPORT AUTO ISSUE RECOVERY REPORT

Target:
APP

Branch:
...

HEAD before:
08b5b9a9

HEAD after:
...

origin/release HEAD:
...

Commit SHA:
...

Commit message:
...

Files changed:
...

Existing components:

growth-report-scheduler:
REUSED / REPLACED

growth-report-batch-worker:
REUSED / REPLACED

growth-report-analysis-worker:
REUSED / REPLACED

growth-report-eligibility:
REUSED / REPLACED

Scheduler KST:
PASS / FAIL

Monthly trigger:
...

Downtime recovery:
PASS / FAIL
Implementation:
...

Duplicate scheduler run:
PASS / FAIL

Target report month calculation:
...

Eligibility canonical source:
...

Previous-month lesson required:
PASS / FAIL

Next-month continuing/re-registration required:
PASS / FAIL

Withdrawn/ended excluded:
PASS / FAIL

New student without previous lessons excluded:
PASS / FAIL

Batch retry:
PASS / FAIL

Batch stale recovery:
PASS / FAIL

Analysis retry:
PASS / FAIL

Analysis stale recovery:
PASS / FAIL

Retry limit:
...

Permanent failure infinite retry:
NO

REVIEW_REQUIRED:
...

Partial batch retry:
PASS / FAIL

Successful reports regenerated:
YES / NO

Duplicate report protection:
...

Duplicate reports:
0 / ...

Success notification duplicate:
0 / ...

Manual recovery action:
...

Super Admin only:
PASS / FAIL / NOT REQUIRED

Audit:
PASS / FAIL / NOT REQUIRED

Sep 4 KST:
...

Sep 5 KST:
...

Sep 6 recovery:
...

Oct 5 KST:
...

Production date manipulation:
NO

Production reports generated:
NO

DB schema changes:
YES / NO

Migration:
YES / NO

Staging migration:
PASS / FAIL / NOT REQUIRED

Production DB changes:
NO

WP9 monitoring compatibility:
PASS / FAIL

Tests:
...

Test result:
PASS / FAIL

Render required:
YES / NO

iOS OTA required:
YES / NO

Android OTA:
NO

Native binary required:
NO

Remaining blocker:
NONE / ...

Final:
PASS / HOLD / FAIL

중요:

FINAL MASTER WP는 Freeze 상태입니다.

기존 Growth 파이프라인을 재구축하지 마십시오.

WP10 완료조건을 충족하면
WP11 선행 구현 없이 즉시 STOP하십시오.

Production에서 날짜를 변경하거나
실제 월간 Growth batch를 강제 실행하지 마십시오.
```

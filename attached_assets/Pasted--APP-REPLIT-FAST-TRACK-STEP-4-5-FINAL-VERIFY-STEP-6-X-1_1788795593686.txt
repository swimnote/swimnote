[보낼 곳: APP REPLIT]

[FAST TRACK — STEP 4/5 FINAL VERIFY + STEP 6 X 운영현황]

목표:
1. Render daa8d597 LIVE 즉시 STEP 4/5 Production verify
2. 대기 중 STEP 6 X 운영현황 최소 수정
3. 불필요한 재감사 금지
4. OTA는 아직 발행하지 않음

━━━━━━━━━━━━━━━━━━━━━━
A. STEP 4/5 FINAL VERIFY
━━━━━━━━━━━━━━━━━━━━━━

Render target:
main
daa8d59755a85280241cf6822aaf53f67ea0544e

LIVE 후 실제 Production API 확인:

1.
GET /admin/growth-reports/:id
→ HTTP 200

확인:
- student.name
- class_name
- teacher_name
- report_period
- product_status
- report_content
- 내부 metadata 비노출

2.
GET /admin/growth-reports/monthly-list
→ HTTP 200
→ items에 class_name / teacher_name / analysis_status 정상

3.
앱 navigation 코드/가능하면 실제 화면:
report-hub
→ 학생 row
→ report-detail
→ back
→ report-hub

4.
Dashboard:
- KPI 8개
- 상태 필터
- 검색
- multi-select
- 선택 발송
- 전체 발송 조건
정상 확인

STEP 4 / STEP 5 각각 PASS/HOLD 판정.

━━━━━━━━━━━━━━━━━━━━━━
B. STEP 6 — X 운영현황
━━━━━━━━━━━━━━━━━━━━━━

파일 중심:
app/(admin)/x-hub.tsx

목표:
- navigation back 정상화
- 의미 불명확한 지표 정리
- 실제 운영자가 이해할 수 있는 표현으로 변경

큰 backend 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
C. 빠른관리 Navigation
━━━━━━━━━━━━━━━━━━━━━━

현재:

X 운영현황
→ 커리큘럼
→ AI 리포트
→ AI 일지
→ 성장 이벤트
등 하위화면 진입 후

back이 홈으로 튀는 문제.

목표:

X 운영현황
→ 하위화면
→ back
→ X 운영현황

단:

dashboard 등 다른 곳에서 직접 진입한 경우
기존 back 동작 유지.

권장:
진입 시 backTo=x-hub 같은 param 전달
→ 해당 param이 있을 때만 x-hub로 복귀.

공통 SubScreenHeader 자체를 전역 수정하지 말 것.

수정 대상 후보:
- x-hub.tsx
- curriculum-hub.tsx
- report-hub.tsx
- diary-hub.tsx
- x-growth.tsx

실제 필요한 파일만 수정.

Teacher x-growth는 별도 화면이므로
admin navigation 수정 영향 없는지 VERIFY만.

━━━━━━━━━━━━━━━━━━━━━━
D. X 운영현황 지표 정리
━━━━━━━━━━━━━━━━━━━━━━

현재 의미가 불명확한:

"성장추적"

실제 데이터:
live.growth_events_week

라면 사용자 문구를:

"이번 주 성장 이벤트"
또는
"AI 성장 이벤트(주간)"

처럼 의미가 바로 보이게 변경.

기존:
"성장리포트 발송"
과 구분되도록 함.

관리자에게 보여줄 우선 지표:

- AI 일지
- 성장리포트 발송
- 검수 대기 리포트
- 성장 이벤트
- 커리큘럼 활용 상태

단:
현재 API에 없는 숫자를 새로 만들지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
E. unavailable 경고
━━━━━━━━━━━━━━━━━━━━━━

현재 상단:

"일부 지표를 불러오지 못했습니다 (—)"

가 실제로 어떤 metric 때문에 뜨는지
기존 x-hub summary 응답의 unavailable[]만 확인.

필요한 지표가 null:
→ 해당 카드에 "—" 또는 "데이터 없음"

불필요한 지표:
→ 경고를 전체 화면 상단에 크게 띄우지 않도록 정리.

하나의 보조 지표가 null이라고
전체 X 운영현황이 오류처럼 보이면 안 됨.

서버 API 실패인 경우에만
실제 오류 상태 사용.

━━━━━━━━━━━━━━━━━━━━━━
F. 플랜 표시
━━━━━━━━━━━━━━━━━━━━━━

STEP 1의 NaN 수정 유지.

추가 확인:

- 현재 plan name
- active 상태
- 시작일
- 만료일

이 실제 entitlement/mode 데이터와 맞는지 표시만 검증.

중요:
X semantics 변경 금지.

절대:
mode === 'x' = paid
로 단순화 금지.

canonical resolver 유지.

━━━━━━━━━━━━━━━━━━━━━━
G. 커리큘럼 배정 0명
━━━━━━━━━━━━━━━━━━━━━━

이번 STEP에서는
커리큘럼 개념 재설계 금지.

X 운영현황에
"커리큘럼 배정 0명"
같은 오해 가능한 표현이 있다면

실제 current API 의미를 확인 후
더 중립적인 표현으로만 최소 조정.

커리큘럼 본체는 STEP 12에서 처리.

━━━━━━━━━━━━━━━━━━━━━━
H. BASE/X/Teacher 영향
━━━━━━━━━━━━━━━━━━━━━━

x-hub = X_ONLY.

하위 화면:
report-hub / diary-hub / curriculum-hub는 BASE/X 공유 가능.

따라서 backTo param이 있을 때만
X hub 복귀하도록 조건부 처리.

기본 SWIMNOTE에서 직접 진입했을 때
navigation 회귀 금지.

Teacher:
별도 navigation 회귀 VERIFY.

━━━━━━━━━━━━━━━━━━━━━━
I. 배포
━━━━━━━━━━━━━━━━━━━━━━

STEP 6 frontend-only면:

release/v2.0.0 commit/push

OTA 발행 금지.

이번까지 쌓인 frontend:
STEP 4 + STEP 5 + STEP 6
묶어서 다음 단계 후 1회 OTA 예정.

server 수정이 꼭 필요하면
최소 변경만 보고.
임의 Render deploy 금지.

━━━━━━━━━━━━━━━━━━━━━━
J. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[STEP 4/5]
1. Render LIVE SHA
2. detail API HTTP 결과
3. monthly-list HTTP 결과
4. report navigation 결과
5. KPI/filter/multi-select 결과
6. STEP 4 PASS/HOLD
7. STEP 5 PASS/HOLD

[STEP 6]
8. navigation root cause
9. 수정한 backTo 방식
10. 영향 받은 화면
11. "성장추적" 최종 문구
12. unavailable 처리 결과
13. plan 표시 검증
14. BASE SWIMNOTE navigation 회귀
15. teacher 회귀
16. 수정 파일
17. server 수정 여부
18. 테스트
19. commit SHA
20. OTA 미발행 확인
21. STEP 6 PASS/HOLD/FAIL

완료 후 STOP.
[SWIMNOTE 보강 기능 즉시 복원 — 추가 조사 금지]

중요:

더 이상 원인 조사 반복하지 마십시오.

이미 확인된 회귀를 기존 정상 동작으로 즉시 복원합니다.

이번 작업 목적은 새 설계가 아닙니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 지난 보강 미표시 — 원인 확정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실기기:

전체 1
일반 1
지난 보강 0

CONFIRMED.

기존 정상 코드와 현재 서버 비교에서:

기존 정상:
pool 내 waiting 보강 반환
teacher ownership 필터 없음

현재:
original_teacher_id
handed_to_teacher_id
co_teacher_ids

기준의 teacher ownership 필터 추가

가 이미 확인됐습니다.

이 필터 때문에 기존 지난 보강이 현재 선생님에게
반환되지 않는 회귀가 발생하고 있습니다.

더 이상 이 원인을 다시 조사하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. /teacher/makeups 정상 동작 복원
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

대상:

artifacts/api-server/src/routes/teachers.ts

GET /teacher/makeups?status=waiting

에서 기존 보강관리 정상 정책으로 복원합니다.

조건:

swimming_pool_id = 현재 pool

cancelled_at IS NULL

status IN ('waiting','expired')

을 기준으로 반환.

현재 추가된:

original_teacher_id = userId
handed_to_teacher_id = userId
co_teacher_ids teacher ownership 제한

때문에 기존 보강이 누락된다면
이 제한을 기존 정상 동작 수준으로 복원하십시오.

중요:

다른 /teacher API 권한 로직은 건드리지 마십시오.

오직 보강 목록 GET handler의
회귀된 ownership filtering만 최소 수정.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. expired 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

지난달 미처리 보강은 다음 달에도 반드시 남습니다.

waiting 또는 expired 상태 모두 보강 대기 화면에 반환.

클라이언트에서는 현재 이미:

firstExpiredIdx
expiredCount
separator
expired card

가 있으므로 다시 수정하지 마십시오.

APP makeups.tsx의 expired UI 추가 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 날짜/회차 정책
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

확정 정책:

KST today -14일
~
KST today +28일

today 포함.

미래 결석 선보강 허용.

예:

결석 예정 8/20
보강 8/17

→ 허용.

현재 공용:

getMakeupDateRange
validateMakeupDateRange

정책은 유지.

absence_date 이후만 허용하는 조건 금지.
expire_at까지만 허용하는 조건 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. eligible-occurrences 복원
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 실기기:

"수업 회차를 불러오지 못했습니다"

발생.

정상 동작에서는 보강반 선택 후
-14/+28 범위의 실제 회차가 표시되어야 합니다.

대상:

GET /teacher/makeups/:makeupId/eligible-occurrences

정상 조건:

1. makeup_session 존재
2. 같은 swimming_pool_id
3. 선택 class_group 존재
4. 같은 swimming_pool_id
5. 실제 수업 회차 조회
6. -14/+28 범위 적용
7. occurrences[] 반환

현재 정상적으로 존재하는 반을
불필요한 조건으로 CLASS_NOT_FOUND 처리하는 부분이 있다면
기존 정상 동작에 맞게 최소 복원하십시오.

특히 다음 조건 때문에
일반 정상 반이 제외되는지 확인 후 바로 수정:

cg.is_deleted
cg.is_one_time

단:

삭제된 반을 허용하라는 뜻이 아닙니다.

기존 정상 보강반 선택 목록에서 선택 가능한 반과
eligible-occurrences가 인정하는 반의 조건이
서로 달라서 실패한다면 두 조건을 동일하게 맞추십시오.

새 정책을 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. doAssign CONFIRMED 버그 유지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이미 수정된:

onPress={() => doAssign()}

유지.

다시 되돌리지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 진단 UI 제거
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 원인 확인용:

진단: 전체 N · 일반 N · 지난 보강 N

표시는 최종 복구 후 제거합니다.

HTTP 오류 상세 진단도
최종 기능 정상 확인 뒤 제거.

이번 코드 구현에서는 제거해도 되지만
기능 복구 코드와 혼동하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 절대 동결
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

절대 수정 금지:

parent/notifications.tsx
parent/requests.tsx
teacher/messages-inbox.tsx

parent_student_requests
notifications
parent_messages
parent_conversations
diary_messages

link-result
notify-result

SessionContext
auth
JWT
apiRequest
AI
결제
구독

학부모 요청/알림/선생님 메신저:
0줄 변경.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. DB 수정 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 7월 보강 데이터를 현재 필터에 맞추기 위해:

original_teacher_id UPDATE
handed_to_teacher_id UPDATE
status UPDATE
expire_at UPDATE

절대 하지 마십시오.

DB 데이터를 고치는 것이 아니라
회귀된 조회 정책을 정상화합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 허용 수정 파일
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

우선 허용:

artifacts/api-server/src/routes/teachers.ts

필요한 경우에만:

artifacts/swim-app/app/(teacher)/makeups.tsx

makeup-date-range.ts는 이미 정상 정책이므로 수정 금지.

2개 초과 파일 수정 필요하면 즉시 중단.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 이번에는 실제 코드 수정까지 실행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

추가 조사 보고만 하고 멈추지 마십시오.

이미 확인된 회귀:

A.
teacher ownership filter 때문에 지난 보강 누락

B.
eligible class 조회 조건과 실제 선택 가능한 class 조건 불일치 가능

를 기존 정상 동작 기준으로 최소 수정하십시오.

수정 후:

git diff --name-status
git diff --stat

확인.

Commit / Push / Render / OTA는 아직 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 완료 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음만 제출:

1. /teacher/makeups에서 제거/복원한 정확한 WHERE 조건
2. expired 반환 조건
3. eligible-occurrences 실패의 직접 조건
4. 정상 class 선택 조건과 eligible class 조건 차이
5. 수정한 코드
6. -14/+28 유지 확인
7. 8/20 결석 → 8/17 보강 허용 확인
8. doAssign fix 유지 확인
9. 수정 파일
10. git diff --stat

무결성:

학부모 요청 변경 0
학부모 알림 변경 0
선생님 메신저 변경 0
DB 변경 0

Commit 없음
Push 없음
Render 없음
OTA 없음

보고 후 멈추십시오.
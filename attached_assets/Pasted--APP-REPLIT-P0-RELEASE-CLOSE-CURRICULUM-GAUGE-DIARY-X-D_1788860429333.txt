[보낼 곳: APP REPLIT]

[P0 RELEASE CLOSE — CURRICULUM / GAUGE / DIARY / X DASHBOARD 실제 연결]

커리큘럼 서버 구조/Importer/One-click 작업은 PASS 완료.
추가 구조 변경, taxonomy 작업, review 작업 금지.

지금부터 목표는 실제 Production 앱 화면 정상화 하나뿐이다.

Toykids:
pool_id = pool_1780849364252_l9k44rbk3
active Local = cv_3052fb8454754cadb917379eb85cffda
items = 332
status = READY

━━━━━━━━━━━━━━━━━━━━━━
1. 커리큘럼 화면
━━━━━━━━━━━━━━━━━━━━━━

실제 Production API + 앱 화면에서 확인/수정:

- 커리큘럼 메뉴 클릭 정상
- active Pool Local 332개 조회
- 레벨/목록 표시
- 상세 클릭 정상
- HTTP 500 없음
- server error 없음
- NaN/undefined 없음
- Global 50을 Local 목록으로 표시하지 않음

문제 있으면 즉시 최소 수정.

━━━━━━━━━━━━━━━━━━━━━━
2. 관리자/학부모 진도 게이지
━━━━━━━━━━━━━━━━━━━━━━

canonical SCP 기준으로 연결.

실제 Toykids 학생 최소 3명 확인:

student_curriculum_progress
→ API
→ Admin gauge
→ Parent gauge

확인:

- 퍼센트 정상 숫자
- 화면 정상 표시
- NaN 없음
- 500 없음
- Local Curriculum 기준
- Global은 progress denominator/numerator에 직접 포함 금지

기존 SCP가 과거 version 기준이어도 crash 금지.
데이터 삭제/초기화/backfill 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. 일지
━━━━━━━━━━━━━━━━━━━━━━

Teacher Diary 실제 Production 확인:

Manual Diary
AI Diary

둘 다 현재 active Local Curriculum을 candidate source로 사용.

AI Diary 실제 1건 E2E:

생성
→ 화면에 AI 생성 내용 표시
→ 학생별 내용 정상
→ 저장
→ 다시 열기
→ 동일 내용 유지

반드시 확인:

teacher input 원문만 저장되는 회귀 없음.

그 후:

Local match
→ growth_event
→ CPO
→ SCP

실제 1건 연결 확인.

━━━━━━━━━━━━━━━━━━━━━━
4. X Dashboard
━━━━━━━━━━━━━━━━━━━━━━

Toykids X Dashboard 실제 Production 확인.

특히:

progress_students_total
progress_students_confirmed
progress_avg_pct

실제 정상 숫자 표시.

기존 assignment-count 기반 값 사용 금지.

커리큘럼/진도 관련 카드 클릭 시
정상 커리큘럼 또는 진도 화면으로 연결.

NaN
500
server error
빈 잘못된 화면

전부 0.

━━━━━━━━━━━━━━━━━━━━━━
5. 관련 화면 서버오류 제거
━━━━━━━━━━━━━━━━━━━━━━

실제 호출:

A. X Dashboard
B. Curriculum
C. Curriculum Detail
D. Admin Gauge
E. Parent Gauge
F. Teacher Diary
G. AI Diary 생성/저장/reopen
H. Member Detail의 progress 영역

발견한 오류는 보고만 하지 말고:

fix
→ test
→ deploy
→ smoke

까지 즉시 처리.

광범위 audit/리팩터링 금지.

━━━━━━━━━━━━━━━━━━━━━━
6. 배포
━━━━━━━━━━━━━━━━━━━━━━

Server 변경:
test
→ main push
→ Render 자동배포
→ LIVE 확인
→ /health SHA == main

APP JS/TS 변경:
release/v2.0.0 반영
→ production preflight
→ iOS production-v2 OTA

Android OTA 금지.

실제 Production iOS build 기준:
buildNumber 255
runtimeVersion 2.1.0

━━━━━━━━━━━━━━━━━━━━━━
7. PASS 조건
━━━━━━━━━━━━━━━━━━━━━━

아래가 실제 동작해야 PASS:

- 커리큘럼 클릭 → 정상
- Local 332 표시
- 커리큘럼 상세 → 정상
- Admin gauge → 정상
- Parent gauge → 정상
- Teacher Diary → 정상
- AI Diary 생성/저장/reopen → 정상
- Diary → growth_event → CPO → SCP 연결
- X Dashboard 진도 KPI → 정상
- HTTP 500 = 0
- server error UI = 0
- NaN = 0

━━━━━━━━━━━━━━━━━━━━━━
8. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

짧게 아래만 보고:

1. Curriculum 화면
2. Curriculum detail
3. Admin gauge
4. Parent gauge
5. Teacher Diary
6. AI Diary save/reopen
7. growth_event/CPO/SCP E2E
8. X Dashboard 실제 KPI
9. HTTP 500 수
10. server error UI 수
11. 수정 파일
12. main SHA / Render LIVE SHA
13. OTA ID
14. 실기기 확인 결과
15. 잔존 blocker

완료 후 STOP.
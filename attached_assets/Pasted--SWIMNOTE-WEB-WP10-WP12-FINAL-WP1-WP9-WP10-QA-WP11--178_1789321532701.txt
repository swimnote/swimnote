[SWIMNOTE WEB — WP10~WP12 FINAL 통합 작업]

현재 WP1~WP9까지 완료됐다.

이번 작업 목표:

WP10 전체 QA
WP11 최종 수정
WP12 Production 배포 준비

까지 한 번에 완료한다.

중요:
이번 작업에서는 실제 Production 배포하지 않는다.
최종 Preview와 검증 결과를 대표에게 보고한 뒤
대표가 명시적으로 승인해야 Production 배포한다.

━━━━━━━━━━━━━━━━━━━━━━
0. 절대 인프라 기준
━━━━━━━━━━━━━━━━━━━━━━

SWIMNOTE APP/Web Production 기준:

Repository:
https://github.com/swimnote/swimnote

Canonical branch:
main

Render service:
swimnote-api

Production API:
https://swimnote-api.onrender.com

절대 금지:

- GitLab 연결
- AI ENGINE repo 접근
- Render service 변경
- Production branch 변경
- deploy-photo-clone 복귀
- DB migration
- API schema 변경
- Auth 재설계
- Production deploy

APP = GitHub main → swimnote-api

이 기준을 변경하지 마라.

━━━━━━━━━━━━━━━━━━━━━━
1. FINAL 전체 ROUTE QA
━━━━━━━━━━━━━━━━━━━━━━

다음 public routes 전부 검사:

/
/swimnote
/swimnote-x
/swimnote-office
/ai
/technology
/technology#investment
/patents
/company
/support
/contact
/terms
/privacy

추가 보호 route:

/login
/admin
/admin/*
/super/*
/pool/:id
/:slug
/delete-account

확인:

- 404/오매칭 없음
- 신규 public route가 /:slug로 빠지지 않음
- 실제 PoolHomepage slug 정상
- /pool/:id 정상
- 모든 Header link 정상
- Footer link 정상
- ⋯ Menu link 정상
- browser back/forward 정상
- hash anchor 정상

━━━━━━━━━━━━━━━━━━━━━━
2. VISUAL FINAL QA
━━━━━━━━━━━━━━━━━━━━━━

최우선 기준:

"Apple 같은 절제된 고급스러움"

전 페이지를 한 사이트처럼 검수.

확인:

- font 통일
- font weight 통일
- headline 크기 과도하지 않음
- button shape 통일
- border radius 통일
- neutral color 중심
- excessive blue 없음
- gradient/glow/neon 없음
- shadow 과도하지 않음
- giant empty whitespace 없음
- 카드 난립 없음
- 화면마다 다른 SaaS template 느낌 없음
- 실제 Product UI가 주인공
- SWIMNOTE와 X visual weight 동일

특히 HOME은:

HEADER
→ SWIMNOTE
→ SWIMNOTE X
→ FOOTER

구조가 단순하고
불필요한 section이 다시 생기지 않았는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
3. RESPONSIVE FINAL QA
━━━━━━━━━━━━━━━━━━━━━━

반드시 실제 다음 width 확인:

1440
1280
1024
834
768
430
390
375

공개 페이지 전부.

특히 확인:

Header:
- 항상 1줄
- 제품 tab scroll 정상
- ⋯ 정상
- PC 버튼 정상
- 잘림 없음

Body:
- horizontal overflow 없음
- text clipping 없음
- image overflow 없음
- FAQ 정상
- accordion 정상
- footer 정상

HOME:
모바일에서 지나치게 긴 페이지가 되지 않음.

━━━━━━━━━━━━━━━━━━━━━━
4. PUBLIC DATA / PRIVACY AUDIT
━━━━━━━━━━━━━━━━━━━━━━

웹 공개 source/assets 전체를 검색한다.

다음이 외부 페이지에 노출되는지 확인:

- 실제 회원 이름
- 실제 학부모 이름
- 전화번호
- 개인 휴대전화
- 학생 연락처
- 실제 운영 메모
- JWT/token
- 내부 API secret
- 실제 수영장 민감 운영정보
- test account credential

특히:

app-admin.jpeg

는 현재 HOME에서는 사용하지 않지만
public static asset으로 그대로 외부 접근 가능한 상태인지 확인.

민감정보가 실제 포함된 asset이라면:

- 단순히 화면에서 숨기는 것으로 끝내지 말고
- public web asset 경로에서 제거하거나
- 안전한 asset으로 교체

한다.

단 APP 모바일에서 실제로 필요한 asset이면
영향 범위를 먼저 확인하고 무작정 삭제하지 않는다.

이번 QA 결과에 명확히 보고.

또 전체 Web source에서:

010-7787-1507
swimnote.admin@gmail.com

검색.

공개 홈페이지에서 의도치 않게 남아 있으면 제거.

━━━━━━━━━━━━━━━━━━━━━━
5. CONTACT 페이지 FINAL 판단
━━━━━━━━━━━━━━━━━━━━━━

현재 /contact에는
웹 문의 저장 API가 없어
"도입 문의 준비 중" 상태다.

이번 WP에서 새 DB/API는 만들지 않는다.

따라서:

A.
현재 repo에 실제 사용 가능한
기존 비공개/안전한 도입문의 기능이 발견되면
그것만 재사용.

B.
없다면 현재 상태를 유지하되
Production blocker로 명확히 보고.

가짜 문의 form 생성 금지.
작동하지 않는 submit button 금지.
개인 휴대전화 공개 금지.
개인 이메일 공개 금지.

대표가 최종 채널을 결정할 수 있게
현 상태만 정확히 보고.

━━━━━━━━━━━━━━━━━━━━━━
6. TERMS / PRIVACY FINAL AUDIT
━━━━━━━━━━━━━━━━━━━━━━

현재 /terms와 /privacy는
앱 기존 원문 재사용 상태.

이번에 확인:

- 실제 앱 현재 원문과 웹 내용 동일 여부
- 시행일
- 현재 서비스 구조와 명백히 충돌하는 오래된 표현 존재 여부
- X / AI / 웹 Dashboard 등 현재 서비스와 관련해
  기존 정책에서 누락된 것이 명백한지

중요:

법률 내용을 임의 작성하거나
AI가 새로운 약관을 추가하지 않는다.

내용이 오래됐거나 법률 검토가 필요하면:

"대표/법률 검토 필요"

로 보고.

임의 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
7. LOGIN / SUPER ADMIN DEVICE GUARD P0
━━━━━━━━━━━━━━━━━━━━━━

현재 Login.tsx 전체에 MobileGuard가 들어갔다.

이 부분을 반드시 실제 auth flow 기준으로 재검토한다.

확인:

- /login이 pool_admin과 super_admin이 모두 사용하는 공용 entry인지
- super_admin/TOTP가 동일 /login을 사용하는지
- super_admin이 모바일 접근을 기존에 필요로 하는 구조인지

목표:

Pool Admin PC Dashboard:
- Phone BLOCK
- iPad BLOCK
- Android tablet BLOCK
- Desktop/Laptop ALLOW
- Touch Windows laptop ALLOW

그러나:

Super Admin/TOTP 기존 흐름을
새 MobileGuard 때문에 깨뜨리면 안 된다.

현재 Login.tsx에 blanket MobileGuard가
super_admin 기존 flow까지 차단한다면
최소 범위로 수정한다.

새 Auth 방식 만들지 마라.
새 endpoint 만들지 마라.
role 판별 로직을 임의 창작하지 마라.

기존 인증 구조를 활용해
Pool Dashboard에만 Desktop-only 정책이 적용되도록 한다.

만약 로그인 전에는 role을 알 수 없어
기존 구조상 두 요구를 동시에 충족할 수 없다면
임의 구현하지 말고 정확한 구조와 해결 선택지를 보고한다.

━━━━━━━━━━━━━━━━━━━━━━
8. DESKTOP GUARD QA
━━━━━━━━━━━━━━━━━━━━━━

검증:

iPhone → BLOCK
Android phone → BLOCK
iPad → BLOCK
Android tablet → BLOCK

Windows desktop → ALLOW
Mac → ALLOW
Windows laptop → ALLOW

Touchscreen Windows laptop:
→ 반드시 ALLOW

단순 touch 여부만으로 판단 금지.

직접 /admin URL 접근도
pool admin 모바일에서는 Dashboard UI가 렌더링되지 않아야 한다.

Guard는 UX 제한일 뿐
Auth 보안 대체가 아님.

━━━━━━━━━━━━━━━━━━━━━━
9. DESIGN SYSTEM DEVELOPMENT ROUTE
━━━━━━━━━━━━━━━━━━━━━━

/design-system은 개발 Preview 용이었다.

Production 공개 전 반드시:

- Production build에서 접근 불가 처리
또는
- route 제거

한다.

개발 환경에서는 필요하면 유지 가능.

Production에서 일반 사용자가 접근 가능하면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
10. CONTENT FACT CHECK
━━━━━━━━━━━━━━━━━━━━━━

공개 페이지 전체 문구 검사.

금지:

- 미구현 기능을 현재 기능으로 설명
- AI 인사이트 전략 리포트를 구현된 기능처럼 표시
- 특허 출원을 등록특허라고 표현
- 검증되지 않은 정확도
- "세계 최고" 등 검증 불가능 주장
- 존재하지 않는 투자액
- OFFICE가 출시된 것처럼 표현
- SWIMNOTE를 X보다 하위/basic 제품처럼 표현

고정 positioning:

SWIMNOTE
= 수영장 운영의 디지털화

SWIMNOTE X
= 교육 시스템의 데이터화·AI화

두 제품은 다른 목적이지
Basic/Premium 관계가 아니다.

SWIMNOTE OFFICE:
출시 예정.

AI 성장리포트 외부 설명:

- X 이용 수영장
- 재등록 회원
- 지난달 수업기록 기반
- 매월 5일
- 관리자 검토/발송
- 학부모 확인

내부 DB 필드명 노출 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. ACCESSIBILITY / INTERACTION
━━━━━━━━━━━━━━━━━━━━━━

최소 검수:

- keyboard Header navigation
- ⋯ open/close
- ESC
- focus return
- Tab navigation
- FAQ accordion keyboard
- visible focus
- aria-expanded
- disabled future item semantics
- buttons/links 역할 정확
- image alt

과도한 accessibility framework 추가 금지.

기존 구현 최소 수정.

━━━━━━━━━━━━━━━━━━━━━━
12. BUILD / CONSOLE / NETWORK
━━━━━━━━━━━━━━━━━━━━━━

Production build 실행.

확인:

- TypeScript errors 0
- runtime console error 0
- broken image 0
- failed public asset request 0
- dead internal link 0

기존 admin bundle size warning처럼
이번 Web redesign과 무관한 known warning은
새 문제처럼 수정하지 않는다.

새 package 설치 금지 unless absolutely required.

━━━━━━━━━━━━━━━━━━━━━━
13. 수정 원칙
━━━━━━━━━━━━━━━━━━━━━━

이번 QA에서 발견된:

P0:
- broken navigation
- privacy leak
- auth regression
- device guard failure
- blank page
- broken layout
- misleading product statement

→ 즉시 최소 수정.

P1:
- spacing
- text clipping
- responsive issue
- minor accessibility

→ 이번에 같이 수정.

P2:
- 개인 취향 수준의 미세 변경

→ 크레딧 낭비하지 말고 건드리지 않는다.

전체 페이지를 다시 디자인하지 마라.

━━━━━━━━━━━━━━━━━━━━━━
14. FINAL PREVIEW
━━━━━━━━━━━━━━━━━━━━━━

최종 수정 후 대표가 확인할 경로:

/
 /swimnote
 /swimnote-x
 /swimnote-office
 /ai
 /technology
 /patents
 /company
 /support
 /contact
 /terms
 /privacy
 /login

Desktop 1280 기준.

추가:
HOME 390
SWIMNOTE 390
SWIMNOTE X 390
⋯ menu 390
/login 390

Preview에서 모두 실제 렌더링 확인.

"코드상 정상 예상"으로 PASS 처리 금지.

━━━━━━━━━━━━━━━━━━━━━━
15. GIT / PRODUCTION PREP
━━━━━━━━━━━━━━━━━━━━━━

현재 실제 Git 상태 확인.

보고:

- current branch
- HEAD commit
- 변경 파일
- uncommitted changes 존재 여부
- GitHub origin
- APP repo인지 확인

중요:

APP repo에는 GitHub remote만 사용.

GitLab remote가 발견되면
push/deploy하지 말고 즉시 보고.

Production 대상은 main.

이번 작업에서는
Production push/deploy 금지.

대표 승인 전:
Render deploy trigger 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. 최종 완료 보고 — 짧게
━━━━━━━━━━━━━━━━━━━━━━

크레딧 절약을 위해 아래만 보고.

1. FINAL QA
   - Desktop PASS/FAIL
   - Tablet PASS/FAIL
   - Mobile PASS/FAIL

2. 수정한 P0/P1 목록

3. 개인정보/Public asset 결과

4. Contact 상태
   - READY 또는 BLOCKER

5. Terms/Privacy
   - READY 또는 REVIEW REQUIRED

6. Login/Super Admin Guard 결과

7. /design-system Production 차단 결과

8. Content fact-check PASS/FAIL

9. Build
   - PASS/FAIL

10. Git
   - branch
   - HEAD
   - origin
   - dirty/clean

11. Production readiness:
   - READY
   또는
   - BLOCKED + 이유

12. Production deploy:
   NO

여기까지 완료 후 반드시 중단.

대표가 최종 승인하기 전
push/deploy하지 마라.
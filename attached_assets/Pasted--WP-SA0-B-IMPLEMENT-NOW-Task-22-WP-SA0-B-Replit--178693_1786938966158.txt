[WP-SA0-B — IMPLEMENT NOW]

Task #22 초안 및 기존 감사 결과를 기준으로
WP-SA0-B 구현을 지금 시작한다.

현재 Replit은 실행 중이 아니므로
초안 작성으로 끝내지 말고 실제 코드 구현 → 테스트 → GitHub push → Render deploy까지 진행한다.

확정 구현 범위:

1. super_incidents 테이블 migration
2. Incidents CRUD API + audit_logs
3. /super/pools/search 확장
   - pool name
   - representative
   - phone
   - admin_email
   - pool_id
4. /super/billing/list
   - Basic/X 통합 조회
   - normalized_status
   - anomaly detection
5. /super/servers/status 통합 API
6. SuperOverview live data
   - 30초 polling
   - section별 error isolation
7. SuperPoolDetail A~I 실제 데이터 연결
8. SuperBilling UI
9. SuperIncidents CRUD UI
10. SuperServers live status
11. SuperAI /super/ai-traces 연결
12. SuperLayout Global Pool Search
13. TS check → test → GitHub push → Render deploy

──────────────────────────────
IMPLEMENTATION LOCK
──────────────────────────────

A. 기존 endpoint 최대 재사용

현재 확인된 기존 endpoint:

/super/dashboard-stats
/super/pools-summary
/super/operators/:id
/super/pools/search?q=
/super/system-health
/super/ai-traces

중복 구현 금지.

EXISTING_ENDPOINTS_REUSED 숫자로 보고.

──────────────────────────────
B. Pool Search
──────────────────────────────

기존 이름/대표자 검색 유지.

추가:

phone
admin_email
pool_id

server-side search.

전체 pool client preload 후 검색 금지.

──────────────────────────────
C. Billing Status
──────────────────────────────

UI용 normalized_status:

ACTIVE
CANCELLED_BUT_ACTIVE
EXPIRED
BILLING_ISSUE
SYNC_PENDING
UNKNOWN

raw/source 상태는 보존.

RevenueCat/server 원본 값을
normalized 값으로 덮어쓰지 않는다.

──────────────────────────────
D. Billing Anomaly
──────────────────────────────

탐지만 한다.

예:

RC active / server inactive
server active / RC missing
sync stale
billing issue
expired but mode X

자동 수정 버튼 생성 금지.

OBSERVE / DIAGNOSE only.

──────────────────────────────
E. Server Status Isolation
──────────────────────────────

/super/servers/status에서

APP API
AI Engine
Database
Storage
RevenueCat
OpenAI
Push

중 하나가 실패해도
endpoint 전체 500 금지.

각 서비스 개별:

LIVE
DEGRADED
UNKNOWN

UNKNOWN != NORMAL
UNKNOWN != 0

──────────────────────────────
F. Super Admin Accuracy
──────────────────────────────

source 없는 숫자를 0으로 표시 금지.

실제 0일 때만 0.

source 없음 / fetch 실패:

UNKNOWN
NOT_AVAILABLE
-

중 하나.

FAKE_ZERO_PATHS_FOUND
FAKE_ZERO_PATHS_FIXED

반드시 보고.

──────────────────────────────
G. Overview
──────────────────────────────

30초 polling 가능.

하지만 전체 dashboard를
Promise.all 한 번 실패로 전부 crash시키는 구조 금지.

section별 독립 실패.

RevenueCat 실패
→ RevenueCat UNKNOWN

Pools / incidents / support는 계속 표시.

──────────────────────────────
H. AI Usage / Errors
──────────────────────────────

현재 /super/ai-traces 재사용.

이번 WP에서는:

feature
status
pool
timestamp
error

등 현재 trace로 확인 가능한 것만 표시.

Partner Analytics용 새 usage schema 생성 금지.

token/cost/adoption 통합계측은
Support AI + Partner Analytics WP에서 구현.

──────────────────────────────
I. Incident
──────────────────────────────

super_incidents 신규 table.

severity:

SEV1
SEV2
SEV3
SEV4

status:

OPEN
INVESTIGATING
MITIGATED
RESOLVED

필수:

GET list
POST create
PATCH update

DELETE 우선 구현 금지.

audit_logs:

INCIDENT_CREATED
INCIDENT_UPDATED
INCIDENT_RESOLVED

──────────────────────────────
J. Security
──────────────────────────────

모든 신규 /super endpoint:

super_admin only.

UI guard만으로 끝내지 않는다.

API authorization 필수.

──────────────────────────────
K. Scope Lock
──────────────────────────────

변경 금지:

mobile app
iOS
Android
Normal theme
X theme
scheduler
ModeContext
RevenueCat purchase flow
Support AI
Partner Analytics 전체 구현
홈페이지 생성
커리큘럼 AI 생성

OTA 없음.

──────────────────────────────
L. DB
──────────────────────────────

신규 DB는 기본적으로:

super_incidents

1개만.

idempotent migration.

기존 DB schema 임의 변경 금지.

실제 구현상 추가 변경이 필요하면
이유를 먼저 report.

──────────────────────────────
M. Test
──────────────────────────────

필수:

Global Pool Search
Pool Detail live data
Basic billing
X billing
cancelled-but-active
billing issue
sync pending
billing anomaly
incident create/update/resolve
pool incident link
APP API status
AI Engine status
DB status
RevenueCat status
OpenAI status
one status API failure → page survives
Overview metrics
non-superadmin denied
X03/X04 regression 없음

기존 test suite도 전부 실행.

──────────────────────────────
N. Deployment
──────────────────────────────

코드 완료 후:

GitHub push
→ Render deploy
→ live 확인

iOS OTA = NO
Android OTA = NO

──────────────────────────────
FINAL REPORT
──────────────────────────────

GLOBAL_POOL_SEARCH =
POOL_SEARCH_FIELDS =

POOL_DETAIL_LIVE =
POOL_DETAIL_SECTIONS =

BILLING_OVERVIEW =
BASIC_BILLING =
X_BILLING =
BILLING_ANOMALY_DETECTION =

INCIDENT_TABLE =
INCIDENT_CRUD =
INCIDENT_POOL_LINK =

APP_API_STATUS =
AI_ENGINE_STATUS =
DATABASE_STATUS =
STORAGE_STATUS =
REVENUECAT_STATUS =
OPENAI_STATUS =
PUSH_STATUS =

OVERVIEW_LIVE =
SECTION_ERROR_ISOLATION =
UNKNOWN_NOT_FAKE_ZERO =

AI_USAGE_FOUNDATION =
AI_ERRORS_FOUNDATION =

EXISTING_ENDPOINTS_REUSED =
NEW_ENDPOINTS_CREATED =
NEW_DB_TABLES =

FAKE_ZERO_PATHS_FOUND =
FAKE_ZERO_PATHS_FIXED =
SECTION_ERROR_ISOLATION_TEST =
BILLING_ANOMALIES_SUPPORTED =

SUPER_ADMIN_AUTH =
AUDIT_LOG =

FILES_AUDITED =
FILES_CHANGED =

DB_CHANGED =
SERVER_CHANGED =
WEB_CHANGED =
MOBILE_CHANGED =

UNIT_TEST =
API_TEST =
WEB_TEST =
SECURITY_TEST =

COMMIT_SHA =
SERVER_DEPLOYMENT =

성공 조건:

GLOBAL_POOL_SEARCH = YES
POOL_DETAIL_LIVE = YES
BILLING_OVERVIEW = YES

INCIDENT_TABLE = YES
INCIDENT_CRUD = YES

UNKNOWN_NOT_FAKE_ZERO = YES
SECTION_ERROR_ISOLATION = YES

SUPER_ADMIN_AUTH = PASS

MOBILE_CHANGED = NO

완료 문구:

WP_SA0_B_SUPER_ADMIN_LIVE_OPERATIONS_DATA_COMPLETE ✅
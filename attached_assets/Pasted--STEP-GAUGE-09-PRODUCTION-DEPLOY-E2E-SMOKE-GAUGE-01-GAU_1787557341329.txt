[앱] STEP-GAUGE-09 — PRODUCTION DEPLOY + E2E SMOKE

기준:
GAUGE-01 ~ GAUGE-08 COMPLETE

latest GAUGE commit:
554b1fa8

추가 확인 완료:
swimnote-kf-worker의 반복 ETIMEDOUT은
현재 GAUGE / Parent Curriculum / Growth Report 코드와
직접 dependency가 없는 별도 legacy Render service 문제로 판정.

중요:
swimnote-kf-worker는 이번 작업에서 완전히 제외한다.

금지:
- swimnote-kf-worker deploy 금지
- swimnote-kf-worker 설정 변경 금지
- worker env 수정 금지
- worker SSL 수정 금지
- worker 장애를 GAUGE 장애로 간주 금지

이번 Step 대상은 오직:
swimnote-api

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. GitHub Push
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 branch:
deploy-photo-clone

GAUGE-01~08 전체 변경이 포함된
현재 최신 local HEAD 확인.

git status 확인.

필요한 commit이 모두 포함되어 있으면
origin/deploy-photo-clone으로 push.

보고:
- local HEAD
- remote HEAD
- push result

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. Render 대상 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

대상 service:

swimnote-api

반드시 Render branch:
deploy-photo-clone

확인.

main이면 STOP.

swimnote-kf-worker는 확인/수정/배포 대상 아님.

RENDER_API_KEY 요청 금지.

기존 GitHub auto deploy 또는
사용자가 Render에서 수행하는 기존 배포 방식만 사용.

build/start command 변경 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. Deployment 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

swimnote-api의 새 deployment가 완료되면:

- deployed SHA
- status Live
- startup error 없음
- DB connection 정상
- health route 정상

확인.

KF worker의 ETIMEDOUT log는
swimnote-api 판정에 포함하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. Parent Gauge API Smoke
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

실제 승인된 X-mode parent/student 관계 1개로:

GET /parent/students/:studentId/curriculum-progress

확인:

HTTP 200

필드:
- student_id
- display_confirmed_pct
- active_confirmed_pct
- active_confirmed_rank
- active_confirmed_total
- active_curriculum_version_id
- observation_session_count
- confirmed_at
- display_updated_at
- is_version_transition

SCP가 아직 없으면
정상 zero/empty response도 PASS.

기록:
- request_id
- endpoint
- latency
- status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. Ownership / Pool Isolation
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 안전한 실제 계정/fixture가 있으면:

authorized parent/student → 200

다른 parent student → 차단

다른 pool SCP leakage → 없음

안전한 대상이 없으면
임의 production data 생성하지 말고 NOT RUN.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. Diary → CPO → SCP Smoke
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

가짜 production Diary 생성 금지.

실제 정상 수업 일지를 작성할 기회가 있는 경우에만
그 정상 작업 1건을 이용해 확인.

Diary CREATE 후:

growth_events
→ CPO
→ SCP

흐름 확인.

CPO:
- session/student 1행
- valid curriculum item
- rank/total/pct
- observation_type
- eligibility
- is_invalidated=false

SCP:
- active version
- session_count
- active confirmed rank/pct
- display pct

eligible session <3이면:
active confirmed rank=0
active pct=0
이어도 정상.

정상 실제 수업 입력이 없으면:
DIARY→CPO→SCP = NOT RUN

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 3-Session Production Verification
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

가짜 세션 생성 금지.

실제 eligible CPO가 3개 이상 있는 학생만
read-only 검증.

있으면:
eligible ranks DESC의 3번째 값
=
SCP.active_confirmed_rank

확인.

없으면:
NOT ENOUGH REAL SESSIONS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. Parent Curriculum Regression
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 실제 Parent Curriculum 요청 1회:

예:
"롤링 언제 해"

확인:
- HTTP 200
- 기존 intent 정상
- 기존 retrieval 정상
- optional student_progress 때문에 request 실패 없음
- quota 정책 동일
- 새 AI call 없음

swimnote-kf-worker는 이 경로와 무관.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. Growth Report Regression
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

새 production Growth Report 생성 금지.

기존 PUBLISHED report 조회만 확인.

GAUGE-07 type/schema 확장 때문에
기존 report read가 깨지지 않았는지만 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. OTA READY 판정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음이 모두 만족되면:

SERVER/API PRODUCTION READY FOR OTA

- swimnote-api Live
- correct branch deployed
- Parent Gauge API 200
- auth/pool 문제 없음
- Parent Curriculum 정상
- 기존 Growth Report read 정상
- 새로운 API 500 없음
- unexpected DB mutation 없음

KF worker ETIMEDOUT은
이번 OTA READY 판정에서 제외.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. OTA 실행 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이번 Step에서는 아직 OTA 금지.

SERVER/API READY 보고 후
사용자 승인 받고 iOS OTA production 1회 수행.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 완료 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[앱] STEP-GAUGE-09 PRODUCTION DEPLOY REPORT

- local SHA
- remote deploy-photo-clone SHA
- Render service: swimnote-api
- Render branch
- deployed SHA
- deploy status
- server health
- Parent Gauge API
- request_id / latency
- ownership protection
- cross-pool protection
- Diary→CPO smoke: PASS / NOT RUN
- CPO→SCP smoke: PASS / NOT RUN
- real 3-session verification
- Parent Curriculum regression
- Growth Report read regression
- unexpected production DB changes: NO
- swimnote-kf-worker touched: NO
- server errors

Final Verdict:
SERVER/API PRODUCTION READY FOR OTA
또는
BLOCKED + exact reason

STOP.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SWIMNOTE SUPABASE SECURITY — PHASE 6
TABLE INVENTORY RECONCILIATION + RLS PILOT 3
CORE IDENTITY TABLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Phase 5 RLS_PILOT_2_VERIFIED 승인.

현재 확인된 상태:

Production Data API public 제거 완료
Pilot 1 = 5개 RLS
Pilot 2 = 7개 RLS
기존 diary_template_levels = RLS

현재 보고:
TOTAL TABLES = 105
RLS ENABLED = 13
RLS DISABLED = 92

중요:
기존 최초 감사에서는 TOTAL TABLES = 100으로 보고됐다.

따라서 먼저 100 → 105 차이를
정확히 설명한 뒤 Pilot 3 진행한다.

DB SOURCE:
SUPABASE_DATABASE_URL

executeSql production 사용 금지.

━━━━━━━━━━━━━━━━━━━━
1. TABLE INVENTORY 정합성 확인
━━━━━━━━━━━━━━━━━━━━

Production public schema에서
현재 실제 테이블 전체 이름을 조회.

제출:

- current total table count
- 105개 전체 목록
- 현재 RLS=true 목록
- 현재 RLS=false 목록

그리고 최초 감사의 100개 목록과 비교해서:

ADDED_OR_PREVIOUSLY_MISSED = 정확한 5개 이름

제출.

각 5개에 대해:

- created_at 확인 가능한 schema metadata
- 현재 row count
- RLS 상태
- 주요 용도
- 최근 migration/init에서 생성된 것인지

확인.

추측 금지.

━━━━━━━━━━━━━━━━━━━━
2. 기존 13개 RLS 유지 확인
━━━━━━━━━━━━━━━━━━━━

현재 RLS=true여야 하는 13개:

diary_template_levels

global_template_sets
curriculum_requests
curriculum_versions
curriculum_items
feature_flags

payment_cards
phone_verifications
push_tokens
monthly_settlements
revenue_logs
audit_logs
backup_settings

13개 전부:

relrowsecurity=true

확인.

하나라도 false면 Pilot 3 진행 금지 후 STOP.

━━━━━━━━━━━━━━━━━━━━
3. Pilot 3 대상
━━━━━━━━━━━━━━━━━━━━

이번 대상은 핵심 개인정보 4개만:

1. students
2. users
3. parent_accounts
4. members

다른 테이블은 아직 건드리지 않는다.

이번 목적:
가장 중요한 개인정보 테이블에
RLS defense-in-depth 적용.

━━━━━━━━━━━━━━━━━━━━
4. 적용 전 증거
━━━━━━━━━━━━━━━━━━━━

4개 각각:

- relrowsecurity
- relforcerowsecurity
- owner
- row count
- policy count
- anon SELECT/INSERT/UPDATE/DELETE grants
- authenticated SELECT/INSERT/UPDATE/DELETE grants

기존 policy가 하나라도 존재하면
임의 변경하지 말고 STOP.

━━━━━━━━━━━━━━━━━━━━
5. Production role 재확인
━━━━━━━━━━━━━━━━━━━━

같은 DB session에서:

current_user
session_user
rolsuper
rolbypassrls

기대:

current_user = postgres
rolsuper = false
rolbypassrls = true

다르면 STOP.

━━━━━━━━━━━━━━━━━━━━
6. RLS Pilot 3 적용
━━━━━━━━━━━━━━━━━━━━

정확히 아래 4개만:

ALTER TABLE students ENABLE ROW LEVEL SECURITY;
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE parent_accounts ENABLE ROW LEVEL SECURITY;
ALTER TABLE members ENABLE ROW LEVEL SECURITY;

policy 생성 금지.

FORCE ROW LEVEL SECURITY 금지.

GRANT revoke도 이번 단계에서 하지 않는다.

다른 테이블 변경 금지.

━━━━━━━━━━━━━━━━━━━━
7. 즉시 READ-BACK
━━━━━━━━━━━━━━━━━━━━

적용 직후:

students
users
parent_accounts
members

각각:

relrowsecurity=true
relforcerowsecurity=false
policy count=0

확인.

row count 전후 동일 확인.

━━━━━━━━━━━━━━━━━━━━
8. DIRECT POSTGRES 기능 검증
━━━━━━━━━━━━━━━━━━━━

SUPABASE_DATABASE_URL direct postgres로:

students COUNT
users COUNT
parent_accounts COUNT
members COUNT

정상 조회 확인.

개인정보 값 출력 금지.

그리고 안전한 SELECT로
각 테이블 1건 존재 여부만 확인 가능.

실제 이름/전화/email/password hash 등 출력 금지.

━━━━━━━━━━━━━━━━━━━━
9. Render 핵심 인증 회귀 검증
━━━━━━━━━━━━━━━━━━━━

이번 Pilot 3는 인증 핵심 테이블을 건드리므로
/api/health만 확인하고 끝내면 안 된다.

가능한 실제 READ/LOGIN smoke 수행.

최소:

A.
GET /api/health
→ 200

B.
정상 pool_admin 로그인
→ 실제 로그인 성공 여부

C.
정상 teacher 로그인
→ 가능하면 확인

D.
정상 parent login/auth path
→ 가능하면 확인

계정 비밀번호 변경 금지.
테스트용 기존 계정 사용.

실제 토큰/비밀번호 출력 금지.

실계정 접근이 불가능하면
해당 항목은 NOT_TESTED로 명시.

━━━━━━━━━━━━━━━━━━━━
10. 핵심 API 회귀
━━━━━━━━━━━━━━━━━━━━

가능한 기존 인증 토큰으로 READ ONLY:

- /api/pools/x-mode
- 학생 목록 관련 기존 GET
- 회원/사용자 관련 안전한 GET
- parent 관련 안전한 GET

최소 1개 이상 실제 인증 API를 확인.

401만 확인하고
"runtime verified" 처리 금지.

━━━━━━━━━━━━━━━━━━━━
11. Render 로그 검사
━━━━━━━━━━━━━━━━━━━━

적용 직후 로그에서 검색:

permission denied
row-level security
policy
students
users
parent_accounts
members
authentication
login

신규 오류가 있는지 확인.

기존 오류와 분리.

━━━━━━━━━━━━━━━━━━━━
12. Background job 영향
━━━━━━━━━━━━━━━━━━━━

users/students/members 관련:

scheduler
backup
standby-sync
push
notification
attendance

에서 신규 RLS 오류가 없는지 확인.

direct postgres 경로라면 정상이어야 함.

━━━━━━━━━━━━━━━━━━━━
13. 실패 시 즉시 rollback
━━━━━━━━━━━━━━━━━━━━

하나라도 신규 기능 장애가 발생하면
이번 4개만 즉시:

ALTER TABLE students DISABLE ROW LEVEL SECURITY;
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE parent_accounts DISABLE ROW LEVEL SECURITY;
ALTER TABLE members DISABLE ROW LEVEL SECURITY;

rollback 후:

- login
- API
- Render logs

재확인.

다른 Pilot 1/2 테이블은 건드리지 않는다.

━━━━━━━━━━━━━━━━━━━━
14. 적용 후 총계
━━━━━━━━━━━━━━━━━━━━

성공하면 예상:

기존 RLS enabled 13
+
Pilot 3 4

= 17

단 실제 현재 table inventory 기준으로
정확한 count를 다시 계산.

remaining RLS-disabled도
105 기준으로 실제 계산해서 제출.

숫자 추측 금지.

━━━━━━━━━━━━━━━━━━━━
15. Security Advisor
━━━━━━━━━━━━━━━━━━━━

가능하면 Dashboard에서:

students
users
parent_accounts
members

RLS disabled 경고 해소 확인.

불가능하면:
MANUAL_CHECK_REQUIRED.

━━━━━━━━━━━━━━━━━━━━
16. 다음 그룹 제안
━━━━━━━━━━━━━━━━━━━━

Pilot 3 성공 후
아직 적용하지 않은 테이블을 기능별로 분류:

A.
student/parent content
- swim_diary
- diary_messages
- student_photos
- student_videos
- parent_students
- class_diary_student_notes

B.
운영/수업
- attendance
- classes
- class_groups
- class_members
- makeup_sessions
- student_class_history
...

C.
system/jobs/logs
...

각 그룹 개수 제출.

이번 단계에서는 적용 금지.

━━━━━━━━━━━━━━━━━━━━
17. 제출 보고
━━━━━━━━━━━━━━━━━━━━

1. 현재 실제 table count
2. 최초 100개 대비 추가/누락 5개
3. 현재 13개 RLS 유지 확인
4. Pilot 3 pre-state
5. current_user / rolbypassrls
6. 실제 실행 SQL
7. Pilot 3 post-state
8. row count 전후
9. direct postgres smoke
10. 실제 로그인 runtime 결과
11. 인증된 API runtime 결과
12. Render 신규 RLS 오류
13. worker/scheduler 영향
14. rollback 여부
15. 총 RLS enabled
16. remaining disabled
17. Security Advisor
18. 다음 그룹 분류
19. 최종 판정

FINAL STATUS:

RLS_PILOT_3_VERIFIED

또는

RLS_PILOT_3_ROLLED_BACK

STOP.

POOL_DB RLS 적용 금지.
standby-sync 수정 금지.
WP4 재개 금지.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
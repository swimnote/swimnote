[보낼 곳: APP REPLIT]

[긴급 P0 — X 플랜 저장공간 quota 불일치 원인 규명 + 단일 Resolver로 완전 수정]

현재 Production 실기기에서 명확한 오류가 확인되었습니다.

실제 화면:

구독 관리:
SWIMNOTE X1000
최대 1,000명
1TB
현재 플랜

하지만 같은 수영장의 저장공간 현황:
사용량 3.29GB
제공 용량 10GB
남은 용량 6.71GB
사용률 32.9%

즉 같은 Pool / 같은 로그인 상태에서

Current Plan Resolver = X1000
Storage Quota Resolver = SWIMNOTE BASE 10GB

로 서로 다른 결과가 나오고 있습니다.

이 작업에서 단순 UI 숫자 하드코딩 또는 Toykids 전용 if문 수정은 절대 금지합니다.

문제의 Source of Truth를 추적하고
BASE / Paid X / Manual X / Management Override X 전체가
동일한 canonical plan 기준으로 저장공간을 계산하도록 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0. 공식 정책 — 절대 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 공식 플랜:

SWIMNOTE
- storage 10GB

X300
- storage 300GB

X500
- storage 500GB

X1000
- storage 1TB (= 현재 DB catalog의 canonical X1000 storage)

DATA add-on:
- effective base/X quota + extra_storage_gb

중요:

Current Plan / Storage canonical resolver는

1. active/effective X 여부
2. x_plan_key / canonical X plan
3. X source
   - PAID
   - MANUAL
   - MANAGEMENT OVERRIDE
4. 그 다음 BASE subscription

순으로 판단해야 합니다.

legacy subscription_tier 또는
swimming_pools.base_storage_gb만 보고
effective quota를 결정하지 마십시오.

Toykids는 이름 하드코딩 금지입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 수정 전에 실제 원인을 먼저 추적
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 파일/경로를 실제 Repository에서 전수 조사하십시오.

APP:
- app/(admin)/data-storage-overview.tsx
- 저장공간 현황 화면이 실제 호출하는 service/hook/API
- dashboard의 저장공간 카드
- 카테고리별 저장공간 화면
- 계정별 저장공간 화면
- 구독 화면

SERVER:
- src/lib/storageQuota.ts
- src/lib/subscriptionResolver.ts
- src/lib/subscriptionService.ts
- src/routes/billing.ts
- src/routes/storage.ts
- src/routes/uploads.ts
- src/routes/photos.ts
- src/routes/videos.ts

그리고 전역 검색:

storage_gb
storage_mb
base_storage_gb
display_storage
extra_storage_gb
storage_quota_gb
quota
getPoolQuotaGb
checkStorageLimit
pool_subscriptions
subscription_plans
x_plan_key
x_paid_entitlement
x_manual_entitlement
x_management_override

먼저 다음 표를 제출하십시오.

PATH
→ 어떤 API 호출
→ 어떤 server function
→ 어떤 DB field/table
→ 현재 quota source
→ X resolver 사용 여부
→ 잘못될 수 있는 상태

특히 실기기 저장공간 현황 화면의
“10GB”가 정확히 어느 DB/API 값에서 나온 것인지
끝까지 추적하십시오.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. Toykids Production READ-ONLY 증거 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Production에서 데이터 변경 없이 READ ONLY로 확인하십시오.

대상은 현재 로그인한 Toykids pool.

확인:

- pool_id
- effective mode
- effective X
- X source
- x_paid_entitlement
- x_manual_entitlement
- x_management_override
- x_force_disabled
- x_plan_key
- subscription_tier
- pool_subscriptions.tier
- swimming_pools.base_storage_gb
- swimming_pools.storage_mb
- swimming_pools.extra_storage_gb
- canonical current plan
- canonical plan storage_gb
- storage overview API 반환 quota
- billing/status 또는 features가 반환하는 quota
- actual used bytes

현재 화면 증거상 기대 결과:

effective plan = X1000
base quota = 1TB
used ≈ 3.29GB

따라서 저장공간 화면도 X1000 quota를 반환해야 합니다.

Production DB UPDATE/INSERT 금지.
READ ONLY만 수행하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 일반 X 유료 고객 전수 READ-ONLY 검사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이 문제가 Toykids Management Override에만 있는지
실제 X 구매 고객까지 영향을 받는지 반드시 확인하십시오.

Production의 모든 effective X pool을 대상으로
READ ONLY audit를 실행하십시오.

각 pool별:

pool_id
X source = PAID / MANUAL / MANAGEMENT
x_plan_key
pool_subscriptions.tier
canonical effective plan
expected storage
현재 storage API quota
현재 upload enforcement quota
mismatch YES/NO

개인정보/수영장 이름은 완료보고에서 마스킹 가능.

최종 집계:

effective X pools total = N
storage mismatch pools = N

source별:
PAID mismatch = N
MANUAL mismatch = N
MANAGEMENT mismatch = N

이 결과 없이
“일반 구매자는 정상입니다”라고 추정하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. Root Cause 수정 원칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 이미 canonical plan resolver가 있다면 반드시 재사용하십시오.

새로운 제2의 plan resolver를 만들지 마십시오.

목표 구조:

poolId
↓
canonical effective plan resolver
↓
effective plan key
  - swimnote
  - x300
  - x500
  - x1000
↓
subscription_plans canonical storage
↓
+ swimming_pools.extra_storage_gb
↓
effective storage quota

즉:

effective_storage =
canonical_effective_plan.storage_gb
+ extra_storage_gb

이어야 합니다.

Management Override에서
billing subscription_tier가 BASE여도
effective plan이 X1000이면
storage quota 역시 X1000이어야 합니다.

Manual X도 동일.

Paid X도 동일.

X entitlement와 billing source는 분리하되
“사용자가 실제로 부여받은 effective plan”이
member limit과 storage quota의 기준이어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. storageQuota.ts를 진짜 단일 Source of Truth로 만들 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이미 src/lib/storageQuota.ts가 있다면
삭제하거나 별도 helper를 새로 만들지 말고
그 파일을 canonical quota resolver로 완성하십시오.

권장 contract 예:

resolveEffectiveStorageQuota(poolId)

return:
{
  effectivePlanKey,
  planSource,
  baseStorageGb,
  extraStorageGb,
  totalStorageGb,
  totalQuotaBytes
}

planSource는 디버그/테스트용으로:

BASE
PAID_X
MANUAL_X
MANAGEMENT_X

등 현재 repository convention에 맞추십시오.

중요:
APP에서 이 로직을 복제하지 마십시오.
서버가 계산해서 내려주십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 모든 quota 소비 경로 통일
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 모든 곳에서 동일 helper를 사용해야 합니다.

A. 저장공간 현황 화면
B. dashboard 저장공간 카드
C. 카테고리별 저장공간
D. 계정별 저장공간
E. billing/status 또는 billing/features의 quota
F. 사진 업로드 차단
G. 영상 업로드 차단
H. 기타 파일 업로드 차단
I. 80/90/100% warning
J. Super Admin storage health/summary

어떤 화면은 1TB인데
실제 업로드는 10GB에서 차단되는 상태는 P0입니다.

반대로 화면만 10GB이고 업로드는 1TB여도 실패입니다.

DISPLAY QUOTA
=
WARNING QUOTA
=
UPLOAD BLOCK QUOTA
=
SUPER ADMIN QUOTA

전부 동일해야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. stale pool copy 컬럼 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

swimming_pools.base_storage_gb
swimming_pools.storage_mb
display_storage

등 비정규화/stale copy가 존재해도
canonical effective quota의 Source of Truth로 사용하지 마십시오.

필요한 호환성 때문에 컬럼을 유지하는 것은 허용하지만
effective X quota 판정에는 사용하지 않습니다.

DB backfill이 꼭 필요한지 먼저 판단하십시오.

가능하면:
canonical resolver 수정만으로 해결하고
Production DB mutation 없이 끝내십시오.

DB migration 또는 Production data UPDATE가 정말 필요하면
실행하지 말고 이유와 대상 row 수를 보고한 후 STOP하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 필수 테스트 Matrix
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최소 다음 테스트를 추가하십시오.

1. SWIMNOTE BASE
expected = 10GB

2. Paid X300
expected = 300GB

3. Paid X500
expected = 500GB

4. Paid X1000
expected = 1TB

5. Manual X300
expected = 300GB

6. Manual X500
expected = 500GB

7. Manual X1000
expected = 1TB

8. Management Override X1000
legacy/base subscription이 SWIMNOTE 10GB여도
expected = 1TB

9. X1000 + DATA100
expected = X1000 base + 100GB

10. X500 + DATA300
expected = 500GB + 300GB

11. force_disabled X + BASE active
canonical 정책에 따른 BASE quota 확인

12. downgrade pending
현재 결제기간에 적용 중인 plan quota 유지 여부 확인

13. expired X
정상 BASE/current entitlement quota로 복귀

각 케이스에서:

current plan UI
storage overview API
upload enforcement helper
billing quota

4개 값이 전부 일치해야 PASS.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. Toykids 실기기 기대값
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

수정 후 Toykids:

구독 관리:
X1000
1TB

저장공간 현황:
제공 용량 = 1TB
사용량 = 현재 실제 사용량 약 3.29GB
남은 용량 = 1TB - actual used
사용률 = 약 0.3% 수준

이렇게 일치해야 합니다.

10GB / 32.9% 상태가 남으면 FAIL입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. APP 캐시/OTA까지 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

서버가 이미 1TB를 반환하는데
앱만 10GB를 표시하는 경우에만
APP cache/query 문제를 조사하십시오.

확인:
- React Query cache key
- staleTime
- plan 변경 후 invalidate
- refreshPool
- subscription sync 후 storage refetch
- 화면 focus refetch
- AsyncStorage/local fallback
- hardcoded default 10

하지만 서버가 10GB를 반환하는데
클라이언트 숫자만 1TB로 바꾸는 수정은 금지입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 완료 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

“코드 고쳤습니다”라고 보고하지 마십시오.

아래 증거가 모두 있어야 완료입니다.

1. 실제 Root Cause
2. 잘못된 10GB의 정확한 source
3. 수정 파일 목록
4. canonical quota resolver 코드 위치
5. 전 quota consumer 목록
6. Paid/Manual/Management Matrix 결과
7. Production 전체 X pool READ-ONLY mismatch audit 결과
8. Toykids API BEFORE:
   effective plan X1000 / storage quota 10GB
9. Toykids API AFTER:
   effective plan X1000 / storage quota 1TB
10. upload enforcement도 1TB인지 증거
11. tests PASS
12. TypeScript 신규 오류 0
13. DB 변경 여부
14. Render deploy 필요 여부
15. APP OTA 필요 여부

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 배포 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

코드 수정 + 테스트 + commit + push까지 가능.

Production DB 직접 변경 금지.

Render Production 자동배포 금지.
필요하면 사용자에게 수동 Render deploy를 요청하십시오.

OTA가 필요한 경우에도
Production OTA를 임의 발행하지 말고
필요 여부와 대상 runtime/channel만 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. 금지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

절대 금지:

- Toykids 이름 if문
- pool_id별 1000GB 하드코딩
- data-storage-overview.tsx에 X1000=1000 직접 하드코딩
- current plan UI 값만 가져와 화면 숫자만 변경
- base_storage_gb만 강제로 1000으로 UPDATE
- 유료 X 고객 영향 확인 없이 “Toykids만 문제” 결론
- 테스트 없이 완료 선언
- 서로 다른 quota resolver 유지

이번 작업의 최종 목표:

ONE EFFECTIVE PLAN
→ ONE STORAGE QUOTA
→ SAME VALUE EVERYWHERE
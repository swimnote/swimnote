[앱] Growth Report 좋아요·댓글 PHASE 3 계획 수정 요청 — 구현 금지

현재 구현계획서에서 2가지 수정이 필요합니다.

아직 PHASE 3-A 구현하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. Parent 대상 화면을 다시 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 사용자가 실제로 보고 있는 Growth Report 좋아요/댓글 버튼은
Parent Home Feed 안의 Growth Report ActionRow입니다.

따라서 PHASE 3-A / 3-B를
growth-report-detail.tsx만 수정하는 계획으로 진행하면 안 됩니다.

실제 코드에서 확인하십시오.

Parent Home
→ Growth Report Feed item
→ GrowthReportFeedCard
→ GrowthReportFullFeed
→ ActionRow

현재 좋아요 / 댓글 / PDF·공유가 실제로 어느 component에 렌더링되는지
정확한 파일과 component를 다시 보고하십시오.

그리고 최종 요구사항은:

[Parent Home Growth Report Feed]
좋아요 → 즉시 실제 reaction API 동작

댓글 → 기존 Diary처럼 댓글 화면 진입

이어야 합니다.

Detail 화면이 별도로 존재한다면
Feed와 Detail 양쪽에 동일 기능을 제공해야 하는지,
아니면 Feed만 실제 사용자 진입점인지
현재 route/UX 기준으로 판단해서 보고하십시오.

추측 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. Diary ActionRow UI 재사용 방식 확정
━━━━━━━━━━━━━━━━━━━━━━

사용자 요구는
"비슷한 디자인"이 아니라
기존 Diary 좋아요/댓글과 같은 디자인/동작입니다.

현재 DiaryFeedItem 안의:

- heart
- active filled heart
- message-circle
- icon size
- icon color
- spacing
- press 영역

을 조사한 뒤 아래 중 어떤 방식이 가장 안전한지 결정하십시오.

A. 기존 공통 Action component가 이미 있으면 그대로 재사용

B. 공통 component가 없다면
   Diary와 Growth Report가 함께 사용하는 최소 공통 component로 추출

C. 구조상 공통화가 오히려 regression 위험이 크다면
   동일 style constants/component source를 재사용

단순 copy/paste로 서로 다른 ActionRow 두 개를 계속 유지하는 방식은
우선안으로 선택하지 마십시오.

PDF·공유는 Growth Report 전용 action으로
같은 row의 세 번째 action으로 유지합니다.

최종 형태:

Diary:
[좋아요] [댓글] [Instagram Story]

Growth Report:
[좋아요] [댓글] [PDF·공유]

앞의 2개는 동일 UI/UX/component family.

━━━━━━━━━━━━━━━━━━━━━━
3. 수정된 PHASE 3-A
━━━━━━━━━━━━━━━━━━━━━━

PHASE 3-A는:

"Parent Home Growth Report Feed의 좋아요 실제 연결"

이어야 합니다.

계획서에 정확히 작성:

- 실제 수정 파일
- 실제 ActionRow component
- reportId source
- GET reactions 호출 시점
- POST toggle 호출
- active heart 표시
- 중복 tap 방지
- feed 재진입/refresh 시 상태 갱신
- 기존 Diary UI와 동일하게 만드는 방식

DB/server 수정 없음.

━━━━━━━━━━━━━━━━━━━━━━
4. 수정된 PHASE 3-B
━━━━━━━━━━━━━━━━━━━━━━

PHASE 3-B는:

"Parent Home Growth Report Feed 댓글 실제 연결"

이어야 합니다.

Feed의 댓글 버튼
→ gr-comments 화면
→ Growth Report comments API

흐름을 명시.

기존 diary-comments와 동일한 UX를 최대한 재사용.

반드시 확인:

- reportId
- studentId
- studentName
- report period/date 등 header에 필요한 값

을 Feed item에서 route에 전달 가능한지.

━━━━━━━━━━━━━━━━━━━━━━
5. PHASE 3-C / 3-D
━━━━━━━━━━━━━━━━━━━━━━

기존 계획 방향은 유지 가능.

3-C:
Teacher notification
→ growth-report-reactions
→ 좋아요/댓글 실제 확인

3-D:
Teacher reply
→ Parent notification 저장
→ Parent가 push를 놓쳐도 notifications 또는 댓글 화면에서 재확인 가능

단 구현은 아직 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. 다시 제출할 내용
━━━━━━━━━━━━━━━━━━━━━━

[ACTUAL FEED ACTION LOCATION]
- file
- component
- 현재 onPress 상태

[DIARY ACTION REUSE]
- 현재 Diary 구현
- 공통화/재사용 방법
- 선택 이유

[REVISED PHASE 3-A]
- 수정 파일
- 함수/component
- API
- 테스트

[REVISED PHASE 3-B]
- 수정 파일
- route
- API
- 테스트

[PHASE 3-C / 3-D]
- 기존 계획 변경 여부만 보고

코드 수정 금지.
OTA 금지.
Render deploy 금지.
DB write 금지.

수정된 구현계획서 제출 후 STOP.
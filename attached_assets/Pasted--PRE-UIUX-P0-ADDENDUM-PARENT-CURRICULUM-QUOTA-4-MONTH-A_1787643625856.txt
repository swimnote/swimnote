[앱] PRE-UIUX P0 ADDENDUM — PARENT CURRICULUM QUOTA 4/MONTH

실기기 확인에서 추가 문제 발견.

현재 화면:

"이번 달 AI 검색 3/10회"

하지만 확정된 Product Contract:

Parent Curriculum Search
= 학생/학부모 기준 월 최대 4회

따라서 Production 화면의 10회 표시는 잘못됨.

이번 수정은 UI/UX 작업이 아니라
PRE-UIUX 기능 계약 P0 수정으로 처리.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. ROOT CAUSE 최소 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 AI 커리큘럼 검색 화면에서

"3/10회"

를 만드는 실제 source만 추적.

확인할 것:

A.
Production API response usage:

{
  limit,
  used,
  remaining,
  period,
  resets_at
}

에서 실제 limit 값이 얼마인지.

B.
화면 component가
server usage.limit을 사용하는지,
legacy hardcoded 10 또는 fallback 10을 사용하는지.

관련 route/component만 확인.
repo-wide audit 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. CONTRACT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정식 정책:

limit = 4 / month

UI 표시 예:

used=3, limit=4

→ "이번 달 AI 검색 3/4회"

remaining=1

서버가 usage.limit=4를 보내면
UI는 반드시 그 값을 authority로 사용.

UI에서 별도의 10 상수 사용 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. ROOT CAUSE별 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CASE A
Production server response가 limit=4인데
UI가 10 표시

→ UI legacy hardcode/fallback만 최소 수정.

CASE B
Production server response 자체가 limit=10

→ 왜 기존 확정된 quota=4 contract가
Production에 반영되지 않았는지
관련 config/route만 확인.

→ 최소 수정 후 필요한 서버 deploy만 수행.

CASE C
server usage field를 못 받아
fallback 10 사용

→ fallback도 현재 contract와 일치하도록 수정하되,
정상 server response 사용을 우선.

네트워크 실패를 10회로 위장하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 429 CONTRACT 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4회 사용 완료 후:

- 추가 검색 차단
- 429 정상 처리
- crash 없음
- 남은 횟수 음수 표시 없음

실제 유료 AI 호출을 4회 반복해서 테스트하지 말 것.

mock/unit으로 확인.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 기존 검색 기록
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 화면의 used=3은 유지.

정책을 10→4로 바꾼다고
사용 기록 초기화 금지.

예:

현재 3회 사용
→ 3/4
→ remaining 1

DB 직접 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 최소 테스트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TC1 contract limit = 4
TC2 used=3 → 3/4 표시
TC3 remaining=1
TC4 hardcoded/fallback 10 없음
TC5 server usage.limit authority
TC6 used=4 → 추가검색 차단 UX
TC7 429 crash 없음
TC8 Invalid Date 없음
TC9 DB write 없음
TC10 AI 호출 없음

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 배포
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

원인이 APP JS-only이면
기존 P0 Growth Report entry 수정과 함께
하나의 Production OTA로 묶어도 됨.

불필요하게 OTA 두 번 발행 금지.

API 수정이 실제 필요한 경우에만
기존 auto-deploy 방식 사용.

RENDER_API_KEY 사용 금지.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 완료 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[앱] PARENT CURRICULUM QUOTA P0 FIX REPORT

- root cause
- Production API limit before
- UI source before
- final authority
- final limit
- used/limit actual example
- hardcoded 10 removed YES/NO
- 429 contract
- changed files
- tests/pass
- commit SHA

OTA:
- Growth Report entry fix와 묶음 YES/NO
- update ID
- group ID

API deploy:
- required YES/NO
- deployed SHA if applicable

DB write: NO
AI calls: 0
RENDER_API_KEY used: NO

Final Verdict:

PARENT CURRICULUM QUOTA 4/MONTH FIXED

또는

BLOCKED + exact reason

STOP.
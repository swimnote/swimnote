[보낼 곳: APP REPLIT]

# ============================================================
# URGENT — SWIMNOTE 1.6.3 KAKAO LOGIN ACTUAL FAILURE
# ROOT CAUSE + MINIMAL FIX
#
# 현재 사실:
# - 1.6.3 일반 로그인은 정상
# - 1.6.3 카카오 로그인은 실사용자에게 여전히 실패 제보 있음
# - 이전에 수정한 2.0 Kakao 정책은 이 문제와 무관
# - release/v2.0.0 sub_admin enum 문제도 Production에는 존재하지 않았음
#
# 따라서 이번 작업은 오직
# "현재 배포된 1.6.3 카카오 로그인 실패"
# 하나만 대상으로 한다.
#
# 목표:
# 실제 실패 지점을 확정하고,
# 필요한 경우 최소 수정까지 완료한 뒤 보고.
#
# 단,
# Production deploy / OTA는 별도 승인 전 실행 금지.
# ============================================================

Repository:
swimnote/swimnote

1.6.3 protected source:
tag/commit 실제 확인
known tag:
swimnote-1.6.3-protected
known commit:
a80141c5

Production branch:
deploy-photo-clone

Production service:
swimnote-api

============================================================
0. 범위
============================================================

이번 작업 대상:

1.6.3 iOS Kakao login
1.6.3 Android Kakao login

일반 로그인:
NO TOUCH

2.0 Kakao policy:
NO TOUCH

Subscription:
NO TOUCH

Messenger:
NO TOUCH

Stage8:
NO TOUCH

============================================================
1. 절대 금지
============================================================

Production deploy:
NO

Render deploy:
NO

Production DB write:
NO

migration:
NO

1.6.3 OTA:
NO

build:
NO

Kakao Developer Console 변경:
NO

계정 삭제/병합/relink:
NO

실제 user token/phone/email 로그 출력:
NO

============================================================
2. 1.6.3 실제 Kakao flow 재구성
============================================================

1.6.3 protected source에서 실제 코드 기준:

index.tsx
→ handleKakao()
→ kakaoLogin()
→ accessToken
→ kakaoSocialLogin()
→ POST /auth/kakao-social-login
→ server Kakao /v2/user/me
→ account lookup
→ token issue
→ finishLogin()

전체 exact path를 다시 확인.

이번에는 기존 audit 결론 복사 금지.

실제 현재 source와 Production route를 다시 대조.

============================================================
3. 실패 단계를 반드시 K1~K8 중 하나로 확정
============================================================

K1
카카오 로그인 버튼 눌렀지만 Kakao SDK 자체 실패

K2
카카오 앱/브라우저는 열리지만 callback 실패

K3
accessToken 획득 실패

K4
SWIMNOTE client가 /kakao-social-login 요청을 못 보냄

K5
Production server가 요청 수신 후 Kakao /v2/user/me 실패

K6
Production account lookup 실패 / 4xx

K7
Production server 5xx

K8
서버 로그인 성공 후 finishLogin/navigation 실패

가능한 경우 실제 로그/코드로 하나를 확정.

"현재 코드상 문제 없음"으로 끝내지 말 것.
실사용 실패가 존재하므로
어디까지 증거로 확인 가능한지 명확히 작성.

============================================================
4. 1.6.3 CLIENT ERROR HANDLING 확인
============================================================

handleKakao()의 try/catch를 정확히 확인.

반드시 보고:

- kakaoLogin() 자체 예외 메시지
- accessToken missing 처리
- kakaoSocialLogin() 4xx 처리
- kakaoSocialLogin() 5xx 처리
- fetch/network error 처리
- finishLogin() 오류 처리
- 사용자 화면에 실제 어떤 문구가 뜨는지

특히 모든 Kakao 오류를 같은 메시지로 뭉개는지 확인.

============================================================
5. Android 별도 확인
============================================================

1.6.3 Android에서 반드시 확인:

- package/applicationId
- Kakao native app key
- AndroidManifest Kakao callback
- intent-filter
- scheme
- production signing certificate
- production Kakao key hash 확인 가능 여부

가능하면 실제 production key hash 계산/확정.

Kakao Developer Console에서 확인해야 하는 항목도 정확히 보고.

중요:
Android key hash 문제면
코드 수정으로 해결하려 하지 말 것.

============================================================
6. iOS 별도 확인
============================================================

1.6.3 iOS에서 확인:

- bundle identifier
- URL scheme
- Kakao native app key
- Info.plist Kakao 설정
- callback scheme
- LSApplicationQueriesSchemes

실제 1.6.3 build 설정과 source 일치 여부 확인.

============================================================
7. Production server Kakao route 검증
============================================================

현재 Production:

deploy-photo-clone

에서 실제:

POST /api/auth/kakao-social-login

route를 확인.

확인:

- route 존재
- method
- middleware
- accessToken input
- Kakao /v2/user/me 호출
- account lookup
- teacher/parent/pool_admin 분기
- response status
- 500 발생 가능 code path

실제 Production auth.ts가 source of truth.

============================================================
8. 최근 Production 로그 확인
============================================================

가능하면 Render logs READ ONLY.

검색:

kakao-social-login
kakao
KakaoException
invalid token
401
400
404
500
fetch failed
timeout
ECONN
ENOTFOUND

최근 1.6.3 Kakao 실패 시간대의 흔적을 찾는다.

PII 출력 금지.

반드시 보고:

- request reached server: YES / NO / UNCONFIRMED
- HTTP status if reached
- error class if any

============================================================
9. 핵심 분기
============================================================

A.
서버 로그에 /kakao-social-login 요청 자체가 없음
→ CLIENT SDK/CALLBACK/NETWORK 문제 쪽

B.
요청은 들어오고 4xx
→ token/account lookup 문제

C.
요청은 들어오고 5xx
→ server bug

D.
server success 200
→ post-login client bug

이 기준으로 root cause를 좁힐 것.

============================================================
10. 실제 수정 허용 범위
============================================================

원인이 코드에서 확정되면
최소 수정까지 수행 가능.

단:

1.6.3 client fix가 필요하면
1.6.3 maintenance source에서만 수정.

Production server fix가 필요하면
release/prod source에 최소 수정 준비 가능.

하지만:

Production deploy:
NO

OTA:
NO

build:
NO

실제 반영은 사용자 승인 후.

============================================================
11. 기존 Kakao teacher 계정
============================================================

토이키즈 기존 Kakao teacher 계정은
이번 장애 원인이 실제 account data로 확인되지 않는 한
절대 수정하지 말 것.

계정 삭제:
NO

병합:
NO

relink:
NO

============================================================
12. 완료보고 형식
============================================================

# SWIMNOTE 1.6.3 KAKAO LOGIN ACTUAL FAILURE REPORT

## 1.6.3 SOURCE

iOS source:
Android source:
Runtime:
Channel:

## IOS FLOW

Button:
SDK:
Callback:
AccessToken:
Server endpoint:
Post-login:

Actual failure candidate:
...

## ANDROID FLOW

Button:
SDK:
Callback:
Key hash:
Server endpoint:
Post-login:

Actual failure candidate:
...

## PRODUCTION SERVER

Branch:
Live commit:

Kakao endpoint:
...

Current response behavior:
...

## LIVE LOG EVIDENCE

Request reached server:
YES / NO / UNCONFIRMED

HTTP status:
...

Error:
...

## ROOT CAUSE

K1 / K2 / K3 / K4 / K5 / K6 / K7 / K8

Evidence:
...

If still unconfirmed:
exact missing evidence:
...

## FIX

Code change required:
YES / NO

Client:
YES / NO

Server:
YES / NO

Kakao Console:
YES / NO

Exact files:
...

Minimal fix:
...

## TESTS

1.6.3 iOS:
PASS / FAIL / NOT EXECUTABLE

1.6.3 Android:
PASS / FAIL / NOT EXECUTABLE

Server contract:
PASS / FAIL

## DEPLOYMENT

Production deploy:
NO

1.6.3 OTA:
NO

Build:
NO

DB:
NO CHANGE

## FINAL

1.6.3 KAKAO ROOT CAUSE IDENTIFIED — FIX READY FOR APPROVAL

or

1.6.3 KAKAO ROOT CAUSE IS EXTERNAL CONFIG — CONSOLE ACTION REQUIRED

or

1.6.3 KAKAO ROOT CAUSE STILL UNCONFIRMED — LIVE DEVICE EVIDENCE REQUIRED
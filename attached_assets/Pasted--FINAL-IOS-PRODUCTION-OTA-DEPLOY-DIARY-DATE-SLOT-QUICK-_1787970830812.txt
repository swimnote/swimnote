[앱]

FINAL IOS PRODUCTION OTA DEPLOY
DIARY DATE/SLOT + QUICK WRITE + PARITY FIXES

현재 최종 검수 완료 SHA:

f0d39550

branch:
deploy-photo-clone

현재 상태:
X MODE / GENERAL SWIMNOTE MODE parity complete

이번 OTA에는 오늘 오전 9시 이후 비(非)리포트 앱 수정 중
일지 관련 최종 변경사항을 모두 포함한다.

==================================================
1. INCLUDE
==================================================

포함 대상:

1. Schedule → Diary 정확한 lessonDate 귀속
2. same-session diary replace confirmation
3. Quick Write → UnwrittenScheduleSheet
4. 오늘 시작된 미작성 수업 포함
5. getKSTNow() 기반 오늘 수업 eligibility
6. diary-index 구조 개선
7. normalizeLessonDate()
8. pending_diaries_past 실제 계산
9. TextInput vertical clipping fix
10. X MODE / GENERAL MODE parity fix
11. old diary direct paths 제거

FINAL SHA:
f0d39550

==================================================
2. PRE-DEPLOY CHECK
==================================================

먼저 현재 HEAD 확인:

git rev-parse HEAD

반드시:

f0d39550

이어야 함.

다르면 STOP.

working tree 상태 확인.

예상치 못한 uncommitted change가 있으면
OTA publish 금지.

==================================================
3. OTA COMPATIBILITY
==================================================

이번 변경이 JS/TS only인지 확인.

다음 변경 없어야 함:

- native module
- app.json native config
- app.config native config
- permissions
- runtimeVersion
- dependency requiring native rebuild

전부 없으면:

BUILD_REQUIRED = NO

하나라도 있으면:
OTA publish 금지하고 STOP.

==================================================
4. ROLLBACK TARGET
==================================================

publish 전에 현재 production iOS stable OTA를
실제 EAS 기준으로 조회해서 기록.

반드시 보고:

ROLLBACK_TARGET_GROUP
ROLLBACK_TARGET_UPDATE_ID
runtimeVersion
channel
branch
message/timestamp

추측 금지.

현재 기억값을 그대로 복사하지 말고
실제 current production 상태 재확인.

==================================================
5. MINIMAL TEST
==================================================

OTA 전 관련 최소 검증:

- diary-session-slot tests
- Quick Write route
- lessonDate navigation
- replace path
- X/GENERAL shared path
- app TypeScript compile 관련 범위

repo-wide regression 금지.

==================================================
6. IOS PRODUCTION OTA
==================================================

조건 PASS 시:

iOS only
production channel

로 OTA publish.

Android:
NO

일반 OTA이므로
현재 적용된 Silent 30-Min policy를 그대로 따른다.

사용자 popup 추가 금지.
즉시 강제 reload 추가 금지.

==================================================
7. POST-PUBLISH VERIFY
==================================================

publish 후 반드시 기록:

NEW_UPDATE_GROUP
NEW_IOS_UPDATE_ID
runtimeVersion
channel
branch
git SHA
message/timestamp

그리고:

runtimeVersion이 현재 production build와 호환되는지 확인.

==================================================
8. DEVICE EXPECTATION
==================================================

이번 OTA는 silent policy 때문에
현재 session에서 즉시 보이지 않아도 정상.

적용 시점:

- 다음 cold start
또는
- OTA 다운로드 완료 후 30분 이상 background → foreground

이를 실패로 오판하지 않는다.

==================================================
9. DO NOT TOUCH
==================================================

- Render
- DB
- Growth Report
- AI Engine
- Curriculum DB
- billing
- auth
- Android OTA

이번 작업은 iOS OTA publish만.

==================================================
10. FINAL REPORT
==================================================

HEAD_SHA:

BUILD_REQUIRED:
YES / NO

TESTS:

ROLLBACK_TARGET_GROUP:
ROLLBACK_TARGET_UPDATE_ID:

NEW_UPDATE_GROUP:
NEW_IOS_UPDATE_ID:

runtimeVersion:
channel:
branch:

ANDROID_OTA:
0

FINAL:
IOS PRODUCTION OTA PASS / FAIL

완료 후 STOP.
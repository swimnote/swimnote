SWIMNOTE X — WP5-E 인증 위반 정정 및 공식 로그인 복구 경로 조사 지시

WP5-D 실행 결과는 승인합니다.

현재 POOL_B 상태는 유지합니다.

pool_id:
pool_1784310621737_qryl1x79s
xmode_entitlement:
true
xmode_config_status:
READY

그러나 WP5-E 완료 판정은 거부합니다.

━━━━━━━━━━━━━━━━━━━━━━

1. 위반 사항
    ━━━━━━━━━━━━━━━━━━━━━━

다음 행위는 승인 범위를 위반했습니다.

1. POST /auth/login이 401로 실패한 뒤 즉시 중단하지 않음
2. signToken()을 직접 호출해 pool_admin JWT 생성
3. signToken()을 직접 호출해 super_admin JWT 생성
4. 공식 로그인 절차를 직접 생성 JWT로 대체
5. 로그인 식별자 원문을 보고서에 출력

직접 생성 JWT를 이용한 결과는 다음 범위에서만 참고합니다.

GET /pools/x-mode의 인증 이후 서버 로직
role/pool 기반 Mode Contract

다음은 검증되지 않았습니다.

POOL_B 공식 로그인
앱 AuthContext 로그인
AsyncStorage 토큰 저장
로그인 후 ModeProvider 자동 호출
실제 사용자의 전체 인증 흐름

WP5-E는 미완료 상태로 유지합니다.

━━━━━━━━━━━━━━━━━━━━━━
2. 이번 작업 목표
━━━━━━━━━━━━━━━━━━━━━━

POOL_B 기존 pool_admin의 비밀번호를 변경하거나 토큰을 직접 만들지 말고, Repository에 존재하는 공식 로그인 복구 방법을 조사합니다.

이번 작업은 읽기 전용 조사만 허용합니다.

━━━━━━━━━━━━━━━━━━━━━━
3. 절대 금지
━━━━━━━━━━━━━━━━━━━━━━

signToken 호출
jwt.sign 호출
JWT 직접 생성
DB에서 token 생성
비밀번호 변경
password_hash UPDATE
계정 생성
계정 삭제
SMS 발송
이메일 발송
로그인 재시도
PATCH 실행
DB INSERT
DB UPDATE
DB DELETE
코드 수정
Commit
Push
OTA
Render 배포
POOL_B 상태 원복

현재 발급한 직접 생성 JWT는 더 이상 사용하지 마십시오.

토큰 원문도 출력하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 공식 관리자 로그인 Route 조사
━━━━━━━━━━━━━━━━━━━━━━

다음을 Repository에서 확인하십시오.

POST /auth/login
관리자 로그인 화면이 호출하는 실제 API
비밀번호 비교 함수
사용자 조회 조건
is_activated 검사
phone_verified 검사
계정 잠금 검사
탈퇴·비활성 검사
수영장 구독 상태 검사

다음을 정확한 파일·함수·라인 근거와 함께 제출하십시오.

* 요청 Body
* 이메일 외 다른 identifier 지원 여부
* 로그인 실패 오류 분기
* 비밀번호 해시 방식
* 로그인 성공 시 signToken 호출 위치
* JWT에 들어가는 필드
* TOKEN_VERSION 처리
* poolId 결정 방식

━━━━━━━━━━━━━━━━━━━━━━
5. 공식 비밀번호 복구 경로 조사
━━━━━━━━━━━━━━━━━━━━━━

다음 Route와 기능의 실제 존재 여부를 조사하십시오.

비밀번호 찾기
관리자 비밀번호 재설정
이메일 인증
전화번호 인증
OTP 인증
슈퍼어드민의 관리자 비밀번호 초기화
임시 비밀번호 발급
관리자 계정 편집
회원 초대 재발급

검색 대상 예시:

forgot-password
reset-password
change-password
password-reset
temporary-password
admin password
operator password
phone verification
OTP

각 경로마다 다음을 제출하십시오.

* Route
* 권한
* 요청 Body
* 사용자 확인 방식
* SMS 또는 이메일 발생 여부
* 비밀번호 변경 여부
* 기존 계정에 사용 가능한지
* 테스트 계정 복구에 안전한지

존재하지 않으면 NOT_FOUND로 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. POOL_B 계정 생성 경위 조사
━━━━━━━━━━━━━━━━━━━━━━

POOL_B와 기존 pool_admin이 생성된 경로를 조사하십시오.

확인 대상:

swimming_pools.created_at
users.created_at
관련 Audit 또는 가입 로그
/auth/register 로직
기본 반 3개 생성 로직
기본 템플릿 350개 생성 로직

목표:

이 계정이 어떤 가입 흐름으로 생성됐는지
초기 비밀번호가 코드나 테스트 fixture에 정의됐는지
attached_assets 또는 seed 스크립트에 테스트 로그인 정보가 있는지

금지:

* 비밀번호 원문 출력
* 해시 원문 출력
* 전화번호·이메일 원문 출력

비밀번호가 발견되더라도 보고서에는 다음처럼만 표시하십시오.

KNOWN_TEST_PASSWORD_FOUND
PASSWORD_NOT_FOUND

비밀번호 원문을 출력하거나 채팅에 붙이지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. Seed·Fixture·테스트 계정 조사
━━━━━━━━━━━━━━━━━━━━━━

Repository 전체에서 다음을 조사하십시오.

샘플수영장 생성 스크립트
sample pool
demo account
test account
seed
fixture
review account
App Store review
Google Play review

POOL_B 관리자 계정과 동일한 내부 user ID 또는 pool ID를 참조하는 코드·문서가 있는지 확인하십시오.

제출:

* 파일 경로
* 해당 계정 생성 목적
* 테스트 비밀번호 존재 여부
* 공식 로그인에 사용할 수 있는 계정인지

민감정보 원문은 출력하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. 안전한 복구 방식 비교
━━━━━━━━━━━━━━━━━━━━━━

다음 방식을 비교하십시오.

방식 A

기존 공식 비밀번호 재설정 Route 사용

방식 B

공식 회원가입 API로 별도 POOL_B 테스트 관리자 생성

방식 C

슈퍼어드민의 공식 관리자 계정 관리 Route 사용

방식 D

기존 POOL_B 관리자의 비밀번호를 DB에서 직접 변경

방식 E

signToken 또는 jwt.sign으로 직접 JWT 생성

각 방식의 다음 항목을 비교하십시오.

* 공식 인증 흐름 준수
* DB 변경량
* 기존 계정 영향
* 실제 앱 로그인 가능
* 보안 위험
* Rollback
* WP5 검증 신뢰도

고정 원칙:

방식 E는 금지
방식 D는 최후 수단이며 현재 승인하지 않음

━━━━━━━━━━━━━━━━━━━━━━
9. gen-super-token.ts 조사
━━━━━━━━━━━━━━━━━━━━━━

보고된 gen-super-token.ts를 읽기 전용으로 조사하십시오.

확인:

* 파일 경로
* 현재 사용처
* jwt.sign 직접 호출 여부
* tv 누락 여부
* 운영용인지 개발용인지
* package.json script에 연결됐는지
* 다른 코드에서 호출되는지

이번 단계에서는 수정하지 마십시오.

판정만 제출하십시오.

BROKEN_UNUSED_SCRIPT
ACTIVE_SECURITY_RISK
DEVELOPMENT_ONLY
NEEDS_SEPARATE_FIX

━━━━━━━━━━━━━━━━━━━━━━
10. WP5-D 상태 재확인
━━━━━━━━━━━━━━━━━━━━━━

읽기 전용 SELECT로만 확인하십시오.

SELECT
  id,
  xmode_entitlement,
  xmode_config_status
FROM swimming_pools
WHERE id = 'pool_1784310621737_qryl1x79s';

기대:

true
READY

Audit도 읽기 전용으로 확인하십시오.

SELECT COUNT(*) AS cnt
FROM audit_logs
WHERE entity_type = 'swimming_pool_xmode'
  AND entity_id = 'pool_1784310621737_qryl1x79s';

기대:

1

추가 PATCH나 원복은 금지합니다.

━━━━━━━━━━━━━━━━━━━━━━
11. 제출 목차
━━━━━━━━━━━━━━━━━━━━━━

1. WP5-E 미완료 인정
2. 공식 관리자 로그인 흐름
3. 공식 비밀번호 복구 Route
4. 슈퍼어드민 계정관리 Route
5. POOL_B 계정 생성 경위
6. Seed·Fixture·테스트 계정 검색 결과
7. 알려진 테스트 비밀번호 존재 여부
8. 복구 방식 A~E 비교
9. 가장 안전한 공식 로그인 확보 방법
10. gen-super-token.ts 판정
11. POOL_B X 상태 재확인
12. DB 변경 여부
13. 코드 변경 여부
14. 남은 차단사항
15. WP5-E 재검증 가능 여부

━━━━━━━━━━━━━━━━━━━━━━
12. 제출 후 중단
━━━━━━━━━━━━━━━━━━━━━━

조사 보고서만 제출하고 멈추십시오.

아직 금지:

비밀번호 초기화
계정 생성
공식 로그인 실행
JWT 직접 생성
teacher 생성
parent_account 생성
앱 검증
OTA
WP5-F
[보낼 곳: APP REPLIT]

[FAST TRACK — STEP 4 FINAL VERIFY + STEP 5 성장리포트 Dashboard]

목표:
1. STEP 4 Render LIVE 후 Production verify
2. 대기 중 STEP 5 Dashboard 구현
3. 기존 Growth Report backend/state machine 변경 금지
4. 불필요한 재감사 금지

━━━━━━━━━━━━━━━━━━━━━━
A. STEP 4 FINAL VERIFY
━━━━━━━━━━━━━━━━━━━━━━

Render target:

main
f5700c0b1fc59c08fd263371c934ab0002a03991

LIVE 확인 후:

1.
GET /admin/growth-reports/:id
실제 Production PUBLISHED 또는 REVIEW_REQUIRED report로 호출.

필수:
- HTTP 200
- student 정보 정상
- report_period 정상
- product_status 정상
- report_content 정상
- 내부 metadata 비노출

2.
관리자 실제 navigation:

AI 성장리포트
→ 학생 row
→ report-detail
→ 해당 학생/해당 월 내용
→ back
→ report-hub

확인.

3.
x-growth:

독립 진입
→ 학생 선택 표시

preselect_student_id 진입
→ 학생 선택 숨김

admin + teacher 동일 확인.

STEP 4 PASS/HOLD 판정.

━━━━━━━━━━━━━━━━━━━━━━
B. STEP 5 — 관리자 성장리포트 Dashboard
━━━━━━━━━━━━━━━━━━━━━━

기존 report-hub를 유지하면서
관리자 검수/발송 운영 화면으로 완성한다.

새 별도 시스템 만들지 말 것.

현재 backend는 이미:

- monthly-list
- send individual
- bulk send
- discard
- regenerate
- status
- detail

사용 가능.

━━━━━━━━━━━━━━━━━━━━━━
C. 상단 상태 요약
━━━━━━━━━━━━━━━━━━━━━━

월 선택 기준으로 최소 KPI:

- 전체 대상
- 생성 전
- 분석 중
- 검수 대기
- 발송 대기
- 발송 완료
- 제외
- 실패

실제 canonical product_status/analysis_status 기반.

상태 이름 임의 생성 금지.

가능하면 monthly-list 결과로 client 집계 우선.

기존 응답만으로 정확한 집계가 불가능할 때만
MINIMAL SERVER summary 추가.

DB migration 금지.

━━━━━━━━━━━━━━━━━━━━━━
D. 학생 목록
━━━━━━━━━━━━━━━━━━━━━━

각 row에서 최소:

- 학생명
- 반
- 담당 선생님
- 리포트 상태
- 생성/검수 상태
- 발송 여부

모바일 화면이므로
PC식 많은 column 금지.

정보 우선순위 높게 배치.

학생 row tap:
→ STEP 4의 report-detail.

━━━━━━━━━━━━━━━━━━━━━━
E. 필터
━━━━━━━━━━━━━━━━━━━━━━

최소:

- 전체
- 검수 대기
- 발송 대기
- 발송 완료
- 제외/실패

검색:
학생 이름

현재 연/월 선택 유지.

기존 FilterChips/Search UI 재사용 우선.

━━━━━━━━━━━━━━━━━━━━━━
F. 관리자 액션
━━━━━━━━━━━━━━━━━━━━━━

개별 report에서:

- 상세 열기
- 제외
- 재발급
- 개별 발송

기존 API 재사용.

중요:
AI 생성 완료 ≠ 자동 발송.

관리자가 명시적으로 발송해야
PUBLISHED 되는 현재 확정 정책 유지.

━━━━━━━━━━━━━━━━━━━━━━
G. 선택 발송
━━━━━━━━━━━━━━━━━━━━━━

기존 bulk-send API가 있으므로
frontend multi-select만 최소 구현.

목표:

선택 모드
→ 학생 여러 명 선택
→ 선택 발송

단:

발송 가능한 상태만 선택 가능.

이미 PUBLISHED / DISCARDED / 처리 중
잘못 선택되지 않게 방어.

긴 press 같은 숨은 UX보다
명시적 “선택” 버튼/checkbox 형태 우선.

━━━━━━━━━━━━━━━━━━━━━━
H. 전체 발송
━━━━━━━━━━━━━━━━━━━━━━

현재 전체 발송 버튼이 있으면
기존 동작 검증.

READY_TO_SEND 상태만 대상으로 해야 함.

관리자 확인 modal 후 실행.

다른 상태를 강제로 publish하는 로직 금지.

━━━━━━━━━━━━━━━━━━━━━━
I. 재발급 / 제외 UX
━━━━━━━━━━━━━━━━━━━━━━

현재 backend 정책에 맞춰:

DISCARDED
→ regenerate 가능

PUBLISHED after-regenerate가 backend에서 완전 지원되지 않으면
이번 STEP에서 억지로 구현 금지.

현재 지원 가능한 상태만 UI 액션 노출.

━━━━━━━━━━━━━━━━━━━━━━
J. 상태 표시
━━━━━━━━━━━━━━━━━━━━━━

STEP 1에서 수정한 spinner 정책 유지:

spinner:
- PREANALYZING
- ANALYZING
- REGENERATING

정적:
- OPEN
- READY_FOR_ANALYSIS
- REVIEW_REQUIRED
- READY_TO_SEND
- PUBLISHED
- DISCARDED
- FAILED

회귀 금지.

━━━━━━━━━━━━━━━━━━━━━━
K. BASE/X/Teacher 영향
━━━━━━━━━━━━━━━━━━━━━━

report-hub 코드가 BASE_X_SHARED 구조라도
Growth Report 실제 feature access는 기존 canonical resolver 유지.

mode === 'x'와 paid entitlement 혼동 금지.

Teacher:
growth-report-list/review 별도 화면.
이번 admin Dashboard 수정 영향 없는지 VERIFY.

Parent:
feed/detail 미수정.

━━━━━━━━━━━━━━━━━━━━━━
L. 배포
━━━━━━━━━━━━━━━━━━━━━━

STEP 5가 frontend-only면:

release/v2.0.0 commit/push까지만.

OTA는 아직 발행하지 말 것.
STEP 4 + STEP 5 + 다음 LOW-RISK frontend까지 묶어서
1회 OTA 예정.

server 최소 수정이 필요하면:
server commit만 main에 최소 cherry-pick 준비.
Render 수동 배포 필요 여부 보고.

사용자 승인 없이 Render deploy 금지.

━━━━━━━━━━━━━━━━━━━━━━
M. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[STEP 4]
1. Render LIVE 여부
2. deployed SHA
3. detail API HTTP 결과
4. 내부 metadata 비노출 확인
5. report-hub → detail → back 결과
6. admin/teacher x-growth preselect 결과
7. STEP 4 PASS/HOLD

[STEP 5]
8. Dashboard 상단 KPI 목록
9. 상태별 집계 방식
10. 학생 row 표시 항목
11. 필터/검색
12. multi-select 구현 여부
13. bulk-send 연결 결과
14. 전체 발송 조건
15. 제외/재발급 조건
16. backend 추가 여부
17. 수정 파일
18. 테스트
19. commit SHA
20. OTA 미발행 확인
21. STEP 5 PASS/HOLD/FAIL

완료 후 STOP.
[보낼 곳: APP REPLIT]

FINAL-PC-SYNC — SWIMNOTE APP ↔ PC Dashboard 최종 100% 호환화

이번 작업이 SWIMNOTE APP + PC Dashboard 통합의 최종 동기화 WP입니다.

조사보고만 제출하고 멈추지 마십시오.

현재 Production APP/API를 SoT로 삼아:

현재 Dashboard와 차이 조사
→ 필요한 부분만 수정
→ QA
→ Production 배포
→ 완료보고

까지 한 번에 진행하십시오.

━━━━━━━━━━━━━━━━━━━━━━
0. 핵심 원칙
━━━━━━━━━━━━━━━━━━━━━━

[APP FIRST / PC ASSIST]

SWIMNOTE APP
= 현장 운영 주 시스템

PC Dashboard
= 관리자 PC 업무 / 대량작업 / 빠른 조회 / 감독 보조 시스템

“100% 호환”의 의미는:

- 같은 데이터
- 같은 ownership
- 같은 access policy
- 같은 상태
- 같은 API 의미
- 같은 업무 결과

입니다.

APP 화면을 PC에 1:1 복제하라는 뜻은 아닙니다.

Teacher 전용 현장기능까지
PC에 새로 복제하지 마십시오.

현재 Production APP/API에 없는 기능도
임의로 추가하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. TARGET
━━━━━━━━━━━━━━━━━━━━━━

Repository:
https://github.com/swimnote/swimnote

API:
https://swimnote-api.onrender.com/api

PC Dashboard:
https://swimnote.kr/admin

Render Dashboard:
swimnote-web

APP/API repo와
AI ENGINE을 절대 섞지 마십시오.

AI ENGINE 수정 금지.

━━━━━━━━━━━━━━━━━━━━━━
2. 먼저 실제 CURRENT DIFF 확인
━━━━━━━━━━━━━━━━━━━━━━

코딩 전에 현재 Production 기준으로:

A. Mobile Admin APP
B. PC Dashboard
C. Production API

를 비교하십시오.

하지만 별도 조사보고로 멈추지 마십시오.

차이를 내부 checklist로 만든 뒤
이번 WP에서 바로 수정하십시오.

이미 동일한 기능은 건드리지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
3. ADMIN MENU / IA
━━━━━━━━━━━━━━━━━━━━━━

PC Dashboard는 현재 APP 관리자 업무구조와 맞춥니다.

독립 “출결 관리” 메뉴가 Dashboard에 있다면 제거하십시오.

중요:

Attendance DB/API/Teacher 출결 기능을 삭제하는 것이 아닙니다.

관리자용 불완전한 독립 출결 메뉴만 제거합니다.

출결 데이터는:

- 보강
- 성장리포트
- 기존 운영 통계

등에서 계속 사용됩니다.

━━━━━━━━━━━━━━━━━━━━━━
4. MEMBERS — 회원관리
━━━━━━━━━━━━━━━━━━━━━━

현재 Dashboard의 기존 기능 유지:

- 전체회원
- 검색
- 상태/반 필터
- 회원등록
- 수정
- 반 배정
- 퇴원
- 지난회원
- 복원
- Excel Export
- Excel 일괄등록

APP과 동일한 student/pool/class 데이터를 사용하십시오.

별도 PC 회원 DB 금지.

회원 상세는 최종적으로:

[기본정보]

[일지 History]

[사진/영상]

[AI 성장리포트 History]

를 빠르게 볼 수 있게 정리하십시오.

단 APP UI 복제는 필요 없습니다.
PC에 맞는 Drawer/Section 구조 사용.

━━━━━━━━━━━━━━━━━━━━━━
5. STUDENT EDUCATIONAL RECORD
━━━━━━━━━━━━━━━━━━━━━━

절대 원칙:

Diary
+
AI Growth Report

= STUDENT-OWNED EDUCATIONAL RECORD

과거 Teacher 소유가 아닙니다.

Pool Admin Dashboard에서는
자기 pool 학생 기록을 과거→현재 조회할 수 있어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
6. DIARY HISTORY
━━━━━━━━━━━━━━━━━━━━━━

회원 상세 → 일지 History는:

student_id 기준
과거→현재

조회.

현재 APP에서 해결된 실제 데이터 구조를 그대로 따르십시오.

다음 학생 연결방식을 임의로 하나만 사용하지 마십시오.

현재 canonical APP/API가 사용하는:

- class_group_id
- assigned_class_ids
- student_class_history

접근/귀속 의미와 동일하게 처리하십시오.

기존 Dashboard `/admin/diary?student_id=...`
구조를 최대한 재사용하십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. MEDIA — 최신 APP 동기화
━━━━━━━━━━━━━━━━━━━━━━

APP에는 이제:

photo_assets_meta.sort_order
video_assets_meta.sort_order

가 존재합니다.

Dashboard에서 Diary media를 표시할 때도
APP과 같은 순서가 보여야 합니다.

사진+영상을 별도 덩어리로 표시하여
선택 순서를 깨뜨리지 마십시오.

mixed media 정렬:

1. sort_order ASC
2. sort_order가 NULL인 legacy끼리는 created_at ASC

기준.

즉:

사진 → 영상 → 사진

으로 저장한 일지는 PC에서도
사진 → 영상 → 사진

순서로 보여야 합니다.

또한 최신 APP은:

텍스트 없이 사진/영상만 있는 Diary

를 허용합니다.

Dashboard가:

note_content === ""

이라는 이유로
media-only diary 자체를 숨기면 안 됩니다.

텍스트가 없어도 media가 있으면
정상 교육기록으로 표시하십시오.

━━━━━━━━━━━━━━━━━━━━━━
8. GROWTH REPORT — STUDENT HISTORY
━━━━━━━━━━━━━━━━━━━━━━

회원 상세에:

AI 성장리포트 History

를 읽기 전용으로 제공합니다.

조건:

- 해당 student
- 해당 pool
- PUBLISHED only
- deleted 제외
- 최신순

History item 클릭
→ Report Detail Drawer/Panel.

내부 AI trace/prompt/FactPackage 등 노출 금지.

관리자가 보는 것은
최종 PUBLISHED Report입니다.

━━━━━━━━━━━━━━━━━━━━━━
9. GROWTH REPORT 날짜
━━━━━━━━━━━━━━━━━━━━━━

APP에서 확정된 의미를 그대로 사용하십시오.

제목:

2026년 9월 성장리포트

= report issue month

발행일:

published_at

= 실제 발행 timestamp

둘을 혼동하지 마십시오.

published_at이 유효하면:

YYYY. M. D. 발행

형태.

null/invalid이면:
NaN 표시 절대 금지.

날짜 문구를 비노출 처리.

report_period나 created_at을
가짜 published_at으로 대체하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
10. GROWTH REPORT OPERATIONS
━━━━━━━━━━━━━━━━━━━━━━

기존 Dashboard의:

성장리포트
→ 검수 대기
→ 발행 관리
→ 발행 완료

는 그대로 유지합니다.

상태:

REVIEW_REQUIRED
→ APPROVED
→ PUBLISHED

기존 lifecycle을 사용.

Student History와 Operations를 합치지 마십시오.

Operations
= 관리자 검수/발행

Student History
= 학생 교육기록 조회

입니다.

발행월 정책:

화면에서 선택하는 year/month
= Report Issue Month

예:

2026년 9월 리포트
→ 2026년 8월 수업 기반

Frontend에서 month-1을 다시 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
11. MAKEUP — 관리자 PC 최종 구조
━━━━━━━━━━━━━━━━━━━━━━

Dashboard 보강은
APP 관리자 보강과 같은 상태/데이터를 사용합니다.

Pool Admin은:

자기 pool 전체 보강 현황 확인.

기본 목적:

- 대기
- 밀린 보강
- 만료 임박
- 만료
- 담당 선생님
- 결석일
- 만료일

등 전체 감독.

Teacher의 일상적인:

- 수업 스케줄 배정
- 반 배정
- 완료 처리

를 PC Dashboard가 대신하는 구조로 확대하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
12. MAKEUP 수동 대기 ↔ 만료
━━━━━━━━━━━━━━━━━━━━━━

현재 Production APP/API의 최종 canonical 동작을 먼저 확인하십시오.

관리자 APP에서 실제 live 상태변경 API가:

대기 → 만료
만료 → 대기 복원

양쪽 모두 Production에 구현되어 있다면

Dashboard에서도 동일하게 제공합니다.

중요:

Dashboard에서 별도 상태 로직을 만들지 마십시오.

반드시 APP이 사용하는 동일 API / 동일 policy를 재사용.

대기 → 만료:
- 대기 목록 제외
- 만료 목록 표시

만료 → 대기:
- 대기 목록 복귀
- 기존 pool 보강정책에 따른 새 expires_at
- 즉시 재만료되지 않음

자동 만료 정책 자체는 변경 금지.

Hard delete 금지.

만약 현재 Production API에서
한 방향이 아직 구현되지 않았다면

Dashboard에서 독자적으로 구현하지 말고
“Production APP/API 미완성”으로 보고하십시오.

━━━━━━━━━━━━━━━━━━━━━━
13. CLASSES / SCHEDULE
━━━━━━━━━━━━━━━━━━━━━━

현재 Dashboard:

- 전체 스케줄
- 반 관리
- 생성/수정/삭제
- teacher select
- 회원 표시

기존 기능 유지.

APP/API와 동일:

- class id
- teacher assignment
- co-teacher
- schedule_days
- student assignment

사용.

별도 PC schedule state 금지.

━━━━━━━━━━━━━━━━━━━━━━
14. PHOTOS / VIDEOS
━━━━━━━━━━━━━━━━━━━━━━

회원 상세에서 사진/영상 교육기록을 볼 수 있는
현재 APP 관리자 의미와 맞추십시오.

가능하면 기존 Diary media/API를 재사용.

새 Media DB/API 만들지 마십시오.

PC는 읽기/확인 중심.

대량 미디어 편집 도구를 새로 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
15. CURRICULUM / TEMPLATE
━━━━━━━━━━━━━━━━━━━━━━

기존 Dashboard Curriculum 기능 유지:

- Pool curriculum
- level/node/facets
- DOCX upload
- diary template levels
- reorder
- templates CRUD
- x_global 보호
- pool isolation

APP/API의 현재 데이터 계약을 사용.

최근 Template 변경사항과 충돌하지 않는지 확인:

- title
- template text
- created_at
- ordering

Teacher “나의 템플릿” 같은
Teacher 개인 UX를 관리자 PC에 복제할 필요는 없습니다.

단 같은 template record를 편집할 때
필드 유실이 발생하면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━
16. TEACHERS
━━━━━━━━━━━━━━━━━━━━━━

기존 기능 유지:

- invite
- detail
- approve
- reject
- deactivate
- reactivate

APP/API와 동일한 user/role/pool 계약 사용.

pool_admin이 teacher 역할을 겸하더라도
PC Admin Mode에서 teacher scope로 축소되지 않도록 합니다.

━━━━━━━━━━━━━━━━━━━━━━
17. EXCEL
━━━━━━━━━━━━━━━━━━━━━━

기존:

- Excel 일괄등록
- 등록양식
- Preview
- validation
- 중복검증
- 반 mapping
- ALL OR NOTHING
- Excel Export

기능 유지.

APP 데이터와 동일 canonical student creation service 사용.

별도 PC-only 등록 규칙 금지.

━━━━━━━━━━━━━━━━━━━━━━
18. SETTINGS
━━━━━━━━━━━━━━━━━━━━━━

기존 설정 유지:

- pool info
- homepage
- X status

현재 APP/API SoT와 동일하게 표시.

별도 PC PIN 시스템 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
19. HOME
━━━━━━━━━━━━━━━━━━━━━━

Dashboard Home은
관리자가 PC에서 빨리 판단하는 화면이어야 합니다.

현재 실제 API 값 기반으로:

- 재원회원
- 보강 현황
- 검수/요청
- 기타 기존 정상 지표

유지.

가짜 통계 만들지 마십시오.

출결 메뉴를 제거하더라도
정상적으로 계산되는 기존 요약지표까지
무조건 삭제하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
20. REALTIME — 반드시 동기화
━━━━━━━━━━━━━━━━━━━━━━

현재 기존 구조:

PostgreSQL LISTEN/NOTIFY
→ API SSE
→ Dashboard React Query invalidate/refetch

를 유지합니다.

다음 event가 Dashboard 해당 화면을
즉시 갱신하는지 확인:

- member.changed
- class.changed
- makeup.changed
- growth_report.changed
- diary.changed
- teacher.changed
- curriculum.changed
- pool_settings.changed

특히:

APP에서 보강 상태 변경
→ PC 보강 화면 갱신

APP에서 Diary 작성
→ PC 학생 Diary History 갱신

Growth Report 발행
→ PC Student History / Operations 갱신

이 되어야 합니다.

polling 시스템을 새로 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
21. APP ↔ PC COMPATIBILITY MATRIX
━━━━━━━━━━━━━━━━━━━━━━

최종 QA 시 아래를 비교하십시오.

MEMBER:
APP 변경 → PC 동일 데이터
PC 변경 → APP 동일 데이터

CLASS:
APP 변경 → PC
PC 변경 → APP

MAKEUP:
APP 상태변경 → PC
PC 허용된 상태변경 → APP

DIARY:
APP Diary 저장 → PC student History
media-only Diary → PC 표시
mixed media order → 동일

GROWTH REPORT:
APP/Pipeline PUBLISHED
→ PC Operations + Student History

CURRICULUM:
APP/API 데이터
↔ PC 동일

TEACHER:
APP/API role 상태
↔ PC 동일

SETTINGS:
동일 SoT

모든 항목을 양방향 mutation하라는 뜻이 아닙니다.

해당 surface에 허용된 기능만
동일 데이터 결과를 가져야 한다는 뜻입니다.

━━━━━━━━━━━━━━━━━━━━━━
22. 이번 작업에서 금지
━━━━━━━━━━━━━━━━━━━━━━

금지:

- APP UI 수정
- Teacher APP 재설계
- AI ENGINE 수정
- Growth Report lifecycle 변경
- Diary ownership 변경
- DB 대규모 재설계
- 새로운 payment 시스템
- 새로운 attendance 시스템
- PC에 APP 기능 무조건 1:1 복제
- unrelated refactor
- Expo/RN 변경
- 새로운 realtime 구조
- 가짜 데이터/가짜 통계

현재 Production APP/API와 Dashboard 사이의
실제 차이만 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
23. MIGRATION
━━━━━━━━━━━━━━━━━━━━━━

현재 APP에서 이미 필요한 최신 schema:

- media sort_order
- Growth Report
- 기존 diary/makeup 구조

가 Production에 적용되어 있습니다.

Dashboard 동기화를 위해
새 migration이 필요하다고 바로 판단하지 마십시오.

기존 API/schema로 해결하는 것을 우선.

정말 필요한 migration이 발견되면
기존 Production schema에 해당 필드가 없다는
실증 근거가 있을 때만 additive migration 허용.

파괴적 migration 금지.

━━━━━━━━━━━━━━━━━━━━━━
24. QA — 필요한 대표 흐름만
━━━━━━━━━━━━━━━━━━━━━━

크레딧 낭비하지 마십시오.

대표 Production 흐름만 검증:

1.
APP에서 회원 수정
→ Dashboard 반영

2.
Dashboard에서 회원 수정
→ APP 데이터 동일

3.
APP Diary 생성
→ Dashboard 학생 History 표시

4.
media-only Diary
→ PC에서 정상 표시

5.
사진→영상→사진 Diary
→ PC에서도 동일 순서

6.
Growth Report PUBLISHED
→ 회원 상세 History 표시

7.
Growth Report 발행일
→ NaN 없이 실제 published_at 표시

8.
APP 보강 상태 변경
→ Dashboard 갱신

9.
Dashboard의 허용된 보강 상태 변경
→ APP 동일 반영

10.
다른 pool 데이터 접근 불가

전체 permutation 테스트하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
25. DEPLOY
━━━━━━━━━━━━━━━━━━━━━━

Dashboard 수정:

swimnote-web Production 배포.

API 변경이 실제로 필요한 경우에만:
swimnote-api Production Render 배포.

APP OTA:
이번 작업에서는 원칙적으로 금지.

Dashboard 동기화 과정에서
APP 버그를 발견하더라도
임의로 APP OTA하지 말고 완료보고에 별도 명시하십시오.

━━━━━━━━━━━━━━━━━━━━━━
26. 완료 조건
━━━━━━━━━━━━━━━━━━━━━━

완료 시:

PC Dashboard가 APP의 별도 제품이 아니라

동일한:

- 회원
- 반
- 스케줄
- 보강
- Diary
- Media
- Growth Report
- Curriculum
- Teacher
- Settings

데이터를 사용하고,

관리자에게 허용된 업무는
APP/PC 어디서 수행해도
같은 결과가 되어야 합니다.

APP FIRST / PC ASSIST 원칙은 유지.

━━━━━━━━━━━━━━━━━━━━━━
27. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

길게 설명하지 마십시오.

다음만 제출:

A. COMPATIBILITY
- 이미 동일했던 영역
- 이번에 수정한 차이
- 의도적으로 PC에 복제하지 않은 Teacher-only 기능

B. DOMAIN RESULT
- Members
- Classes/Schedule
- Makeup
- Diary/Media
- Growth Report
- Curriculum/Templates
- Teachers
- Settings
각 PASS/FAIL

C. REALTIME
- APP → PC
- PC → APP
대표 결과

D. QA
위 대표 10개 PASS/FAIL

E. DEPLOY
- 수정 파일
- repo
- branch
- full work commit SHA
- Dashboard Production full LIVE SHA
- Render service name/id
- Render Deploy ID
- deploy status
- API를 수정했다면 API full LIVE SHA / Deploy ID
- migration 여부

확인하지 못한 것은
“확인하지 못함”
이라고 명시하십시오.

완료했다고만 보고하지 마십시오.
추측해서 PASS 처리하지 마십시오.

이번 WP 종료 후
SWIMNOTE APP + PC Dashboard 통합 개발을
최종 CLOSED할 예정입니다.
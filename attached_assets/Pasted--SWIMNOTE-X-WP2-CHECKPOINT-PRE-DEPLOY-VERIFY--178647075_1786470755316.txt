━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SWIMNOTE X — WP2 CHECKPOINT & PRE-DEPLOY VERIFY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WP2 구현 검수 승인.

현재 상태:
IMPLEMENTED / NOT DEPLOYED

아직 Render / OTA 금지.

━━━━━━━━━━━━━━━━━━━━
1. 먼저 코드 안전 저장
━━━━━━━━━━━━━━━━━━━━

현재 WP2 변경 4개 파일만 확인:

- api-server/src/lib/x-entitlement.ts
- api-server/src/routes/billing.ts
- swim-app/app/(admin)/subscription.tsx
- api-server/src/routes/__tests__/wp2-billing-webhook.test.ts

다른 파일이 섞이지 않았는지 확인.

그 후:

1. Commit
2. origin/deploy-photo-clone Push
3. Commit SHA 제출

force push / rebase 금지.

Commit message 예:
feat: add X mode RevenueCat entitlement sync

━━━━━━━━━━━━━━━━━━━━
2. Render secret 실제 존재 확인
━━━━━━━━━━━━━━━━━━━━

Production Render env READ ONLY 확인:

REVENUECAT_WEBHOOK_SECRET

EXISTS / MISSING만 제출.
secret 값 출력 금지.

중요:
현재 코드가 fail-closed이므로
MISSING이면 서버 배포 절대 금지.

또 현재 RevenueCat Dashboard webhook에
Authorization 값이 이미 설정돼 있는지
Repository만으로 확인 불가능하면
"Dashboard 확인 필요"로 표시.

━━━━━━━━━━━━━━━━━━━━
3. X Product ID는 아직 활성화하지 말 것
━━━━━━━━━━━━━━━━━━━━

REVENUECAT_X_PRODUCT_IDS 현재 Production env 존재 여부 확인.

없어도 정상.

실제 App Store Connect /
Google Play Console /
RevenueCat Dashboard에
X 상품이 생성되기 전에는
임의 product id 설정 금지.

예제 문자열을 Production 값으로 넣지 않는다.

━━━━━━━━━━━━━━━━━━━━
4. 기존 일반 구독 보호 확인
━━━━━━━━━━━━━━━━━━━━

WP2 변경 이후에도:

- solo 상품
- center 상품
- 기존 INITIAL_PURCHASE
- RENEWAL
- CANCELLATION
- EXPIRATION
- BILLING_ISSUE
- PRODUCT_CHANGE

흐름이 기존 코드로 그대로 가는지 최종 diff 확인.

X product가 아닐 경우
handleXEntitlementEvent가 실행되지 않는지 확인.

━━━━━━━━━━━━━━━━━━━━
5. refreshMode 영향 확인
━━━━━━━━━━━━━━━━━━━━

subscription.tsx의 refreshMode() 추가가
일반 구독 구매에도 호출되는 구조인지 제출.

일반 구독에도 호출된다면:

- 단순 GET /pools/x-mode 재조회만 하는지
- 일반 subscription 상태를 변경하지 않는지
- 실패 시 구독 성공 UX를 막지 않는지

확인.

불필요한 수정은 하지 않는다.

━━━━━━━━━━━━━━━━━━━━
6. WP2 현재 상태 기록
━━━━━━━━━━━━━━━━━━━━

최종 상태:

WP2 CODE:
IMPLEMENTED

WP2 TEST:
224/224 PASS

WP2 PRODUCTION:
NOT_DEPLOYED

WP2 EXTERNAL CONFIG:
PENDING

으로 기록.

━━━━━━━━━━━━━━━━━━━━
7. 제출

1. WP2 commit SHA
2. origin push 확인
3. git status
4. 변경 파일 4개 확인
5. REVENUECAT_WEBHOOK_SECRET EXISTS/MISSING
6. REVENUECAT_X_PRODUCT_IDS EXISTS/MISSING
7. 기존 일반 구독 regression 확인
8. refreshMode 일반 구독 영향
9. 현재 WP2 최종 상태

Render deploy 금지.
OTA 금지.
RevenueCat Dashboard 변경 금지.
보고 후 STOP.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
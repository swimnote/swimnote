[보낼 곳: APP REPLIT]

[SWIMNOTE — 회원 일괄등록 APP/WEB 안내문 완전 동일화]

현재 회원 일괄등록 기능과 정책은 이미 확정되어 있습니다.

이번 작업에서는 기능을 변경하지 말고,
APP과 WEB에서 사용자에게 보여주는
회원 일괄등록 설명을 완전히 동일하게 맞추십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. 핵심 원칙
━━━━━━━━━━━━━━━━━━━━━━

APP과 WEB의 회원 일괄등록 설명은
서로 다른 문구를 사용하면 안 됩니다.

두 화면 모두
아래 “CANONICAL COPY”를
동일한 문장으로 사용하십시오.

허용되는 차이:

- 줄바꿈
- 글자 크기
- 카드/박스 UI
- 접기/펼치기 UI
- 모바일 화면에 맞춘 레이아웃

허용되지 않는 차이:

- 문장 생략
- 문구 축약
- 의미 변경
- APP 전용 표현
- WEB 전용 표현
- 필수/선택 정책을 다르게 설명

즉 사용자가 APP에서 읽든 WEB에서 읽든
동일한 내용을 받아야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
2. CANONICAL COPY
━━━━━━━━━━━━━━━━━━━━━━

제목:

사용 방법

본문:

기본 양식을 다운로드하고 이름과 보호자 연락처를 입력하세요.
이름과 보호자 연락처만 필수이며, 학생 연락처·생년·보호자 이름·반 이름·메모는 선택사항입니다.
한 번에 최대 1,000명까지 등록할 수 있습니다.

동명이인과 동일한 보호자 연락처를 사용하는 형제·자매도 등록할 수 있습니다.
기존 회원과 이름과 보호자 연락처가 같아도 중복 의심으로 안내만 하며 등록할 수 있습니다.

반 이름을 입력하지 않거나 등록된 반을 찾지 못하면 미배정 회원으로 등록됩니다.

파일을 선택하면 등록 전에 전체 회원을 한 번에 검사합니다.
수정이 필요한 항목이 있으면 엑셀 행 번호와 오류 이유를 모두 알려드립니다.
이 경우 파일을 수정해 다시 업로드하면 되며, 수정 전에는 아무 회원도 등록되지 않습니다.

중복 의심이나 반 미발견 같은 주의사항만 있는 경우에는 그대로 전체 등록할 수 있습니다.

기본 양식은 이름 / 보호자 연락처 2개 항목이며, 기존 7컬럼 파일도 그대로 업로드할 수 있습니다.

━━━━━━━━━━━━━━━━━━━━━━
3. APP 적용
━━━━━━━━━━━━━━━━━━━━━━

대상:

artifacts/swim-app/app/(admin)/bulk-register.tsx

실제 회원 일괄등록 화면의:

- 사용 방법
- 설명문
- 안내문
- helper text

를 확인하십시오.

기존 설명 중
CANONICAL COPY와 충돌하거나
중복되는 문구는 제거하고

최종적으로 위 CANONICAL COPY만
사용하도록 정리하십시오.

모바일 공간이 부족하면:

[사용 방법 보기]

등의 접기/펼치기 UI를 사용할 수 있습니다.

하지만 펼쳤을 때 표시되는 본문은
WEB과 동일한 CANONICAL COPY여야 합니다.

문구를 모바일용으로 임의 축약하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. WEB 적용
━━━━━━━━━━━━━━━━━━━━━━

대상:

artifacts/swimnote-dashboard/src/pages/members/BulkMembersPage.tsx

현재 “사용 방법” 영역의 설명을
위 CANONICAL COPY로 교체하십시오.

현재 화면에 존재하는:

- 동명이인 안내
- 반 이름 안내
- 파일 검증 안내
- 기존 7컬럼 안내

중 CANONICAL COPY와 중복되는
기존 문장은 제거하십시오.

결과적으로 WEB에서도
APP과 동일한 문장만 표시되어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
5. 필드 이름도 동일하게
━━━━━━━━━━━━━━━━━━━━━━

APP과 WEB에서 사용자 표시명을
다음과 같이 통일하십시오.

필수:

- 이름
- 보호자 연락처

선택:

- 학생 연락처
- 생년
- 보호자 이름
- 반 이름
- 메모

특히 기존 7컬럼의:

“연락처”

가 students.phone을 의미한다면
사용자에게는 반드시:

“학생 연락처”

라고 표시하십시오.

“보호자 연락처”와 혼동되지 않게 합니다.

━━━━━━━━━━━━━━━━━━━━━━
6. 기본 양식 설명 통일
━━━━━━━━━━━━━━━━━━━━━━

APP/WEB 모두:

기본 양식:
이름 / 보호자 연락처

2컬럼.

기존 7컬럼 파일:

이름
학생 연락처
생년
보호자 이름
보호자 연락처
반 이름
메모

도 계속 지원.

APP과 WEB에서
이 설명 역시 서로 다르게 쓰지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 공통 문구 재발 방지
━━━━━━━━━━━━━━━━━━━━━━

가능하면 현재 repository 구조에서
APP과 WEB이 함께 참조할 수 있는
공통 상수/설정 파일이 이미 존재하는지 확인하십시오.

안전하게 공유 가능하다면
회원 일괄등록 CANONICAL COPY를
한 곳에서 관리하도록 하십시오.

단 이를 위해
새 패키지 구조나 대규모 shared architecture를
만들지는 마십시오.

공통 import가 구조상 적절하지 않다면
APP/WEB 각각에 동일 문자열을 두되

최종 QA에서
두 문구가 정확히 동일한지 비교하십시오.

핵심은
APP/WEB 설명이 다시 서로 달라지지 않는 것입니다.

━━━━━━━━━━━━━━━━━━━━━━
8. 기능 변경 절대 금지
━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 다음은 변경하지 마십시오.

- 필수값 정책
- duplicate 정책
- class warning 정책
- 1,000명 제한
- validation
- bulk commit
- transaction
- API
- DB
- Excel parser
- normalization
- 회원 데이터

이미 확정된 실제 기능에
설명만 맞추는 작업입니다.

━━━━━━━━━━━━━━━━━━━━━━
9. 배포
━━━━━━━━━━━━━━━━━━━━━━

WEB 변경:

Production WEB deploy

APP 변경:

production-v2 OTA
runtimeVersion 2.2.0

API deploy 없음.
DB migration 없음.
Native build 없음.

━━━━━━━━━━━━━━━━━━━━━━
10. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[A. APP COPY]

- 수정 파일
- 최종 표시 문구
- CANONICAL COPY와 동일 여부

[B. WEB COPY]

- 수정 파일
- 최종 표시 문구
- CANONICAL COPY와 동일 여부

[C. COPY CONSISTENCY]

APP과 WEB 문구를 비교하여:

- 빠진 문장 없음
- 추가 문장 없음
- 의미 변경 없음
- 필드 이름 동일

확인.

[D. FIELD LABEL]

- 이름
- 보호자 연락처
- 학생 연락처
- 생년
- 보호자 이름
- 반 이름
- 메모

APP/WEB 동일 여부

[E. DEPLOY]

WEB:
- full Git SHA
- Production deploy ID/status

APP:
- full OTA Git SHA 또는 확인 가능한 실제 배포 SHA
- EAS Update Group ID
- iOS Update ID
- branch/channel
- runtimeVersion

[F. UNCHANGED]

- API 변경 없음
- validation 변경 없음
- DB 변경 없음
- 회원 데이터 변경 없음
- Native build 없음

완료했다고만 보고하지 마십시오.

APP과 WEB 중 한쪽만 수정하고
“동일하다”고 보고하지 마십시오.

두 실제 화면의 최종 문자열을 비교해서
동일한 정책 설명인지 확인한 뒤 보고하십시오.
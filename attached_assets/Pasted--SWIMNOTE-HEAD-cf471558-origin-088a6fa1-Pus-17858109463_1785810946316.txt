[SWIMNOTE 긴급 수정 — 반 상세 모달 성능·터치 개선 배포 승인]

최종 검수 완료.

현재 기준:

HEAD:
cf471558

origin:
088a6fa1

미Push 커밋:
3건

변경 파일:

서버
- artifacts/api-server/src/routes/attendance.ts
- artifacts/api-server/src/routes/students.ts

앱
- artifacts/swim-app/components/teacher/my-schedule/ClassDetailSheet.tsx
- artifacts/swim-app/components/admin/AdminClassDetailSheet.tsx

핵심 변경:

- 상위 responder 선점 제거
- 학생 목록 ScrollView + map → FlatList 전환
- StudentAttendanceRow React.memo 적용
- 학생별 pending action 분리
- 출석·결석 optimistic update
- 다른 학생 요청 동시 처리 가능
- 반이동 버튼은 출결 요청 중에도 즉시 동작
- 버튼 hitSlop 확대
- API 성능 로그 추가
- 기존 반이동·보충·미배정 서브시트 구조 유지

아래 순서대로 진행한다.

━━━━━━━━━━━━━━━━━━━━━━
STEP 1. Push 전 상태 확인
━━━━━━━━━━━━━━━━━━━━━━

1. 현재 branch가 deploy-photo-clone인지 확인
2. HEAD가 cf471558인지 확인
3. git status --short 확인
4. 의도하지 않은 untracked 첨부파일 제외
5. 수정 파일 4개가 커밋에 포함됐는지 확인
6. force push 금지
7. rebase/reset/squash 금지

확인 후 현재 브랜치를 origin에 일반 Push한다.

Push 충돌 발생 시 임의 해결하지 말고 즉시 중단·보고한다.

제출:

- push 전 HEAD
- push 후 origin SHA
- push된 커밋 목록
- push 대상 브랜치

━━━━━━━━━━━━━━━━━━━━━━
STEP 2. Render 서버 배포
━━━━━━━━━━━━━━━━━━━━━━

서버 파일이 변경됐으므로 OTA보다 먼저 Render 배포를 완료한다.

확인:

1. Render 자동 배포 시작
2. 빌드 성공
3. 서버 기동 성공
4. migration 실행 없음
5. attendance.ts 라우트 정상 등록
6. students.ts 라우트 정상 등록
7. health check 정상
8. 반복 5xx 로그 없음
9. DB 연결 오류 없음

Render 배포 실패 시 OTA 금지.

제출:

- Render deployment ID
- 배포 commit SHA
- 배포 상태
- 완료 시각
- health check 결과

━━━━━━━━━━━━━━━━━━━━━━
STEP 3. 서버 최소 검증
━━━━━━━━━━━━━━━━━━━━━━

운영 API에서 아래를 확인한다.

출석 처리:

- 출석 저장 정상
- 결석 저장 정상
- 기존 응답 Contract 변경 없음
- 같은 학생 중복 요청이 서버 오류를 만들지 않음
- [PERF][attendance-post] 로그 출력
- elapsed_ms 확인

학생 관련:

- 날짜별 학생 명단 정상
- 반이동 정상
- 미배정 처리 정상
- [PERF][students-list]
- [PERF][move-class]
- [PERF][remove-from-class]
  로그 출력 확인

개인정보 로그 금지:

- 학생 이름
- 전화번호
- 일지 내용

서버 응답이 1초 이상이면 elapsed_ms를 보고하되,
이번 배포를 임의 중단하지 말고 실제 오류 여부를 구분한다.

━━━━━━━━━━━━━━━━━━━━━━
STEP 4. 앱 OTA 배포
━━━━━━━━━━━━━━━━━━━━━━

서버 배포가 정상일 때만 OTA를 진행한다.

현재 운영 앱이 사용하는 정확한 EAS channel과 branch를 확인한다.

필수:

- production channel
- 필요하면 preview channel도 동일 코드 배포
- runtime version 확인
- 새 네이티브 빌드 생성 금지
- 기존 production channel과 다른 곳에 임의 배포 금지

OTA 메시지:

fix: improve class detail scrolling and student action responsiveness

제출:

- production update group ID
- preview update group ID
- channel
- branch
- runtime version
- OTA 배포 시각
- 포함 commit SHA

━━━━━━━━━━━━━━━━━━━━━━
STEP 5. 운영 앱 실기기 검증
━━━━━━━━━━━━━━━━━━━━━━

앱을 완전히 종료 후 다시 실행하여 OTA를 적용한다.

선생님 ClassDetailSheet:

1. 학생 목록 빠르게 위·아래 스크롤
2. 스크롤 후 바로 출석 클릭
3. 스크롤 후 바로 결석 클릭
4. 스크롤 후 바로 반이동 클릭
5. 학생 A 출석 처리 중 학생 B 결석 클릭
6. 학생 A 처리 중 반이동 클릭
7. 같은 학생 출석 버튼 연속 클릭
8. API 실패 시 해당 학생만 원복
9. 다른 학생 pending 상태 유지
10. 마지막 학생까지 접근
11. 반이동 서브시트 정상
12. 보충수업 서브시트 정상
13. 미배정 정상
14. 모달 닫기 정상
15. 과거 날짜 학생 명단 정상

관리자 AdminClassDetailSheet:

1. 빠른 스크롤
2. 마지막 학생 접근
3. 출석·결석 버튼 반응
4. 반이동 또는 상세 버튼 반응
5. 기존 관리자 기능 회귀 없음

iOS 우선 확인 후 Android도 확인한다.

━━━━━━━━━━━━━━━━━━━━━━
STEP 6. 성공 기준
━━━━━━━━━━━━━━━━━━━━━━

다음이 모두 충족돼야 완료 처리한다.

- 스크롤 제스처가 즉시 시작됨
- 스크롤 중간 멈춤 현상 없음
- 버튼 터치 누락 없음
- 버튼을 누르면 즉시 spinner 또는 상태 변화 표시
- 학생 A 요청이 학생 B를 막지 않음
- 같은 학생 중복 요청만 차단
- 반이동은 즉시 열림
- 전체 모달 loading overlay 없음
- 마지막 학생까지 접근 가능
- 기존 과거 명단·보강·미배정 기능 회귀 없음

━━━━━━━━━━━━━━━━━━━━━━
STEP 7. 문제 발생 시 롤백
━━━━━━━━━━━━━━━━━━━━━━

앱 UI 문제:

- 직전 정상 commit 기준으로 동일 production channel에 새 OTA 발행
- 기존 update 삭제만으로 처리하지 않음
- force push 금지

서버 문제:

- 직전 정상 서버 commit으로 revert commit 생성
- origin push
- Render 재배포
- migration이 없으므로 DB schema rollback 불필요

롤백 전 기록:

- 문제 화면
- 학생 수
- 수행 동작
- 서버 응답 코드
- API elapsed_ms
- OTA update group ID
- 현재 commit SHA

━━━━━━━━━━━━━━━━━━━━━━
STEP 8. 최종 보고
━━━━━━━━━━━━━━━━━━━━━━

제출:

1. Push 결과
2. origin 최종 SHA
3. Render 배포 ID와 상태
4. 서버 health
5. API별 elapsed_ms
6. production OTA update group ID
7. preview OTA update group ID
8. runtime/channel/branch
9. 선생님 화면 실기기 결과
10. 관리자 화면 실기기 결과
11. 남은 버벅임 또는 터치 누락
12. 롤백 기준점

완료 후 X모드 작업은 자동 재개하지 말고 멈춘다.
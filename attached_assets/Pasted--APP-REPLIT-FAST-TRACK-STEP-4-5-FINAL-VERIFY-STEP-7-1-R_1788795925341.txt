[보낼 곳: APP REPLIT]

[FAST TRACK — STEP 4/5 FINAL VERIFY + STEP 7 회원카드 압축]

목표:
1. Render d75f9f4d LIVE 즉시 STEP 4/5 Production verify
2. 대기 중 STEP 7 MemberCard UI 압축
3. 재감사/대규모 refactor 금지
4. OTA는 아직 발행하지 않음

━━━━━━━━━━━━━━━━━━━━━━
A. STEP 4/5 FINAL VERIFY
━━━━━━━━━━━━━━━━━━━━━━

Render target:
d75f9f4dda54c2fcbf5647ad2d5e2c888582cbf7

LIVE 후:

1.
GET /admin/growth-reports/:id
→ HTTP 200

확인:
- student.name
- class_name
- teacher_name
- report_period
- product_status
- report_content

2.
GET /admin/growth-reports/monthly-list
→ HTTP 200
→ class_name / teacher_name / analysis_status 포함

3.
report-hub:
학생 row → report-detail → back → report-hub

4.
Dashboard:
- KPI 8개
- KPI ↔ 필터 연동
- 학생검색
- multi-select
- READY_TO_SEND만 선택 가능
- 선택발송
- 전체발송 조건
확인

STEP 4 / STEP 5 각각 PASS/HOLD 판정.

━━━━━━━━━━━━━━━━━━━━━━
B. STEP 7 — 회원관리 학생카드 높이 축소
━━━━━━━━━━━━━━━━━━━━━━

현재 관리자 회원관리 카드가 너무 높음.

목표:
한 화면에 더 많은 회원이 보이도록
정보는 유지하면서 카드 높이를 줄인다.

현재 요구:

- 학생명
- 상태
- 주횟수
- 반
- 담당 선생님
- 보호자/연락처
- 학부모 연결 상태
- 상세

은 유지.

━━━━━━━━━━━━━━━━━━━━━━
C. 레이아웃 방향
━━━━━━━━━━━━━━━━━━━━━━

관리자 MemberCard를 가능한 범위에서:

Row 1:
학생명 + 상태 + 주횟수 + 상세버튼(우측)

Row 2:
반 + 담당 선생님 + 상태성 badge

Row 3:
보호자 / 전화번호

구조로 압축.

현재:
- 상세 버튼이 아래쪽을 크게 차지하거나
- 구분선/패딩/빈 공간이 과도하면
축소.

“상세”는 우측 상단 또는 이름 줄과 같은 높이로 올린다.

불필요한 카드 하단 여백 제거.

━━━━━━━━━━━━━━━━━━━━━━
D. 공통 컴포넌트 영향
━━━━━━━━━━━━━━━━━━━━━━

사전 감사 결과:

components/common/MemberCard.tsx
또는 UnifiedMemberCard가

admin:
members.tsx

teacher:
students.tsx
class-assign.tsx

에 영향을 줄 가능성 있음.

따라서 먼저 실제 사용구조 확인.

Case A.
admin 전용 스타일 분기 가능
→ admin만 최소 수정

Case B.
공통 MemberCard 자체 수정
→ teacher students.tsx / class-assign.tsx 반드시 같이 검증

중요:
teacher 카드까지 무조건 관리자와 똑같이 만들 필요 없음.

공통 수정으로 teacher UI가 깨지지만 않게 검증.

━━━━━━━━━━━━━━━━━━━━━━
E. 기능 변경 금지
━━━━━━━━━━━━━━━━━━━━━━

이번 STEP은 레이아웃만.

금지:

- 회원 상세 API
- 학부모 연결
- 초대 로직
- student id
- 상태 로직
- 카드 onPress 동작
- Member 데이터 contract
- backend
- DB

STEP 2에서 해결한 상세 진입도 회귀 없어야 함.

━━━━━━━━━━━━━━━━━━━━━━
F. BASE / X / Teacher 검증
━━━━━━━━━━━━━━━━━━━━━━

Admin members는 BASE/X 공용.

따라서:
- 기본 SWIMNOTE 관리자
- X 관리자

둘 다 동일 카드 정상 확인.

Teacher:
공통 MemberCard 영향 있으면
students.tsx 최소 화면 확인.

정보 잘림/버튼 겹침/텍스트 overflow 없어야 함.

작은 화면 기준도 확인.

━━━━━━━━━━━━━━━━━━━━━━
G. OTA
━━━━━━━━━━━━━━━━━━━━━━

frontend-only 작업.

release/v2.0.0 commit/push.

OTA는 아직 발행하지 말 것.

STEP 4/5/6/7 frontend 변경을
다음 묶음까지 합쳐 1회 OTA 예정.

━━━━━━━━━━━━━━━━━━━━━━
H. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[STEP 4/5]
1. Render LIVE SHA
2. detail API HTTP
3. monthly-list HTTP
4. report navigation
5. KPI/filter/multi-select
6. STEP 4 PASS/HOLD
7. STEP 5 PASS/HOLD

[STEP 7]
8. MemberCard 실제 사용구조
9. admin 전용/공통 수정 여부
10. 최종 카드 레이아웃
11. 카드 높이 감소 내용
12. 상세 버튼 위치
13. teacher 영향 여부
14. BASE/X 회귀
15. 수정 파일
16. backend 수정 없음 확인
17. 테스트
18. commit SHA
19. OTA 미발행 확인
20. STEP 7 PASS/HOLD/FAIL

완료 후 STOP.
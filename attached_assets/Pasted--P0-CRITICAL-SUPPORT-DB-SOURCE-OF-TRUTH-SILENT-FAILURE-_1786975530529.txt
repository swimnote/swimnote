[P0-CRITICAL — SUPPORT DB SOURCE OF TRUTH + SILENT FAILURE AUDIT]

목적:

"x모드 알려줘" 무응답 defect 수정 과정에서 발견된
support_ticket_replies DB 연결 구조를 정확히 확정한다.

현재 보고:

- support_ticket_replies가 POOL_DATABASE_URL(NeonDB)에 없음
- ensureCs01rSchema() CREATE 누락
- CREATE TABLE IF NOT EXISTS를 추가함
- 서버는 기존 실패 상황에서도 HTTP 200을 반환했음

중요:

단순히 Neon에 테이블을 만들어 증상을 덮지 않는다.

==================================================
0. STOP
==================================================

아직 CS08R FULL CLOSE 금지.

아직 device retest 금지.

추가 production schema write 금지
until DB source-of-truth audit 완료.

==================================================
1. ACTUAL DB CONNECTION TRACE
==================================================

다음 파일/객체의 실제 DB connection을 추적:

support-case-service.ts
support-respond.ts
support-cases.ts
super-support.ts
ensureCs01rSchema()
support_cases CRUD
support_ticket_replies CRUD
support_tickets CRUD

각각 실제 사용하는 객체:

db
superAdminDb
poolDb
backupDb
기타

그리고 각 객체가 어떤 env를 사용하는지 확인:

SUPABASE_DATABASE_URL
DATABASE_URL
POOL_DATABASE_URL
기타

보고:

SUPPORT_CASES_DB_OBJECT =
SUPPORT_CASES_DB_ENV =

SUPPORT_REPLIES_DB_OBJECT =
SUPPORT_REPLIES_DB_ENV =

SUPPORT_TICKETS_DB_OBJECT =
SUPPORT_TICKETS_DB_ENV =

ENSURE_CS01R_SCHEMA_DB_OBJECT =
ENSURE_CS01R_SCHEMA_DB_ENV =

==================================================
2. SOURCE OF TRUTH VERIFY
==================================================

실제 repository architecture 기준으로
APP transactional data source of truth를 확인.

특히 기존 감사에서:

db = superAdminDb
SUPABASE_DATABASE_URL 사용 여부

POOL_DATABASE_URL의 실제 용도 확인:

APP CRUD
BACKUP
STANDBY
MIGRATION
OTHER

추측 금지.

보고:

APP_PRIMARY_DB =
POOL_DATABASE_URL_PURPOSE =

==================================================
3. EXISTING TABLE LOCATION
==================================================

실제 production DB 대상으로 read-only 확인.

각 DB에 아래 테이블 존재 여부:

support_cases
support_tickets
support_ticket_replies
support_knowledge_items
super_incidents

보고:

SUPABASE_TABLES =
POOL_DB_TABLES =

민감 데이터 내용 조회 금지.
schema/existence만.

==================================================
4. CRITICAL DECISION
==================================================

정상 구조는 support_cases와
support_ticket_replies가 같은 transactional source of truth에 있어야 한다.

금지:

support_cases = Supabase
support_ticket_replies = Neon backup

같은 split-brain 구조.

만약 현재 그렇게 되어 있다면
ROOT CAUSE는 단순 "table missing"이 아니라:

WRONG_DB_ROUTING

이다.

==================================================
5. REVIEW PREVIOUS FIX
==================================================

이번에 추가한:

CREATE TABLE IF NOT EXISTS support_ticket_replies

가 어느 DB에서 실행되는지 확인.

만약 POOL_DATABASE_URL이
backup/standby DB라면:

그 CREATE를 production fix로 인정하지 않는다.

필요하면 rollback/remove 또는
올바른 DB object로 이동.

production data delete 금지.

==================================================
6. CORRECT SCHEMA OWNER
==================================================

support_ticket_replies schema는
support_cases와 동일 primary APP DB에서 관리.

기존 migration/source-of-truth가 있으면 재사용.

ensureCs01rSchema() 같은 runtime repair가
정말 필요한지 검토.

가능하면:

정식 migration
+
startup verification

구조 우선.

하지만 이번 P0에서는 최소 안전 수정.

==================================================
7. SILENT FAILURE DEFECT
==================================================

현재 더 중요한 결함:

schema ensure 실패
INSERT 실패
SELECT 실패

인데도 client에 HTTP 200 반환 가능.

이 구조 수정.

최소:

user message INSERT 실패
→ success 200 금지

AI message INSERT 실패
→ success 200 금지

필수 DB write 실패 시:

non-2xx
명확한 safe error code
loading 종료 가능

telemetry/audit 실패만 best-effort 허용.

핵심 support message storage는
best-effort 아님.

==================================================
8. SCHEMA ENSURE ERROR HANDLING
==================================================

ensureCs01rSchema():

DDL 실패를 무조건 swallow 금지.

startup에서 schema ensure 실패 시
최소 structured error log.

요청 처리에서 필요한 table이 없으면
silent empty array로 정상 처리 금지.

==================================================
9. 200 RESPONSE CONTRACT
==================================================

POST /support/respond가 200을 반환하려면 최소:

사용자 message 저장 성공

그리고 경로에 따라:

AI/deterministic message 저장 성공
또는
명시적 HUMAN_REQUIRED/error response 성공

이어야 함.

"DB 저장 실패 + 빈 messages + HTTP 200"
금지.

==================================================
10. DATA CONSISTENCY
==================================================

case
ticket
reply

tenant/pool 관계가 동일 DB 내에서 유지되는지 확인.

cross-DB pseudo relation 금지.

==================================================
11. TEST MATRIX
==================================================

DBSRC-01
support_cases and replies same primary DB

DBSRC-02
support_tickets same intended DB

DBSRC-03
POOL_DATABASE_URL purpose confirmed

DBSRC-04
backup DB absence does not break app CRUD

DBSRC-05
primary replies table missing → request fails safely

DBSRC-06
message insert failure → non-200

DBSRC-07
AI message insert failure → non-200 or explicit recoverable failure

DBSRC-08
schema ensure failure not silent

DBSRC-09
successful respond → user message persisted

DBSRC-10
successful respond → AI message persisted

DBSRC-11
GET conversation reads same DB written by POST

DBSRC-12
super admin reads same conversation source

DBSRC-13
cross-pool isolation preserved

DBSRC-14
no production data migration to backup DB

DBSRC-15
full regression

==================================================
12. DEPLOYMENT
==================================================

Server only expected.

DB migration:
only if correct primary APP DB genuinely lacks required schema.

Do NOT create operational support schema in backup DB
just to satisfy tests.

Mobile = NO
iOS OTA = NO
Android OTA = NO

==================================================
13. FINAL REPORT
==================================================

APP_PRIMARY_DB =
POOL_DATABASE_URL_PURPOSE =

SUPPORT_CASES_DB =
SUPPORT_TICKETS_DB =
SUPPORT_REPLIES_DB =

ALL_SUPPORT_TRANSACTIONAL_DATA_SAME_DB =

SUPABASE_SUPPORT_TABLES =
POOL_DB_SUPPORT_TABLES =

PREVIOUS_CREATE_TARGET_DB =
PREVIOUS_FIX_VALID =

ROOT_CAUSE_FINAL =

WRONG_DB_ROUTING_FOUND =
MISSING_PRIMARY_SCHEMA_FOUND =

SILENT_SCHEMA_FAILURE_FIXED =
MESSAGE_INSERT_FAILURE_RETURNS_200 =
AI_INSERT_FAILURE_RETURNS_200 =

CORRECTIVE_FIX =

PRODUCTION_DATA_MOVED =
PRODUCTION_DATA_DELETED =

FILES_CHANGED =
DB_CHANGED =
SERVER_CHANGED =
MOBILE_CHANGED =

TEST_RESULT =
FULL_TEST_RESULT =

COMMIT_SHA =
RENDER_DEPLOYMENT =

IOS_OTA = NO
ANDROID_OTA = NO

성공 조건:

ALL_SUPPORT_TRANSACTIONAL_DATA_SAME_DB = YES

POOL_DATABASE_URL가 backup/standby라면
Support runtime CRUD에 사용 = NO

MESSAGE_INSERT_FAILURE_RETURNS_200 = NO
AI_INSERT_FAILURE_RETURNS_200 = NO

schema failure silent = NO

PRODUCTION_DATA_DELETED = NO

MOBILE_CHANGED = NO
IOS_OTA = NO
ANDROID_OTA = NO

완료:

P0_SUPPORT_DB_SOURCE_OF_TRUTH_HARDEN_COMPLETE ✅
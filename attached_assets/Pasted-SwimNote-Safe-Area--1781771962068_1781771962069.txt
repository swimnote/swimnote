SwimNote 일지 첨부 통합앨범 + Safe Area 수정 작업 지시문

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
작업 목표
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재:

* 일지 작성/수정 화면에서 “앨범에서 선택” 시 사진만 선택 가능
* 영상은 업로드 및 썸네일은 가능하지만 일지 첨부 불가
* AlbumPickerModal 상단 헤더가 아이폰 Dynamic Island / 노치에 가려짐

목표:

1. 일지에서 사진 + 영상을 함께 선택 가능
2. 상단 필터 제공 (전체 / 사진 / 영상)
3. 일지에 사진과 영상을 분리 표시
4. AlbumPickerModal Safe Area 적용
5. 기존 사진 기능 영향 없이 확장

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
확인 완료 사항
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

video_assets_meta

* journal_id 컬럼 존재 ✅
* thumbnail_key 컬럼 존재 ✅

현재 영상 구조 활용 가능

추가 DB 마이그레이션 없음

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
수정 대상 파일
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[서버]

artifacts/api-server/src/routes/videos.ts

추가:

* GET /videos/picker
* POST /videos/diary-attach
* POST /videos/diary-detach
* GET /videos/diary/:diaryId

[앱]

artifacts/swim-app/components/teacher/diary/AlbumPickerModal.tsx

수정:

* 통합 선택 모달
* 사진 + 영상 표시
* Safe Area 적용

artifacts/swim-app/app/(teacher)/diary.tsx

수정:

* 영상 attach/detach 처리
* 영상 불러오기
* 일지 저장/수정 처리
* 첨부 영상 UI 추가

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AlbumPickerModal 구조 변경
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재:

사진만 표시

변경 후:

상단 필터

전체 | 사진 | 영상

기본값

전체

전체:

* 사진 + 영상 함께 표시

사진:

* 사진만 표시

영상:

* 영상만 표시

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
영상 표시 방식
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

영상 카드

* thumbnail_presigned_url 사용
* 썸네일 표시
* 중앙 재생 아이콘 표시

예시

[사진]

[▶ 영상]

영상 재생 기능은 이번 작업 필수 아님

목표:

영상 첨부 + 썸네일 표시

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
onConfirm 구조 변경
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존

onConfirm(
photoIds,
photos
)

변경

onConfirm({
photos,
videos
})

예시

{
photos: […],
videos: […]
}

이후 확장성 고려

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
일지 저장 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

저장 시

사진:

POST /photos/diary-attach

영상:

POST /videos/diary-attach

수정 시

사진 제거:

POST /photos/diary-detach

영상 제거:

POST /videos/diary-detach

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
일지 로딩 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

openEditDiary()

병렬 조회

GET /photos/diary/:diaryId
GET /videos/diary/:diaryId

Promise.all 사용

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
일지 표시 방식
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사진과 영상을 분리 표시

예시

첨부 사진 (3)

[사진]
[사진]
[사진]

첨부 영상 (2)

[▶영상]
[▶영상]

영상은 썸네일 + 재생 아이콘 표시

사진/영상 혼합 표시 금지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Safe Area 수정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

대상

AlbumPickerModal.tsx

현재 문제

* X 버튼
* 제목
* 선택 개수

가 Dynamic Island / 노치 영역과 겹침

수정

react-native-safe-area-context 사용

const insets = useSafeAreaInsets();

헤더 컨테이너

paddingTop: insets.top

또는 SafeAreaView 적용

주의

* 하드코딩 paddingTop 금지
* iPhone Dynamic Island 대응
* Android 과도한 여백 금지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
이번 작업에서 하지 않는 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

* 부모앱 앨범 개편
* 선생님 앨범 사용이력 개편
* 영상 14일 만료
* 영상 200MB 변경
* 영상 자동 압축
* 다운로드 로그
* 즐겨찾기 정책 변경
* 영상 재생 플레이어 구현

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
완료 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① AlbumPickerModal Safe Area 적용 완료

② Dynamic Island 환경에서 제목 정상 표시

③ 전체 / 사진 / 영상 필터 동작

④ 사진 선택 가능

⑤ 영상 선택 가능

⑥ 영상 썸네일 정상 표시

⑦ 영상 재생 아이콘 표시

⑧ 일지 저장 시 사진 첨부 정상

⑨ 일지 저장 시 영상 첨부 정상

⑩ 일지 수정 시 사진 제거 가능

⑪ 일지 수정 시 영상 제거 가능

⑫ 일지 재진입 시 사진 유지

⑬ 일지 재진입 시 영상 유지

⑭ 기존 사진 업로드 기능 영향 없음

⑮ 기존 사진 선택 기능 영향 없음

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
작업 완료 후 보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 수정 파일 목록
2. 추가된 영상 API 목록
3. 실제 attach/detach 흐름
4. Safe Area 적용 스크린샷 확인 결과
5. 사진/영상 선택 저장 테스트 결과
[보낼 곳: APP REPLIT]

[SWIMNOTE — 가로 프로모션 배너 V2 구현]

제안한 방향 중
“최대 4장 / 이미지·텍스트 / 실제비율 미리보기 / 자동 슬라이드”
는 승인합니다.

단 DB 구조는 변경합니다.

━━━━━━━━━━━━━━━━━━━━━━
1. slides JSONB 추가 금지
━━━━━━━━━━━━━━━━━━━━━━

platform_banners의 현재 구조:

1 row = 1 banner

를 그대로 유지합니다.

가로 배너에서는:

1 row = 1 slide

로 해석합니다.

즉 활성 strip banner가:

1개 → 1장
2개 → 2장
3개 → 3장
4개 → 4장

입니다.

신규:

slides jsonb

컬럼 만들지 마십시오.

DB migration도 하지 마십시오.

기존:

sort_order
status
display_seconds
title
description
image_key
image_url
color_theme
display_start
display_end
link_url
link_type

구조를 재사용하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 최대 4장 정책
━━━━━━━━━━━━━━━━━━━━━━

banner_type='strip'

중 실제 활성/예약 가능한 가로 프로모션 배너는
최대 4개까지 관리할 수 있게 합니다.

Super Admin UI에서:

현재 4개가 등록되어 있으면
[+ 등록]

을 비활성 또는 제한 안내.

API에서도 필요하면
동일 정책 validation.

단 기존 데이터가 이미 4개를 초과하는 경우
임의 삭제 금지.

그 경우 기존 데이터 유지 후
신규 활성 등록만 제한.

━━━━━━━━━━━━━━━━━━━━━━
3. 배너 비율
━━━━━━━━━━━━━━━━━━━━━━

110px 고정 높이로 만들지 마십시오.

최종 비율:

16 : 5

사용.

Parent 화면:

width:
화면 폭 - 좌우 margin

aspectRatio:
16 / 5

로 렌더링.

즉 기기 폭에 따라 높이는 자연스럽게 결정됩니다.

Super Admin 미리보기도
정확히 같은 16:5 비율을 사용하십시오.

목표:

관리화면에서 본 모습
≈
학부모 홈 실제 노출 모습

이어야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
4. TEXT / IMAGE 타입 UX
━━━━━━━━━━━━━━━━━━━━━━

Super Admin 등록/수정에서:

○ 텍스트 배너
○ 이미지 배너

를 명확히 선택할 수 있게 합니다.

단 이를 위해 신규 DB 컬럼을
우선 추가하지 마십시오.

저장 규칙:

TEXT:

title
description
color_theme

사용.

IMAGE:

image_key / image_url

사용.

이미지 존재 여부를 통해
현재 row의 렌더링 타입을 판별 가능합니다.

UI에서 TEXT ↔ IMAGE 전환 시
서로 충돌하는 데이터가 실제 렌더링되지 않게
현재 저장 convention에 맞춰 정리하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 기존 “환영합니다” 배너
━━━━━━━━━━━━━━━━━━━━━━

migration 또는 재등록하지 않습니다.

현재 등록된:

“환영합니다”

strip banner row를
그대로 slide 1로 사용합니다.

즉 기존 데이터 100% 호환.

━━━━━━━━━━━━━━━━━━━━━━
6. Super Admin 가로 배너 관리 UX
━━━━━━━━━━━━━━━━━━━━━━

기존 배너 관리 화면을 유지하면서
가로 프로모션 편집 UX를 개선합니다.

상단:

[실제 비율 미리보기 16:5]

현재 선택된 배너가
실제 ParentPromoStrip과 최대한 동일하게 렌더링.

아래:

배너/슬라이드 목록

[1] [2] [3] [4] [+ 추가]

최대 4개.

각 항목은 기존 platform_banners row와 연결.

선택하면 아래에서 편집.

━━━━━━━━━━━━━━━━━━━━━━
7. 순서 변경
━━━━━━━━━━━━━━━━━━━━━━

기존 sort_order 사용.

UI에서:

←
→

또는 현재 디자인에 맞는 단순 순서 이동 제공.

새 order table 금지.

sort_order만 변경.

━━━━━━━━━━━━━━━━━━━━━━
8. 자동 전환
━━━━━━━━━━━━━━━━━━━━━━

ParentPromoStrip:

활성 strip banner 1개:
자동전환 없음.

2~4개:
자동 슬라이드.

기본:

15초

사용.

단 이미 존재하는:

display_seconds

를 반드시 재사용하십시오.

최종 규칙:

display_seconds 값이 유효하면
해당 배너 설정값 사용.

미설정이면:
15초.

허용 범위는 기존 3~30초 정책 유지 가능.

새 duration 컬럼 금지.

━━━━━━━━━━━━━━━━━━━━━━
9. 수동 스와이프
━━━━━━━━━━━━━━━━━━━━━━

자동전환 + 수동 swipe
둘 다 지원.

ParentPromoStrip:

FlatList horizontal
pagingEnabled

또는 현재 React Native 구조에 맞는
안정적인 구현 사용.

사용자가 직접 swipe하면
현재 index 갱신.

자동 timer도
현재 index 기준 다음 slide로 이동.

마지막 slide 다음:
첫 slide.

━━━━━━━━━━━━━━━━━━━━━━
10. APP BACKGROUND
━━━━━━━━━━━━━━━━━━━━━━

AppState가 background/inactive이면
자동전환 timer 중지.

active 복귀 시
다시 시작.

timer 중복 생성 금지.

component unmount 시
cleanup 필수.

━━━━━━━━━━━━━━━━━━━━━━
11. PAGE INDICATOR
━━━━━━━━━━━━━━━━━━━━━━

2개 이상일 경우에만:

● ○ ○

형태의 최소 indicator 표시.

배너 콘텐츠를 방해하지 않는 위치 사용.

1개면 indicator 표시하지 않아도 됨.

과도한 carousel UI 금지.

━━━━━━━━━━━━━━━━━━━━━━
12. 이미지 선택 / CROP
━━━━━━━━━━━━━━━━━━━━━━

이미지 배너 선택 시:

16:5 기준으로 사용자에게
crop/edit 가능하게 합니다.

expo-image-picker가
현재 버전에서 실제 16:5 editing을 안정적으로 지원하는지
먼저 확인하십시오.

지원된다면:

allowsEditing
aspect [16,5]

사용.

해당 플랫폼/버전에서 aspect가 실제로 보장되지 않으면
지원된다고 허위 구현하지 말고
현재 가능한 최소 crop UX를 사용하고 보고하십시오.

이미지는 실제 ParentPromoStrip에서:

resizeMode / contentFit = cover

기준.

━━━━━━━━━━━━━━━━━━━━━━
13. 이미지 R2 노출 버그
━━━━━━━━━━━━━━━━━━━━━━

현재:

이미지는 업로드되지만
ParentPromoStrip에 표시되지 않는 문제가 있습니다.

이 문제는 반드시 실제 코드 기준으로
원인을 먼저 확인하십시오.

확인:

A.
banner-upload 응답:

image_key
image_url

실제 값

B.
platform_banners에 저장된 값

C.
GET /platform/banners 응답에
어떤 image URL/key가 내려오는지

D.
ParentPromoStrip이 최종적으로
어떤 URI를 Image source로 사용하는지

E.
그 URI가 실제 HTTP 200 이미지 응답을 주는지

추측으로:

${API_BASE}/uploads/${image_key}

를 만들지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
14. R2 URL 정책
━━━━━━━━━━━━━━━━━━━━━━

image_key를
R2 object Source of Truth로 유지하는 것은 허용.

하지만:

만료되는 presigned URL을 DB image_url에
영구 저장하는 방식은 금지.

필요하면 서버가 GET banners 응답 시
현재 유효한 display URL을 만들어 반환하거나,

이미 존재하는 stable R2/CDN/proxy URL 구조를
재사용하십시오.

기존 파일 서빙 convention이 있다면
그걸 우선 사용.

새 public bucket 전환은
이번 버그 때문에 임의로 하지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
15. 인증
━━━━━━━━━━━━━━━━━━━━━━

GET /platform/banners는
requireAuth 유지.

ParentPromoStrip은
반드시 기존 authenticated API client/helper를 사용.

무인증 fetch 금지.

ParentAdBanner도 동일 endpoint를 사용한다면
Authorization 누락 여부만 같이 확인.

API 인증 제거 금지.

━━━━━━━━━━━━━━━━━━━━━━
16. 텍스트 배너
━━━━━━━━━━━━━━━━━━━━━━

TEXT 타입:

title
description
color_theme

즉시 preview 반영.

title/description은
실제 16:5 영역 안에서
잘리지 않는 현실적인 길이 제한 사용.

현재 30자 제한이 불필요하게 짧으면
실제 화면을 기준으로 조정 가능.

하지만 무제한 텍스트 금지.

미리보기와 실제 Parent 렌더링은
가능하면 동일 renderer 또는 동일 style 기준 사용.

━━━━━━━━━━━━━━━━━━━━━━
17. 이미지 배너
━━━━━━━━━━━━━━━━━━━━━━

IMAGE 타입에서는:

텍스트 placeholder를 이미지 위에
자동 표시하지 마십시오.

이미지가 배너 전체를 채우도록 표시.

향후 overlay 기능은 별도 WP.

이번에는:

순수 이미지 배너

만 구현.

━━━━━━━━━━━━━━━━━━━━━━
18. 링크
━━━━━━━━━━━━━━━━━━━━━━

현재 배너 링크 정책:

external https
기타 기존 허용 방식

유지.

슬라이드(row) 각각
독립 링크를 가질 수 있습니다.

기존 link_url/link_type 재사용.

새 link schema 금지.

━━━━━━━━━━━━━━━━━━━━━━
19. 예약 / 상태
━━━━━━━━━━━━━━━━━━━━━━

기존:

status
display_start
display_end

유지.

노출 대상은 기존 서버 필터링 결과 사용.

ParentPromoStrip은
서버가 반환한 활성 strip 목록만 표시.

프론트에서 별도 예약 계산 로직 복제 금지.

━━━━━━━━━━━━━━━━━━━━━━
20. FALLBACK
━━━━━━━━━━━━━━━━━━━━━━

활성 strip banner가 실제로 0개일 때만:

“스윔노트 — 우리 아이 수영 성장을 기록해보세요”

fallback 유지.

활성 banner가 존재하지만:

인증 실패
이미지 URL 오류
렌더링 버그

때문에 fallback이 정상 상태처럼 보이게 만들지 마십시오.

개발/QA에서는 해당 오류를 구분 가능해야 합니다.

━━━━━━━━━━━━━━━━━━━━━━
21. 디자인
━━━━━━━━━━━━━━━━━━━━━━

목표:

심플하고 직관적.

관리자는:

1.
지금 보이는 배너를 본다.

2.
1~4번 중 하나를 고른다.

3.
텍스트/이미지를 선택한다.

4.
내용을 수정한다.

5.
실제 모습 그대로 미리본다.

6.
저장한다.

이 흐름이면 끝.

별도 “campaign → slides → content” 같은
복잡한 계층을 만들지 마십시오.

1 row = 1 slide

가 핵심입니다.

━━━━━━━━━━━━━━━━━━━━━━
22. 이번 작업에서 금지
━━━━━━━━━━━━━━━━━━━━━━

- slides JSONB
- banner child table
- 신규 campaign table
- 불필요한 DB migration
- public banner API 전환
- R2 bucket 정책 전체 변경
- 배너 시스템 전면 재설계
- Dashboard와 무관한 수정

━━━━━━━━━━━━━━━━━━━━━━
23. 대표 QA
━━━━━━━━━━━━━━━━━━━━━━

A.

기존 “환영합니다” text banner
→ migration 없이 slide 1로 정상 노출

B.

text banner 2개
→ 자동 슬라이드

C.

15초 기본 전환

D.

수동 swipe

E.

background 진입
→ timer pause

foreground
→ resume

F.

최대 4개 제한

G.

sort_order 변경
→ slide 순서 변경

H.

image banner 업로드
→ R2 저장
→ API 응답
→ Parent 실제 이미지 표시

I.

text + image 혼합
→ 각각 정상 표시

J.

활성 strip 0개
→ fallback 표시

K.

GET /platform/banners
→ 인증된 요청 HTTP 200

━━━━━━━━━━━━━━━━━━━━━━
24. 배포
━━━━━━━━━━━━━━━━━━━━━━

가능하면 DB migration 없음.

API 수정이 실제 이미지 URL 반환 등에 필요한 경우에만
swimnote-api Production 배포.

APP:

production-v2
runtimeVersion 2.2.0
iOS OTA.

Dashboard는 이번 작업 대상이 아니면 배포 금지.

━━━━━━━━━━━━━━━━━━━━━━
25. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[A. DATA MODEL]

- slides JSONB 추가 안 했는지
- 1 row = 1 slide 유지
- migration 여부

[B. SUPER ADMIN UX]

- 최대 4개
- 실제 16:5 preview
- text/image 선택
- 순서 변경

[C. PARENT]

- 자동전환
- 수동 swipe
- indicator
- background pause

[D. IMAGE ROOT CAUSE]

- 기존 이미지가 왜 노출되지 않았는지
- 실제 image_key
- 최종 serving 방식
- 실제 이미지 HTTP 성공 여부

[E. AUTH]

- ParentPromoStrip 인증 방식
- ParentAdBanner 동일 문제 여부

[F. QA]

A~K PASS/FAIL

[G. DEPLOY]

- 수정 파일
- full work SHA 40자
- API 수정 시 full LIVE SHA / Deploy ID
- EAS Update Group full ID
- iOS Update full ID
- branch
- runtimeVersion
- migration 여부

실제 기기에서
텍스트 자동슬라이드와 이미지 노출을 확인하지 못했다면
절대로 PASS라고 보고하지 마십시오.

완료했다고만 보고하지 마십시오.
확인하지 못한 항목은
‘확인하지 못함’이라고 명시하십시오.
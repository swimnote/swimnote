[보낼 곳: APP REPLIT]

[P0 — FINAL IMPORT CANONICAL PARSER FIX + TOYKIDS DRAFT RESTRUCTURE]

현재 Toykids FINAL IMPORT 검증 작업은 FAIL 상태다.

이유:

1. 기존 parseCurriculumDocx가 FINAL_IMPORT 포맷을 지원하지 않아
   표준 POST /super/x-setup/:poolId/structure 경로가 0개 반환함.

2. 임시 tsx 직접 파서로 우회하여 DB에 넣었으나
   파일 canonical_node_count=332인데 실제 curriculum_items=331.
   review_required_count도 파일=161 / 실제 추출=160으로 1개 유실.

목표:
임시 스크립트 우회를 제거하고
FINAL_IMPORT DOCX가 500개 모든 pool에서
표준 upload → structure 경로로 정확히 처리되게 한다.

새 구조 설계/리팩터링 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. parseCurriculumDocx에 FINAL_IMPORT 지원 추가
━━━━━━━━━━━━━━━━━━━━━━

기존 legacy/template 포맷 지원은 유지.

DOCX 헤더에서:

FILE_TYPE=FINAL_IMPORT

를 감지하면 FINAL_IMPORT canonical parser 사용.

FINAL_IMPORT parser는 문서의 canonical section을
있는 그대로 읽어야 한다.

금지:

- 교육 내용 재해석
- stroke/domain/skill_group 재분류
- 레벨 추론
- Global 지식으로 보정
- canonical_key 재생성

문서에 기록된 canonical fields를 그대로 import.

━━━━━━━━━━━━━━━━━━━━━━
2. Canonical node 100% 추출
━━━━━━━━━━━━━━━━━━━━━━

현재 Toykids 파일 기준 반드시:

canonical_node_count = 332

정확히 332개를 추출해야 한다.

331이면 FAIL.

review_required_count:

161

정확히 161개 인식.

레벨 분포:

흰색 117
파란 28
빨간 26
TBD 161

합계 332

문서 section boundary 때문에 첫/마지막 node가 유실되는
off-by-one parser bug를 정확히 수정.

━━━━━━━━━━━━━━━━━━━━━━
3. APP/AI Canonical identity 유지
━━━━━━━━━━━━━━━━━━━━━━

APP FINAL IMPORT와 AI ENGINE FINAL IMPORT의
canonical section을 같은 parser 규칙으로 읽었을 때:

canonical_key_diff = 0
level_order_diff = 0
stroke_diff = 0
domain_diff = 0
skill_group_diff = 0
atomic_skill_diff = 0

그리고 문서의:

CANONICAL_HASH =
02b31aba79a9200f17e7d42dbe82394021551e7fdff9e30603af3e7a4e5f9c86

를 그대로 확인.

AI projection 전용 필드는 APP importer가 재해석하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━
4. REVIEW_REQUIRED 처리
━━━━━━━━━━━━━━━━━━━━━━

CANONICAL_STATUS=REVIEW_REQUIRED이면:

structure는 정상 수행하되
자동 APPROVE / ACTIVATE 금지.

161개 TBD를 임의 레벨에 배정 금지.

profile/status는
현재 표준 review/structured 상태를 유지.

━━━━━━━━━━━━━━━━━━━━━━
5. 현재 잘못 생성된 Toykids Draft 정리
━━━━━━━━━━━━━━━━━━━━━━

현재 임시 스크립트로 생성된:

curriculum_version:
cv_13f78367c65144b68c7e7ce6f9573c50

items:
331개

는 아직:

is_active=false
import_status=DRAFT
approved_at=NULL

인 미사용 draft다.

이 draft만 안전하게 정리한 뒤
같은 업로드 파일을 표준 structure API로 다시 처리.

중요:

기존 Global
기존 LEGACY Local
SCA 231
growth_events 194
CPO
SCP

절대 변경 금지.

정리 전에 이 version이
active/approved/progress reference가 없는지 확인.

해당 신규 draft에 한해서만 cleanup 허용.

━━━━━━━━━━━━━━━━━━━━━━
6. content_hash idempotency 충돌 처리
━━━━━━━━━━━━━━━━━━━━━━

같은 파일 hash가 이미 잘못된 draft에 연결되어 있으므로
단순 duplicate reuse 때문에 331개 draft를 다시 쓰면 안 된다.

cleanup 후
표준 structure가 동일 업로드 파일을 다시 정상 처리하도록 한다.

일반 Production 정책은 그대로:

동일 정상 import → duplicate safe
다른 content → new version

이번 incomplete draft repair만 예외적으로 복구.

Toykids 전용 운영 special-case 코드를 남기지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
7. 표준 API 실제 실행
━━━━━━━━━━━━━━━━━━━━━━

반드시 실제 canonical endpoint 사용:

POST /super/x-setup/:poolId/structure

pool:
pool_1780849364252_l9k44rbk3

임시 tsx direct DB insert 금지.

결과:

new Local version
is_global_reference=false
is_active=false
import_status=DRAFT
approved_at=NULL

curriculum_items = 332

review_required = 161

이어야 한다.

━━━━━━━━━━━━━━━━━━━━━━
8. CPO 존재 확인
━━━━━━━━━━━━━━━━━━━━━━

이전 보고에서:

CPO = "테이블 없음"

이라고 나왔는데
기존 curriculum_progress_observations 사용 이력이 있다.

Production schema에서 실제 확인:

curriculum_progress_observations 존재 여부
row count

만 보고.

삭제/변경/백필 금지.

없다면 즉시 schema 변경하지 말고 blocker로 보고.

━━━━━━━━━━━━━━━━━━━━━━
9. Parser regression tests
━━━━━━━━━━━━━━━━━━━━━━

최소 테스트:

A. 기존 legacy curriculum DOCX
→ 기존 parsing 유지

B. FINAL_IMPORT Toykids APP
→ 332 nodes

C. FINAL_IMPORT Toykids AI canonical section
→ 332 nodes

D. review_required
→ 161

E. section boundary first/last node 보존

F. APP/AI canonical diff 0

G. malformed FINAL_IMPORT
→ silent partial import 금지
→ validation failure

중요:
파일 validation summary가 332인데 parser가 331이면
structure 자체를 실패시켜야 한다.

partial import 금지.

━━━━━━━━━━━━━━━━━━━━━━
10. Production 배포
━━━━━━━━━━━━━━━━━━━━━━

코드 수정
→ 테스트
→ main push
→ Render Production 자동 배포
→ LIVE 완료 대기
→ /health commit == main HEAD
→ Production structure smoke

까지 Replit이 직접 완료.

deploy triggered에서 완료보고 금지.

━━━━━━━━━━━━━━━━━━━━━━
11. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

1. FINAL_IMPORT parser 구현 방식
2. 기존 legacy parser 영향 여부
3. 1개 유실 root cause
4. APP parsed node count
5. AI parsed canonical node count
6. review_required count
7. level distribution
8. APP/AI canonical diff
9. 신규 Toykids version id
10. 실제 DB curriculum_items count
11. approve 여부 = NO
12. activate 여부 = NO
13. CPO 실제 존재/row count
14. Global 50 보존
15. SCA 231 보존
16. growth_events 194 보존
17. SCP/CPO 변화 0
18. 관련 테스트
19. main SHA
20. Render LIVE SHA
21. /health commit
22. 잔존 blocker
23. Toykids FINAL 파일 수정 단계 진행 준비 YES/NO

PASS 조건:

- 표준 structure API로 처리
- 임시 direct DB script 불필요
- APP 332/332
- AI canonical 332/332
- review 161/161
- partial import 방어
- 기존 Production data 변화 0
- Render LIVE == main

완료 후 STOP.
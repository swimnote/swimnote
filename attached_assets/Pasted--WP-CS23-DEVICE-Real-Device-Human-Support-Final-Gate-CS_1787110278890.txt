[WP-CS23-DEVICE — Real Device Human Support Final Gate]

목적:

CS23의 서버/DB/Direct Answer 구현은 완료되었다.

이번 WP에서는 신규 개발을 하지 않는다.

실제 사용자 디바이스에서 Human Support 흐름만 최종 검증하여
CS23 운영 Closure를 완료한다.

━━━━━━━━━━━━━━━━━━━━━━

1. 금지
   ━━━━━━━━━━━━━━━━━━━━━━

코드 변경 금지.

DB 구조 변경 금지.

Canonical Answer 추가 금지.

Utterance 추가 금지.

Matcher 수정 금지.

GPT Prompt 수정 금지.

문제가 실제 발견된 경우에만
증거와 함께 별도 BUG로 보고한다.

━━━━━━━━━━━━━━━━━━━━━━
2. 실제 디바이스
━━━━━━━━━━━━━━━━━━━━━━

Production 앱/OTA 환경 사용.

실제 사용자 Role 하나로
AI 문의 화면 진입.

━━━━━━━━━━━━━━━━━━━━━━
3. DIRECT DB 확인
━━━━━━━━━━━━━━━━━━━━━━

현재 ACTIVE Direct Answer 질문 1개 입력.

예:

기능/권한/사용법 관련
현재 승인된 Knowledge 사용.

확인:

답변 정상 표시
대기 없음
오류 없음

가능하면 trace에서:

source = DIRECT_DB

확인.

━━━━━━━━━━━━━━━━━━━━━━
4. GPT FALLBACK 확인
━━━━━━━━━━━━━━━━━━━━━━

Direct DB에 정확히 매칭되지 않지만
기존 Knowledge로 설명 가능한 질문 1개 입력.

확인:

기존 Grounded GPT 정상 동작.

Direct DB와 GPT UI가 깨지지 않음.

━━━━━━━━━━━━━━━━━━━━━━
5. 직접 문의하기
━━━━━━━━━━━━━━━━━━━━━━

AI/GPT 답변 후:

[직접 문의하기]

버튼 탭.

확인:

request-human 성공

사용자 화면:

담당자가 확인 중임을 정상 표시.

Circular fallback 문구 없음.

━━━━━━━━━━━━━━━━━━━━━━
6. SUPER ADMIN
━━━━━━━━━━━━━━━━━━━━━━

실제 Super Admin 계정에서:

Push 수신 확인.

Support Inbox에서
해당 Case 확인.

확인:

사용자 질문
기존 AI/GPT 답변
대화 흐름

이 동일 Case에서 보이는지 확인.

━━━━━━━━━━━━━━━━━━━━━━
7. HUMAN REPLY
━━━━━━━━━━━━━━━━━━━━━━

Super Admin에서 테스트 답변 작성.

확인:

agent-reply 성공.

━━━━━━━━━━━━━━━━━━━━━━
8. USER PUSH
━━━━━━━━━━━━━━━━━━━━━━

실제 사용자 디바이스에서:

답변 도착 Push 수신.

Push 또는 앱 진입 후:

기존 AI 문의 Conversation에서
Human 답변 확인.

새 Conversation 생성 금지.

━━━━━━━━━━━━━━━━━━━━━━
9. SAME CONVERSATION
━━━━━━━━━━━━━━━━━━━━━━

타임라인이:

User
→ Direct/GPT
→ Human pending
→ Super Admin reply

순서로 동일 Conversation에 존재해야 함.

필수:

SAME_CONVERSATION = PASS

━━━━━━━━━━━━━━━━━━━━━━
10. DUPLICATE
━━━━━━━━━━━━━━━━━━━━━━

Human Pending 상태에서:

직접 문의하기 버튼 재탭
또는 추가 메시지 전송.

새 Case/Ticket이 생성되지 않는지 확인.

필수:

DUPLICATE_OPEN_CASE = 0

━━━━━━━━━━━━━━━━━━━━━━
11. HUMAN_ONLY OPTIONAL CHECK
━━━━━━━━━━━━━━━━━━━━━━

가능하면 가격 또는 환불 질문 1개 테스트.

현재 정책 미승인 상태이므로:

Direct 가격/환불 답변 금지.

GPT 추론 금지.

담당자 확인 CTA가 나와야 함.

필수:

HUMAN_ONLY_POLICY_INFERENCE = 0

━━━━━━━━━━━━━━━━━━━━━━
12. COMPLETE REPORT
━━━━━━━━━━━━━━━━━━━━━━

WP-CS23-DEVICE COMPLETE REPORT

DEVICE

OS:
App version/build:
OTA:

DIRECT DB

Question:
Answer displayed:
Source:

GPT

Fallback:
Grounded response:

HUMAN REQUEST

Button:
request-human:
Pending state:

SUPER ADMIN

Push:
Case visible:
Conversation context:

AGENT

Reply success:

USER

Push:
Human reply visible:
Same conversation:

DUPLICATE

Duplicate open case:

HUMAN ONLY

Tested:
Policy inference:

FINAL

DIRECT_DEVICE_PASS:
GPT_DEVICE_PASS:
SUPER_ADMIN_PUSH_PASS:
USER_PUSH_PASS:
SAME_CONVERSATION_PASS:
DUPLICATE_OPEN_CASE:

CS23_FINAL_OPERATIONAL_CLOSED: YES / NO

SUPPORT_SYSTEM_DEVICE_VERIFIED: YES / NO

WP_CS23_DEVICE_FINAL_GATE_COMPLETE ✅

━━━━━━━━━━━━━━━━━━━━━━
13. 종료
━━━━━━━━━━━━━━━━━━━━━━

완료 후 멈춘다.

모두 PASS면 CS23 전체를 완전히 종료한다.

다음 단계는 신규 기능 개발이 아니라,
실사용 과정에서 발생하는:

NO_MATCH
AMBIGUOUS
HUMAN 문의

를 수집하여
새 Canonical / Utterance Candidate로 승격시키는
운영 개선 루프로 넘어간다.

[/끝]

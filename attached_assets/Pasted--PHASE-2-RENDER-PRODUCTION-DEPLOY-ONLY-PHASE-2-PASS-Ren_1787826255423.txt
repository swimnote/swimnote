[PHASE 2 — RENDER PRODUCTION DEPLOY ONLY]

현재 PHASE 2 앱 구현은 PASS.

남은 작업은
Render production에 이미 완성된 서버 코드를 반영하고
실제 API를 검증하는 것뿐입니다.

PHASE 3 시작 금지.
신규 기능 구현 금지.
서버 코드 수정 금지.
DB migration 금지.
앱 코드 수정 금지.
OTA 추가 배포 금지.

━━━━━━━━━━━━━━━━━━━━━━
1. 배포 대상 확인
━━━━━━━━━━━━━━━━━━━━━━

GitHub branch:

deploy-photo-clone

현재 최신 대상 SHA:

16ae6299

반드시 먼저 확인:

16ae6299가
89bbd628 서버 변경을 ancestor로 포함하는지 확인.

즉:

89bbd628
→ GET /admin/reports/summary 구현

이 commit이
16ae6299 history에 포함되어 있어야 함.

포함되어 있으면:
16ae6299를 production 배포 대상으로 사용.

포함되어 있지 않으면:
임의 merge/cherry-pick 금지.
STOP 후 보고.

━━━━━━━━━━━━━━━━━━━━━━
2. Render 수동 배포
━━━━━━━━━━━━━━━━━━━━━━

Render production service가
실제로 어떤 branch를 추적하는지 확인.

확인:

- service name
- connected repository
- connected branch
- 현재 live commit
- auto-deploy 설정

현재 deploy-photo-clone이
auto-deploy 대상이 아니라면
이번에는 수동 deploy 수행.

배포 대상:

16ae6299

또는 Render UI가 branch HEAD deploy 방식이면
deploy-photo-clone HEAD가 정확히 16ae6299인지 확인 후 배포.

다른 branch 배포 금지.

━━━━━━━━━━━━━━━━━━━━━━
3. 배포 완료 확인
━━━━━━━━━━━━━━━━━━━━━━

Render가:

building
→ deploying
→ live

까지 실제 완료된 뒤 검증.

중간 상태에서 PASS 보고 금지.

확인:

- build 성공
- startup 성공
- crash/restart 없음
- production service LIVE

━━━━━━━━━━━━━━━━━━━━━━
4. HEALTH VERIFY
━━━━━━━━━━━━━━━━━━━━━━

기존 health endpoint 호출.

확인:

- HTTP 정상
- JSON 응답
- server error 없음

기존 version 문자열이 hardcoded라면
running SHA 증빙으로 사용하지 말 것.

가능하면 Render deploy metadata/UI의 commit SHA로
실제 배포 commit 확인.

━━━━━━━━━━━━━━━━━━━━━━
5. API ROUTE REGISTRATION VERIFY
━━━━━━━━━━━━━━━━━━━━━━

production:

GET /admin/reports/summary

먼저 인증 없이 호출.

목표:

HTML SPA 반환 금지.

정상 예:

401 JSON
또는
403 JSON

즉 Express API route/middleware가
실제 production에 등록되어 있다는 것을 확인.

HTML이 반환되면 FAIL.

━━━━━━━━━━━━━━━━━━━━━━
6. AUTHENTICATED API VERIFY
━━━━━━━━━━━━━━━━━━━━━━

가능하면 실제 X 관리자 인증으로 호출.

확인:

GET /admin/reports/summary
?year=...
&month=...

기대:

HTTP 200

JSON contract:

summary
filters
students
pagination

확인:

summary:
- published
- review_required
- analyzing
- failed

students:
- 실제 해당 pool 학생/report만
- 다른 pool 데이터 없음

pagination:
- page
- limit
- total
- has_more

실제 데이터가 0건이어도
정상 JSON contract면 PASS.

━━━━━━━━━━━━━━━━━━━━━━
7. SECURITY VERIFY
━━━━━━━━━━━━━━━━━━━━━━

반드시 확인:

A.
인증 없음
→ 401/403 JSON

B.
Normal/non-X 권한
→ 기존 middleware 정책대로 차단

C.
X pool_admin
→ 200

D.
client가 임의 pool_id를 query/body로 넘겨도
다른 pool 조회 불가

현재 구현된
getAdminPoolId(req)
+
growth_reports.swimming_pool_id isolation
그대로 작동해야 함.

━━━━━━━━━━━━━━━━━━━━━━
8. 금지사항
━━━━━━━━━━━━━━━━━━━━━━

이번 작업에서 절대 금지:

- API 코드 수정
- route rename
- middleware 수정
- DB schema 수정
- migration
- report-hub 수정
- x-growth 수정
- 신규 OTA
- PHASE 3 시작

배포 문제를
코드 문제로 착각해서 수정하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━
9. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

[COMMIT]

- deploy branch:
- deploy SHA:
- 89bbd628 포함 여부:
- Render connected branch:

[RENDER]

- manual deploy:
- build:
- deploy:
- live:
- production commit SHA:
- crash/restart:
- health:

[API ROUTE]

- unauthenticated status:
- content-type:
- HTML 여부:
- route registered PASS/FAIL:

[AUTHENTICATED]

- status:
- response keys:
- summary:
- students:
- pagination:
- pool isolation:

[SECURITY]

- unauthenticated:
- non-X:
- X pool_admin:
- cross-pool:

[PHASE 2 FINAL]

PASS / FAIL

PASS일 경우에도
PHASE 3 자동 시작 금지.

STOP.
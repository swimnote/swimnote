[SWIMNOTE RELEASE MASTER WORK ORDER]
[PHASE A / A6 — iOS FULL DEVICE REGRESSION]

목적:

현재 iOS TestFlight 환경에서
SWIMNOTE 전체 주요 기능을 실제 기기 기준으로 최종 회귀 검증한다.

이번 단계는 RELEASE REGRESSION이다.

중요:
- 새 기능 개발 금지
- DB schema 변경 금지
- API contract 변경 금지
- AI ENGINE 변경 금지
- X canonical mode 변경 금지
- RevenueCat 구조 변경 금지
- 디자인 전면 수정 금지
- Production fake data 생성 금지

문제가 발견되면:
재현
→ 원인
→ 영향
→ OTA 가능 여부
를 먼저 보고.

승인 없이 광범위 수정 금지.

==================================================
1. CURRENT BASELINE
==================================================

APP VERSION:
1.6.3

iOS:
TestFlight build 246

runtime:
1.6.3

Production API:
https://swimnote.kr/api

AI ENGINE:
https://swimnote.ai.kr

현재 최신 OTA 기준으로 검증.

==================================================
2. TEST PRINCIPLE
==================================================

이번 검증은:

코드가 존재하는지 확인하는 단계가 아니라
실제 iPhone에서 사용자가 정상적으로 사용할 수 있는지 확인하는 단계다.

각 테스트를 다음으로 판정:

PASS
FAIL
NO_SAFE_TEST_DATA
NOT_APPLICABLE

추측으로 PASS 금지.

==================================================
3. APP LAUNCH / UPDATE
==================================================

확인:

- 앱 정상 실행
- crash 없음
- 최신 production OTA 로드
- splash 이상 없음
- 초기 화면 layout 깨짐 없음
- 로그인 전 API 오류 팝업 없음

==================================================
4. LOGIN / AUTH
==================================================

최소 확인:

- Super Admin 로그인
- Owner/Admin 로그인
- Teacher 로그인
- Parent 로그인

각각:

- 로그인 성공
- role routing 정상
- 무한 loading 없음
- 로그인 직후 500/502 없음
- 로그아웃 정상
- 재로그인 정상

==================================================
5. GLOBAL DESIGN REGRESSION
==================================================

최근 디자인 수정 영향 확인.

공통:

- Primary CTA Navy
- random Orange CTA 없음
- random Green CTA 없음
- decorative icon tile 과다 없음
- 불필요한 thick border 없음
- header icon touch 정상
- text clipping 없음
- 작은 화면에서 버튼 겹침 없음

디자인 취향에 대한 추가 개선은
별도 backlog로 기록만 하고
이번 regression PASS 기준과 섞지 않는다.

==================================================
6. ADMIN / OWNER — HOME
==================================================

확인:

- 홈 정상 로드
- 주요 카드 정상
- 오늘 일정/요약 정상
- navigation 정상
- X 상태 표시가 실제 mode와 일치
- normal에서 X AI entry 없음
- x에서 X entry 정상

==================================================
7. MEMBER / STUDENT
==================================================

안전한 기존 데이터로 확인:

- 회원 목록
- 검색
- 상세
- 수정 진입
- 상태 표시
- 학생 연결 정보

실제 변경 저장이 안전하지 않으면
read-only 흐름만 확인하고
NO_SAFE_WRITE_TEST로 기록.

==================================================
8. CLASS / SCHEDULE
==================================================

확인:

- 반 목록
- 반 상세
- scheduler
- 월/주/일
- 날짜 이동
- schedule block
- 학생 목록
- action buttons
- modal/sheet

최근 디자인/Modal 변경으로
기능이 손상되지 않았는지 확인.

==================================================
9. ATTENDANCE
==================================================

안전한 실제 수업이 존재하면:

- 출결 화면 진입
- 학생 목록
- 기존 상태 표시
- 상태 선택 UI

실제 Production 출결 변경이 위험하면
변경 저장 금지.

NO_SAFE_ATTENDANCE_WRITE
로 기록 가능.

==================================================
10. MAKEUP
==================================================

확인:

- 보강 목록
- 상세
- 배정 UI
- modal/sheet
- 버튼 중복 탭 문제 없음

Production 데이터 변경이 위험하면
read-only 검증.

==================================================
11. TEACHER — TODAY
==================================================

Teacher 계정:

- 오늘 스케줄
- 담당 수업
- 학생 목록
- 수업 없음 empty state
- diary entry
- attendance entry

정상 확인.

==================================================
12. MANUAL DIARY
==================================================

Normal 기능.

확인:

- 일지 작성 화면
- 수동 text 입력
- 사진/영상 UI
- 학생 선택
- 저장 버튼

실제 저장이 안전한 테스트 일지가 없으면
NO_SAFE_DIARY_WRITE.

중요:
A2 이후에도 수동 일지는 normal에서 정상이어야 함.

==================================================
13. X TEACHER AI DIARY
==================================================

X mode 안전한 운영처에서만 테스트.

확인:

- AI 버튼 노출
- Normal에서는 미노출
- AI 입력 modal
- AI 생성 요청
- 결과 표시
- 수정 가능
- 최종 저장 흐름

실제 안전한 AI 일지 생성이 가능하면
최소 1건 테스트.

AI Analytics는 아직 미구현이므로
Partner telemetry 검증은 하지 않는다.

==================================================
14. ALBUM / PHOTO / VIDEO
==================================================

확인:

- 앨범 목록
- 사진 상세
- lightbox
- video detail
- 닫기/back
- safe area
- modal overlay

업로드는 안전할 때만.

==================================================
15. NOTICE
==================================================

확인:

- 공지 목록
- 상세
- 작성/수정 modal UI
- 저장 버튼 중복 탭 방지

Production 공지 발행이 위험하면
실제 publish 금지.

==================================================
16. NOTIFICATIONS
==================================================

확인:

- 받은 알림
- 내 요청
- timeAgo 표시
- deep link
- back 1회 동작
- 연속 탭 중복 navigation 없음

==================================================
17. PARENT REQUESTS
==================================================

확인:

- 학부모 요청 목록
- 요청 상세
- 처리 결과 표시
- 보강 요청 연결 UI

실제 신규 요청 생성이 위험하면
기존 데이터 기반 read-only 검증.

==================================================
18. PARENT HOME
==================================================

확인:

- 자녀 선택
- 공지
- 일지
- 앨범
- 일정
- 교육 관련 entry
- Normal/X visibility

자녀 미선택 empty state도 확인.

==================================================
19. PARENT CURRICULUM CHAT
==================================================

X mode에서만.

가능하면 실제 안전한 계정으로:

- history load
- 질문 입력
- 응답
- follow-up
- quota 표시
- 앱 재진입 후 history 유지

중요:

같은 request retry/replay가
중복 quota를 소비하지 않는지
기존 구현 기준으로 확인 가능한 범위만 테스트.

실제 quota 소모가 부담되면
1건만 테스트.

==================================================
20. GROWTH REPORT
==================================================

구분:

A. Normal 3개월 성장요약
B. X AI Growth Report

둘을 혼동하지 않는다.

확인:

- Normal 성장요약 정상
- X report entry
- report detail
- teacher review
- parent view

안전한 기존 report가 있으면 사용.

없으면:
NO_SAFE_GROWTH_REPORT_DATA

가짜 Production report 생성 금지.

==================================================
21. PDF / SHARE
==================================================

기존 안전한 report가 있을 경우:

- PDF 생성
- 파일 열림
- share sheet
- cancel
- 재시도

확인.

새 native dependency 사용 여부는 변경하지 않는다.

==================================================
22. X MODE
==================================================

최소 다음 3상태를 실제 가능한 범위에서 확인:

normal
x_pending
x

확인:

- badge
- X setup
- entry visibility
- XModeGuard
- role별 visibility

상태 변경 자체가 Production write라면
A5 코드 evidence를 재사용하고
A6에서는 현재 상태의 실기기 UI만 확인해도 됨.

==================================================
23. X SETUP
==================================================

확인:

- 상태 문구
- 완료/대기 표시
- 내부 enum 노출 없음
- 작은 화면 clipping 없음
- back/navigation 정상

==================================================
24. SUPER ADMIN
==================================================

확인:

- 운영처 관리
- 운영처 목록
- operator detail
- X status
- entitlement/config controls
- loading
- modal/alert
- pull-to-refresh

실제 X 상태 write는
A5에서 검증했으므로
이번에는 위험하면 재실행하지 않는다.

==================================================
25. MODAL / ALERT / SHEET
==================================================

A4 변경 회귀 확인.

주요 화면에서:

- content clipping 없음
- keyboard 가림 없음
- close 정상
- back 정상
- backdrop behavior 정상
- double tap 없음
- loading 정상

확인.

==================================================
26. ONBOARDING / EMPTY STATE
==================================================

A3 변경 회귀 확인.

- 기존 사용자 onboarding 폭탄 없음
- Teacher no-schedule 문구 정상
- Parent no-child CTA 정상
- X pending 문구 정상
- internal enum 노출 없음

==================================================
27. NETWORK FAILURE SANITY
==================================================

의도적으로 Production 장애를 만들지 않는다.

기존 error handling 코드 + 실제 발생 가능한
일시적 네트워크 실패 수준만 확인.

확인:

- raw HTML 노출 없음
- stack trace 노출 없음
- loading 무한 지속 없음
- retry 가능

==================================================
28. PERFORMANCE SANITY
==================================================

정밀 benchmark는 이번 범위 아님.

체감 기준:

- 주요 화면 진입 과도한 지연 없음
- spinner 무한 없음
- navigation freeze 없음
- modal open/close lag 없음
- AI 요청은 기존 정상 범위

이상 징후만 기록.

==================================================
29. CRASH / FATAL
==================================================

다음은 즉시 BLOCKER:

- app crash
- login 불가
- role routing 불가
- production API 500 반복
- diary save 파손
- attendance 핵심 flow 파손
- X mode 접근 제어 파손
- parent data 접근 파손
- Super Admin control 파손

==================================================
30. ISSUE CLASSIFICATION
==================================================

발견 문제:

P0
출시 불가 / 데이터/보안/로그인/핵심기능

P1
주요 기능 장애지만 우회 가능

P2
UI/디자인/카피/경미한 UX

P3
향후 polish

디자인이 마음에 들지 않는 부분은
기능 정상이라면 P2/P3 backlog로 기록.

==================================================
31. FIX POLICY
==================================================

P0/P1 발견 시:

1. 재현
2. root cause
3. 수정 파일
4. OTA 가능?
5. Native rebuild 필요?
6. 영향 범위

먼저 보고.

내 승인 후 수정.

단 명백한 typo/layout-only P2는
이번 regression 중 임의 수정하지 않는다.

==================================================
32. NO FAKE PRODUCTION DATA
==================================================

가짜:

회원
학생
반
일지
공지
리포트
AI history

Production 생성 금지.

안전한 기존 데이터가 없으면:

NO_SAFE_TEST_DATA

로 기록.

==================================================
33. FINAL PASS CONDITION
==================================================

A6 PASS 조건:

P0 = 0
P1 = 0

P2/P3는
release blocker가 아니면 backlog 가능.

==================================================
34. FINAL REPORT FORMAT
==================================================

다음 순서로 보고:

1. DEVICE / BUILD / RUNTIME
2. OTA VERSION
3. LOGIN RESULT
4. ADMIN/OWNER RESULT
5. TEACHER RESULT
6. PARENT RESULT
7. MEMBER/STUDENT RESULT
8. CLASS/SCHEDULE RESULT
9. ATTENDANCE RESULT
10. MAKEUP RESULT
11. MANUAL DIARY RESULT
12. AI DIARY RESULT
13. ALBUM/MEDIA RESULT
14. NOTICE RESULT
15. NOTIFICATION RESULT
16. PARENT REQUEST RESULT
17. PARENT CURRICULUM RESULT
18. NORMAL GROWTH RESULT
19. X GROWTH REPORT RESULT
20. PDF/SHARE RESULT
21. NORMAL/X_PENDING/X RESULT
22. X SETUP RESULT
23. SUPER ADMIN RESULT
24. MODAL REGRESSION
25. ONBOARDING REGRESSION
26. CRASH/FATAL
27. P0 ISSUES
28. P1 ISSUES
29. P2/P3 BACKLOG
30. NO_SAFE_TEST_DATA ITEMS
31. CODE CHANGE?
32. OTA/DEPLOY?
33. FINAL RELEASE VERDICT

성공:

A6_IOS_FULL_DEVICE_REGRESSION_COMPLETE ✅

실패:

A6_IOS_FULL_DEVICE_REGRESSION_BLOCKED ❌

A6 PASS 후 다음:

PHASE B
WP-PARTNER-00 AI ANALYTICS REPOSITORY AUDIT

또는
Android build timing을 Master Order 기준으로 결정.

임의로 다음 단계 시작 금지.
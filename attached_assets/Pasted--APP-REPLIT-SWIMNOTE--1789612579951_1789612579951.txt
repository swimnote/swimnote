[보낼 곳: APP REPLIT]

[SWIMNOTE — 매출 확인 수업 단가표 링크 버그 재조사 / 이전 완료보고 무효]

이전 완료보고는 실제 기기 화면과 일치하지 않았습니다.

사용자 실제 기기에서는 OTA 이후에도:

- 안내 문구가 그대로임
- CTA를 누르면 여전히 수영장 설정으로 이동함

따라서 이전의:

“admin-revenue.tsx가 실제 대상 화면이며
route는 이미 unit-pricing이었다”

라는 판단은 신뢰하지 않습니다.

이번에는 파일명을 추측하지 말고
실제 화면을 코드에서 역추적하십시오.

━━━━━━━━━━━━━━━━━━━━━━
1. 스크린샷의 실제 화면부터 찾아라
━━━━━━━━━━━━━━━━━━━━━━

프로젝트 전체에서 아래 실제 표시 문자열을 검색하십시오.

정확히 검색:

“매출 확인”

“수업 단가가 설정되지 않았습니다”

“수업 설정에서 단가표를 등록하세요”

“총 수업시수”

“주간 횟수별 현황”

“수업시수 = 회원수”

이 문자열이 실제로 존재하는
파일/컴포넌트가
사용자가 보고 있는 Production 화면의 source 후보입니다.

특히:

admin-revenue.tsx라고 미리 가정하지 마십시오.

실제로 어느 파일이
스크린샷의:

- 상단 제목 “매출 확인”
- 월 선택
- 수업 단가 미설정 경고
- 총 수업시수
- 주1회/주2회/주3회 카드

를 렌더링하는지
코드 근거로 특정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
2. 실제 CTA의 onPress를 추적
━━━━━━━━━━━━━━━━━━━━━━

실제 화면 source를 찾은 뒤
스크린샷의 경고 박스 전체 또는 우측 화살표가
어떤 Pressable/Touchable/onPress에 연결되어 있는지 확인하십시오.

다음까지 추적:

- router.push(...)
- router.replace(...)
- navigation.navigate(...)
- 공통 handler
- wrapper Pressable
- 상위 카드 onPress

실제 최종 destination을 보고하십시오.

수영장 설정으로 이동하는 코드가 있다면
그게 실제 버그입니다.

━━━━━━━━━━━━━━━━━━━━━━
3. 목적지는 하나로 고정
━━━━━━━━━━━━━━━━━━━━━━

수업 단가 미설정 경고의 최종 목적지는 반드시:

/(admin)/unit-pricing

이어야 합니다.

실제 화면에서:

경고 박스
우측 화살표
“수업 단가표 설정” CTA

등 동일 목적의 터치 영역이 있다면
모두 unit-pricing으로 연결하십시오.

pool-settings로 가는 연결은 제거하십시오.

━━━━━━━━━━━━━━━━━━━━━━
4. 실제 보이는 문구도 수정
━━━━━━━━━━━━━━━━━━━━━━

사용자가 실제로 보고 있는 화면의 문구를:

기존:
“수업 단가가 설정되지 않았습니다.
수업 설정에서 단가표를 등록하세요.”

→

“수업 단가가 설정되지 않았습니다.
수업 단가표에서 수업료를 설정해주세요.”

로 수정하십시오.

CTA가 별도 존재하면:

“수업 단가표 설정”

으로 표시.

중요:

다른 파일에 같은 문구를 새로 넣지 말고
실제로 스크린샷 화면을 렌더링하는 source를 수정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
5. 왜 이전 수정이 반영되지 않았는지 원인 확정
━━━━━━━━━━━━━━━━━━━━━━

아래 중 무엇인지 반드시 하나로 특정하십시오.

A.
admin-revenue.tsx가 실제 사용 화면이 아니었음

B.
동일 기능의 다른 화면/legacy component가 실제 렌더링됨

C.
route/layout에서 다른 파일로 연결됨

D.
OTA에 수정 파일이 실제 포함되지 않았음

E.
production-v2가 아닌 다른 update/runtime을 실제 기기가 사용 중

F.
다른 원인

추측 보고 금지.

코드/OTA metadata 근거로 확정하십시오.

━━━━━━━━━━━━━━━━━━━━━━
6. OTA 실제 반영 검증
━━━━━━━━━━━━━━━━━━━━━━

수정 후 production-v2 OTA 배포.

그 다음 단순히:

“파일 수정됨”
“route 맞음”

이라고 보고하지 마십시오.

최소 확인:

1.
배포 bundle에 실제 수정 source가 포함되는지

2.
production-v2
runtimeVersion 2.2.0
대상인지

3.
최신 EAS Update ID가 새 수정 commit과 대응하는지

확인.

가능하면 실제 실행 가능한 환경에서
해당 화면 렌더링 source와 route까지 확인하십시오.

실제 기기 터치 검증이 불가능하면:

“실제 기기 검증하지 못함”

이라고 명시하십시오.

PASS라고 쓰지 마십시오.

━━━━━━━━━━━━━━━━━━━━━━
7. 범위 제한
━━━━━━━━━━━━━━━━━━━━━━

이번 작업은 이 버그만 수정.

금지:

- Settlement API 수정
- DB 수정
- Dashboard 수정
- pricing 계산 변경
- pool-settings 수정
- 전체 navigation audit
- 관련 없는 UI refactor

━━━━━━━━━━━━━━━━━━━━━━
8. 완료보고 형식
━━━━━━━━━━━━━━━━━━━━━━

다음만 보고:

[A. ACTUAL SOURCE]
스크린샷의 “매출 확인” 화면을 실제 렌더링하는
정확한 파일 경로

[B. EVIDENCE]
위 화면임을 특정한 실제 문자열/컴포넌트 근거

[C. ROOT CAUSE]
왜 이전 admin-revenue.tsx 수정이
사용자 화면에 반영되지 않았는지

[D. ACTUAL OLD NAVIGATION]
실제 경고 CTA가 기존에 이동하던 route

[E. FIX]
수정 파일
수정된 실제 route
수정된 실제 문구

[F. OTA]
full work SHA 40자
EAS Update Group full ID
iOS Update full ID
branch
runtimeVersion

[G. VERIFICATION]
- bundle 반영 확인
- 실제 화면 source 확인
- 실제 기기 터치 검증 여부

실제 기기에서 확인하지 못했으면
절대로 PASS라고 보고하지 마십시오.

이번에는
“어디를 수정했는가”가 아니라
“사용자가 보고 있는 바로 그 화면을 수정했는가”
를 증명하십시오.
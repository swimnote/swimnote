[보낼 곳: APP REPLIT]

[P0 QUICK AUDIT — MEDIA UPLOAD PERFORMANCE]

Super Admin MASTER CLOSED.

Android + Onboarding 작업 전에
사진/영상 업로드 체감속도가 느린 원인만 빠르게 조사한다.

이번 응답은 조사 1회.
코드 수정/배포 금지.

목표:
서버 요금제를 올려야 하는 문제인지,
현재 upload architecture를 개선하면 되는 문제인지
실제 코드 기준으로 판정.

━━━━━━━━━━━━━━━━━━━━━━
1. 현재 PHOTO UPLOAD 전체 경로
━━━━━━━━━━━━━━━━━━━━━━

실제 코드 기준으로 확인:

- Teacher Diary 사진
- Album/Feed 사진
- Notice 이미지
- Parent/기타 media upload

각각:

ImagePicker
→ client image processing
→ upload service
→ API endpoint
→ server multipart 처리
→ R2
→ DB
→ 후처리

경로 제출.

공통 upload service를 쓰는지도 확인.

━━━━━━━━━━━━━━━━━━━━━━
2. 가장 중요 — R2 업로드 방식
━━━━━━━━━━━━━━━━━━━━━━

현재 정확히 어느 구조인지 확인:

A.
APP
→ Render API
→ R2

또는

B.
APP
→ presigned/signed URL
→ R2 direct upload

Render가 실제 사진 binary 전체를 중계하는지 명확히 보고.

━━━━━━━━━━━━━━━━━━━━━━
3. CLIENT IMAGE SIZE
━━━━━━━━━━━━━━━━━━━━━━

업로드 전에:

- resize 여부
- max width/height
- JPEG/WebP compression
- quality
- EXIF 처리
- HEIC 변환
- 원본 그대로 전송 여부

확인.

실제 최신 iPhone/Android 사진이
5~15MB급으로 그대로 전송될 가능성이 있는지도 판정.

━━━━━━━━━━━━━━━━━━━━━━
4. MULTI PHOTO UPLOAD
━━━━━━━━━━━━━━━━━━━━━━

사진 여러 장 선택 시 현재 방식:

- sequential
- Promise.all 무제한
- concurrency-limited

중 무엇인지 확인.

특히 Diary 사진 최대 여러 장 업로드 시
한 장씩 끝날 때까지 기다리는 구조인지 확인.

━━━━━━━━━━━━━━━━━━━━━━
5. SERVER-SIDE PROCESSING
━━━━━━━━━━━━━━━━━━━━━━

업로드 request 안에서:

- compression
- thumbnail
- metadata
- DB work
- image transform

등 무거운 처리를 동기적으로 수행하는지 확인.

사용자가 upload 완료를 기다리는 동안
불필요한 후처리를 같이 기다리고 있는지 판정.

━━━━━━━━━━━━━━━━━━━━━━
6. 실제 병목 우선순위
━━━━━━━━━━━━━━━━━━━━━━

아래 각각:

효과
난이도
위험
native rebuild 필요 여부

평가.

1.
client-side resize/compression

2.
R2 direct presigned upload

3.
3~4개 제한 병렬 업로드

4.
thumbnail/후처리 async

5.
upload progress UI

6.
optimistic UI

7.
Render instance upgrade

8.
R2 설정/plan upgrade

━━━━━━━━━━━━━━━━━━━━━━
7. 비용을 쓰기 전에 판정
━━━━━━━━━━━━━━━━━━━━━━

반드시 결론 제출:

A.
현재 Render 사양을 올리면
실제 체감 개선이 클 가능성이 있는가?

B.
아니면 architecture 개선이 우선인가?

C.
R2 자체 업그레이드가 필요한가?

추측하지 말고 현재 upload flow 기준.

━━━━━━━━━━━━━━━━━━━━━━
8. 권장 목표 구조
━━━━━━━━━━━━━━━━━━━━━━

현재 구조가 필요하다면
가장 작은 변경으로 아래 목표가 가능한지 평가:

사진 선택
→ client resize/compress
→ signed URL 발급
→ R2 direct upload
→ concurrency 3
→ metadata/API commit
→ 완료

기존 auth / tenant / R2 보안 깨지지 않게.

━━━━━━━━━━━━━━━━━━━━━━
9. Android와 같이 적용 가능 여부
━━━━━━━━━━━━━━━━━━━━━━

곧 Android 작업 예정.

이번 개선이:

- iOS
- Android

공통 React Native 코드에서 처리 가능한지 확인.

native dependency 추가가 필요한지,
Expo 현재 환경에서 가능한지도 확인.

━━━━━━━━━━━━━━━━━━━━━━
10. 완료보고
━━━━━━━━━━━━━━━━━━━━━━

1. 현재 photo upload architecture
2. Render 중계 여부
3. direct R2 여부
4. client resize/compression 여부
5. 실제 이미지 size 제한
6. multi-photo upload 방식
7. server 동기 후처리
8. 가장 큰 병목 1순위
9. 개선 1순위
10. 개선 2순위
11. Render upgrade 필요 여부
12. R2 upgrade 필요 여부
13. 예상 수정 파일
14. native rebuild 필요 여부
15. iOS/Android 공통 적용 여부
16. 구현 예상 위험

이번에는 조사만.
완료 후 STOP.
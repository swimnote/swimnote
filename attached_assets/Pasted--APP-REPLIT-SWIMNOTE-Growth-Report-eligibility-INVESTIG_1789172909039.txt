[보낼 곳: APP REPLIT]

SWIMNOTE 출석 인정 방식 전수 조사
Growth Report eligibility 수정 전 INVESTIGATION ONLY

중요:
이번 작업에서는 코드 수정하지 마십시오.
DB mutation 하지 마십시오.
Production deploy 하지 마십시오.

현재 AI 성장리포트 대상 168명 중
출석 3회 이상으로 판정되는 학생이 지나치게 적은 문제가 확인되었습니다.

운영자의 실제 출석 정책은 다음과 같습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[확정 운영 개념]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정상적으로 예정된 수업에 학생이 배정되어 있고

그 수업에서
"결석" 처리를 하지 않았다면

기본적으로 출석한 것으로 인정하는 운영 방식입니다.

즉 반드시:

attendance.status = 'present'

row를 선생님이 직접 생성해야만
출석으로 인정하는 시스템이 아닐 가능성이 있습니다.

따라서 현재 Growth Report에서 사용 중인:

attendance table의
status IN ('present','late')

row만 세는 방식이
실제 SWIMNOTE 출석 의미와 일치하는지
먼저 조사해야 합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 출석 UI 실제 동작 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

선생님 출석 화면에서 실제 동작을 추적하십시오.

확인:

A.
수업에 들어가면 학생 전체가 기본적으로
"출석" 상태로 보이는가?

B.
선생님이 결석 학생만 눌러서
"결석"으로 변경하는 구조인가?

C.
아무것도 누르지 않은 학생은
UI상 어떤 상태인가?

D.
아무것도 누르지 않은 학생의
attendance DB row가 생성되는가?

E.
출석 버튼을 명시적으로 눌러야
present row가 생기는가?

F.
출석 화면 자체를 열지 않아도
정상 수업 학생은 출석으로 간주되는가?

UI component
→ hook/service
→ API
→ server handler
→ DB query

까지 실제 파일/함수 기준으로 추적하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. attendance table row 생성 조건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

attendance table의 row가
정확히 언제 생성되는지 조사하십시오.

다음 경우 각각 확인:

1.
선생님이 아무것도 누르지 않음

2.
출석 클릭

3.
결석 클릭

4.
late 클릭

5.
출석 화면 저장

6.
출석 화면을 아예 열지 않음

7.
기존 결석을 출석으로 되돌림

각 상황에서:

- row 생성 여부
- status 값
- update/upsert 여부
- row 삭제 여부

를 제출하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 출석의 실제 Source of Truth 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

현재 SWIMNOTE에서 실제로

"이 학생이 오늘 이 수업에 왔다"

를 판단할 때 사용되는 데이터가 무엇인지 확인하십시오.

후보:

- attendance
- absence / absence_requests
- makeup
- class schedule
- student_class_history
- class diary
- lesson/session
- 기타 attendance state

특히 다음 구조인지 확인하십시오.

[가능성 A]

정상 수업 예정
+
결석 기록 없음
=
출석

또는

[가능성 B]

present row 존재
=
출석

둘 중 현재 실제 앱 로직이 무엇인지
명확히 판정하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. 결석 중심 시스템인지 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Repository 전체에서 다음을 조사하십시오.

- absent
- absence
- absence_request
- missed lesson
- makeup
- 보강
- 결석등록
- 결석취소
- 출석

결석 등록이 별도로 존재하고,
정상 학생은 아무 작업 없이 출석으로 처리되는 구조인지 확인하십시오.

특히 학부모 결석 요청과
선생님 출석 화면의 결석 처리가
어떻게 연결되는지 조사하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. "예정된 정상 수업"을 식별하는 Source 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

출석을:

예정수업 - 결석

으로 계산하려면
먼저 학생에게 그날 실제 수업이 예정되어 있었는지를
확실하게 알아야 합니다.

현재 DB에서 학생별 수업 예정 정보를
어떤 구조로 계산하는지 조사하십시오.

후보:

- class_groups 요일/시간
- class schedule
- student_class_history
- lesson/session table
- holiday/cancelled lesson
- 보강 schedule

다음을 구분 가능해야 합니다.

- 정상 정규수업
- 휴강
- 공휴일
- 센터 휴무
- 학생 등록 전
- 학생 퇴원 후
- 반 이동 전/후
- 보강수업
- 취소된 수업

단순히 매주 요일만 계산해서
없는 수업을 출석으로 세면 안 됩니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 현재 8월 데이터를 실제 운영정책으로 재계산
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

코드 수정 없이 read-only SQL/dry-run으로

주 pool 168명을 대상으로
8월 출석을 다시 계산하십시오.

비교안 2개:

A. 현재 방식
attendance status present/late row count

B. 운영정책 후보
실제 예정/진행 수업
-
명시적 결석/취소
=
출석 인정

각 학생별 두 count의 차이를 계산하십시오.

그리고 분포 제출:

현재 방식:
0 / 1 / 2 / 3 / 4 / 5+

운영정책 후보 방식:
0 / 1 / 2 / 3 / 4 / 5+

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. 실제 사례 10명 샘플 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

개인정보는 마스킹하여
대표 학생 10명을 추출하십시오.

포함 대상:

- 현재 attendance 0인 학생
- 1회인 학생
- 2회인 학생
- 일지 3회 이상인데 attendance <3인 학생
- 주1회 학생
- 주2회 학생
- 중간입회 학생

각 학생에 대해:

- 8월 수업 예정일
- 실제 class diary 날짜
- attendance row
- absent 기록
- makeup 기록
- 현재 방식 출석 count
- 운영정책 후보 방식 출석 count

를 나란히 보여주십시오.

목적은
"결석을 누르지 않은 정상 수업이
현재 Growth Report에서는 출석으로 누락되고 있는지"
실증하는 것입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 보강수업 처리 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

보강 수업은 별도로 확인하십시오.

정규수업 결석
→ 보강 참여

인 경우:

정규수업은 결석 1회
보강수업은 출석 1회

로 계산할 수 있는지 조사하십시오.

보강 수업의 실제 참여 상태가
어느 table/field에 저장되는지도 제출하십시오.

Growth Report에서는
실제로 학생을 관찰한 보강수업도
출석 1회로 인정할 예정입니다.

아직 구현하지 마십시오.
구조만 조사하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 수업일지와 출석의 관계 조사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

특히 다음 학생을 조사하십시오.

source_event_count >= 3
AND
현재 attendance_count < 3

이 학생들이 몇 명인지 다시 제출하고,

그 학생들의 diary 날짜에:

- absent가 실제 존재하는지
- attendance row가 아예 없는지
- present row가 누락됐는지
- 다른 반/보강으로 기록됐는지

를 구분하십시오.

만약:

Diary 있음
+
Absent 없음
+
Attendance row 없음

패턴이 많다면

현재 present row 방식이
출석을 심각하게 과소계산하고 있다는 증거입니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 최종 판정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

조사 후 반드시 아래 중 하나로 판정하십시오.

A.
SWIMNOTE는 명시적 present 기반 시스템이다.
현재 Growth Report 계산이 맞다.

B.
SWIMNOTE는 기본출석 + 결석표시 방식이다.
현재 Growth Report 계산이 잘못됐다.

C.
혼합형이다.
일부 화면/반에서는 present row를 쓰고
일부는 결석만 기록한다.

D.
현재 DB만으로 출석을 신뢰성 있게 계산할 수 없다.

그리고 B/C/D라면
현재 16명 ELIGIBLE 결과는
무효 또는 과소계산 결과로 표시하십시오.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 이번 조사에서 절대 하지 말 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Growth Report eligibility 코드 수정 금지
- 152명 EXCLUDED 처리 금지
- attendance 데이터 생성/보정 금지
- 기존 결석 상태 변경 금지
- Production migration 금지
- Production deploy 금지
- correction 실행 금지

이번에는
"출석 1회를 SWIMNOTE가 실제로 어떻게 인정해야 하는가"
만 조사합니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 완료보고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 순서로만 보고하십시오.

1. PASS / HOLD

2. 선생님 출석 UI의 기본 상태

3. 아무것도 누르지 않은 학생의 UI 상태

4. 아무것도 누르지 않은 학생의 DB 상태

5. present row 생성 시점

6. absent row 생성 시점

7. 현재 출석 Source of Truth

8. 결석 관련 table/API 구조

9. 실제 예정수업 Source of Truth

10. 휴강/공휴일/반이동 처리 방식

11. 보강 출석 저장 방식

12. 현재 present/late 방식 출석 분포

13. "예정수업 - 결석" 방식 출석 분포

14. 두 방식 차이 학생 수

15. diary>=3 + current attendance<3 학생 분석

16. 대표 학생 10명 비교 결과

17. 최종 판정 A/B/C/D

18. Growth Report의 올바른 attendance_count 계산식을
    코드 수정 없이 제안

19. 예상 ELIGIBLE 수 재계산
    단, 실제 변경은 금지

보고 후 멈추십시오.
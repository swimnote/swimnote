[P0 — GROUP8 CRASH LOOP ROOT CAUSE FINAL TRACE]

지금부터 빌드/배포 반복 중지.

현재 핵심은
parent_ai_daily_usage_feature_unique
ADD CONSTRAINT 시 SQLSTATE 42P07 duplicate_table이 왜 계속 나는지
정확히 증명하는 것이다.

추측 금지.

==================================================
1. DEPLOYED COMMIT VERIFY
==================================================

현재 Render production이 실제로 배포한 commit SHA 확인.

반드시 보고:

RENDER_DEPLOY_COMMIT =
LOCAL_HEAD =
ORIGIN_DEPLOY_PHOTO_CLONE_HEAD =

세 값이 같은지 확인.

다르면 즉시 원인 보고하고 수정/재배포 금지.

==================================================
2. GROUP 8 ALL REFERENCES SEARCH
==================================================

repository 전체에서 정확 문자열:

parent_ai_daily_usage_feature_unique

전수 검색.

반드시 모든 발생 위치 보고.

source
dist
migration
compiled output
legacy file
duplicate startup init

어디든 전부.

OLD 패턴:
EXCEPTION WHEN duplicate_object

NEW 패턴:
pg_constraint pre-check

각 위치별로 어떤 코드가 실제 실행 가능한지 판정.

==================================================
3. PRODUCTION DB OBJECT INSPECTION
==================================================

Production APP DB에서 다음을 읽기 전용으로 조회.

A. constraint 존재 여부

SELECT
  conname,
  contype,
  conrelid::regclass,
  pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conname = 'parent_ai_daily_usage_feature_unique';

B. index 존재 여부

SELECT
  schemaname,
  tablename,
  indexname,
  indexdef
FROM pg_indexes
WHERE indexname = 'parent_ai_daily_usage_feature_unique';

C. relation 존재 여부

SELECT
  n.nspname,
  c.relname,
  c.relkind
FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE c.relname = 'parent_ai_daily_usage_feature_unique';

중요:
42P07 duplicate_table은
constraint가 아니라 동일 이름 index/relation 충돌일 수 있다.

==================================================
4. EXACT FAILURE SQL
==================================================

Render 로그에서 실제 실패한 SQL 전문 확보.

반드시:

SQLSTATE
error message
failing SQL
table
schema
object name

보고.

stack만 말하지 말 것.

==================================================
5. DETERMINE EXACT COLLISION
==================================================

다음 중 하나로 정확히 판정:

A.
constraint already exists

B.
constraint는 없지만 same-name unique index exists

C.
same-name other relation exists

D.
Group 8가 중복 실행되어 race 발생

E.
다른 migration path가 같은 object 생성

F.
기타

하나로 확정.

==================================================
6. FIX RULE
==================================================

만약 B:
constraint 없음 + 동일 이름 unique index 존재

이면 단순 pg_constraint 검사만으로는 부족하다.

그 경우 기존 index가
의도한 uniqueness와 정확히 동일한지 먼저 검증.

동일하면:
새 constraint 생성 시도하지 말고
기존 index/constraint 상태를 그대로 정상 상태로 취급할 수 있는지 판단.

단:
DROP INDEX 금지
DROP CONSTRAINT 금지
RENAME 금지
데이터 변경 금지

필요하면 startup migration을
constraint OR equivalent unique index 존재 시 SKIP
하도록 최소 수정.

==================================================
7. NO MORE DEPLOY UNTIL PROVEN
==================================================

다음이 확정되기 전:

build 금지
commit 금지
push 금지
Render deploy 금지

반드시 먼저 보고:

EXACT_COLLISION_OBJECT =
WHY_42P07 =
WHICH_CODE_PATH_EXECUTED =
MINIMAL_FIX =

==================================================
8. REPORT ONLY
==================================================

보고:

1. RENDER_DEPLOY_COMMIT
2. LOCAL_HEAD
3. ALL_GROUP8_REFERENCES
4. CONSTRAINT_QUERY_RESULT
5. INDEX_QUERY_RESULT
6. PG_CLASS_QUERY_RESULT
7. EXACT_FAILING_SQL
8. SQLSTATE
9. EXACT_COLLISION_OBJECT
10. WHY_CURRENT_PRECHECK_FAILED
11. MINIMAL_FIX_PROPOSAL

마지막:

P0_GROUP8_ROOT_CAUSE_CONFIRMED ✅

또는

P0_GROUP8_ROOT_CAUSE_NOT_CONFIRMED ❌

ROOT CAUSE CONFIRMED 전
수정/배포 절대 금지.